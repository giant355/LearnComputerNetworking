# 第 28 课：知道 IP 了，为什么朋友仍连不进房间？——NAT、端口映射、防火墙与中继

> 预计学习时间：约 2～3 小时，包含交互和检查站。不安排新 Lab。读完不必会配置自己的路由器；目标是能判断一次公网连接卡在哪个关口。
>
> 衔接：[[22_Unity联机第一步_两个进程与一个世界|第 22 课的 Host 模式]]、[[26_CSharp的TCP入口_Socket与NetworkStream|第 26 课的监听套接字]]、[[27_DNS_域名怎样找到服务器|第 27 课的 DNS]]。第 27 课还没通过检查站也可以先读这一课；需要用到的结论会在情境里重新交代。

## 先看一个确实会失败的房间

房主 H 的电脑在家中运行一份游戏：同一进程承担本地客户端和服务器角色。服务器部分监听 TCP 端口 `7777`。房主在自己电脑上连接 `127.0.0.1:7777` 成功；同一 Wi-Fi 下的另一台电脑连接 `192.168.1.10:7777` 也成功。房主于是把 `192.168.1.10:7777` 发给外地朋友 F，却一直连不上。

先暂停十秒：**房主确实在监听，朋友也知道地址和端口；为什么仍不行？**

三次测试经过的范围不同：

```text
127.0.0.1:7777          → 只在房主自己的电脑内部绕一圈
192.168.1.10:7777       → 在房主家中的局域网内找到这台电脑
从外地连接房主            → 必须先经过公网，再进入房主家的网络
```

`192.168.1.10` 是房主在家中使用的**私有 IPv4 地址**。另一户人家也可以有自己的 `192.168.1.10`；它不能充当全世界唯一的公网入口。常见私有地址范围还包括 `10.0.0.0/8` 和 `172.16.0.0/12`，但现在不需要背这些范围。[RFC 1918](https://www.rfc-editor.org/rfc/rfc1918.html)

假设房主家路由器对外的一侧有示例公网地址 `203.0.113.8`，房主电脑仍是 `192.168.1.10`：

```text
外地朋友 F                 家庭路由器                  房主 H
198.51.100.44       →       203.0.113.8       →       192.168.1.10:7777
                           公网侧 / 家庭内侧
```

以上地址只用于讲解，不能拿来连接真实服务。**公网地址在路由器的公网侧，并不直接在房主电脑上。**就算第 27 课的 DNS 或房间目录告诉朋友 `203.0.113.8`，朋友也只是找到了房子的大门；路由器还得知道这次送给家里的哪台设备。这里的“房间目录”只是游戏用来公布房间入口的服务，不一定搬运游戏消息。

## 为什么家里能主动访问外面：从一份出站包推导 NAT

房主 H 作为游戏客户端，主动连接公网的房间目录 S，地址为 `198.51.100.20:443`。H 从游戏配置或第 27 课的 DNS 查询得到 S 的地址；操作系统为这条 TCP 连接选了本地端口 `53000`。同时，H 上的游戏服务器仍监听 `7777`；**这两个端口服务于不同的连接或角色**。

先看 H 发出的 TCP SYN 所在 IP 数据报。这里用“`IP:端口`”并排写法，方便同时追踪 IP 层和传输层字段：

```text
出家门前：TCP  192.168.1.10:53000 → 198.51.100.20:443
```

如果直接带着私有来源地址跑到公网，外面的 S 没有办法把回复按这个家庭内部地址正常送回来。家庭路由器于是做两件事：

1. 把对外发送的来源改为自己的公网端点，例如 `203.0.113.8:40001`；
2. 暂时记住“这个公网端点对应家里的 `192.168.1.10:53000`”。

```text
出家门后：TCP  203.0.113.8:40001 → 198.51.100.20:443

路由器里的简化记录：
TCP，公网侧 203.0.113.8:40001 ↔ 家庭内侧 192.168.1.10:53000
相关外部对端：198.51.100.20:443
```

S 看到的客户端来源是 `203.0.113.8:40001`，并向它回复：

```text
S 发回：      TCP  198.51.100.20:443 → 203.0.113.8:40001
路由器改写后：TCP  198.51.100.20:443 → 192.168.1.10:53000
```

路由器根据刚记下的记录，把**返回包的目标 IP 和目标端口**改回 H 的内部端点。H 的操作系统再把这份 TCP 段交给本机端口 `53000` 所属的连接。此时还只是 TCP 握手的一部分；随后仍要完成握手，应用才能认为连接建立。路由器会随着连接状态或超时管理这类记录，并非永久保存。

现在才给机制起名：**NAT（Network Address Translation，网络地址转换）**。上面的家庭 IPv4 例子不只改 IP，也改端口，更具体叫 **NAPT（Network Address and Port Translation，网络地址与端口转换）**。日常说“家用路由器做 NAT”时，常把这种地址加端口的转换也一起算进去。路由器仍要正常选择出口并转发 IP 数据报；NAT 另外改写 IP 和 TCP 的地址/端口字段，并更新因此受影响的检错值，不替应用修改 Unity 消息正文。[RFC 3022](https://www.rfc-editor.org/rfc/rfc3022.html)

为什么还要用端口？假如家里另一台电脑 `192.168.1.11:53000` 也连接同一个 S，它可以使用路由器公网地址上的另一个端口，比如 `40002`。两台电脑共享一个公网 IPv4，却仍能让路由器区分回来的数据。**路由器有时可以保留原端口，并非每次都一定改成新数字**；这里故意选不同的数字，让映射关系更显眼。

请把两张“表”分开：普通**路由表**帮助决定数据报下一步从哪里送；这里的 **NAT 映射记录**帮助边界路由器改写并辨认内外端点。它也不是 ARP 缓存，更不是应用层的请求 ID。真实 NAT 还可能按外部对端进行过滤；“有一条出站映射”不等于全世界任何人都能利用它向家里发包。[RFC 4787](https://www.rfc-editor.org/rfc/rfc4787.html)

## 为什么出得去，不等于朋友进得来

现在朋友 F 已经知道 `203.0.113.8`，尝试连接房主游戏服务：

```text
TCP  198.51.100.44:51000 → 203.0.113.8:7777
```

家庭路由器刚才那条**自动产生的出站记录**是 `40001 ↔ 192.168.1.10:53000`，用来接回房主向 S 发起的连接。朋友打的是 **`7777`**，不是 `40001`；两者根本不是同一个入口。假设路由器没有别的入站规则，它无法从 `203.0.113.8:7777` 推断目标一定是 H：也许家里有几台电脑都在监听 `7777`。

在我们这台“没有对应规则就不交付”的简化路由器中，朋友的 SYN 到了家门口就无法交给 H。不同设备对这种包可能丢弃，也可能回报错误；不要把某一种外在现象当作 NAT 的固定表现。**房主本机 `7777` 监听成功，与公网入口 `7777` 已经安排好送给谁，是两件事。**

还要注意：就算朋友猜到动态映射的 `40001`，也不能推断一定能连入。NAT 的映射方式和对外部来源的过滤策略各有差异，而且这个端口对应的是房主向 S 发起的那条连接，不是监听 `7777` 的服务。[RFC 4787](https://www.rfc-editor.org/rfc/rfc4787.html)、[RFC 5382](https://www.rfc-editor.org/rfc/rfc5382.html)

[打开第 28 课：NAT 映射与公网连入的逐步交互](28_NAT_公网连入_交互.html)

> [!question] 看完交互先自己答
> 交互里房主已经成功收到房间目录 S 的回复。为什么这并没有给朋友 F 打开通向房主游戏端口 `7777` 的路？不用术语，指出那两组不同的端口即可。

## 手动指定“这个门牌号交给谁”：端口映射

如果房主确实想从公网直接接待玩家，就要预先告诉家用路由器一个明确的入站规则。常说的**端口映射**或**端口转发**，在本例可以写成：

```text
协议：TCP
路由器公网侧：203.0.113.8:7777
房主内网侧：192.168.1.10:7777
规则：到公网侧 TCP 7777 的连接请求，转交房主 TCP 7777
```

朋友再发 SYN 到 `203.0.113.8:7777`，路由器现在有明确的去向，把**目标**改为 `192.168.1.10:7777` 并送进家中。房主回应时，路由器相应地把对外的**来源**写成 `203.0.113.8:7777`，这样朋友仍在和自己访问的那个公网端点通信。外部和内部端口不必相同，例如也可约定“外部 TCP `18077` → 内部 TCP `7777`”；`TCP 7777` 和 `UDP 7777` 是不同的协议入口，不能只记一个数字。[RFC 6887](https://www.rfc-editor.org/rfc/rfc6887.html)

这里的配置**没有把监听套接字搬到路由器上**。监听套接字仍在房主 H 的操作系统里；路由器只负责把符合规则的包转发到 H。到达的 SYN 先由 H 的操作系统 TCP 栈处理；握手建立后，游戏程序才通过监听套接字的 `Accept` 接到这条连接。不是把 SYN 原样交给 C# 的 `ReadAsync`。

还有个与第 26 课直接相关的陷阱：`new TcpListener(IPAddress.Loopback, 7777)` 只在本机回环地址上监听，适合上一课的同机实验；即使路由器已转发到 H 的 `192.168.1.10:7777`，这个监听也接不到来自该局域网接口的连接。若程序需要在这台电脑的所有本地 IPv4 网络接口上监听，可以绑定 `IPAddress.Any`（`0.0.0.0`）；也可以明确绑定 H 的局域网地址。这只是**本机监听范围**，不会自动完成路由器端口转发或防火墙放行。`203.0.113.8` 属于路由器公网侧，不是 H 本机可随意绑定的地址。[Microsoft：IPAddress.Any](https://learn.microsoft.com/en-us/dotnet/api/system.net.ipaddress.any)

## NAT 和防火墙：先问“送给谁”，再问“准不准”

假设路由器已经把 TCP `7777` 转给 H，但 H 的操作系统防火墙设置为拒绝外部进入这个端口。朋友的包可能已经过了路由器，仍在 H 这里被拦下。**地址转换成功不等于访问获准。**

- **NAT / 端口转发**回答：内外地址和端口怎样对应，包该送到家里的哪个端点？
- **防火墙**回答：符合条件的流量是否被允许通过？
- **本机监听程序**回答：这个端口上是否真有应用接收并继续处理连接？

家用路由器通常把地址转换和访问控制做在同一个盒子里，所以界面上的一个“端口转发”按钮可能同时影响多个环节；概念上仍要分开。路由器防火墙与 H 自己的系统防火墙也可能是两道不同的关口。开了端口映射并不证明程序已经可达，更不证明游戏登录或业务校验成功。[RFC 6887](https://www.rfc-editor.org/rfc/rfc6887.html)

反过来，NAT 也不是可靠的安全身份验证。把一个服务暴露给公网后，任何符合网络规则的来源都可能尝试连接；游戏仍需要自己的身份验证与输入校验。这节课不要求真的去开放家中端口。

## 家用路由器配置好了，为什么仍可能不通：两层 NAT

前面假设家用路由器的外网侧（WAN 口）**直接**拿到了公网地址 `203.0.113.8`。现在换一种情况：运营商先给家用路由器的 WAN 口 `100.64.1.5`，再由运营商自己的设备共享真正的公网地址 `203.0.113.8`。

```text
朋友 F
  → 203.0.113.8:7777         运营商设备：这里还缺入站映射
  → 100.64.1.5:7777          家用路由器：你只在这里配了端口转发
  → 192.168.1.10:7777        房主 H
```

`100.64.0.0/10` 是供运营商共享地址使用的专门范围，与前面的家庭私有地址范围不同；看到家用路由器 WAN 口落在这里，是存在运营商级 NAT 的常见线索，**不是判断所有网络的唯一方法**。[RFC 6598](https://www.rfc-editor.org/rfc/rfc6598.html)

这种由运营商集中做的 NAT 常叫 **CGNAT（Carrier-Grade NAT，运营商级 NAT）**。朋友的包先遇到运营商那一层；如果那里没有相应入站安排，包根本到不了你的家用路由器。你在自家路由器配置得再正确，也只解决了**第二道门**。家里串了两台路由器时，也可能出现类似的“双重 NAT”。是否能取得公网地址或让上游建立映射，取决于上游网络提供的能力，并不能由家用路由器单方面保证。[RFC 6888](https://www.rfc-editor.org/rfc/rfc6888.html)

## 不要求朋友闯进家门：使用中继

如果房主无法控制上游映射，可以换一条路：准备一台双方都能主动连接的公网服务器 R，地址在示例中是 `192.0.2.50:9000`。

```text
房主 H ──主动向外建立连接──→ 公网中继 R ←──主动向外建立连接── 朋友 F
房主 H ←──通过既有连接收消息── R ──通过既有连接送消息──→ 朋友 F
```

**中继**在中间实际接收并转发数据：房主与朋友不需要彼此主动闯过对方的家用 NAT。前提是双方至少能向 R 发起所需的出站连接，R 也确实提供这种转发服务。它不是免费的魔法：中继要承担双方的流量和服务成本，绕路也可能增加延迟。

“房间目录 / 大厅服务”只告诉玩家“房间在哪里、怎样加入”，不一定搬运游戏数据；“中继”真的搬运双方数据。同一台服务器可以兼任两种角色，但**知道房间地址**与**替房间转发消息**是两种不同的工作。

还存在尝试直连的办法：通过协调和连通性探测，让双方利用各自主动向外发送时形成的映射。常见体系里会遇到 STUN、ICE、TURN 等名字：STUN 可帮助观察映射出的地址，ICE 用候选地址做连通性检查，TURN 提供一种标准中继。这里先了解更广义的穿越思路：常见 ICE 流程以 UDP 通信为主，**不能把它的步骤原样套在本课的 TCP 监听服务上**；TCP 直连还需要相应的协议和实现支持。它们也不是“只要知道公网 IP 就必然打洞成功”的保证；NAT 与防火墙策略不允许直连时，仍需中继。本课先建立问题模型，不要求掌握这些协议的完整流程。[RFC 8445](https://www.rfc-editor.org/rfc/rfc8445.html)、[RFC 6544](https://www.rfc-editor.org/rfc/rfc6544.html)、[RFC 8656](https://www.rfc-editor.org/rfc/rfc8656.html)

## IPv6 会不会让这些问题消失

如果房主和朋友都有可用的全球 IPv6 地址，通常不需要用上面这种**共享一个公网 IPv4 的家用 NAPT**来区分房主电脑。可是“有全球地址”仍不等于“任何人都能连”：网络防火墙可能限制入站，程序可能只绑定回环地址或根本没监听，对端也可能没有可用的 IPv6 路径。

因此应问的是“朋友到房主这个**地址、协议和端口**的路径是否可用”，不要把“IPv6”“公网 IP”“DNS 成功”任何一个单独当作连通保证。

## 把失败定位到具体关口

遇到“外地朋友连不上”时，按数据会经过的顺序想，而不是一口咬定是“网络不好”：

| 关口 | 具体问题 | 不能据此跳过的下一关 |
| --- | --- | --- |
| 名字或邀请信息 | 朋友拿到的地址和端口是这次真正对外公布的吗？ | DNS / 房间目录正确，不代表能到房主。 |
| 公网入口 | 该地址真是当前可从外面到达的入口吗？是否还有运营商 NAT？ | 家用路由器的端口转发不能替上游开门。 |
| 家用路由器 | 对应**协议与外部端口**是否转向正确的内网 IP 和端口？ | 映射存在，不代表防火墙放行。 |
| 防火墙 | 路由器和房主操作系统是否允许这次进入？ | 放行不代表端口有人监听。 |
| 本机程序 | 服务是否监听正确的本地接口和端口？ | TCP 成功也不代表玩家通过业务验证。 |

同机用 `127.0.0.1` 测试和同 Wi-Fi 用私网地址测试，只分别证实了更短的一段路径。即使在家里访问自家**公网**地址，结果也可能受路由器是否支持“回环到自己内网”的行为影响，不能代替真正从外部网络测试。[RFC 4787](https://www.rfc-editor.org/rfc/rfc4787.html)

## 这节课留下的模型

```text
DNS / 房间目录：找到候选公网入口
TCP 监听：房主电脑确实准备接这个端口
出站 NAT 映射：房主主动出去后的回复怎样回来
端口转发：陌生入站该交给哪台内网设备
防火墙：这份流量是否准许通过
CGNAT：家门之外是否还有一道不由你控制的门
中继：双方主动连到第三方，由第三方转发数据
```

这些层次不互相替代。你已经知道第 26 课的 `TcpListener` 在做什么；本课补的是：**到达这个监听套接字之前，跨公网还有哪些门要过。**

## 检查站

合上正文或滚动到看不到答案的位置，按具体情境回答。不用背缩写，能指出包在哪里、谁会怎么处理就行。

1. 房主自己连 `127.0.0.1:7777` 成功，家人连 `192.168.1.10:7777` 也成功。为什么朋友在外地仍不能直接用 `192.168.1.10:7777`？这两次成功各验证了哪一段路径？
2. H 发出 `192.168.1.10:53000 → 198.51.100.20:443`，路由器把来源改成 `203.0.113.8:40001`。S 回复时把目标写成什么？路由器收到后要把目标改回什么？映射记录保存在哪里？
3. 沿用第 2 题，H 同时监听 TCP `7777`。朋友向 `203.0.113.8:7777` 发送 SYN。为什么“房主刚才已经成功向外连接”仍不足以让这一份 SYN 到达 H 的 TCP 栈？
4. 家用路由器已配置 `TCP 外部 7777 → H 的 TCP 7777`，朋友仍超时。分别给出一个**防火墙原因**和一个**监听地址原因**；这说明端口映射、放行和监听的关系是什么？
5. 如果游戏实际使用 UDP `7777`，而路由器只配置了 TCP `7777` 的转发，是否已经满足需要？如果外部端口改成 TCP `18077`、内部仍为 TCP `7777`，朋友应该连哪一个端口？
6. 房主家路由器的 WAN 口得到 `100.64.1.5`，运营商设备对外是 `203.0.113.8`。朋友尝试连接 `203.0.113.8:7777`，而你只在家用路由器配置了转发。包可能先卡在哪一层？为什么？
7. 说出“房间目录”和“中继”的不同工作。为什么中继能在无法直接连入房主时成为退路？它付出什么代价？
8. 一位朋友说：“DNS 已返回房主入口 IP，所以 TCP 肯定能连接；而且 NAT 就是防火墙。”这句话至少有哪两处混淆？
9. 迁移到 IPv6：如果双方都有全球 IPv6 地址，哪一项本课 IPv4 问题通常可以绕开？哪两类阻碍仍可能存在？

## 参考资料

- [RFC 1918：私有 IPv4 地址](https://www.rfc-editor.org/rfc/rfc1918.html)
- [RFC 3022：传统 NAT 与地址/端口转换](https://www.rfc-editor.org/rfc/rfc3022.html)
- [RFC 4787：UDP NAT 映射、过滤与回环行为](https://www.rfc-editor.org/rfc/rfc4787.html)
- [RFC 5382：TCP NAT 行为](https://www.rfc-editor.org/rfc/rfc5382.html)
- [RFC 6887：端口映射控制与防火墙边界](https://www.rfc-editor.org/rfc/rfc6887.html)
- [RFC 6598：运营商共享地址范围](https://www.rfc-editor.org/rfc/rfc6598.html)
- [RFC 8445：ICE 连通性检查](https://www.rfc-editor.org/rfc/rfc8445.html)、[RFC 6544：ICE 的 TCP 候选扩展](https://www.rfc-editor.org/rfc/rfc6544.html)、[RFC 8656：TURN 中继](https://www.rfc-editor.org/rfc/rfc8656.html)
- [Microsoft Learn：`IPAddress.Any`](https://learn.microsoft.com/en-us/dotnet/api/system.net.ipaddress.any)
