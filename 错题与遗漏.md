# 计算机网络错题与遗漏

这里记录的不是“失败”，而是下一轮学习最有价值的导航点。遗漏暴露出来，说明下一步练习终于有了明确目标。

## 课程表达修订记录

这部分记录教材本身造成的阻碍，不算作学习者错误。

| 日期 | 课程 | 初读反馈 | 修订 |
| --- | --- | --- | --- |
| 2026-09-07 | 07_Ethernet帧与局域网交付 | 交换机首次出现时尚未解释是什么 | 在学习目标前加入局域网、交换机、路由器的最小角色图，并在“直连”处解释交换机不构成 IP 路由跳 |
| 2026-09-07 | 07_Ethernet帧与局域网交付 | 不清楚交换机为什么要学习来源 MAC、记录后有何用途 | 先展示“永远泛洪”的带宽代价，再明确学习结果用于未来该 MAC 成为目标时的定向转发 |
| 2026-09-07 | 07_Ethernet帧与局域网交付 | 广播 MAC 的存在理由不清楚 | 从“不知道具体 MAC，却必须询问所有接口”的需求推导广播，并区分有意广播与未知单播泛洪 |
| 2026-09-07 | 07_Ethernet帧与局域网交付 | `tracert` 与“二层”未在使用处解释 | 补入链路层/二层叫法及 `tracert` 利用 TTL 观察 IP 路由器的最小机制 |
| 2026-09-07 | 07_Ethernet帧与局域网交付 | 疑惑一台路由器是否可以有多个 IP | 明确 IP 通常配置在接口上；路由器连接多张网络，多个接口通常各有自己的 IP 和 MAC |
| 2026-09-07 | 08_ARP与网络接口 | 检查站仅写“一台接口”，没有重申它是主机或路由器接口 | 将题干改为“主机或路由器上的网络接口，不是交换机”；保留交换机收到帧时的独立处理规则 |
| 2026-09-08 | 09_网络接口状态机 | 检查题先断言“三类状态都以下一跳 IP 为键”，却未立即写明对照对象 | 改为先给出两个最终目标共用 `192.168.1.1` 的具体场景，明确对照“最终目标 IP”；再单列“为什么不能用未知的 MAC 作键” |
| 2026-09-11 | 11_有限容量字节流 | 检查站第 9 题把容器选择与 `string_view` 生命周期挤在同一问中，导致对比理由不清 | 改成分别询问 `deque` 的两端操作、环形缓冲区的额外边界，以及 view 的借用语义 |
| 2026-09-13 | 12_乱序子串重组 | 检查站使用“输出”“最终输出”，没有区分本次 push、历史累计交付与 ByteStream 当前缓冲内容 | 第 4 题明确要求“本次推入 ByteStream 的字节”；第 6 题改为“历史上按顺序交付出的完整字节序列”，并说明不假设 Reader 尚未读取 |
| 2026-09-13 | 13_TCP接收端_序号_ACK与窗口 | `unwrap` 一行使用加号连接三项输入，且“相对序号”在公式前铺垫不足，容易被理解成第三套长期状态 | 将 `unwrap` 改为函数输入形式，补出取模、候选圈与 checkpoint；统一用“内部绝对序号”，先画 SYN 与 payload 的两套坐标再给公式 |
| 2026-09-14 | 14_TCP发送端_滑动窗口与重传 | 首次出现 `window` 时没有回扣接收端的 `output+pending` 容量，也没有说明发送端还需扣除在途数据 | 在首次定义后补入接收端窗口公式、三组容量状态和 `window-bytes_in_flight` 的完整发送过程 |
| 2026-09-14 | 14_TCP发送端_滑动窗口与重传 | `acknowledged` 被定义成 64 位内部位置，但后续状态表却填入 `1004` 等 32 位线上值，看起来与 ACK 完全相同 | 明确区分首部里的 `ACK` 与 unwrap 后的 `acknowledged_abs`；统一内部变量命名，并把状态表改成从 0 开始的内部绝对位置 |
| 2026-09-15 | Lab5_TinyTCPSender | README 虽有分阶段说明，但复杂函数内只有几条抽象 TODO；从代码入口开始实现时仍需在两个文档之间来回映射，体感过于抽象 | 从 Lab 6 起在复杂 TODO 函数中直接加入“输入事件 → 判断分支 → 状态变化 → 输出动作”的路线图；Lab 5 按学习者要求不回填 |

## 活跃遗漏

| 日期 | 来源课程 | 遗漏点 | 原回答问题 | 正确理解 | 下次复习重点 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-07 | 07_Ethernet帧与局域网交付 | 未知单播与 ARP 广播处理混淆，间隔后再次出现 | 把其他主机收到未知单播后的动作说成“看目标 IP、学习来源并回复” | 交换机因未知目标 MAC 而泛洪；普通网卡先检查目标 MAC，不匹配就丢弃。检查目标 IP 并回复属于 ARP 请求语境 | 区分“未知单播 IPv4 帧”和“广播 ARP 请求”各自经过哪些判断 | 活跃 |
| 2026-09-01 | 03_IP服务模型 | IP 不保证列表只想出送达、顺序和不重复 | “还有一个想不到了” | IP 还不保证时延和带宽，也不提供端到端保密与身份认证 | 不看原文列出至少五项 IP 不保证 | 活跃 |
| 2026-09-01 | 03_IP服务模型 | 逐跳变化只写了链路头尾 | “链路头尾；IP 源地址目标地址” | 链路头尾逐跳更换；TTL 逐跳减少；IPv4 头部校验和因 TTL 改变而重算；来源/目标 IP 通常保持 | 推演 A→R1→R2→B 的三组链路地址和 TTL | 活跃 |
| 2026-09-12 | 06_网络前缀与最长前缀匹配 | 同网段目标的下一跳在间隔复习中复发 | 把 `192.168.1.200` 的当前下一跳写成默认网关 `192.168.1.1` | 与本机同属直连前缀时，当前下一跳就是目标主机本身；跨网段才先交给默认网关，最终目标 IP 都不变 | 给出一个同网段和一个跨网段目标，只回答各自当前下一跳 | 活跃 |
| 2026-09-12 | 10_端口与UDP | UDP 目标端口与校验和职责在间隔复习中短暂混淆 | 四字段写对，但把负责解复用的字段答成校验和 | 目标端口选择接收套接字；校验和负责检错；校验失败后 UDP 不自动重传 | 对一份 UDP 首部标出“交给谁”和“数据是否损坏”分别看哪个字段 | 活跃 |
| 2026-09-12 | 02_深入理解 | 服务、接口、协议缺少新的具体编程迁移例子 | 定义主线会说，但全量复习中没有主动给出 C++/C# 例子 | 服务是能力，接口是本机调用入口，协议是通信双方交换信息的共同规则；三者可映射到 socket 服务、API 与线上报文约定 | 用一个 C++ 或 C# 网络调用同时指出服务、接口和协议 | 活跃 |

## 已解决遗漏

| 解决日期 | 来源课程 | 原遗漏点 | 解决证据 |
| --- | --- | --- | --- |
| 2026-09-03 | 06_网络前缀与最长前缀匹配 / 06b | `/26` 推导停在中间 | 新题 `/27` 正确给出块大小 32、前缀 `10.0.0.64/27` 对应范围 64～95；仅 `32-27` 算式有笔误 |
| 2026-09-03 | 06_网络前缀与最长前缀匹配 / 06b | 混淆直连目标与默认网关下一跳 | 正确回答同网段下一跳 `172.16.5.200`，跨网段下一跳 `172.16.5.1` |
| 2026-09-03 | 06_网络前缀与最长前缀匹配 / 06b | 遗漏 ARP 名称 | 正确回答 Ethernet 还缺链路 MAC 地址，并由 ARP 获得 |
| 2026-09-03 | 06_网络前缀与最长前缀匹配 / 06b | 聚合倍数原因表述不精确 | 能用“可变位数加 3”解释 `/21` 相对 `/24` 的扩大；补全为 `2³=8` 倍 |
| 2026-09-02 | 04_Lab0_TinyNet | 实验报告调试记录未完成 | 补全了错误现象、无初始猜测、实际原因和修复方法；没有为填表虚构不存在的思考过程 |
| 2026-09-02 | 04_Lab0_TinyNet | 把出口接口索引当成路由表下标来选择下一跳 | 将下一跳来源改为匹配路由的 `tmp->next_hop`；公开测试与路由顺序/接口编号错开的额外测试全部通过 |
| 2026-09-02 | 05_一次发送的完整旅程 / 05b | 把最终主机 B 多算作一次 TTL 减少 | 迁移题 `A→R1→R2→B`、初始 TTL=5，正确回答 R1=4、R2=3、B=3 |
| 2026-09-02 | 05_一次发送的完整旅程 / 05b | 未换算字节并混淆发送延迟与传播延迟 | 正确列出 `2000×8/20,000,000`，识别提高带宽直接改变发送延迟，并说明传播介质决定传播延迟；教师补全结果 0.8 ms 与距离因素 |
| 2026-09-01 | 01_核心概念 | 正确定义了延迟和带宽，但没有完成自拟类比 | 水管中，水从一端到另一端所需时间对应延迟，单位时间通过的水量对应带宽 |
| 2026-09-01 | 02_深入理解 | 正确定义了服务、接口和协议，但没有完成 C++/C# 迁移例子 | 补答：服务像一个类对外提供的功能，接口是调用这一功能的具体函数，协议是通信双方共同遵守的约定；并理解协议不要求双方是同一个类 |
| 2026-09-01 | 01_核心概念 | 把 `GameObject` / C# 对象说成“二进制代码” | 首次主动回忆时说明：对象引用像本地地址门牌号，在另一台电脑上完全不对应；需要序列化信息而不是发送引用 |
| 2026-09-08 | 09_网络接口状态机 / Lab1_TinyARPInterface | ARP 回复顺序、IPv4/ARP 接收分流与缓存过期触发条件不够精确 | 检查站中混淆部分接收路径 | Lab 代码实现了请求与回复学习映射、队列冲刷、IPv4 向上交付与缓存到期，6/6 测试通过 |
| 2026-09-09 | 10_端口与UDP / 10b | 未回忆出 UDP 首部的完整四字段和校验和作用 | 收尾题完整写出来源端口、目标端口、长度、校验和；正确指出目标端口负责解复用、校验和负责检错且 UDP 不自动重传 |
| 2026-09-09 | 10_端口与UDP / 10b | 把发送端封装长度误算成逐层减去控制信息 | 新题正确算出应用数据 350 字节时 UDP=358、IP=378、Ethernet=396；反向也列出 `546-18-20-8=500` 的正确解封装算式 |
| 2026-09-11 | 11_有限容量字节流 | 连续操作追踪时中间缓冲区和实际接受量不稳定 | 新复测容量为 4 时正确给出 `WXYZ → YZ → YZ12 → YZ12 → 空`，两次 push 接受 `4、2`，最终 `bytes_pushed=6`、`bytes_popped=6`、`is_finished=true` |
| 2026-09-12 | 07_Ethernet帧与局域网交付 | FCS 失败后的处理和恢复边界 | 全量复习正确回答：接收端丢弃失败帧，基础 Ethernet 不自动完成端到端重传 |
| 2026-09-12 | 08_ARP与网络接口 | 待解析队列的首要原因与处理顺序 | 全量复习正确解释：先学习 IP→MAC，再找到该下一跳的待解析队列，最后制作并发送 Ethernet 帧 |
| 2026-09-13 | 12_乱序子串重组 / Lab3 | 重组进度、Writer 关闭与 Reader 读空的区别 | TinyReassembler 八组测试全部通过，包括 EOF 先到、连续到 EOF 后关闭、未读时未 finished、Reader 排空后 finished |
| 2026-09-14 | 13_TCP接收端_序号_ACK与窗口 | SYN 占位到 ACK/first_index 的换算尚未稳定 | 收尾复测 4/4：正确算出 SYN-only 的 `ACK=ISN+1`、SYN+payload 的 `first_index=0`，并完成普通段位置换算和零窗口解释 |
| 2026-09-14 | Lab4_TinyTCPReceiver | unwrap 初版候选初始化与有符号/无符号混合运算 | 当前实现以本圈合法候选初始化，分别检查相邻圈，并正确处理上一圈平局；10/10 测试通过 |
