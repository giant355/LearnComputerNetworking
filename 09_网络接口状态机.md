# 第 9 课：把 ARP 变成状态机——同一规则遇到时间后会发生什么

> 课堂阅读：70–95 分钟；纸笔推演：15–20 分钟
>
> 回到电脑后：[[Labs/Lab1_TinyARPInterface/README|Lab 1：Tiny ARP Network Interface]]，预计 1.5–2.5 小时
>
> 本课不要求在教室编译代码。明天先读到检查站；Lab 留到电脑旁完成。

第 8 课已经给出 ARP 的规则：不知道下一跳 MAC 就广播询问，收到答案后把等待的数据报发出去。

但只背这句话还不够。现在把三个很普通的事件放在一起：

```text
0 ms：D1 要交给网关 X
100 ms：D2 也要交给网关 X
500 ms：X 的 ARP 回复到达
```

如果接口每次看到数据报都“立刻 ARP”，会发两份重复请求；如果它收到回复后没有记得 D1、D2 在哪里，它又无法补发它们。问题不再是“ARP 报文有哪些字段”，而是：

> 一个网络接口必须记住什么，才能让未来到来的事件得到正确处理？

这就是本课的主题：**状态机**。

## 学习目标

读完后，你应该能够：

1. 用“状态 + 事件 + 动作”描述网络接口，不把规则理解成彼此孤立的句子；
2. 说清 ARP 缓存、待解析队列和最近请求时间分别记住什么；
3. 推演多个数据报等待同一 ARP 映射时的全过程；
4. 区分主机/路由器网络接口的接收行为与二层交换机的转发行为；
5. 读懂 Lab 1 中 `send_datagram`、`recv_frame`、`tick` 三个入口的职责。

---

## 0. 先把设备角色钉住

后面会多次出现“收到 Ethernet 帧”。这句话必须先说明**谁**收到，否则同一句话可能有不同答案。

| 设备或组件 | 它收到帧后主要做什么 | 它关心的表 |
| --- | --- | --- |
| 主机/路由器的网络接口 | 只接受属于自己或广播的帧；IPv4 交给本机 IP 层，ARP 交给 ARP 处理 | ARP 缓存、待解析队列 |
| 普通二层交换机 | 学来源 MAC，按目标 MAC 决定转发、泛洪或不转发 | `MAC → 端口` 表 |
| 路由器的 IP 层 | 收到 IPv4 数据报后，可能查路由表、减 TTL、决定下一跳 | IP 路由表 |

本课和 Lab 中的 `NetworkInterface` 始终是第一行：**主机或路由器的一张网络接口，不是交换机。**

```text
有效 Ethernet 帧
  ↓ 网络接口
IPv4 → 本机 IP 层 →（若设备是路由器）路由决定
ARP  → 本机 ARP 状态机
```

交换机当然也会收到同一条链路上的帧，但那是另一台设备的独立工作，不由本课的 `NetworkInterface` 代替。

---

## 1. “状态机”不神秘

一个普通函数如果只由当前参数决定输出，可以近似写成：

```text
输出 = f(输入)
```

但网络接口不是这样。D2 到来时的行为取决于 D1 之前是否已经触发过 ARP 请求；同一份回复到来时，行为又取决于先前是否有数据报在等待。

所以它更像：

```text
新状态 + 动作 = f(旧状态, 当前事件)
```

这里的“状态机”只表示：

> 一个组件保存少量过去事实；新事件到来时，依据当前保存的事实更新自己，并决定要做什么。

它不是额外的一层网络协议，也不是复杂的图论术语。我们已经见过类似例子：Lab 0 的出口 FIFO 队列会记住先到的是 D1 还是 D2；这也是状态。

---

## 2. 网络接口需要保留的三类状态

为了让 ARP 的规则真正连续运行，最小模型保留下面三张“本地记事本”。

```text
ARP 缓存：
  下一跳 IP → { 下一跳 MAC，过期时刻 }

待解析队列：
  下一跳 IP → [还无法封装的 D1、D2、...]

最近请求时间：
  下一跳 IP → 上一次为它发 ARP 请求的时刻
```

三个键都使用**当前下一跳 IP**，不是最终目标 IP。理由可以直接从一个常见场景推出：

```text
D1 的最终目标 = 203.0.113.20
D2 的最终目标 = 198.51.100.8

两者当前下一跳 = 默认网关 192.168.1.1
```

它们最终会去不同的远端服务器，却都要先交给同一台网关。因此两份数据报可以等待同一个 ARP 答案：

```text
192.168.1.1 → [D1, D2]
```

### 2.1 三张表各自回答什么

| 状态 | 它回答的问题 | 不能替代什么 |
| --- | --- | --- |
| ARP 缓存 | “现在已知 X 的 MAC 吗？” | 不决定最终目的地，也不决定交换机端口 |
| 待解析队列 | “在等 X 的 MAC 时，哪些数据报不能丢？” | 不是链路拥塞时的发送队列 |
| 最近请求时间 | “刚刚是否已经问过 X？” | 不代表已经得到了 X 的回答 |

尤其要分清最后两项：

```text
已经发送 ARP 请求 ≠ 已经知道 MAC
```

所以 D2 在 100 ms 到来时，必须仍排入待解析队列；只是暂时不必重复广播。

---

## 3. 三种事件就是状态机的三个入口

把网络接口想成一个小对象。外界只会从三个方向影响它：上层交来数据报、链路交来帧、时间经过。

```text
IP 层             Ethernet 链路              时钟
  │                    │                      │
  ├─ send_datagram ───▶ NetworkInterface ◀── recv_frame
  │                                           │
  └──────────────────────────────────── tick ─┘
```

### 3.1 事件一：上层要求发送数据报

调用形式是：

```cpp
send_datagram(dgram, next_hop_ip);
```

注意第二个参数不是“最终目标 MAC”，而是路由表已经算出的当前下一跳 IP。

最小逻辑：

```text
若 ARP 缓存命中且未过期：
    用缓存 MAC 制作 IPv4 Ethernet 帧，放入待发送帧队列

否则：
    把数据报放入 next_hop_ip 对应的待解析队列
    若最近没有为该 IP 发送过请求：
        制作 ARP 广播帧
        记下本次请求时刻
```

这里“待发送帧队列”只是 Lab 为观察输出准备的收件箱：表示已经制作好、可以交给链路的帧。它和“待解析队列”不是一回事。

```text
待解析队列：还缺 MAC，里面是 IP 数据报
待发送帧队列：MAC 已有，里面是 Ethernet 帧
```

### 3.2 事件二：当前链路送来一帧

调用形式是：

```cpp
recv_frame(frame);
```

先只接受与本接口有关的帧：

```text
目标 MAC = 自己的 MAC，或广播 MAC
```

随后检查 FCS。若帧已经损坏，直接停止处理。若它有效，才按类型分流：

```text
IPv4：取出 IPv4 数据报，交给本机 IP 层

ARP：学习发送者 IP → MAC
     若它询问的是自己的 IP，制作 ARP 单播回复
     若新学到的发送者正是某个等待中的下一跳，取出并封装所有等待数据报
```

其中“交给 IP 层”不等价于“最终应用收到”，也不等价于“交换机转发”。若接口属于路由器，IP 层之后可能继续调用路由逻辑；若属于普通主机，IP 层最终可能交给 UDP/TCP。

### 3.3 事件三：时间经过

调用形式是：

```cpp
tick(elapsed_ms);
```

本课的教学规则：

```text
ARP 缓存有效期：30 秒
同一下一跳的请求抑制窗口：5 秒
```

`tick` 的最低职责是让过期缓存失效。时间规则并不意味着每 5 秒无条件重发 ARP；只有后来真的还有数据报需要发送且缓存未命中时，才会再次请求。

---

## 4. 完整推演：D1、D2 等待 X

初始状态：

```text
现在 = 0 ms
缓存 = 空
等待 = 空
最近请求时间 = 空
待发送帧 = 空
```

假设下一跳 `X = 192.168.1.1`，其 MAC 最终会是 `MAC_X`。

| 时间 | 事件 | 缓存 | 待解析队列 | 待发送帧 |
| ---: | --- | --- | --- | --- |
| 0 ms | D1 要交给 X | 没有 X | `X → [D1]` | ARP 请求 X（广播） |
| 100 ms | D2 要交给 X | 仍没有 X | `X → [D1,D2]` | 不新增 ARP 请求 |
| 500 ms | 收到 X 的 ARP 回复 | `X → MAC_X` | 取出后为空 | IPv4(D1→MAC_X)、IPv4(D2→MAC_X) |
| 1000 ms | D3 要交给 X | 命中 X | 保持为空 | IPv4(D3→MAC_X) |

这里的因果链很重要：

```text
回复到达
→ 学到 X 的 MAC
→ 找到以 X 为键的待解析队列
→ 每份数据报分别制作 Ethernet 帧
→ 清空该等待队列
```

不是“回复神奇地让 D1 自动继续走”；接口必须显式保留 D1、D2，才能在正确的时机拿出来。

---

## 5. 一个小陷阱：ARP 请求也可能带来答案

收到 ARP **回复**时学习映射很好理解：对方明确说“我是 X，我的 MAC 是 MAC_X”。

ARP **请求**同样写着发送者自己的 IP 和 MAC：

```text
发送者 IP = Y
发送者 MAC = MAC_Y
目标 IP = X
```

因此接口通常也可以先学习：

```text
Y → MAC_Y
```

如果自己正好在等待 Y 的 MAC，理论上也可以借此把等待的数据报发出。Lab 1 会采用这个一般规则：**收到有效 ARP 消息就学习它的发送者**。这不是说任意 ARP 声明都可信；第 8 课已经指出，基础 ARP 缺少身份认证。

---

## 6. 课堂纸笔题：先写状态，再写动作

这道题不要求背代码。请在纸上写出每一步的三张状态表。

```text
接口自身：IP = 10.0.0.2，MAC = AA
网关 G：IP = 10.0.0.1，MAC = GG

0 ms：D1(next hop = G) 到来
50 ms：D2(next hop = G) 到来
100 ms：收到目标 MAC = AA、发送者 IP = G、发送者 MAC = GG 的 ARP 回复
31,000 ms：时间从 100 ms 再经过 30,900 ms
31,100 ms：D3(next hop = G) 到来
```

分别写出：

1. 第 50 ms 时已经发出了几份 ARP 请求？D1、D2 分别在哪？
2. 100 ms 时发出的两份 IPv4 帧，它们的目标 MAC 是什么？D1/D2 的最终目标 IP 会被 ARP 改掉吗？
3. 31,000 ms 时缓存怎样变化？31,100 ms 的 D3 会发生什么？

<details>
<summary>完成后展开参考结论</summary>

1. 只有一份请求；D1、D2 都在以 G 为键的待解析队列。
2. 两帧目标 MAC 都是 `GG`；IP 数据报里的最终目标各自保持原样。
3. 缓存 30 秒到期后被删除。D3 到来时缓存未命中，会排队并发起新的 ARP 请求。

</details>

---

## 7. 明天的安排与 Lab 边界

明天上课时这样使用这份笔记即可：

| 时间 | 做什么 | 电脑是否必需 |
| ---: | --- | --- |
| 0–10 分钟 | 不看正文，复述“目标 IP → 路由表 → 下一跳 IP → ARP → MAC” | 否 |
| 10–70 分钟 | 阅读第 1–5 节，重点画出 D1/D2 的状态变化 | 否 |
| 70–90 分钟 | 完成纸笔题与文末检查站 | 否 |
| 回寝后 90–150 分钟 | 做 Lab 1，运行测试，写精简报告 | 是 |

Lab 不会处理真实网卡、字节序列化、ARP 解析字节或 Socket；它只让你实现状态转移。每一帧先作为 C++ 结构体存在，这样你能直接看到“发出了 ARP 请求”或“D1 被封装成 IPv4 帧”。

---

## 小结

1. 状态机 = 组件根据旧状态和当前事件，更新状态并产生动作。
2. 网络接口的核心事件是：上层数据报到来、Ethernet 帧到来、时间经过。
3. 缓存解决“已知 MAC 时直接发送”；待解析队列解决“未知 MAC 时数据报放哪里”。
4. 重复请求抑制只避免 ARP 广播浪费，不能替代待解析队列。
5. 收到 IPv4 帧的主机/路由器接口交给 IP 层；普通交换机另按目标 MAC 转发现成帧。

---

## 检查站

合上正文后回答；不确定可以写“不知道”。

1. 用一句话解释为什么网络接口需要“状态”，而不能只把每个 `send_datagram` 当作独立函数调用。
2. 为什么 ARP 缓存、待解析队列、最近请求时间都应以**下一跳 IP**为键？请举两个不同最终目标共用同一网关的例子。
3. 缓存未命中时，D1 和 D2 在 100 ms 内到达同一下一跳。分别说明它们在哪里，以及总共会产生几份 ARP 请求，为什么。
4. 收到有效 ARP 回复 `X → MAC_X` 后，接口要按什么顺序处理缓存、等待数据报和待发送帧？
5. 主机网络接口收到有效、目标 MAC 属于自己的 IPv4 帧时怎么处理？普通二层交换机收到同一帧时又怎么处理？
6. 缓存到期为什么不等于“接口立刻广播 ARP”？
7. 看 Lab 1 的函数名，分别写出 `send_datagram`、`recv_frame`、`tick` 的事件来源。

完成课堂阅读和检查站后，把答案发给我；Lab 完成时再单独发测试结果与精简报告。理论掌握和代码验收分开判断。
