# 第 21 课：坏消息为什么传得慢——距离向量环路与计数到无穷

> 预计时间：1.75～2.5 小时
>
> 场景：适合课堂阅读，不需要电脑
>
> 前置：第 20 课的距离向量、RIB、FIB 与路由收敛

[打开交互：两台路由器怎样把一条断路“数”到无穷](21_计数到无穷_交互.html)

## 本课从一个反直觉现象开始

第 20 课中，距离向量的规则看起来很合理：

```text
我到目标的估计
= min(我到邻居的代价 + 邻居声称的目标代价)
```

邻居说它离目标只剩 1 跳，而我到邻居需要 1 跳，那么我到目标就是 2 跳。

问题在于：

> 邻居说出的“2 跳”，会不会本来就是根据我以前说过的话算出来的？

如果双方只交换最终结论，却不知道结论背后的完整路径，就可能互相把同一条错误信息当成新证据。

这节课要回答：

1. 一条已经断开的路线为什么会在路由表中“复活”；
2. 路由环路与计数到无穷分别是什么；
3. 为什么 TTL 不能修复路由协议；
4. 触发更新、水平分割和毒性逆转分别解决什么；
5. 为什么这些机制仍不能把距离向量变成绝对无环；
6. 这些控制平面错误怎样影响 RIB、FIB 和普通数据报。

## 1. 先建立最小网络

只有两台路由器 A、B，以及目标网络 D：

```text
A -------- B -------- D
   代价 1      代价 1
```

B 与 D 直接相连，因此 B 记录：

```text
目标 D
代价 1
下一跳：直接交付
```

B 告诉 A：

```text
我到 D 的代价是 1
```

A 加上自己到 B 的代价：

```text
A 到 D = A 到 B 的 1 + B 报告的 1 = 2
```

稳定状态如下：

| 路由器 | 到 D 的代价 | 下一跳 |
| --- | ---: | --- |
| A | 2 | B |
| B | 1 | 直接交付 |

此时 A 向邻居报告：

```text
我到 D 的代价是 2
```

注意：A 没有把完整路径 `A→B→D` 告诉 B。它只报告结果“2”。

## 2. B—D 链路断开

现在发生故障：

```text
A -------- B ----X---- D
```

B 能直接检测到它与 D 的链路失效，于是 B 应该把 D 标记为不可达。

但是分布式系统中，事件不会严格排队发生：

- B 刚发现故障；
- A 还不知道故障；
- A 的旧路由仍写着“经 B 到 D，代价 2”；
- A 可能正好把这份旧信息发给 B。

B 收到：

```text
A 说：我到 D 的代价是 2
```

如果使用最朴素的距离向量规则，B 会计算：

```text
B 到 D
= B 到 A 的 1 + A 报告的 2
= 3
```

于是 B 得出一个局部上完全符合公式、全局上却错误的结论：

```text
我虽然不能直接到 D，
但可以经过 A，以代价 3 到达 D。
```

## 3. 错误路线为什么会来回增长

B 将新结论告诉 A：

```text
B：我到 D 的代价是 3
```

A 唯一认识的路线仍经过 B，因此更新为：

```text
A 到 D = 1 + 3 = 4
```

A 再告诉 B：

```text
A：我到 D 的代价是 4
```

B 又更新为：

```text
B 到 D = 1 + 4 = 5
```

继续下去：

```text
稳定时：B=1，A=2
链路断开后：B=3，A=4，B=5，A=6，B=7……
```

它们没有发现新路径，只是在轮流把对方的旧结论加一。

[打开交互：两台路由器怎样把一条断路“数”到无穷](21_计数到无穷_交互.html)

打开后逐步观察三件事：

1. A 和 B 当前声称的代价；
2. 两台路由器的下一跳；
3. 一份普通数据报会在哪两台路由器之间往返。

## 4. 路由环路和计数到无穷不是同一件事

这两个现象经常同时出现，但含义不同。

### 路由环路 routing loop

A 的 FIB 写着：

```text
去 D → 交给 B
```

B 的 FIB 写着：

```text
去 D → 交给 A
```

普通数据报于是：

```text
A → B → A → B → ……
```

这是**数据平面**正在发生的转发环路。

### 计数到无穷 count-to-infinity

控制平面中的代价不断变化：

```text
3 → 4 → 5 → 6 → ……
```

路由器要等到代价增长到协议规定的“无穷”，才终于承认目标不可达。

这是**控制平面**的收敛问题。

关系是：

```text
控制平面互相误导、不断增加代价
                ↓
RIB/FIB 暂时安装错误的互指路线
                ↓
普通数据报在数据平面形成环路
```

## 5. 为什么每一步都合理，整体却错误

单看 B 的公式：

```text
B 到 A = 1
A 声称到 D = 2
所以 B 经 A 到 D = 3
```

算术没有错。

错误在于 B 不知道 A 的“2”实际上来自：

```text
A 到 D = 经 B 到 D
```

于是 B 认为：

```text
A 有一条独立于我的路线
```

但真实情况是：

```text
A 的路线依赖 B
B 的新路线又依赖 A
```

这是一种循环依赖。

距离向量只传播：

```text
目标 D，代价 2
```

它没有传播完整路径证明，因此接收者无法单凭这个数字判断“这条路线会不会绕回我”。

## 6. “无穷”为什么可以是一个有限数字

数学上的无穷不是 16。但路由协议不可能真的把代价一直数到无限大，因此会约定一个上限：

```text
代价达到这个值，就视为不可达。
```

RIP 使用跳数作为度量：

```text
1～15：仍可达
16：不可达，也就是协议里的“无穷”
```

所以 RIP 中可能出现：

```text
3 → 4 → 5 → …… → 15 → 16（不可达）
```

为什么不把无穷设置得特别大？

因为上限越大：

- 能表示的网络直径越大；
- 发生计数到无穷时，可能等待得越久。

这是覆盖规模和故障收敛速度之间的取舍。RIP 因此只适合直径较小的网络。

## 7. TTL 能不能解决这个问题

TTL 可以阻止**一份数据报**永远循环：

```text
TTL=4：A→B
TTL=3：B→A
TTL=2：A→B
TTL=1：B 处理后变成 0，丢弃
```

但 TTL 没有修复路由表：

```text
A 的 FIB 仍指向 B
B 的 FIB 仍指向 A
```

下一份新数据报仍然会进入同一个环路。

因此：

> TTL 限制单份数据报造成的危害；路由协议必须另外修复错误路线。

## 8. 改进一：触发更新

朴素做法可能每隔一段时间才发送路由表。链路刚断开时，如果 B 等到下一个周期才说：

```text
D 已不可达
```

错误信息就有更长时间传播。

触发更新的思路是：

> 重要路线发生变化时，不等普通周期，尽快把变化通知邻居。

例如：

```text
B 检测到 B—D 断开
→ 立刻把 D 的度量改为不可达
→ 尽快发送更新
```

它能缩短收敛时间，但不能单独保证没有环路。原因是：

- 更新仍需要传播时间；
- 多份更新可能交错到达；
- 某个邻居可能先发出旧信息；
- 更大的网络中可能存在多台路由器互相依赖。

实际协议还会限制触发更新频率，避免一次变化引发过多控制消息。

## 9. 改进二：水平分割

观察 A 的路线：

```text
A 到 D 是从 B 学来的
```

那么 A 再对 B 说：

```text
我能到 D
```

对 B 没有帮助，因为 A 本来就依赖 B。

水平分割 split horizon 的规则是：

> 从某个邻居学到的路线，不再从面向该邻居的方向通告回去。

所以 A 从 B 学到 D 后，向 B 发送更新时省略 D：

```text
B → A：D 的代价 1
A → B：不再通告从 B 学到的 D
```

当 B—D 断开时，B 不会被 A 的旧路线欺骗。

## 10. 改进三：毒性逆转

简单水平分割是“不说”。毒性逆转 poisoned reverse 是明确说：

```text
我从你这里学到 D，
所以当我对你通告时，会把 D 标成不可达。
```

例如 A 的实际路线是：

```text
A → B → D
```

A 对 B 通告：

```text
D 的代价 = 无穷
```

这不是说 A 对所有人都无法到达 D，而是在明确提醒 B：

> 你绝不能把我当作到 D 的备用下一跳，因为我的路线本来就依赖你。

与单纯省略相比，毒性逆转能更明确、更快地拆掉两台路由器互相指向的错误路线，但会增加更新消息的内容。

## 11. 为什么毒性逆转仍不是万能的

毒性逆转很擅长处理两台路由器直接互相欺骗：

```text
A 经过 B
B 又想经过 A
```

但三台路由器可能形成更长的依赖环：

```text
A 认为经 B 可达
B 认为经 C 可达
C 认为经 A 可达
```

A 知道自己的下一跳是 B，却未必知道 B 的路线最终又经过 C、A 回到了自己。

所以即使每台路由器都不把路线直接通告回原下一跳，更长的环仍可能形成。RIP 的标准说明也明确指出，毒性逆转可以立即打破只涉及两台路由器的环，但三台路由器的互相误导仍可能存在。

结论是：

> 水平分割和毒性逆转是降低风险、加快收敛的机制，不是对所有拓扑的数学无环证明。

## 12. 超时和失效路线为什么还要暂时保留

距离向量协议通常还需要时间状态：

```text
这条路线最后一次被更新是什么时候？
多久没收到更新后判定失效？
失效后多久才彻底删除？
```

如果邻居长时间不再通告一条路线，路由器不能永远相信旧信息，因此要设置超时。

但路线失效后马上彻底删除也有问题：邻居可能还保留旧路线。暂时保留一个“不可达”记录并继续传播，可以帮助其他路由器清除旧知识。

可以把状态简化为：

```text
有效路线
→ 超时或收到不可达更新
→ 标记不可达并传播
→ 等待清理时间
→ 删除
```

这与 ARP 缓存过期不同：两者都使用计时器，但保存的知识、失效的后果和传播范围不同。

## 13. RIB 和 FIB 在环路中发生什么

以 B 错误相信 A 为例，控制平面可能产生候选路线：

```text
目标前缀：D
来源：距离向量协议
下一跳：A
代价：3
```

如果它成为获胜路线，就进入 FIB：

```text
D → 下一跳 A → 某出口接口
```

同时 A 的 FIB 仍可能是：

```text
D → 下一跳 B
```

因此：

```text
错误的控制平面知识
→ 错误的 RIB 候选
→ 错误的 FIB 转发结果
→ 普通数据报形成环路
```

最长前缀匹配没有出错。它只是忠实地选择了当前 FIB 中最具体的错误规则。

## 14. 距离向量和链路状态怎样看待同一次故障

### 距离向量

```text
邻居告诉我目标代价变了
→ 我在邻居的结果上重新计算
→ 再把自己的新结果告诉其他邻居
```

它没有天然掌握完整路径依赖，因此坏消息可能逐跳、缓慢传播。

### 链路状态

```text
某条具体链路失效
→ 传播这条链路状态事实
→ 各路由器更新拓扑图
→ 各自重新运行最短路径算法
```

链路状态也需要检测、传播、计算和安装时间，也可能在收敛期间暂时不一致；它不是“瞬间正确”。但它传播的是拓扑事实，而不是让邻居反复增加一个无法验证来源的距离数字。

## 15. 游戏或应用能观察到什么

应用通常看不到“正在计数到无穷”，它看到的是结果：

- UDP 状态更新连续丢失；
- RTT 突然变高；
- TCP 出现超时和重传；
- 连接可能在路径恢复后继续，也可能因为超时过长而断开；
- 数据报可能暂时绕路或反复经过某些路由器；
- `traceroute` 可能显示重复出现的跳点。

但应用不能只凭一两份丢包断言：

```text
服务器宕机了
```

也可能是中间路由正在收敛。

## 16. 常见混淆

### 混淆一：代价不断增加，说明真的发现了更长的新路径

不一定。计数到无穷中，路由器可能只是在循环引用彼此的旧结论。

### 混淆二：有 TTL，所以路由环路没有危害

TTL 只让每份数据报最终被丢弃；错误 FIB 仍可能让后续数据报持续进入环路。

### 混淆三：触发更新能保证不产生环路

它只让变化更快传播，不能消除所有消息交错和循环依赖。

### 混淆四：毒性逆转表示对所有邻居都宣告不可达

不是。它针对路线来源方向：如果路线从 B 学来，向 B 通告时把它标为不可达。

### 混淆五：OSPF 完全不会在收敛期间出错

OSPF 同样是分布式协议，检测和传播需要时间。区别在于它传播链路状态并基于拓扑重新计算，而不是简单交换目标距离。

## 17. 本课最小模型

```text
距离向量传播：目标 + 估计代价

根本风险：
邻居的估计可能又依赖我

控制平面：
代价 3 → 4 → 5 → …… → 协议定义的无穷
= 计数到无穷

数据平面：
A 的 FIB 指向 B，B 的 FIB 指向 A
= 路由环路

缓解机制：
触发更新：变化后尽快发送
水平分割：不把从邻居学到的路线通告回该方向
毒性逆转：通告回去时明确标成不可达
超时与清理：让陈旧路线最终失效并删除

限制：
两节点环容易阻断，更长依赖环仍可能存在
```

## 检查站

请遮住正文，用自己的话回答。

1. 在 `A—B—D` 中，稳定时 B 到 D 代价为 1，A 经 B 到 D 代价为 2。B—D 断开后，B 为什么可能jisu错误地接受 A 报告的“代价 2”？

因为a是b的邻居，a计算的花费依赖了b，但是b到d已经断路了

2. 按顺序写出朴素距离向量下 `B=3、A=4、B=5、A=6` 是怎样计算出来的。

假如A依赖B，B到目的地的花费是3，A到B的花费是1，那么A依赖B的男，他到目的地就是4。但是现在B和目的地已经断路了，B他又绕回来依赖A， 就要再加上B到A的花费,如此往复。每多一次依赖,他们花费就加一，他们两个到目的地的花费就越来越大。

3. 分别说明路由环路与计数到无穷发生在哪个平面、改变或影响什么。

分别发生在控制平面和数据平面。
他们都是邻居之间循环依赖的产物，本質上是一樣的。

4. 为什么每一步加法都正确，整体路线仍然错误？

可能形成了循环依赖。

5. RIP 把 16 视为无穷有什么含义？它为什么不把无穷设置成一个极大的数字？判定为无穷的时间就会大大增加

让我花费达到十六个十，此间认为无法到达。假如把这个花费设置为一个非常大的数字的话，导致有的断路或者循环依赖不能被更快地发现。

6. 一份 TTL=3 的数据报进入 `A↔B` 环路后会怎样？为什么这没有修复路由协议？

最终因为 TTL变成零而被丢弃。他没有修复路由协议的原因，是因为尽管它阻止了，它让一份数据包被丢弃，但是它并没有修复这个段路。它并没有对这个路由这个链路产生什么影响，用户仍然收不到数据。

7. 触发更新解决什么延迟？为什么它不能单独保证没有环路？

触发更新，它这样的断路能更快地被发现。因为在消息传播的过程中，有的路由器它还保留的是旧的信息，它不知道这个路径已经被更新了，所以还是可能会有环路。

8. 分别用一句话说明水平分割与毒性逆转。A 的 D 路线来自 B 时，A 向 B 怎样通告 D？

水平分割，它就会告诉B，告诉B，A不能到达D，毒性逆转，它就，A它会告诉B，A到达D的花费是无穷大。

9. 为什么毒性逆转能迅速打破两节点互指，却不保证解决 `A→B→C→A`？

因为两个以上的节点它互相利润，它仍然形成一个更大的环。但是独信逆转它只能保证它两个节点之内它不会形成循环。

10. 路线超时后为什么不一定立即从所有状态中彻底删除，而可能先保留“不可达”状态？

防止删除后形成循环引用

11. 在计数到无穷期间，最长前缀匹配本身有没有算错？错误实际来自哪里？

没错，来自rib和fib本来就错误的信息

12. 比较距离向量与链路状态面对断链时交换的信息：一个传播什么结果，一个传播什么事实？

传播自己到目标的花费
局部链路事实

## 检查站反馈（2026-09-22）

总体判断：**部分掌握**。你已经理解两台路由器会循环依赖旧结论，能够推演代价逐渐增加，也知道 TTL 只能终止单份数据报、最长前缀匹配本身没有算错。当前缺口集中在四个机制边界，不需要重读全课。

### 1. 第 3 题把两个平面写反了

```text
计数到无穷：控制平面
路由器交换距离，RIB 中的代价 3、4、5……不断变化

路由环路：数据平面
FIB 互相指向，普通数据报 A→B→A→B
```

它们可能由同一个循环依赖引起，也经常同时存在，但不是同一现象：一个描述路由知识怎样变，一个描述数据报怎样被转发。

### 2. 第 4 题还要指出“输入为什么不可信”

每次加法都正确：`到邻居的代价 + 邻居报告的代价`。整体错误是因为邻居的报告并非一条独立路径，而是间接依赖当前路由器的旧结论。公式没有发现这条循环依赖。

### 3. 第 5 题需要补上另一半取舍

RIP 中度量达到 16 就表示不可达。把“无穷”设得小，可以更快结束计数到无穷；代价是只能支持较小的网络直径。设得很大能表示更长路径，却会让错误路线数更久。

### 4. 第 7 题中，触发更新不负责更早发现断路

链路检测机制先发现变化；触发更新解决的是：**变化已经被某台路由器知道以后，不必等待下一次周期更新，而是尽快传播。**传播仍需要时间，旧更新仍可能交错到达，所以它不能单独保证无环。

### 5. 第 8 题必须区分“不说”和“明确说不可达”

假设 A 的 D 路线来自 B：

```text
水平分割：A 向 B 发送更新时，省略 D，不把这条路线说回去。
毒性逆转：A 向 B 明确通告 D 的度量为无穷。
```

两者目的相同，但发送的消息不同。

### 6. 第 10～11 题补全因果链

超时路线应立即停止用于 FIB 转发，但控制平面可暂时保留“不可达”记录并传播，让邻居清除旧路线，防止陈旧通告使它复活；清理计时器结束后再彻底删除。

计数到无穷的根因来自控制平面使用了陈旧、循环依赖的距离信息，生成错误 RIB 候选；FIB 使用了获胜但错误的路线。最长前缀匹配只是正确查询了错误的 FIB。

### 四道针对题

1. A 的 FIB 写着 `D→B`，B 的 FIB 写着 `D→A`，同时双方通告的代价从 4 增长到 5。哪一个现象属于数据平面，哪一个属于控制平面？

A 的 FIB 写着 `D→B`，B 的 FIB 写着 `D→A    数据
同时双方通告的代价从 4 增长到 5。       控制

2. B 已经检测到 B—D 断开，但普通更新还要等 30 秒。触发更新具体省掉了哪段等待？它有没有让 B 更早检测到断链？

省去了普通更新的半分钟，没有

3. A 的 D 路线来自 B。分别写出 A 使用简单水平分割和毒性逆转时，向 B 发送的内容。

省略
无穷

4. 一条路线超时后，FIB 和控制平面保留的失效记录应分别怎样处理？为什么？

丢弃
暂时保留防止形成环路

### 针对题验收（2026-09-22）

四题全部通过，本课达到**基本掌握**：

- 第 1 题正确区分了数据平面的转发环路与控制平面的代价增长；
- 第 2 题正确指出，触发更新省去等待下一次周期更新的时间，但不会让 B 更早检测自己的直连断路；它会让邻居更早得知故障；
- 第 3 题正确区分了水平分割的“省略”与毒性逆转的“通告无穷”；
- 第 4 题正确：失效路线应从 FIB 移除，控制平面暂时保留不可达记录，用来传播坏消息并压住陈旧路线，随后再清理。

现在已经能把完整因果链说清楚：

```text
控制平面使用陈旧且循环依赖的信息
  → RIB 中出现错误候选并计数到无穷
  → 错误结果进入 FIB
  → 数据平面可能产生转发环路
```


## 资料边界

本课使用 RIP 作为理解距离向量失败与缓解机制的具体例子。RIPv2 对距离向量、拓扑变化、水平分割、毒性逆转、触发更新和计时器的定义见 [RFC 2453](https://www.rfc-editor.org/rfc/rfc2453.html)。RFC 明确说明：毒性逆转可以立即打破只涉及两台路由器的环，但三台路由器仍可能互相误导。

本课不要求背 RIP 报文格式、定时器精确秒数或厂商配置命令。当前目标是能推演状态怎样一步步变错，以及每种缓解机制切断了哪条错误信息。

## 下一步

- 完成本课检查站；
- 随后开始综合项目 A：从空目录建立 TinyRouter；
- 项目第一阶段只实现直连网络和最小路由表，由你参与确定类、状态与接口。
