# 课程总路线：约 180 小时

这是一张地图，不是现在需要背下来的目录。学习时始终只看当前的一小段。

## 最终目标

完成课程后，你应该能够：

1. 从一次 Unity/C# 的发送操作，一直解释到 Socket、TCP/UDP、IP、路由器和以太网。
2. 解释网络为什么会丢包、乱序和延迟，以及可靠传输怎样建立在不可靠服务之上。
3. 实现简化的字节流、重组器、TCP 收发端、网络接口和 IPv4 路由器。
4. 用 Wireshark、`ping`、`traceroute`、`curl` 等工具定位常见网络问题。
5. 阅读陌生网络协议或 Unity 网络库时，能够判断它解决的问题、所在层次与代价。

## 时间规划

| 阶段 | 内容 | 理论与观察 | Lab 与复盘 | 合计 |
| --- | --- | ---: | ---: | ---: |
| 0 | 从一条消息建立网络直觉 | 7h | 5h | 12h |
| 1 | 分层、数据报与 IP 服务模型 | 13h | 9h | 22h |
| 2 | 以太网、ARP、IPv4 与路由 | 14h | 12h | 26h |
| 3 | 字节流、可靠性与 TCP 收发端 | 22h | 26h | 48h |
| 4 | 交换、排队、性能与拥塞控制 | 13h | 11h | 24h |
| 5 | Socket、DNS、HTTP 与 TLS | 12h | 8h | 20h |
| 6 | Unity 需要的 TCP/UDP 与同步直觉 | 5h | 3h | 8h |
| 7 | 综合网络、系统调试与总复盘 | 7h | 13h | 20h |
|  | **总计** | **93h** | **87h** | **180h** |

每天 2.5 小时学习时，纯数学计算约为 72 天。实际计划按约 11 周安排：每周保留一天吸收、复习或处理 Lab 调试时间。

## 课程怎样逐步“长出来”

我们会维护一个叫 **TinyNet** 的小型模型。它最开始只会把一个消息交给另一个程序，随后逐步增加能力：

```text
直接传消息
  ↓ 消息必须变成字节
字节流
  ↓ 数据可能分批、重叠、乱序到达
重组器
  ↓ 网络可能丢包
确认、序号、超时与重传
  ↓ 发送者不能压垮接收者
流量控制
  ↓ 所有人也不能一起压垮网络
拥塞控制
  ↓ 两台机器不一定直接相连
IP 数据报与路由器
  ↓ IP 地址还不知道局域网硬件地址
以太网、ARP 与网络接口
  ↓ 最后把所有组件接起来
一个可以运行的简化互联网
```

顺序会根据理解情况微调。真正的网络协议不是按这张图的顺序被发明的；这是为了让初学者每一步都能看见“新机制为什么需要存在”。

## Lab 原则

每个 Lab 都会包含四层目标：

1. **观察**：先看到现象，例如乱序、丢包或排队。
2. **预测**：动手前先猜改变一个变量会发生什么。
3. **实现**：用最小代码补上一个机制。
4. **解释**：代码通过测试后，用自己的话说明它为什么正确。

后续实验报告采用精简证据格式：测试摘要、一个关键 bug 的“现象 → 原因 → 修复”、核心概念解释和实际用时。没有产生过的猜测不要求补写，报告服务于复盘，不服务于填表。

主要 Lab 暂定为：

- Lab 0：极小的数据报转发网络；
- Lab 1：有限容量字节流；
- Lab 2：乱序子串重组器；
- Lab 3：TCP 接收端；
- Lab 4：TCP 发送端、计时器与重传；
- Lab 5：以太网帧、ARP 与网络接口；
- Lab 6：最长前缀匹配与 IPv4 路由器；
- Lab 7：排队与拥塞控制模拟；
- Lab 8：把简化组件连成一张小互联网。

Unity 只作为应用视角：课程会解释实时游戏为什么在意延迟、UDP、快照、插值和服务器权威，但不会把大量课时转成 Unity API 教程。
