# 课程总路线：约 200 小时

这是一张地图，不是现在需要背下来的目录。学习时始终只看当前的一小段。

## 最终目标

完成课程后，你应该能够：

1. 从一次 Unity/C# 的发送操作，一直解释到 Socket、TCP/UDP、IP、路由器和以太网。
2. 解释网络为什么会丢包、乱序和延迟，以及可靠传输怎样建立在不可靠服务之上。
3. 实现简化的字节流、重组器、TCP 收发端和网络接口，并能把这些机制迁移到 Unity 联机状态同步。
4. 用 Wireshark、`ping`、`traceroute`、`curl` 等工具定位常见网络问题。
5. 阅读陌生网络协议或 Unity 网络库时，能够判断它解决的问题、所在层次与代价。

## 时间规划

| 阶段 | 内容 | 理论与观察 | Lab 与复盘 | 合计 |
| --- | --- | ---: | ---: | ---: |
| 0 | 从一条消息建立网络直觉 | 7h | 5h | 12h |
| 1 | 分层、数据报与 IP 服务模型 | 13h | 9h | 22h |
| 2 | 以太网、ARP、IPv4 与路由 | 14h | 12h | 26h |
| 3 | 字节流、可靠性与 TCP 收发端 | 22h | 26h | 48h |
| 4 | 交换、排队、性能与拥塞控制 | 13h | 11h | 24h |
| 5 | Socket、DNS、HTTP 与 TLS | 12h | 8h | 20h |
| 6 | Unity 联机基础、网络身份、消息与服务器权威 | 8h | 12h | 20h |
| 7 | Unity 状态同步、网络调试与联机综合项目 | 9h | 19h | 28h |
|  | **总计** | **98h** | **102h** | **200h** |

每天 2.5 小时学习时，纯数学计算约为 80 天。实际计划按约 13～15 周安排：每周保留一天吸收、复习或处理 Lab 调试时间。总时长因 Unity 项目调试速度可能上下浮动。

## 课程怎样逐步“长出来”

我们会维护一个叫 **TinyNet** 的小型模型。它最开始只会把一个消息交给另一个程序，随后逐步增加能力：

```text
直接传消息
  ↓ 消息必须变成字节
字节流
  ↓ 数据可能分批、重叠、乱序到达
重组器
  ↓ 网络可能丢包
确认、序号、超时与重传
  ↓ 发送者不能压垮接收者
流量控制
  ↓ 所有人也不能一起压垮网络
拥塞控制
  ↓ 两台机器不一定直接相连
IP 数据报与路由器
  ↓ IP 地址还不知道局域网硬件地址
以太网、ARP 与网络接口
  ↓ 最后把所有组件接起来
一个可以运行的简化互联网
```

顺序会根据理解情况微调。真正的网络协议不是按这张图的顺序被发明的；这是为了让初学者每一步都能看见“新机制为什么需要存在”。

## 当前衔接（2026-09-22）

Lesson 15 对应已完成的 Lab 5；第 16～18 课已经把连接、排队与拥塞控制主线接通。现在一边用 Lab 6 巩固网络接口，一边先在课堂理解路由表如何产生：

1. [[18_TCP拥塞控制_慢启动与拥塞避免|第 18 课：TCP 拥塞控制——慢启动、拥塞避免与丢包回退]]已达到基本掌握。
2. [[Labs/Lab6_TinyNetworkInterface/README|Lesson 19 / Lab 6：Tiny Network Interface]] 已完成：接口自行区分直连目标与默认网关，并集成多邻居 ARP 状态、重试、有限等待队列和 Ethernet 收发；11/11 测试通过。
3. [[20_路由表从哪里来_转发平面与控制平面|第 20 课：路由表从哪里来]] 已达到基本掌握：能够区分转发与路由、数据平面与控制平面，并说明距离向量、链路状态、RIB、FIB 与路由收敛；三道边界收尾题已通过。
4. [[21_距离向量环路与计数到无穷|第 21 课：距离向量环路与计数到无穷]] 已达到基本掌握：能够区分控制平面的计数到无穷与数据平面的转发环路。
5. [[22_Unity联机第一步_两个进程与一个世界|第 22 课：Unity 联机第一步]] 开始把已有网络原理迁移到两个 Unity 进程、网络对象身份、所有权和服务器权威。
6. [[23_Unity消息_事件状态与可靠性|第 23 课：这条消息该不该重传？]] 从游戏语义出发区分命令、事件、状态，并决定消息的可靠性与新鲜度策略。

从第 22 课开始，必修 Lab 重心转向 Unity/C#。不再要求完整实现 C++ TinyRouter；它降为选修或短机制模拟。后续从空 Unity 项目逐步完成网络身份、消息、服务器权威、状态复制、插值、预测与校正。

## Lab 原则

每个 Lab 都会包含四层目标：

1. **观察**：先看到现象，例如乱序、丢包或排队。
2. **预测**：动手前先猜改变一个变量会发生什么。
3. **实现**：用最小代码补上一个机制。
4. **解释**：代码通过测试后，用自己的话或必要的代码注释说明它为什么正确。

后续项目不要求单独报告：测试结果和代码中的解释性注释就是主要证据。综合项目先由学习者参与确定状态、接口与组件边界，再从空目录逐阶段实现；只有出现以后值得回看的真实 bug 时，才可选记录“现象 → 原因 → 修复”。

主要 Lab 暂定为：

- Lab 0：极小的数据报转发网络；
- Lab 1：Tiny ARP Network Interface（ARP 状态机暖身）；
- Lab 2：有限容量字节流；
- Lab 3：乱序子串重组器；
- Lab 4：TCP 接收端；
- [[Labs/Lab5_TinyTCPSender/README|Lab 5：TCP 发送端、计时器与重传]]（已完成，11/11）；
- [[Labs/Lab6_TinyNetworkInterface/README|Lab 6：完整以太网、ARP 与网络接口]]（已完成，11/11）；
- 可选短模拟：TinyRouter 的路由表与多接口转发，预计 1～2 小时；
- [[Labs/Lab7_UnitySharedWorld/README|Lab 7：Unity Shared World]]——连接、网络身份、所有权和服务器权威，预计 4～6 小时（阶段 1 已发布）；
- Lab 8：Unity Messages——序列化、输入、事件以及可靠/不可靠交付，预计 5～8 小时；
- Lab 9：Unity Movement——快照、插值、客户端预测与服务器校正，预计 8～12 小时；
- 综合项目：Unity Multiplayer Mini Game，预计 10～15 小时。

> [!note]
> Lab 1 是当前已完成的 Ethernet/ARP 单元的实现暖身；Lab 6 会在字节流与 TCP 组件完成后，用更完整的约束把网络接口重新接入小型互联网。它们不是重复背诵，而是从“看懂状态机”到“组件集成”的两次不同难度实现。

Unity 成为后续必修实践环境，但课程仍从网络问题推导机制，不会退化成只照着 Inspector 和 API 操作的教程。第 22 课后的 Lab 7 暂缓；先完成第 23 课，把消息模型讲清后再重设计实践。
