# 第 22 课：两台 Unity 为什么不会自动共享一个世界——进程、身份、所有权与权威

> 预计时间：2～2.75 小时
>
> 场景：适合课堂阅读，不需要打开 Unity
>
> 前置：对象不能直接跨进程、序列化、端口与套接字、TCP/UDP 的基本区别

[打开交互：一次移动怎样从客户端进入权威世界](22_Unity联机状态流_交互.html)

## 本课先不安装任何网络包

假设你写了一个最普通的单机 Unity 脚本：

```csharp
transform.position += Vector3.right;
```

它能移动场景中的玩家，是因为脚本与 `Transform` 位于同一个 Unity 进程中。代码可以直接找到对象，再修改这块进程内存。

现在同时运行两份游戏：

```text
进程 A：场景里有 Player_A
进程 B：场景里有 Player_B
```

即使它们都来自同一个 Prefab，它们仍然是两个不同进程中的两个对象：

```text
Player_A 的 Transform 内存 ──只属于进程 A
Player_B 的 Transform 内存 ──只属于进程 B
```

进程 B 执行：

```csharp
transform.position = new Vector3(10, 0, 0);
```

进程 A 不会自动发生任何变化。

这就是 Unity 联机首先要解决的问题：

> 我们不是把两份游戏的内存合并，而是让不同进程通过消息，对某些重要事实逐渐达成一致。

## 1. “连接成功”离“共享世界”还很远

第 16 课已经学过，一条连接可以由四元组区分：

```text
来源 IP、来源端口、目标 IP、目标端口
```

连接建立以后，只说明两个端点之间出现了一条可以交换数据的通道。

它不会自动知道：

- 场景里哪个对象是玩家；
- 哪个客户端控制哪个玩家；
- 玩家当前位置是多少；
- 谁可以生成怪物；
- 两个人同时捡一件道具时听谁的；
- 丢包后旧位置是否还值得使用。

可以把三件事分开：

```text
连接：我能不能把字节送给对方？
会话：哪些玩家现在属于同一局游戏？
状态同步：大家怎样对游戏世界达成足够一致的认识？
```

Unity 官方的 Sessions 文档也明确区分了会话协调与游戏状态同步：Session 管理玩家、连接和一局游戏的组织，而真正的游戏状态复制由 Netcode 完成。[Unity Multiplayer Sessions 概览](https://docs.unity.com/en-us/mps-sdk/sessions-overview)

本课只处理第三件事的最小核心：**两个已经能够通信的进程，怎样共同表示一个玩家。**

## 2. 不能发送 GameObject，只能发送双方约定的信息

假设客户端想告诉服务器：“我的玩家向右移动了。”

下面这种想法不可行：

```text
把 GameObject 的引用发过去
```

原因与第 1 课相同：引用只指向当前进程的内存或 Unity 原生对象句柄。服务器进程中不存在同一个内存地址。

真正可以发送的是一份协议消息，例如：

```text
消息类型 = PlayerInput
网络对象 ID = 42
输入序号 = 17
方向 = Right
```

发送方把这些字段序列化为字节，接收方反序列化后，再在自己的世界中找到“网络对象 42”，执行对应逻辑。

因此整个过程是：

```text
本地输入
  → 生成协议消息
  → 序列化
  → 通过网络发送
  → 反序列化
  → 在远端找到对应对象
  → 修改远端自己的对象
```

网络框架可以替你完成其中许多机械步骤，但这些步骤并没有消失。

## 3. Prefab 身份和对象身份不是一回事

两个进程都有同一个 `PlayerPrefab`，只能说明它们都知道怎样创建“玩家这种东西”。

它没有回答当前场景中的具体实例是谁：

```text
PlayerPrefab：这是什么种类？
网络对象 ID：这一局中的具体哪一个实例？
```

例如：

| 网络对象 ID | 对象种类 | 当前拥有者 | 服务器记录的位置 |
| ---: | --- | --- | ---: |
| 41 | PlayerPrefab | Client 0 | 3.0 |
| 42 | PlayerPrefab | Client 1 | 8.5 |
| 90 | MonsterPrefab | Server | 12.0 |

客户端收到：

```text
ObjectId=42, Position=9.0
```

才能知道应该更新哪个实例。

如果消息只写：

```text
PlayerPrefab 的位置变成 9.0
```

那么两个玩家都使用同一种 Prefab 时，接收方就不知道指的是谁。

Unity 的 Netcode for GameObjects 使用 `NetworkObject` 为联网对象提供网络身份和生命周期。只有双方都能把网络 ID 对应到本地实例，后续状态消息才有落点。[Unity Multiplayer 入门说明](https://docs.unity.com/en-us/multiplayer/quickstarts/casual-co-op-quickstart)

## 4. Server、Client 和 Host 分别是什么

### Server

服务器承担全局协调者的角色。最小模型中，它：

- 接收客户端连接；
- 接收玩家输入；
- 检查输入是否允许；
- 推进权威游戏状态；
- 把结果发送给客户端。

服务器不一定显示画面，也不一定有人操作。

### Client

客户端主要：

- 读取本地玩家输入；
- 把请求或输入发送给服务器；
- 接收服务器状态；
- 为玩家渲染画面、声音和 UI。

### Host

Host 不是第三种神秘网络协议，而是：

```text
同一个进程中同时运行 Server 角色和一个 Client 角色
```

可以画成：

```text
Host 进程
├─ Local Server
└─ Local Client

Remote Client 进程
└─ Remote Client
```

Host 玩家发送输入时，消息可能只在本机内部走很短的路径；远端玩家的消息却必须经过真实网络。这会带来一个重要测试陷阱：

> 只测试 Host，可能感觉移动完全正常；第二个真实客户端一加入，延迟、乱序和权限问题才出现。

### Dedicated Server

Dedicated Server 是只运行服务器角色、不同时充当某个玩家客户端的独立进程。

本课程第一阶段先使用 Host，减少部署工作；但每次设计都要问：

> 如果服务器和玩家客户端不在同一个进程，这段逻辑还成立吗？

## 5. 所有权和权威不是同一件事

这是 Unity 联机最容易混淆的两个概念。

### 所有权 ownership

本课程先把所有权理解为：

> 这个网络对象与哪个客户端存在控制或身份关联？

例如：

```text
Object 41 的 owner = Client 0
Object 42 的 owner = Client 1
```

所有权可以帮助框架判断：

- 哪个客户端可以提交这个玩家的输入；
- 哪个客户端应该读取本地键盘；
- 某些只允许 owner 调用的消息是否有效。

### 权威 authority

权威回答的是另一个问题：

> 发生分歧时，谁保存或决定最终有效的状态？

采用服务器权威时：

```text
Client 1 拥有 Player 42
但 Server 决定 Player 42 的最终位置和生命值
```

因此完全合理的设计是：

```text
所有权属于客户端
权威属于服务器
```

客户端可以说：

```text
“我按下了向右键。”
```

但不能自行宣布：

```text
“我已经瞬移到 x=999，而且这就是最终事实。”
```

### 为什么要分开

如果“拥有对象”就等于“可以决定全部真实状态”，修改过的客户端就可以随意宣布：

- 自己的位置；
- 自己的血量；
- 自己捡到了什么；
- 自己是否命中敌人。

服务器权威并不能自动消灭作弊，但它至少把最终判断放在我们控制的进程中。

## 6. 一次最小的权威移动怎样发生

现在只看横轴位置。初始状态：

```text
网络对象 ID = 42
Owner = Client 1
Server 上的权威位置 = 0
Client 1 显示的位置 = 0
```

### 第一步：客户端采集输入

玩家按下向右键。客户端先形成输入消息：

```text
ObjectId = 42
InputSequence = 17
Direction = Right
```

注意：这里发送的是**输入意图**，不是最终位置。

### 第二步：消息到达服务器

服务器检查：

```text
连接它的人真的是 Client 1 吗？
Object 42 的 owner 是 Client 1 吗？
输入序号是否过旧或重复？
当前状态是否允许移动？
```

### 第三步：服务器推进权威世界

检查通过后，服务器修改自己的对象：

```text
Server：Object 42 的位置 0 → 1
```

现在 `1` 是服务器认可的状态。

### 第四步：服务器发回状态快照

服务器发送：

```text
ObjectId = 42
ServerTick = 1008
Position = 1
LastProcessedInput = 17
```

### 第五步：客户端更新显示

客户端用快照更新本地对象：

```text
Client 1：Object 42 的已知权威位置 0 → 1
```

最简单的第一版会等待服务器回复后再移动，因此操作有延迟感。以后的课程会逐步加入：

- 客户端预测：先在本地表现移动；
- 服务器校正：服务器结果不同时纠正；
- 重演：从服务器确认点重新执行尚未确认的输入；
- 插值：平滑显示其他玩家的离散快照。

本课暂时不要把这些机制揉在一起。先建立最小不变量：

> 服务器保存权威状态；客户端提交输入，并用服务器结果更新自己的世界。

## 7. 如果客户端直接改 Transform，会发生什么

假设 Client 1 直接执行：

```csharp
transform.position = new Vector3(10, 0, 0);
```

此时可能出现：

```text
Client 1 看见 x=10
Server 仍记录 x=0
Client 0 仍看见这个玩家在 x=0
```

三个世界已经分叉。

如果服务器稍后发送权威快照 `x=0`，Client 1 又把自己改回 `x=0`，玩家就会看到一次明显的“回弹”。

回弹不是网络在故意惩罚玩家，而是两个状态发生冲突后的结果：

```text
客户端预测或擅自修改的状态 ≠ 服务器认可的状态
```

以后我们会让预测与校正更加平滑，但第一步必须承认：**本地画面不一定等于全局事实。**

## 8. 每台机器到底保存了几份状态

为了以后理解预测，可以先区分三种概念：

### 权威状态

服务器认可的真实游戏状态。例如：

```text
Server：Player 42 at x=8
```

### 客户端已知的权威状态

客户端最近一次从服务器收到的状态。因为网络有延迟，它可能落后：

```text
Client：最近收到 Player 42 at x=7
```

### 客户端当前显示状态

为了让画面平滑，客户端此刻真正渲染的位置可能经过插值或预测：

```text
Client render：Player 42 at x=7.4
```

所以以后看到三个数字不同，不应立刻判断程序出错。先问每个数字代表哪一种状态。

## 9. Tick、帧和网络消息也不是同一个节奏

Unity 每秒可能渲染 60 帧，服务器可能每秒模拟 30 个 Tick，网络可能每秒只发送 20 份状态更新：

```text
渲染：60 次/秒
服务器模拟：30 Tick/秒
状态发送：20 次/秒
```

它们没有义务一一对应。

如果要求“每渲染一帧就可靠发送一次完整 Transform”，会产生两个问题：

- 带宽和消息数量迅速上升；
- 网络到达节奏仍然不稳定，接收画面未必因此更平滑。

因此联机游戏通常把模拟、发送和显示分开，再用序号、Tick、缓冲与插值连接它们。

这些将在后续 Unity 实验中逐个加入。

## 10. 以前学过的网络层并没有消失

使用 Unity 网络框架时，你可能只写一个 RPC，或修改一个可同步变量。但底下仍然要完成：

```text
游戏含义：Player 42 向右移动
  ↓
网络框架：对象身份、权限、序列化、状态复制
  ↓
传输层：可靠/不可靠交付、端口、拥塞等
  ↓
IP：尽力把数据报送到目标主机
  ↓
链路层：逐跳搬运帧
```

Unity 的高层 API 解决的是“怎样把游戏对象和消息组织起来”，不是替代 IP、UDP、端口和路由器。

当前 Unity 6 官方文档把 Netcode for GameObjects 描述为面向 `GameObject`/`MonoBehaviour` 工作流的高层网络 SDK，它运行在底层 Transport 之上。[Unity 6 Netcode for GameObjects 手册](https://docs.unity3d.com/6000.0/Documentation/Manual/com.unity.netcode.gameobjects.html)

这正适合作为我们的起点：你熟悉 GameObject 和 C#，可以先集中理解身份、所有权、权威和状态流，再逐渐向下观察 Transport。

## 11. 框架可以替我们做什么

以 Netcode for GameObjects 这一类高层框架为例，它通常能帮助处理：

- 启动 Server、Client 或 Host；
- 管理连接生命周期；
- 给网络对象分配身份；
- 在各客户端生成或销毁对应实例；
- 发送 RPC 或同步变量；
- 选择并连接底层 Transport。

Unity 官方入门资料中的几个核心角色是：

```text
NetworkManager：管理网络角色、连接和全局设置
Transport：真正搬运网络消息
NetworkObject：让某个 GameObject 具有网络身份
NetworkBehaviour：编写与网络对象相关的逻辑
```

默认常见组合是 Netcode for GameObjects 使用 Unity Transport。[Unity Multiplayer 入门说明](https://docs.unity.com/en-us/multiplayer/quickstarts/casual-co-op-quickstart)

## 12. 框架不会替我们决定什么

安装网络包后，下面这些设计仍然属于游戏开发者：

- 哪些状态必须同步；
- 哪些消息可靠，哪些允许丢失；
- 谁拥有对象；
- 谁拥有最终权威；
- 服务器怎样验证客户端输入；
- 状态以多高频率发送；
- 延迟时怎样显示；
- 断线重连后怎样恢复；
- 怎样避免把不可信数据直接用于游戏规则。

`NetworkTransform` 能帮忙同步 Transform，并不等于游戏已经拥有正确的联机架构。

例如，同步位置前仍要回答：

```text
谁有权写位置？
写的是输入还是最终坐标？
服务器是否验证？
旧更新晚到时怎么办？
远端玩家怎样平滑显示？
```

## 13. 本地直连、Relay 和 Lobby 不要混在一起

第一轮实验会让两个本地进程直接连接，避免同时引入云服务。

这些概念分别解决：

```text
本地/直接连接：两个端点怎样直接交换数据
Relay：无法或不希望直接连接时，由中继帮助传输
Lobby/Session：玩家怎样发现、加入和组织同一局游戏
Netcode：进入游戏后，状态与事件怎样同步
```

它们可以组合，但不是同一层问题。

Unity 当前推荐的 Multiplayer Services SDK 用 Session 统一协调玩家、Lobby、Relay 或服务器连接；但它不会自动替你设计游戏状态同步。[Unity Sessions 教程入口](https://docs.unity.com/en-us/mps-sdk/build-sessions-landing)

所以我们的顺序是：

```text
先在本机让两个进程共享最小世界
  → 再人为加入延迟和丢包
  → 再讨论跨公网连接、Relay 和 Session
```

## 14. 第一个 Unity Lab 将怎样设计

下一次电脑旁实践暂定名为：

```text
Lab 7：Unity Shared World
```

它不会让你填一堆已经设计好的函数。我们会先共同确定最小组件：

```text
NetworkBootstrap：启动 Host 或 Client
PlayerInputSource：只读取本地拥有者的输入
PlayerCommand：表达输入意图
AuthoritativePlayer：服务器推进的位置状态
PlayerView：在每个客户端显示收到的状态
```

第一阶段验收目标只有这些：

1. 两个独立运行实例可以在本机连接；
2. 每个玩家实例具有可观察的网络 ID 和 owner；
3. 一个客户端不能读取另一个玩家的本地输入；
4. 客户端发送输入，服务器决定位置；
5. 两边最终显示相同的权威位置；
6. 日志能显示角色、Client ID、Object ID 和消息阶段。

暂时不加入：

- 登录账号；
- Lobby；
- Relay；
- 匹配系统；
- 复杂角色控制器；
- 动画；
- 客户端预测。

这些不是不重要，而是它们会遮住第一阶段真正要观察的状态流。

## 15. 为什么不直接照官方样例做完

官方样例适合快速看到结果，但课程还需要回答“为什么”。

如果一步安装样例后就看到两个胶囊移动，很容易把下面的工作都看成一个黑箱：

```text
连接建立
对象生成
身份映射
所有权判断
输入发送
服务器更新
状态复制
客户端显示
```

我们的 Lab 会让每一步都有日志和可观察状态。最终仍使用成熟框架，但不会只记 Inspector 操作。

## 16. 常见混淆

### 混淆一：两个客户端使用同一个 Prefab，所以对象本来就是同一个

Prefab 是创建对象的模板。每个进程创建的是自己的实例，必须通过网络 ID 对应起来。

### 混淆二：已经连接，所以 GameObject 会自动同步

连接只提供通信通道。对象身份、消息含义和同步规则仍需由网络框架与游戏协议定义。

### 混淆三：Owner 就是最终权威

Owner 可以代表“由哪个客户端控制”，最终状态仍可以由服务器决定。

### 混淆四：Host 玩家和远端玩家经历完全相同的路径

Host 的 Server 与 Client 在同一进程中，路径更短，容易掩盖真实网络延迟和权限错误。

### 混淆五：客户端只能显示服务器最后一份状态，不能提前表现

以后可以加入预测。但预测是显示和响应策略，不代表客户端取代服务器权威。

### 混淆六：用了 NGO，就不需要理解 UDP、端口或延迟

高层框架仍建立在 Transport 和真实网络之上。底层性质会继续影响消息是否及时、是否到达以及花费多少带宽。

## 17. 本课最小模型

先记住这条主线：

```text
两个 Unity 进程各自保存自己的世界
        ↓
连接只允许它们交换字节
        ↓
网络 ID 把远端消息对应到本地对象
        ↓
所有权说明对象与哪个客户端关联
        ↓
客户端发送输入意图
        ↓
服务器推进权威状态
        ↓
服务器把状态复制给各客户端
        ↓
客户端用已知状态生成画面
```

如果这条链能说清，后面遇到 RPC、同步变量、NetworkTransform、预测和插值时，就能知道它们分别补在哪个位置。

## 检查站

请先遮住正文，再用自己的话回答。

1. 两台电脑都从同一个 `PlayerPrefab` 创建玩家，为什么其中一台直接修改 `Transform`，另一台不会自动变化？
2. 为什么不能把 `GameObject` 引用直接发送给服务器？一份“向右移动”的输入消息至少可以包含哪些信息？
3. `PlayerPrefab` 与网络对象 ID 分别回答什么问题？为什么只知道 Prefab 不够？
4. 分别用一句话说明 Server、Client 与 Host。为什么只在 Host 上测试可能掩盖问题？
5. 所有权与权威有什么区别？请给出“客户端拥有玩家、服务器拥有权威”的具体含义。
6. 按顺序写出一次服务器权威移动：本地输入、输入消息、服务器检查、权威状态、状态快照、客户端显示。
7. Client 1 擅自把自己改到 `x=10`，服务器仍记录 `x=2`。如果服务器权威，下一份状态到达后最简单的客户端会怎样表现？
8. 连接、Session/Lobby 与状态同步分别解决什么问题？
9. 一款游戏渲染 60 帧/秒，是否意味着必须发送 60 份可靠位置消息？为什么？
10. 从 `PlayerInput` 一直向下写到链路层，Unity 网络框架、Transport、IP 和 Ethernet 分别位于这条路径的大致什么位置？

附加迁移题：如果一个客户端拥有宝箱对象，但服务器拥有权威，客户端发来“宝箱已打开并掉落传说武器”的消息时，服务器应该直接接受结果，还是只接受“尝试打开宝箱”的请求？为什么？

## 资料边界

- 本课使用 Netcode for GameObjects 作为后续实践起点，是因为它面向你熟悉的 `GameObject` 和 `MonoBehaviour` 工作流；这不是说其他 Unity 网络方案都不值得学习。
- 当前 Unity 6 文档列出的 NGO 版本与旧版 Unity 可用版本不同。正式 Lab 会先确认你实际使用的 Unity 版本，再固定包版本和代码；本课概念不依赖具体版本。
- 本课中的 ownership 与 authority 是用于建立清晰模型的课程定义。不同框架的具体命名和权限选项可能不同，阅读 API 时要回到“谁能发请求、谁决定最终事实”这两个问题。
- 本课暂不处理预测、插值、回滚、命中判定和作弊对抗，只为这些机制建立共同地基。

## 下一步

- 完成本课检查站；
- 告诉我你准备实验时使用的 Unity 编辑器版本；
- 随后从空 Unity 项目开始 Lab 7：Unity Shared World。
