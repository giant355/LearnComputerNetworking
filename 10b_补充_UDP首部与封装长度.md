# Lesson 10B：UDP 首部与封装长度补充

> [!info] 为什么只补这一小段
> 第 10 课的端口解复用、UDP 消息边界和游戏消息取舍已经理解。现在只补两个不会自然从直觉中留下来的细节：**UDP 首部为什么有四个字段**，以及**发送端封装时长度为什么逐层增加**。

预计用时：15～25 分钟。

---

## 1. 不先背字段，先看接收方缺什么

客户端从临时端口 `53000` 向服务器端口 `7777` 发送一份应用消息。UDP 要完成自己的工作，至少要回答四个问题：

```text
回复应该发回哪个端口？       → 来源端口
当前主机交给哪个套接字？     → 目标端口
这份 UDP 数据报一共多长？   → 长度
途中是否出现可检测的比特错误？→ 校验和
```

所以 UDP 首部依次包含：

```text
来源端口 | 目标端口 | 长度 | 校验和
```

可以把它压成一句有含义的话：

> 从哪里来、到哪里去、一共多长、途中坏没坏。

这只是回忆入口，真正理解仍是每个字段解决了哪个困难。

### 目标端口与校验和不要混在一起

- **目标端口**参与解复用：操作系统用它帮助查找接收套接字。
- **校验和**参与检错：接收方发现不匹配时丢弃损坏的数据报。

校验和不会选择应用，也不会自动修复、确认或重传。

### 与 Ethernet FCS 的联系和区别

二者都能帮助发现某些比特错误，发现错误后通常都是丢弃，而不是自动修复。

但它们属于不同层、覆盖不同包装：Ethernet FCS 服务于当前链路上的帧；UDP 校验和跟随 UDP 数据报跨越正常的 IP 转发路径。当前只需掌握这个层次边界，不必计算校验和值。

---

## 2. 封装是在外面加包装

假设应用已经交给 UDP `200` 字节。这个 `200` 不包含 UDP、IP 或 Ethernet 的控制信息。

发送端的变化是：

```text
应用数据                         200 字节
  + UDP 首部                       8 字节
= UDP 数据报 / UDP 长度字段       208 字节
  + IPv4 首部                     20 字节
= IP 数据报                       228 字节
  + Ethernet 头尾控制信息         18 字节
= Ethernet 帧                     246 字节
```

箭头所表达的方向很重要：

```text
发送端封装：应用数据 → UDP 数据报 → IP 数据报 → Ethernet 帧
              每向外一层，增加该层的控制信息

接收端解封装：Ethernet 帧 → IP 数据报 → UDP 数据报 → 应用数据
              每向内一层，检查并去掉该层包装
```

所以“去掉链路头尾”只能用于从已经收到的 Ethernet 帧反推里面的内容，不能用于从应用数据计算将要发送的帧。

### 一个快速自检

如果题目说“应用数据为 200 字节”，那么完整 Ethernet 帧不可能仍是 200 字节，因为还没有给各层首部和尾部留下空间。

所有控制开销合计为：

```text
8 + 20 + 18 = 46 字节
```

因此总长度也可以一步算出：

```text
200 + 46 = 246 字节
```

但分三步算更能检查自己是否理解每层的数据单位。

---

## 收尾迁移题

请合上上文，用自己的话回答。直接写在这里或聊天中均可。

1. 不看正文，写出 UDP 首部的四个字段。分别指出哪个用于接收套接字解复用，哪个用于检错；检错失败后 UDP 是否自动重传？

来源端口，目标端口，长度，校验和
目标端口
不

2. 应用数据为 `350` 字节，UDP、IPv4、Ethernet 控制信息仍分别为 `8`、`20`、`18` 字节。依次计算：

   - UDP 长度字段；358
   - IP 数据报长度；378
   - Ethernet 帧长度。396

3. 现在反过来：收到一份总长 `546` 字节的 Ethernet 帧，已知 Ethernet 控制信息为 `18` 字节、IPv4 首部为 `20` 字节、UDP 首部为 `8` 字节。忽略其他开销，最终交给应用多少字节？这次为什么使用减法？

546-18-20-8
逐层解复用
