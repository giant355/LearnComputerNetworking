# 计算机网络 - 第 3 课：IP 服务模型

> 预计总时间：2–2.5 小时  
> 建议分配：阅读 55–75 分钟，手工推演 25–35 分钟，命令观察 30–45 分钟，检查站 15–20 分钟。  
> 如果你提前理解并完成任务，不需要为了凑时间等待；请在末尾记录实际用时。

这一课要回答 CS144 开头最重要的问题之一：

> **互联网最核心的 IP 层，究竟答应为上层做什么？**

答案看起来很弱：IP 只提供一种**无连接、尽力而为的数据报服务**。

这不是互联网设计者能力不足。恰恰相反，正是这个克制的承诺，让极其不同的应用和底层网络能够共同使用同一个互联网。

## 学习目标

本节结束后，你应该能够：

1. 用自己的话解释“数据报”“无连接”和“尽力而为”；
2. 列出 IP 不保证的事情，并理解为什么可靠性仍然能够实现；
3. 说明一个 IP 数据报经过路由器时发生什么；
4. 解释最终目标 IP 与当前下一跳链路地址为什么同时存在；
5. 解释 TTL、队列和丢包之间的关系；
6. 解释域名为什么可能对应多个 IP 地址；
7. 用一个小型 C++ 模型描述路由器最核心的转发行为。

---

## 0. 先补上没有讲清楚的 DNS 多地址

上次复习中，你明确指出：“这个好像没有在课程正文里讲过，或者名词看不懂。”这个判断是对的。我们先不用 CDN、负载均衡之类的词，重新从头建立。

### 0.1 名字与地址解决的不是同一个问题

假设你想联系朋友小明：

- “小明”是你容易记住的名字；
- 电话号码是通信系统实际用来找到某个联系方式的数字。

互联网上也有类似分工：

- `example.com` 是**域名**，是给人和应用使用的名字；
- `93.184.216.34` 这类数字是 IP 地址，网络用它帮助数据朝目标主机前进；
- DNS 是把域名查询成一个或多个地址的系统。

这个类比有一个重要区别：域名通常代表的是一项**服务**，不一定代表某一台永远固定的电脑。

### 0.2 为什么一个名字会得到多个地址？

假设一个网站每天有很多人访问。如果所有请求都只能去一台机器：

- 这台机器可能忙不过来；
- 机器故障后，整个网站都会消失；
- 离这台机器很远的用户可能等待更久。

因此，一个域名可以对应多台服务器的地址：

```text
game.example.com
        │
        ├── 地址 A：服务器 A
        ├── 地址 B：服务器 B
        └── 地址 C：服务器 C
```

这样可以把请求分给不同服务器，也可以在一个地址不可用时尝试另一个地址。

另外，互联网正在同时使用两代 IP：

- IPv4 地址通常写成 `93.184.216.34`；
- IPv6 地址更长，通常包含冒号，例如 `2606:2800:...`。

同一个域名可能同时返回 IPv4 和 IPv6 地址，所以 `nslookup` 中出现多个地址并不奇怪。

### 0.3 程序拿到多个地址后做什么？

简单理解即可：程序选择一个合适地址尝试连接；如果失败，可能尝试列表里的另一个。现代系统也可能让 IPv6 与 IPv4 的尝试短暂交错，尽快使用先成功的路径。

我们暂时不研究精确选择算法。现在只保留这条主线：

```text
人或应用提供域名
        ↓ DNS 查询
得到一个或多个 IP 地址
        ↓ 选择并尝试其中一个
网络按照目标 IP 转发数据
```

> [!important]
> DNS 主要回答“这个名字目前对应哪些地址”；路由器回答“给定这个目标 IP，下一跳往哪里走”。DNS 不是路由协议。

---

## 1. 一个互联网必须容纳非常不同的网络

你的游戏消息可能经过：

- 家里的 Wi-Fi；
- 小区或校园里的 Ethernet；
- 运营商光纤；
- 海底光缆；
- 数据中心内部的高速网络；
- 某些地方的卫星或移动网络。

这些网络的速度、最大数据大小、错误率和硬件地址都可能不同。如果 Unity 游戏必须为每一种底层网络实现一套完整通信方式，互联网无法扩展。

我们需要在中间找到一个足够通用的共同层：

```text
网页、游戏、语音、视频、文件传输、聊天……
                    ↓
                 TCP、UDP……
                    ↓
              ┌──────────┐
              │    IP    │  ← 共同的窄腰
              └──────────┘
                    ↓
Wi-Fi、Ethernet、光纤、移动网络、卫星……
```

这个结构常被称为互联网的“沙漏形”或“窄腰”：上面可以出现许多应用和传输协议，下面可以出现许多链路技术，中间共同支持 IP。

### 1.1 共同层应该承诺多少？

如果 IP 要求每一种底层网络都必须：

- 永不丢数据；
- 永远按顺序；
- 保证固定延迟；
- 保证固定带宽；
- 为每条通信保存连接状态；

那么很多底层网络很难满足，路由器也会变得复杂。

于是 IP 选择了一份更小、更普遍的合同：

> 给我一个写有目标 IP 的数据报，我会尽力把它朝目标转发。

任何能尽量搬运一个有限数据块的链路，都有机会承载 IP。需要更强保证的端系统，再在 IP 之上构建。

---

## 2. 服务模型：一层向上层作出的合同

上一课区分了服务、接口和协议。现在把它真正用起来。

- **IP 服务**：向上提供尽力而为的数据报传递能力；
- **本机接口**：操作系统内部或 Socket 附近用于交付 IP 数据的调用入口；
- **IP 协议**：两端主机和路由器共同理解的数据报格式与处理规则。

所谓**服务模型（service model）**，就是从使用者角度描述这一层承诺什么、不承诺什么。

### 2.1 数据报是什么？

数据报可以理解为一个有明确边界、自己携带投递信息的数据块：

```text
┌────────────────────────────┐
│ IP 头部                    │
│ 来源、目标、TTL、上层类型……│
├────────────────────────────┤
│ 载荷：TCP、UDP 或其他数据   │
└────────────────────────────┘
```

它不像“无边界的连续水流”。发送两个 IP 数据报，接收方看到的仍是两个独立数据报；至于更上层是否把它们重新组织成字节流，是传输层的工作。

每个数据报都包含目标 IP，所以路由器可以逐个处理：

```text
D1 → 查 D1 的目标 → 选择下一跳
D2 → 查 D2 的目标 → 选择下一跳
D3 → 查 D3 的目标 → 选择下一跳
```

### 2.2 无连接是什么意思？

在 IP 层发送数据报前，不要求先与远端建立一条 IP“通话连接”，也不要求沿途路由器为这次通话预留一条固定路线。

因此可以直接发送：

```text
数据报 D1：目标 B
数据报 D2：目标 B
```

路由器通常不会因为 D1 和 D2 来自同一个游戏，就必须保存“这是第几条游戏连接”的逐连接状态。它主要根据每个数据报自身的头部和当前路由表转发。

> [!warning]
> 无连接不等于“没有网线、Wi-Fi 或物理连接”，也不等于路由器完全没有状态。路由器需要路由表、邻居信息和队列；无连接主要指 IP 不先建立端到端会话并为每条会话保留固定路径。

### 2.3 尽力而为意味着什么？

IP 网络会按照协议和路由表认真尝试转发，但它不向上层保证结果。

IP **不保证**：

1. 数据报一定送达；
2. 多个数据报按发送顺序到达；
3. 数据报只到达一次；
4. 多久能够到达；
5. 能获得多少带宽；
6. 内容具有端到端的保密性或身份认证。

例如发送：

```text
D1  D2  D3
```

接收端理论上可能观察到：

```text
D2  D1  D3        // 乱序
D1      D3        // D2 丢失
D1  D2  D2  D3    // 重复
很久以后才到达      // 时延没有保证
```

“不保证”不等于这些现象每次都会发生。正常网络往往能交付绝大多数数据，只是上层不能把成功当成合同的一部分。

---

## 3. 为什么一个好网络反而承诺得这么少？

### 3.1 底层越容易满足，互联网越容易扩展

只要一种链路能够尽力搬运 IP 数据报，它就能接入互联网。链路可以快或慢、稳定或抖动、有线或无线。

### 3.2 故障时可以局部处理

链路中断或路由变化后，后续数据报可以根据新路由继续转发。IP 没有强迫每条通信绑定一条永远不变的预留路线。

### 3.3 路由器可以把精力放在快速转发

互联网核心需要处理海量流量。如果每台路由器都要理解每个 Unity 游戏、记录每个字节是否确认并替所有应用重传，规模会迅速失控。

### 3.4 端系统更清楚应用真正需要什么

文件下载通常需要每个字节准确到达；实时位置更新可能宁可丢掉旧位置，也不愿为它等待很久。

让端系统选择可靠方式，可以满足不同应用：

```text
可靠、有序需求 → TCP 等机制在端系统补上
独立、及时的数据报需求 → UDP/应用自行决定怎样处理
```

这体现了一个重要设计思想：很多只有通信端点才能正确完成的功能，应该主要由端点负责。后面会进一步讨论端到端原则。

---

## 4. 一个 IP 数据报怎样经过路由器

假设电脑 A 要给服务器 B 发送数据，中间经过家庭路由器 R1 和运营商路由器 R2：

```text
A ── R1 ── R2 ── B
```

### 4.1 A：先确定下一跳

A 的 IP 数据报写着最终目标 B：

```text
IP 来源 = A
IP 目标 = B
```

但 A 的网卡不能直接把链路帧送到远方的 B。当前链路上能直接联系的是家庭路由器 R1，所以最外面的链路包装写：

```text
链路来源 = A 的接口
链路目标 = R1 的接口
载荷 = 目标为 B 的 IP 数据报
```

### 4.2 R1：取出 IP 数据报

R1 收到给自己的链路帧后：

1. 检查并移除当前链路包装；
2. 查看里面 IP 数据报的最终目标 B；
3. 减少 TTL；
4. 查询路由表，选出下一跳 R2 和出口接口；
5. 将同一个 IP 数据报装入适合下一段链路的新帧；
6. 把帧放入出口队列，等待发送。

新的链路包装变成：

```text
链路来源 = R1 的出口接口
链路目标 = R2 的接口
载荷 = 目标仍然为 B 的 IP 数据报
```

### 4.3 R2：重复逐跳工作

R2 再次取下旧链路包装、查看目标 IP、减少 TTL、查询路由表，然后为 R2→B 这一段创建新包装。

因此通常有：

| 信息 | 经过路由器时怎样变化 |
| --- | --- |
| 最终目标 IP | 通常保持为 B |
| 来源 IP | 通常保持为 A，但 NAT 等设备可能改写；以后再讲 |
| TTL | 每经过一个路由器减少 |
| 链路来源/目标 | 每一跳都按当前链路重新填写 |
| 应用载荷 | 普通路由转发不需要理解其业务含义 |

---

## 5. TTL：让走错路的数据最终停下来

路由表也可能配置错误。假设 R1 认为目标应该交给 R2，而 R2 又认为应该交回 R1：

```text
R1 → R2 → R1 → R2 → ……
```

如果没有限制，数据报会永远循环，占用带宽和队列。

IPv4 头部有 TTL 字段。它的名字是 Time To Live，但现实转发中主要表现为**跳数上限**：每个路由器至少把它减 1；减到 0 就不再转发，并通常返回一个 ICMP“超时”消息。

例如初始 TTL 为 3：

```text
发送时：3
经过 R1：2
经过 R2：1
下一台路由器再转发前：0 → 丢弃
```

`tracert` 正是巧妙利用这个规则：

1. 先发送 TTL=1 的探测，第一台路由器令其到 0，并返回消息；
2. 再发送 TTL=2，第二台路由器返回消息；
3. 逐渐增加 TTL，于是一步步发现路径上的若干路由器。

这也解释了为什么 `tracert` 的星号不一定代表断网：某台路由器可能继续转发普通数据，只是不返回或限制返回这种探测消息。

---

## 6. 路由器为什么会丢包

路由器收到数据后，通常不能瞬间把所有数据都送出。出口链路有有限带宽，因此数据会先进入队列。

假设出口每个时间单位只能发送一个数据报，队列最多存两个：

```text
某一瞬间同时到达：D1 D2 D3
队列容量：          [ ][ ]
```

D1、D2 进入队列后，D3 没有位置。路由器可能直接丢弃 D3。

这里同时出现两个现象：

- **排队延迟**：D2 必须等 D1 发送；
- **丢包**：队列装满后，新来的 D3 无处存放。

增加无限缓冲区也不是完美答案。队列过长会让数据等待很久；对实时游戏来说，一个两秒后才到达的旧位置，可能和丢失一样没有价值。

后面学习拥塞控制时，我们会研究端系统怎样根据丢包与延迟推测网络过载，并主动调整发送速度。

---

## 7. IPv4 头部目前需要认识什么

不要求背位数。先认识字段存在的理由：

```text
┌──────────────────────────────────────────┐
│ 版本 | 头部长度 | 总长度                 │
│ 标识、分片控制、分片位置                 │
│ TTL | 上层协议 | IPv4 头部校验和         │
│ 来源 IPv4 地址                           │
│ 目标 IPv4 地址                           │
│ 可选项                                   │
├──────────────────────────────────────────┤
│ 载荷：例如 UDP 数据报或 TCP 段           │
└──────────────────────────────────────────┘
```

| 字段 | 为什么需要 |
| --- | --- |
| 版本 | 告诉解析者这是哪一版 IP 格式 |
| 总长度 | 确定整个数据报有多少字节 |
| 分片相关字段 | IPv4 必要时可以把数据报分成片段；后面再深入 |
| TTL | 防止路由环路让数据永久存在 |
| 上层协议 | 解复用给 TCP、UDP、ICMP 等处理逻辑 |
| 头部校验和 | 检查 IPv4 头部是否损坏；它不覆盖载荷 |
| 来源与目标地址 | 标识来源和最终目标 |

因为路由器会修改 TTL，IPv4 头部校验和也需要随之更新。

IPv6 的头部设计有所不同，例如把 TTL 类似字段称为 Hop Limit，并移除了 IPv4 头部校验和。现在重点不是比较版本，而是理解每个机制服务的目的。

---

## 8. IP 不可靠，TCP 为什么可以可靠？

“下层不保证”不等于“上层不可能实现”。

假设 A 给每段数据编号：

```text
1: HEL
2: LO
```

B 收到后进行确认：

```text
收到 1，请继续
```

如果 A 长时间没有收到编号 2 的确认，就重新发送；如果 B 先收到 2、后收到 1，可以先保存并重新按序组合；如果收到两个编号 2，可以识别重复。

于是端系统可以在不可靠 IP 上增加：

- 序号；
- 确认；
- 超时；
- 重传；
- 乱序缓存；
- 重复抑制。

这正是后面 TCP Lab 的主线。

```text
应用看到：可靠、有序的字节流
              ↑
TCP：序号、确认、重传、重组……
              ↑
IP：尽力而为的独立数据报
```

UDP 提供端口和校验等较薄的传输能力，但不替应用建立 TCP 式可靠字节流。因此，实时应用可以根据自身需要选择：旧位置包丢了也许无需补发，关键的匹配结果则可能需要确认。

---

## 9. TinyNet：把路由器缩成一个小函数

下一步 Lab 会真正写 C++。现在先用一个极小模型预演。

```cpp
struct IPv4Address {
    std::uint32_t value;
};

struct Datagram {
    IPv4Address source;
    IPv4Address destination;
    std::uint8_t ttl;
    std::uint8_t protocol;
    std::vector<std::byte> payload;
};
```

路由器最核心的转发轮廓可以写成：

```cpp
std::optional<ForwardingResult> Router::forward(Datagram datagram)
{
    if (datagram.ttl <= 1) {
        return std::nullopt; // 不能继续转发
    }

    --datagram.ttl;

    const Route* route = routing_table_.longest_match(
        datagram.destination
    );

    if (route == nullptr) {
        return std::nullopt; // 没有可用路由
    }

    return ForwardingResult {
        .outgoing_interface = route->interface,
        .next_hop = route->next_hop,
        .datagram = std::move(datagram),
    };
}
```

先不要纠结 `longest_match`，我们后面会实现。现在观察这个函数**没有做什么**：

- 不询问这是 Unity 还是浏览器；
- 不等待远端确认；
- 不替丢失的数据重发；
- 不保证调用者一定收到结果；
- 不因为前一个数据报存在，就假设下一个数据报属于同一连接。

它只检查自己负责的头部字段，减少 TTL，查询下一跳，然后尽力交给出口。

这就是“IP 服务模型”在代码结构中的影子。

---

## 10. 手工推演：让一个数据报走三段链路

请在纸上或 Obsidian 中画：

```text
A ── R1 ── R2 ── B
```

设 A 发送的数据报：

```text
来源 IP = A
目标 IP = B
TTL = 3
载荷 = "JUMP 42"
```

### 第一步：A → R1

填写：

```text
链路来源 = ______
链路目标 = ______
IP 目标   = ______
TTL       = ______
```

### 第二步：R1 → R2

R1 取下第一段链路包装、减少 TTL、查询路由并重新包装。填写新的四项。

### 第三步：R2 → B

再次填写四项，并回答：最终到达 B 时，哪些字段从 A 开始基本没变？哪些字段每一段都不同？

### 改变一个条件

把初始 TTL 改成 2，重新推演。数据能到 B 吗？在哪一步停止？

### 再加入队列

假设 R1→R2 的出口队列容量是 2，同一瞬间来了三个数据报：

```text
D1、D2、D3
```

在最简单的“队尾丢弃”策略中，哪个可能被丢掉？D2 为什么会比 D1 多一段排队等待？

> [!tip]
> 这里没有唯一的真实网络调度策略。题目明确假设“容量 2、队尾丢弃”，是为了先隔离出队列的核心效果。

---

## 11. 实践观察：DNS、TTL 与不同协议

在 Windows PowerShell 中完成。命令可能因防火墙、网络策略或目标站点限制而失败；失败本身也是观察结果。

### 11.1 分开查询 IPv4 与 IPv6

```powershell
Resolve-DnsName example.com -Type A
Resolve-DnsName example.com -Type AAAA
```

- `A` 记录保存 IPv4 地址；
- `AAAA` 记录保存 IPv6 地址。

记录是否两种都有、每种有几个。不要粘贴本机 DNS 配置等无关信息。

### 11.2 不做名字解析地观察路径

```powershell
tracert -d example.com
```

`-d` 表示不要把沿途地址反向查成名字，通常更快，也更容易看见“路径本身使用地址”。记录大约有多少跳和多少行星号。

然后运行：

```powershell
tracert example.com
```

比较两次输出。带名字的输出不一定每一跳都有可读名称，因为有些地址没有对应的反向 DNS 名称。

### 11.3 主动制造 TTL 不足

```powershell
ping -n 1 -i 1 example.com
```

Windows `ping` 的 `-i 1` 把 TTL 设为 1。数据通常在第一台路由器就无法继续，可能看到“TTL 传输中过期”之类的消息。

再尝试：

```powershell
ping -n 1 -i 2 example.com
```

观察返回消息是否来自更远的地址。如果网络屏蔽相关 ICMP 消息，也可能只显示超时。

### 11.4 Ping 失败不等于网站不可用

```powershell
ping -n 2 example.com
Test-NetConnection example.com -Port 443
```

`ping` 主要使用 ICMP；第二个命令尝试检查到 HTTPS 常用 TCP 端口 443 的连通性。某些服务器会忽略 Ping，却仍正常接受网页连接。

因此：

> 某一种协议的请求没有回应，不足以单独证明整台主机或所有服务都不可用。

### 我的实践记录

| 任务 | 观察结果 |
| --- | --- |
| A 记录数量 |  |
| AAAA 记录数量 |  |
| `tracert -d` 大约跳数、星号 |  |
| TTL=1 的结果 |  |
| TTL=2 的结果 |  |
| Ping 是否成功 |  |
| TCP 443 测试是否成功 |  |

---

## 12. 与 Unity 的最小联系

假设游戏连续发送位置：

```text
t=100：位置 (10, 5)
t=101：位置 (11, 5)
t=102：位置 (12, 5)
```

如果 `t=100` 的旧位置丢失，但 `t=101`、`t=102` 已到达，应用不一定值得等待重传旧位置。对这种信息，“最新值”可能比“每个历史值都到达”更重要。

但如果消息是：

```text
本局获胜者 = 玩家 42
```

应用可能必须确认它被正确处理。

所以“游戏使用 UDP”从来不等于“游戏不需要可靠性”；更准确地说，是游戏可能希望针对不同消息自己决定：

- 哪些可以过期；
- 哪些需要确认；
- 哪些需要保持顺序；
- 重传多久之后就没有价值。

在做这些选择之前，先理解 IP 不承诺什么，才能知道应用或传输层必须补上什么。

---

## 13. 常见误解

### “尽力而为就是随机乱发”

不是。路由器按照路由表和协议认真转发。“尽力而为”描述的是没有端到端结果保证。

### “无连接就是没有建立 Wi-Fi 连接”

不是。Wi-Fi 链路可能已经关联；IP 的无连接指发送数据报前不建立并保留一条 IP 端到端会话。

### “目标 IP 能找到具体 Unity 进程”

不够。IP 主要帮助到达主机，UDP/TCP 端口与 Socket 绑定继续帮助操作系统解复用。

### “Ping 不通就是服务器离线”

不一定。服务器或中间防火墙可以阻止 ICMP，却允许 TCP/HTTPS 或游戏使用的其他协议。

### “DNS 负责替每个数据报选择下一跳”

不是。DNS 把名字查询成地址；路由器根据目标地址和路由表逐跳转发。

### “路由器把数据交给下一跳后，下一跳一定收到了”

不一定。把数据交给本机接口、发上链路、下一跳接收、最终主机接收，是不同事件。

## 知识锚点

- [RFC 1122](https://www.rfc-editor.org/rfc/rfc1122.html) 将 IP 描述为无连接的数据报网际服务，并说明可靠交付可由端到端传输协议负责。
- [RFC 791](https://www.rfc-editor.org/rfc/rfc791.html) 定义了 IPv4 数据报格式、地址、TTL、协议字段与分片等基础机制。
- [RFC 1034](https://www.rfc-editor.org/rfc/rfc1034.html) 描述 DNS 的概念与设施；本课只使用“名字查询成地址”这一入门视角。
- Stanford CS144 的课程主线从数据报、封装和不可靠服务出发，再构建可靠传输：[CS144 官方课程页](https://cs144.github.io/)。

## 小结

1. IP 提供无连接、尽力而为的数据报服务。
2. IP 不保证送达、顺序、唯一、时延或带宽；需要的可靠性由端系统继续构建。
3. 每个数据报独立携带最终目标 IP；路由器减少 TTL、查询路由并重新进行链路封装。
4. 最终目标 IP 面向整段旅程，链路地址面向当前一跳。
5. 队列带来等待，队列装满可能丢包；无限缓冲也会制造巨大延迟。
6. 域名可以对应多个 IPv4/IPv6 地址；程序选择并尝试地址，路由器再按选中的 IP 转发。
7. IP 的简单服务形成互联网的共同“窄腰”，让多种应用与底层链路能够互通。

---

## 检查站

请不要查原文，先用自己的话回答。

1. “无连接、尽力而为的数据报服务”中的三个部分分别是什么意思？
2. 请列出至少四项 IP 不保证的事情。既然不保证，TCP 为什么仍然可能给应用可靠、有序的字节流？
3. 域名和 IP 地址分别解决什么问题？一个域名为什么可能返回多个地址？程序得到多个地址后大致怎样处理？
4. 在 `A → R1 → R2 → B` 中，哪些地址或字段通常逐跳改变，哪些信息通常继续指向最终 B？
5. 初始 TTL=2 的数据报需要经过 R1、R2 两台路由器才能到 B。逐步写出 TTL 的变化，并判断能否到达。
6. 一个路由器的出口每秒只能发送 1 个数据报，队列最多保存 2 个；某一瞬间同时到达 D1、D2、D3。最简单的队尾丢弃下会发生什么？延迟从哪里出现？
7. 为什么 `ping` 失败不能单独证明网页或游戏服务器不可用？
8. 对 Unity 中不断更新的位置与“最终比赛结果”两类消息，你会怎样看待丢包和重传？暂时说设计直觉即可。

同时告诉我实践观察：

- A 与 AAAA 查询各得到几个地址；
- `tracert -d` 大约多少跳、有无星号；
- TTL=1 与 TTL=2 分别观察到什么；
- Ping 与 TCP 443 测试是否成功；
- 手工推演中 TTL=3 和 TTL=2 各自是否到达 B。

最后记录本课实际时间：阅读、手工推演、命令观察和检查站各用了多久。以后课程将按你的真实速度调整。

请把答案和观察结果发给我。通过后，我们进入首个 C++ Lab：实现一个极小的 IP 数据报转发网络。

---

## 我的回答、推演与观察

> 在这里填写检查站、命令输出摘要和实际用时；不要粘贴包含隐私的完整网络配置。

### 我的概念检查回答（2026-09-01）

1. 无连接：IP 不会建立、保存端到端的传输链路。尽力而为：努力把每个数据报送到目标，仍然可能丢失。数据报：IP 数据报，大致由上层数据和包含来源/目标等信息的 IP 头部组成。
2. IP 不保证有序、不重复、不丢失；第四项当时没有想到。TCP 会在必要时补发、排序、丢弃重复。
3. 域名是给人看的网站名字，IP 地址是具体服务器地址。一个网站可能有多个服务器；得到地址后先尝试其中一个，不行再换另一个。
4. 逐跳改变的是链路头尾；IP 来源和目标通常继续指向端点。
5. TTL 逐级减一，初始为 2 时不能到达 B，少一跳。
6. 最后到达的 D3 可能被丢弃；延迟来自在队列中累计等待。
7. 有的目标会忽略 Ping。
8. 位置只需要最新值，之前的丢了也可以；最终比赛结果必须完整准确。

课堂环境无法运行命令实验，因此没有凭想象填写实践结果，待有电脑环境时补做。

### 教师反馈（2026-09-01）

总体判断：**基本掌握，允许进入 Lab 0；命令实践保留为待补任务**。

- 第 1 题正确。数据报的载荷不一定也是“数据报”，更通用的说法是：IP 头部加上一段上层载荷。
- 第 2 题已经列出送达、顺序和不重复三项。还应记住 IP 不保证时延、带宽，也不提供端到端保密或身份认证。TCP 通过序号、ACK、计时器、重传、乱序缓存和重复抑制建立可靠有序字节流。
- 第 3 题主线正确。域名不只代表网站，也可以代表其他网络服务；程序还可能交错尝试 IPv4/IPv6，而不一定严格逐个等待。
- 第 4 题中，链路头尾逐跳更换，来源/目标 IP 通常保持；还漏了 TTL 每跳减少，IPv4 头部校验和也因 TTL 改变而重算。
- 第 5、6、8 题正确。TTL=2 时：R1 将 2 减到 1并转发，R2 将 1 减到 0 后丢弃，无法到 B。容量为 2 时 D3 可能队尾丢弃，D2 因等待 D1 发送产生排队延迟。
- 第 7 题方向正确，但更精确的是目标主机或中间防火墙可能不回应 ICMP Echo；网页服务使用的 TCP 443 等其他协议仍可能正常。
- 实践无法在课堂完成不构成错误。实际结果不能用“预想结果”替代观察，之后补做即可。
