# 第一阶段结课检查：用证据把整条网络链讲通

> 前置：读完并完成 [[32_完整网络旅程与故障定位]] 的检查站。
> 预计 2～3 小时，包含回答、反馈和仅针对缺口的补充。不是再来一套大 Lab，也不是背名词考试。
> 可以聊天逐题答，每次 3～4 题；也可直接写在本文件中。不会要求补写实验报告。

## 怎样使用

第一遍先不翻正文。每道题说清“谁做什么、依据哪里来的信息、最多能推出什么”。不确定时直接说不知道，或者指出自己在哪一步失去把握。

允许用图、箭头、数字和短句，不要求复述教材措辞。回答短不等于不会；若省略了关键因果，只追加一个具体追问，不因为术语不熟就让你重学整章。

这是检查文件，不预填标准答案。后续反馈写在你的原答之后。课程已经发布，不等于这些检查已完成。

## 第一组：消息怎样离开这台电脑

设玩家 A 为 `192.168.10.20/24`，默认网关为 `192.168.10.1`。同网段 B 为 `192.168.10.30`；公网服务 S 用 `198.51.100.20:7777` 表示。

1. A 分别向 B 和 S 发送 IPv4 数据报。两个当前下一跳分别是谁？需要通过 ARP 查谁的 MAC？如果不知道 S 的 MAC，能否仍然发送给 S？
2. A 发往 S 的帧先到交换机，再到家用路由器。交换机依据什么转发？路由器依据什么选出口？分别指出它们查的表。交换机不知道目标 MAC 时，为什么不是广播 ARP 的同一个过程？
3. 经过普通路由转发时，哪些链路字段会更换，哪些 IP 信息仍指向最终目标？如果路由器同时做家庭 NAPT，又有哪些字段可能被改写？不要把 NAT 的变化说成所有路由器都必做。
4. IP 不保证无丢失与有序，为什么 TCP 仍能提供可靠有序字节流？如果一次发送 API 返回成功，你最多能先确认哪一类本地事实，而不能立即确认什么业务结果？

## 第二组：字节怎样变成正确的业务消息

5. C# 连续写入两条消息。服务端第一次读取只拿到第一条的一半，第二次拿到剩余部分和完整第二条。为什么这不违反 TCP？谁负责恢复消息边界？
6. 重组器缺少 `[0,3)`，但已经收到 `[3,6)` 的 `DEF`。为什么不能先向应用交付 `DEF`？它应暂存在哪里？若后续 `[0,3)` 到达，会发生什么？使用此前教学模型回答即可。
7. 接收窗口 rwnd=6000，拥塞窗口 cwnd=4000，已发未累计确认量为3000。按课程简化模型还能发多少新字节？收到新累计 ACK 多确认1000字节，两窗口不变且尚未继续发送，此时额度是多少？相同 ACK 再到一次会再次释放额度吗？
8. 请求 A 先发、B 后发。自定义双向协议允许先完成 B 并先发它的响应。客户端怎样正确关联？同一 HTTP/1.1 连接的流水线最终响应能否照搬这个任意顺序？
9. 购买 P 已在服务器执行，但响应未到客户端。TCP 的可靠传输、requestId 关联、业务去重分别能做什么？重连后“没看到成功，所以新建订单再买”为什么危险？

## 第三组：怎样到达并信任一个服务

10. 应用要访问 `https://api.example.com/rooms`。本机存根、递归解析器、权威服务器各做什么？如果已有有效缓存，还必须从根查起吗？
11. 房主本机连 `127.0.0.1:7777` 成功，外地朋友连房主的私有地址失败。指出至少两项需要补充确认的信息；为什么只把域名改正确仍不一定解决？
12. TCP 已建立，但证书不适用于正在访问的主机名。为什么不能因为“已经连到 DNS 返回的 IP”就忽略这个错误？证书验证与游戏登录又有什么区别？
13. 一个经过正常登录的玩家发送移动别人角色的请求。消息加密完整、格式正确。为什么仍应拒绝？应依据哪个可信来源判断请求者是谁？

## 第四组：发生故障时怎样缩小范围

下面每题都回答三件事：**已知什么；不能据此断定什么；下一步做哪项检查以及为什么。** 不要求实际运行命令。

14. DNS 查询成功，连接目标 TCP 端口超时。给两个不同层面的候选原因，再选一个有区分力的检查。
15. `ping` 失败，但 HTTP 得到 403。能否说服务器完全不可达？现在优先检查链路带宽还是账号权限，为什么？
16. WebSocket 重新建立成功，玩家却看见已经离开的旧成员。为何 TCP 再可靠也不能自动补齐旧连接期间的业务状态？设计一个基于快照的恢复步骤。
17. 最后处理的业务版本是 70，重新获取到版本 75 的快照，随后又到达版本 73 的旧事件。客户端应如何处理？这里的版本与 TCP SEQ 有什么区别？
18. 请求变慢：DNS 10 ms、TCP 30 ms、TLS 30 ms、等待响应开始1500 ms、正文接收20 ms。最需要进一步拆查哪一段？为什么还不能只凭这个时间断言“数据库慢”？

## 第五组：一条新的完整旅程

19. 自选“查询房间”或“购买物品”，从游戏拿到配置中的域名开始，讲到客户端 UI 更新。按需要涵盖 DNS、Socket、路由与下一跳、TLS、应用协议、业务处理。不要为了凑词把所有机制都写成每次重新执行。
20. 同一游戏得到 IPv4 和 IPv6 地址，IPv4 连接成功、IPv6 失败。为什么不能由前者推断后者也正常？选择一项 IPv6 路径或监听相关的检查，并说明此前哪些 IPv4 细节不能直接照搬。

## 反馈怎样决定下一步

| 证据 | 处理方式 |
| --- | --- |
| 能独立解释机制并迁移到新情境 | 标记掌握，进入后续框架阅读 |
| 主线正确，少量遗漏经具体追问即可补全 | 标记基本掌握，只安排短回忆 |
| 两种机制反复混用，无法解释某一步由谁执行 | 只补最短的概念桥梁，再换一个情境复测 |
| 只能记名词，不能预测行为 | 对应单元回到具体追踪，不强行进入复杂源码 |

这些等级不对应行业排名。计算题笔误、题干歧义、课程没铺垫清楚与真正的概念误区分开处理。

## 针对性补充的回看入口

下表是**答题后按缺口选一项**，不是要求全部重读。

| 暴露的缺口 | 最小回看范围 |
| --- | --- |
| 把同网段下一跳写成网关 | [[06b_补充_前缀范围与下一跳]] 的直连与网关情境 |
| MAC 表、ARP 缓存、IP 路由表混在一起 | [[08_ARP与网络接口]] 的三种表职责 |
| pending、ByteStream、Reader 混淆 | [[12_乱序子串重组]] 的接收数据流 |
| rwnd、cwnd、flight 混淆 | [[17_排队_瓶颈与拥塞窗口]] 的额度计算 |
| 读取次数当消息边界 | [[24_TCP消息边界与长度前缀]] 的增量拆帧 |
| requestId、响应、TCP ACK 混淆 | [[25_应用消息包络与分派]] 的关联流程 |
| 解析器或公网入口角色混淆 | [[27_DNS_域名怎样找到服务器]] 与 [[28_NAT_端口映射与公网连入]] 的具体路径 |
| HTTP 成功、TLS 成功、业务成功混淆 | [[31_TLS_加密身份与权限]] 的四个关口 |
| 重连后认为状态自动恢复 | [[30_WebSocket_长连接与断线恢复]] 的快照与补发 |
| 排错只能说“网络有问题” | [[32_完整网络旅程与故障定位]] 的证据表 |

## 通过以后做什么

先读 GameFrameX 中一条消息的完整路径，而不是一上来浏览所有模块：连接建立 → 编码与发送 → 接收与分帧 → 分派 → 业务处理 → 响应 → 断开清理。具体仓库版本、模块名字和实际实现到开始阅读时核对，不提前假定它一定采用某种传输协议或 API。

再选一个范围有限的项目，例如登录、房间列表、加入离开和一类状态同步。先画出自己要维护的状态，再建立最小运行链；不恢复“不知道整体设计就填函数”的方式。

阶段通过说明你有理解与排查的基础。真实并发、部署、性能、安全和多人协作经验，需要在后续项目里取得证据，不能由课程题目通过直接推出来。

## 作答与反馈区

尚未作答。保留原答，之后按组追加反馈；不要因为文件已写好就标记结课。
