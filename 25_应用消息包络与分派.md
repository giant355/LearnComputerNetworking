# 第 25 课：完整消息到达后，程序怎样知道交给谁？——消息包络、分派与请求关联

> 预计时间：约 3～4 小时，可分两段学习  
> 场景：先读协议设计，再跟踪 C# 处理流程；不需要打开 Unity  
> 前置：[[22_Unity联机第一步_两个进程与一个世界|网络对象与服务器权威]]、[[23_Unity消息_事件状态与可靠性|消息语义与时效]]、[[24_TCP消息边界与长度前缀|TCP 分帧与长度前缀]]

[打开交互：两个请求、响应乱序返回](25_消息分派_交互.html)

## 本课只补哪一段

上一课的拆帧器解决了这个问题：TCP 给应用一串字节，应用怎样从中取出一条完整消息？

但“完整”不等于“知道怎么处理”。假设拆帧器交出一份完整正文，程序还需要回答：

- 这是什么类型的消息？
- 它是请求、响应，还是单向通知？
- 如果它是某个请求的响应，应该唤醒哪一个等待中的操作？
- 如果消息类型或协议版本不认识，应该怎样处理？

这一课只把上一课之后的处理链补出来：

    TCP 字节流 → 第 24 课拆出完整帧 → 解析消息包络 → 分派到处理器或等待中的请求

**拆帧**回答“一条消息的边界在哪里”；**分派**回答“这条完整消息由谁处理”。两者不是一回事。

## 1. 先遇到一个会出错的假设

客户端几乎同时发出两个请求：

    请求 A：购买药水，requestId = 41
    请求 B：查询背包，requestId = 42

服务器收到后，购买请求要检查库存、扣金币、写入结果，处理较慢；背包查询很快。服务器可能先完成 B，再完成 A：

    客户端发送：A → B
    服务器完成：B → A
    客户端收到：B 的响应 → A 的响应

如果客户端只记得“我先发 A、后发 B”，并把“第一个响应”交给 A，就会把背包查询结果误交给购买流程。

### 这不是 TCP 把字节排乱了

TCP 会按发送顺序交付同一方向的字节。这里可能改变的是**应用处理和生成响应的先后顺序**：服务器可以并发处理两个请求，快的查询先完成；之后服务器按完成顺序写出响应。响应一旦写入同一条 TCP 字节流，TCP 仍会按那个写入顺序交付。

所以需要区分：

- TCP 保证一条连接内的字节有序；
- 应用服务器是否按请求到达顺序完成工作，是另一项设计选择；
- 客户端必须按消息的关联信息匹配响应，不能凭“第几个返回”猜。

## 2. 从完整帧里再增加一个“消息包络”

第 24 课的最小格式是：

    帧 = 4 字节正文长度 + 正文

现在规定正文开头有一段固定格式的**消息包络**，后面才是具体消息的负载：

    正文 = 版本 + 消息种类 + 消息类型 + 请求 ID + 负载

例如，这份教学协议可以这样约定：

    帧 = [4 字节正文长度][正文]
    正文 = [版本 1 字节][种类 1 字节][类型 2 字节][请求 ID 8 字节][负载]

前 12 字节是包络；后面的负载再按这个消息类型对应的格式反序列化。多字节整数统一使用大端序。长度字段计算**整个正文**，也就是包络加负载；它不把自己那 4 个长度字节算进去。

这只是我们为了学习而设计的一种协议，不是 TCP、Unity 或所有游戏框架规定的唯一格式。换成 JSON、Protobuf 或其他编码也可以，但两端必须共享同一份消息契约。

这几个字段分别解决不同问题：

| 字段 | 它回答的问题 |
| --- | --- |
| 版本 | 两端对包络格式是否使用兼容的规则？ |
| 种类 | 这是请求、响应，还是通知？ |
| 类型 | 具体是哪一种业务消息，例如购买请求或玩家加入通知？ |
| 请求 ID | 这份响应对应本连接上哪个尚未完成的请求？ |
| 负载 | 这条具体消息携带的业务数据是什么？ |

以购买为例：

    Request:      kind=Request,      type=Purchase,       requestId=41, payload={itemId:9}
    Response:     kind=Response,     type=PurchaseResult, requestId=41, payload={success:true}
    Notification: kind=Notification, type=PlayerJoined,  requestId=0,  payload={playerId:7}

本例约定请求 ID 为 0 表示“这不是某个请求的响应关联”。如果协议选择这个约定，就必须禁止把 0 分配给普通请求。

## 3. “命令/事件/状态”和“请求/响应/通知”不是同一组分类

这里很容易觉得第 23 课又讲了一遍。我们把两个问题分开：

- **命令、事件、状态**：消息在游戏语义上表达什么？
- **请求、响应、通知**：消息在通信交互上扮演什么角色？

它们是两条不同的分类轴，可能组合使用：

| 游戏含义 | 通信角色示例 | 为什么 |
| --- | --- | --- |
| 购买物品这个命令 | Request → Response | 发起方需要知道服务器怎样裁定 |
| 玩家加入这个事件 | Notification | 收到方只需得知事件，不必逐一回复 |
| 当前玩家位置状态 | Notification 或专用快照流 | 常向订阅者推送状态，不一定为每份状态创建请求 |
| 玩家移动输入 | 取决于协议 | 若不要求逐条响应，可作为单向消息；若要确认某个操作，则可设计请求与结果 |

第 23 课问的是“这件事在游戏里是什么意思、旧消息还有没有价值、丢了怎么办”；本课问的是“解码后如何把它送到正确的处理路径”。知道一条消息是 Notification，并没有说明它一定可靠或一定丢弃；交付策略仍要根据它的语义决定。

## 4. 请求 ID 怎样修复“响应次序变了”

客户端为每个尚未完成的请求分配一个 ID，并保存一张等待表：

    41 → 购买结果的处理函数
    42 → 背包查询结果的处理函数

这里的表通常叫**待处理请求表**（pending requests）。它不是网络设备里保存的路由表；它只属于当前应用连接或会话，用来记“我正在等哪些业务响应”。

服务器先回 ID 42：

    收到 Response(requestId=42, type=InventoryResult)
    → 查表找到 42
    → 把背包结果交给查询流程
    → 删除 42 的等待项

之后再回 ID 41：

    收到 Response(requestId=41, type=PurchaseResult)
    → 查表找到 41
    → 把购买结果交给购买流程
    → 删除 41 的等待项

因此，匹配靠的是**请求 ID**，不是响应抵达的先后，也不是“同类型请求里第几个”。同一时间发出两个购买请求也可以分别用 51 和 52 区分。

请求 ID 的规则需要明确：

1. 发起请求的一端选择一个当前未占用的 ID。
2. 对端在对应响应中原样带回这个 ID。
3. ID 的唯一范围至少覆盖“一个连接上仍在等待的请求”；多连接时查表还要归属到正确连接。
4. 请求完成、失败或超时后，删除等待项；迟到响应不能误交给后来复用该 ID 的新请求。
5. 不应在一个尚未结束的请求还可能收到响应时复用它的 ID。实际实现还需要考虑重连后的会话身份，避免旧连接的迟到数据撞上新连接。

这不是 TCP 的 SEQ 或 ACK。TCP 的序号管理字节位置；请求 ID 管理应用程序发起的业务操作。它们处于不同层，含义也不同。

## 5. 一个最小的 C# 思路

先只看等待表，不引入网络框架：

    private readonly Dictionary<ulong, Action<Envelope>> pending = new Dictionary<ulong, Action<Envelope>>();

Dictionary 是“键 → 值”的查找表；这里键是请求 ID，值是“拿到响应后该调用的函数”。Action<Envelope> 可以先读成“接收一份 Envelope、执行某段处理”的回调类型。

发请求时，先登记等待者，再发送：

    pending.Add(requestId, HandlePurchaseResult);
    Send(new Envelope(version, MessageKind.Request, MessageType.Purchase, requestId, payload));

顺序很重要：先登记，再发。否则极快的响应可能先到，而本地尚未建立等待项。真实异步实现若发送失败，也要清理刚登记的等待项。

收到响应时，简化逻辑如下：

    if (!pending.TryGetValue(message.RequestId, out var onComplete)) { LogLateOrUnknown(message); return; }
    pending.Remove(message.RequestId);
    onComplete(message);

逐行看：

1. 用响应里的 ID 查本地等待表。
2. 没找到，说明它可能迟到、重复、属于旧连接，或协议状态不匹配；不能随便交给另一个请求。
3. 找到了，先取走并删除等待项，再调用对应处理函数。

这是帮助理解的简化片段，不是完整的联网类。它省略了线程安全、超时清理、发送失败回滚和反序列化校验；后面的实践会逐步补上。现在要抓住的是：**等待关系由 ID 显式保存，响应通过 ID 找回原请求。**

## 6. 分派器：把完整消息送到正确处理路径

一个接收端可以按这样的顺序处理完整消息：

    收到完整正文
       ↓
    解析并校验包络：版本、种类、类型、长度
       ↓
    种类是 Response？
       ├─ 是：按 requestId 查询待处理请求表
       └─ 否：按 (种类, 类型) 找对应请求处理器或通知处理器
       ↓
    业务处理器仍要验证发送者、权限和当前游戏状态

最后一行非常重要：**分派器只决定“交给哪个处理函数”，不代表请求已获批准。**服务器仍然要检查玩家身份、对象所有权和游戏规则；它不能因为消息里写了 playerId=7 就相信发送者真的是玩家 7。

客户端和服务器都可能同时发起请求，所以两端都可以有自己的待处理请求表和消息处理器。每个连接上的请求 ID 必须在正确的会话范围内解释，不能拿另一连接的 ID 来匹配。

## 7. 超时、迟到响应、重复请求：三个不同问题

设购买请求 41 超时了：

    1. 客户端把等待项 41 标记为超时并移除；
    2. 客户端稍后收到 PurchaseResult(requestId=41)；
    3. 查表找不到 41，不能把它交给其他操作；记录并丢弃或按产品规则处理。

这里要把三个机制分开：

| 机制 | 主要保存在哪一端 | 它解决什么问题 |
| --- | --- | --- |
| 请求关联 | 发起请求的一端 | 响应到了，怎样找到原先等待它的请求？ |
| 请求去重 | 处理请求的一端 | 同一请求重发时，怎样避免重复扣款等副作用？ |
| 超时与有效期 | 发起方和业务规则 | 等多久算失败；迟到结果还要不要接受？ |

它们可能都使用请求 ID，但目的与状态表不同。第 23 课说的“服务器记住已处理过的请求 ID”，解决的是**重复请求造成重复执行**；本课客户端的“请求 ID → 等待回调”解决的是**响应和请求怎样配对**。有请求 ID 并不自动提供“恰好执行一次”；重试、去重与业务事务仍需单独设计。

遇到同一个响应重复到达时，第一次处理后等待项已经移除，第二次通常查不到 ID。遇到超时后才到达的响应，则必须按游戏语义决定：结果仍可应用、只更新权威状态，还是直接忽略。不能仅凭“ID 对上”就覆盖当前状态。

## 8. 版本和未知消息怎样处理

包络中的版本让双方能识别“你发来的字节按哪种协议解释”。例如客户端发送 Version 1，但服务器只支持 Version 2；如果字段布局已改变，服务器不能按自己的格式硬解。

消息类型也需要验证：

- 未知版本：拒绝该消息或关闭连接，具体策略写入协议。
- 未知类型：若协议允许跳过未知消息，可按完整帧长度跳过；否则作为协议错误处理。
- 不合理长度：在分配大块内存或反序列化前拒绝。
- 种类与类型组合不合法：例如把 PurchaseResult 当成 Request 发送，应拒绝或记录协议错误。
- 已识别的消息但用户无权执行：交给业务校验拒绝，不等同于“类型未知”。

这也是为什么“解析成功”不等于“业务成功”：每一步都应有清晰边界。

## 9. 把整条链放回网络里

从 Unity 中一次“购买药水”到服务器处理，路径可以按本课已学模型串起来：

    客户端业务对象
       ↓ 序列化成负载
    添加版本、Request 种类、Purchase 类型与请求 ID
       ↓ 加第 24 课的长度前缀
    TCP 字节流
       ↓ 接收端先拆出完整帧
    解析包络并分派到 PurchaseRequest 处理器
       ↓ 服务器验证玩家、库存与金币
    生成 PurchaseResult，沿响应路径带回同一个请求 ID
       ↓ 客户端按 ID 唤醒购买流程

这里没有重新讲服务器权威：第 22 课已建立服务器决定最终状态的模型。本课只是指出，**消息被送到正确处理器**还不代表处理器必须接受它。

同样，服务器收到客户端 TCP ACK，只能知道相应字节已被对端 TCP 确认；它不等于客户端已经拿到 PurchaseResult，也不等于服务器已经成功购买。这些层次不能混成一个“已成功”。

## 10. 设计练习：给大厅加消息

用约 30～45 分钟画出下列三种交互，不需要写网络 API：

1. 客户端请求加入房间，服务器可能拒绝或接受。
2. 房间里的一个玩家准备就绪，其他玩家应看到状态改变。
3. 两个客户端几乎同时查询房间信息，响应可能以任意顺序返回。

每种消息都写出：

    发送方 → 接收方 | 种类 | 类型 | 请求 ID | 负载 | 处理器/等待项

然后推演：

- 两个查询都在等待时，响应按相反顺序到达，怎样正确匹配？
- 加入请求超时后又收到接受响应，应用如何避免和当前房间状态冲突？
- Ready 状态是请求、通知，还是两者都会出现？分别在什么时机？

不要求只有一种设计答案；要求你能说清楚为什么这样设计，以及服务器在哪一步验证权限和游戏规则。

## 本课小结

- 完整帧只解决边界；包络为正文提供版本、种类、具体类型和关联信息。
- 消息分派把完整消息送给正确的请求处理器、通知处理器或等待中的响应回调。
- 同一连接的业务处理可以并发，所以响应抵达顺序不能替代请求 ID 匹配。
- 命令/事件/状态描述游戏语义；请求/响应/通知描述通信角色，是两条不同分类轴。
- 请求关联、请求去重、超时与有效期相关但不相同；请求 ID 本身不保证业务只执行一次。
- 分派不等于授权，服务器处理器仍要验证发送者和游戏规则。

## 检查站

先遮住正文，尽量用自己的话回答；不需要背字段宽度。

1. 第 24 课的拆帧器已经交出完整正文，为什么接收程序还需要消息包络和分派器？
2. 客户端先发购买请求 41，再发背包查询请求 42；服务器先回背包结果 42。待处理请求表应怎样变化？如果按返回次序配对，具体会错在哪里？
3. TCP 保证字节有序，为什么应用仍可能先收到请求 42 的响应，再收到请求 41 的响应？这里哪个部分没有乱序？
4. 版本、消息种类、消息类型、请求 ID 各自回答什么问题？哪一个字段用来关联响应？
5. 第 23 课的“命令/事件/状态”和本课的“请求/响应/通知”为什么不能互相替代？各举一个组合例子。
6. 请求 41 超时并从等待表删除后，PurchaseResult(41) 才到。接收端为什么不能把它交给“当前最早等待的请求”？
7. 本课客户端用请求 ID 匹配响应；第 23 课服务器用请求 ID 去重。它们分别在哪一端、解决什么问题？
8. 一份请求成功解析、也被分派到了正确处理器，为什么仍不能说明它应被执行？
9. 试着给“查询房间”“购买物品”“玩家加入通知”设计消息种类、消息类型和请求 ID 规则。

## 下一步

完成检查站后，下一步会从一对 C# 程序开始，自己搭出最小 TCP 通信：先看 Socket/TcpClient/TcpListener 和字节流怎样连接，再把第 24、25 课的拆帧与分派接上。届时会从整体数据流和状态表开始设计，不让你盲填函数。
