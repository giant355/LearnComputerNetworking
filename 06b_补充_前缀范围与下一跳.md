# Lesson 6B：前缀范围、下一跳与 ARP

> 预计时间：15–25 分钟
>
> 目的：只补第 6 课检查站暴露出的三个缺口，不需要重读整课。

你已经掌握了最长前缀匹配的主干：能够判断 `/24`、`/16`、`/0` 的优先级，也能区分目标前缀、下一跳和出口接口。现在只把三处“已经走到门口，但还没写完”的推理补齐。

## 1. 一个具体地址怎样变成“网络前缀 + 范围”

你在 `192.168.1.130/26` 中已经正确写出了：

```text
130 = 10|000010
```

`/26` 表示前三段 24 位固定，再固定最后一段前 2 位。竖线左边是前缀部分，右边 6 位是可变化部分。

求网络前缀时，把右边全部清零：

```text
最小：10|000000 = 128
```

求范围末尾时，把右边全部设为 1：

```text
最大：10|111111 = 191
```

所以完整答案必须写到：

```text
网络前缀：192.168.1.128/26
覆盖范围：192.168.1.128 ～ 192.168.1.191
块大小：2⁶ = 64 个地址
```

你原来的二进制拆分不是错，只是停在了中间步骤。以后看到“求所属前缀”，要继续完成两步：主机位全 0 得起点，主机位全 1 得终点。

### `/24` 的两个特殊名称

`192.168.10.0/24` 覆盖 256 个地址：

- `192.168.10.0` 通常称为**网络地址**；
- `192.168.10.255` 通常称为该子网的**广播地址**。

这两个名称解释了传统 IPv4 子网中常说的“256 个地址、通常 254 个普通主机地址”。前缀本身仍然覆盖 256 个地址。

## 2. 当前下一跳由“是否在直连前缀内”决定

主机配置：

```text
本机：192.168.1.10/24
直连前缀：192.168.1.0/24
默认网关：192.168.1.1
```

### 发送给 `192.168.1.200`

它匹配本机的直连前缀 `192.168.1.0/24`，所以无需先交给路由器：

```text
最终目标 IP：192.168.1.200
当前下一跳 IP：192.168.1.200
```

### 发送给 `192.168.2.5`

它不匹配本机的直连前缀，所以使用默认路由：

```text
最终目标 IP：192.168.2.5
当前下一跳 IP：192.168.1.1（默认网关）
```

最重要的区别是：

```text
同一直接连接的前缀 → 下一跳就是目标本身
不在直接连接的前缀 → 下一跳通常是网关
```

无论走哪一种，最终目标 IP 都不会因为第一跳选择而变成网关。

## 3. 知道下一跳 IP 后，为什么还不能立刻发 Ethernet 帧

Ethernet 帧头需要当前链路的目标 MAC 地址，但路由查询只给出了下一跳 IP。

因此还需要解决：

```text
下一跳 IP → 下一跳 MAC 地址
```

IPv4 局域网中完成这个映射的协议叫 **ARP（地址解析协议）**。

两种情况仍然对应同一条规则：

```text
同网段：查询最终目标 IP 的 MAC
跨网段：查询默认网关 IP 的 MAC
```

查询到 MAC 后，主机才能制作当前这一跳的 Ethernet 帧。下一课会把 ARP 的请求、广播、应答和缓存完整走一遍；现在先记住它填补的是哪一个缺口。

## 4. `/22` 为什么是四个 `/24`

`/24` 有 `32-24=8` 个可变化位；`/22` 有 `32-22=10` 个可变化位。

```text
/24：2⁸  = 256 个地址
/22：2¹⁰ = 1024 个地址
```

`/22` 比 `/24` 少固定 2 位，也就多出 2 个可变化位，所以地址数量扩大：

```text
2² = 4 倍
```

因此一个正确对齐的 `/22` 可以概括四个连续 `/24`。这里更准确的说法是“多了两个可变化位”，而不是笼统地说“左移两位”。

---

## 收尾复测

不要回看上文，直接回答下面四题：

1. `10.0.0.77/27` 中还剩多少个主机位？块大小是多少？它所属前缀和完整范围是什么？
2. 主机为 `172.16.5.10/24`，网关为 `172.16.5.1`。发送到 `172.16.5.200` 与 `172.16.6.20` 时，两个当前下一跳分别是谁？两个最终目标分别是谁？
3. 已知下一跳 IP 后，制作 Ethernet 帧还缺什么？IPv4 局域网通常用什么协议获得它？
4. 为什么 `/21` 覆盖的地址数是 `/24` 的 8 倍？请从可变化位数量解释。

四题通过后，第 6 课即可进入间隔复习队列。
