# 计算机网络 - 第 1 课：一条 Unity 消息是怎样离开电脑的？

> 预计学习时间：约 2 小时  
> 这一课不要求记忆协议名。我们先建立一张“数据旅行地图”，以后所有细节都会挂在这张地图上。

## 学习目标

本节结束后，你应该能够：

1. 用一句通俗的话解释计算机网络在解决什么问题；
2. 区分“游戏对象”“消息”“字节”这三个层次；
3. 说清楚一次发送大致经过哪些角色；
4. 理解“数据已经交给操作系统”不等于“对方程序已经收到”；
5. 通过几个系统命令观察本机与互联网，而不必完全看懂输出。

## 建议节奏

| 时间 | 内容 |
| --- | --- |
| 0:00–0:30 | 从两个 Unity 程序的难题开始 |
| 0:30–1:00 | 追踪一条消息经历的角色 |
| 1:00–1:30 | 动手观察真实网络 |
| 1:30–1:50 | 用五个问题重新推导网络机制 |
| 1:50–2:00 | 完成检查站 |

---

## 1. 先忘掉 TCP/IP

假设你正在做一个最简单的双人 Unity 游戏。

- 玩家 A 按下空格，角色跳起来；
- 玩家 B 的电脑也应该看到 A 跳起来。

在 A 的程序里，这件事可能只是：

```csharp
player.Jump();
```

但 `player` 这个 C# 对象只存在于 A 的进程内存中。B 的电脑上没有通往这块内存的神秘通道。甚至 A 电脑上的另一个普通进程，也不能随便读取它。

所以，第一个问题不是“该用 TCP 还是 UDP”，而是：

> **一个程序脑子里的东西，怎样变成另一个程序能收到的东西？**

先停十秒想想。如果不允许你说 TCP、UDP、IP，你会怎样描述这件事？

一个最朴素的答案是：

1. A 把“玩家跳跃”写成双方约定的符号；
2. 这些符号被转换成可以搬运的字节；
3. 某种系统把字节送到 B；
4. B 按同一套约定解释字节，并执行跳跃。

这已经包含了计算机网络最核心的工作。

> [!note] 第一张定义卡
> **计算机网络帮助不同程序跨越机器和链路交换数据。** 电脑是承载程序的地点；最终真正想交流的通常是程序。

### 1.1 为什么发送的是字节，而不是 `GameObject`？

下面这个对象对 Unity 很有意义：

```csharp
public sealed class JumpEvent
{
    public int PlayerId;
    public float ClientTime;
}
```

但另一台机器看不到 `JumpEvent` 的对象引用。引用本质上只对当前进程的内存布局有意义。要发送它，我们得先约定一种外部表示，例如：

```text
事件类型：01
玩家编号：00 00 00 2A
发生时间：41 48 00 00
```

这里不必理解十六进制。重点是：网络面对的是一串字节，而不是 C# 类、C++ 对象或 Unity 组件。

把程序中的结构变成字节，叫做**序列化**；把字节还原成程序能理解的结构，叫做**反序列化**。

```text
JumpEvent 对象
      ↓ 序列化
一串字节
      ↓ 网络搬运
一串字节
      ↓ 反序列化
JumpEvent 对象
```

现在问一个小问题：如果 A 用“前 4 字节是玩家编号”的规则，而 B 认为“前 8 字节是发生时间”，会怎样？

数据也许一字不差地到达了，但 B 仍然读不懂。这说明**送达字节**与**理解字节含义**是两个问题。双方关于格式和行为的约定，就是以后会反复遇到的“协议”。

> [!note] 第二张定义卡
> **协议是通信双方共同遵守的一组约定。** 它会规定消息长什么样、每部分是什么意思，以及收到消息后应该做什么。

---

## 2. 如果只有一根完美的线

我们先造一个极小的世界：A 与 B 之间有一根完美的线。

这根线拥有魔法能力：

- 放进去的每个字节都会到达；
- 不会变坏，也不会重复；
- 先放进去的一定先出来；
- 可以瞬间传送无限多数据。

在这个世界里，网络编程很简单：序列化，然后发送。

可是现实中的线没有魔法：

- 信号传播需要时间；
- 介质一次能承载的数据有限；
- 设备缓冲区会装满；
- 无线信号会受干扰；
- 中间设备或链路可能故障；
- 两台机器之间往往没有一根专用直连线。

网络课程里许多看似复杂的机制，都是在逐项拆除“完美的线”这个假设。

### 2.1 第一种现实限制：距离

信息传播不可能瞬间完成。即使处理器无限快，信号仍然需要沿光纤、铜线或无线电传播。

从发送到收到所花的时间通常叫**延迟**。如果 A 发出一个小问题，B 回答后答案再回到 A，这个来回所需的时间叫 **RTT（Round-Trip Time，往返时间）**。

在游戏里，RTT 高时，你可能看到：

- 按键后动作反馈变慢；
- 射击判定看起来“已经命中”却被服务器否定；
- 其他玩家的位置跳动或落后。

注意：延迟不是“网速”的同义词。高速公路可以很宽，但路程仍然很远。

### 2.2 第二种现实限制：容量

一条链路每秒能搬运的数据有限。这种能力常用 bit/s 表示，称为**带宽**。

可以用公路类比：

- 延迟像车辆从上海开到北京需要多久；
- 带宽像这条公路每小时能通过多少辆车。

一辆车即使独占十车道，也不会瞬间到北京；但十车道能让更多车辆同时通过。

这个类比并不完美，但足以帮助我们区分两个常被混在一起的量。

### 2.3 第三种现实限制：多人共享

你的电脑同时运行浏览器、Steam、Unity 编辑器和聊天软件。它们都可能发送数据。家庭路由器外的一条线路还要被多台设备共享。

于是系统必须回答：

- 收到的数据属于哪个程序？
- 大家同时发送时先搬谁的？
- 来不及搬的数据暂时放在哪里？
- 临时存放的位置满了怎么办？

后面我们会从这些问题自然推导出端口、队列、丢包和拥塞控制。现在只需知道：**共享会产生排队，有限队列可能装满。**

---

## 3. 一条消息大致经过谁

现在回到 `JumpEvent`。先画一张故意简化的旅行图：

```text
玩家 A 的 Unity 程序
        ↓ 产生并序列化消息
A 的操作系统网络功能
        ↓ 决定怎样发送、交给哪个网络接口
A 的网卡 / Wi-Fi
        ↓ 把数据变成可在介质上传播的信号
交换机、家庭路由器、运营商设备……
        ↓ 一段一段地向目的地搬运
B 的网卡 / Wi-Fi
        ↓ 把信号还原成数据
B 的操作系统网络功能
        ↓ 找到等待这份数据的程序
玩家 B 的 Unity 程序
        ↓ 反序列化并处理 JumpEvent
玩家 B 看见 A 跳起来
```

这张图现在有意省略了许多名称。先认识四类角色：

| 角色 | 现在只需要知道的职责 |
| --- | --- |
| 应用程序 | 决定数据表达的业务含义，例如“玩家 42 跳跃” |
| 操作系统网络功能 | 给应用提供发送/接收入口，并实现许多通用网络规则 |
| 网络接口与链路 | 在相邻设备之间实际承载数据 |
| 中间设备 | 在没有直连线时，一步步把数据推向目的地 |

### 3.1 “发送成功”到底成功了什么？

假设程序调用某个发送函数，函数返回成功。最安全的理解通常只是：

> 操作系统已经接受了这批待发送数据。

它不自动等于：

- 数据已经离开网卡；
- 所有中间设备都处理成功；
- B 的操作系统已经收到；
- B 的 Unity 程序已经读取；
- B 已经理解并执行了消息。

这里像快递员收走包裹：下单成功、快递员揽件、运到城市、送达门口、收件人拆开阅读，是五个不同事件。

以后学习“确认”时，一定要先问：**谁在向谁确认哪一步？**

### 3.2 数据不是在一条巨大的管道里滑行

初学时很容易想象：A 与 B 建立了一根透明管道，字节从里面连续滑过去。

有些上层接口确实故意给程序制造了类似管道的感觉。但在更底层，数据通常被切成一个个有边界的小块，让不同通信可以交错共享链路，也让中间设备能够逐块处理。

我们暂时统一称它们为“数据块”。后面根据所在层次，会准确地区分 frame、packet、datagram、segment。现在提前死记这些名字只会遮住共同结构：

```text
[控制信息][真正想搬运的数据]
```

控制信息可能描述“要送到哪里”“这是什么类型”“还能经过多少步”等。它类似快递单；真正的数据类似包裹里的内容。

---

## 4. 动手：先看见网络，再解释网络

这一部分不是考试。我们的目标只是留下第一组真实观察，后面的课程会逐行解释它们。

请在 Windows PowerShell 中完成。若某条外网命令失败，也记录失败现象；公司网络、防火墙、目标站点策略都可能影响结果。

### 4.1 观察本机回环地址

运行：

```powershell
ping 127.0.0.1
```

`127.0.0.1` 是一个特殊地址，通常表示“这台机器自己”。数据不需要真的走到家庭路由器或互联网。

观察三个信息：

1. 是否收到了回复；
2. 输出里是否出现时间；
3. 是否报告丢失。

现在预测：拔掉网线、关闭 Wi-Fi 后，它还能成功吗？先写下预测，再在方便时验证。

### 4.2 让名字变成地址

运行：

```powershell
nslookup example.com
```

人喜欢 `example.com` 这样的名字，但网络转发更依赖数字地址。这个命令会询问 DNS，把名字解析为地址。

现在不用理解 DNS 服务器、IPv4 或 IPv6，只记录：输出中是否出现了一个或多个地址？为什么同一个名字可能得到多个地址？先保留猜测。

### 4.3 观察一次应用层会话

运行：

```powershell
curl.exe -v https://example.com/
```

`-v` 会展示更多过程。你可能看到：

- 正在尝试某个地址；
- 建立连接；
- 与加密有关的信息；
- 以 `>` 和 `<` 开头的请求、响应；
- 最后的 HTML 内容。

这像把原本藏在浏览器背后的对话稍微掀开一角。请不要查每一行；只圈出你认为分别属于“找谁”“建立交流”“发送请求”“收到内容”的几行。

### 4.4 观察路径中的若干跳

运行：

```powershell
tracert example.com
```

它会尝试显示到目的地沿途的一些“跳”。星号不一定表示互联网坏了，有些设备只是不回答这类探测。

观察：

- 路径是否只有一跳；
- 每一跳显示的时间是否完全相同；
- 是否存在不响应的跳；
- 多运行一次，结果是否完全不变。

> [!tip] 保存观察
> 可以把关键输出复制到本课末尾的“我的观察”区。请删掉你不想保留的公网 IP、用户名或其他个人信息；不要公开贴出完整网络配置。

---

## 5. 从五个问题推导整门课

现在我们已经知道：应用想交换有意义的消息，但现实只能逐步搬运有限的字节。整门课程可以看成不断回答下面五个问题。

### 问题一：数据要交给谁？

“谁”至少有两层：哪台机器、机器里的哪个程序。以后我们会引出地址、端口和复用。

### 问题二：不直连时怎么走？

数据可能经过很多中间设备。它们需要根据有限信息选择下一步。以后会引出数据报、路由表和最长前缀匹配。

### 问题三：数据太大怎么办？

链路不能一次搬运无限数据，不同通信还要交错使用资源。以后会讨论切分、封装、最大传输单元和重组。

### 问题四：丢了、乱了、重复了怎么办？

有的应用需要完整可靠，有的应用更怕等待。以后会从序号、确认和重传构建可靠字节流，也会理解为什么实时游戏有时宁可接受旧数据丢失。

### 问题五：所有人一起发怎么办？

设备处理能力与缓冲区有限。无限制发送会制造长队甚至崩溃。以后会研究排队、流量控制与拥塞控制。

你以后看到任何网络机制，都可以问：它主要在回答这五个问题中的哪一个？这是比死记协议所在层次更牢固的索引。

---

## 6. 第一个心智模型

把目前的理解压缩成下面这幅图：

```text
含义                      搬运                         含义

A 的程序  →  字节  →  操作系统  →  链路/中间设备  →  操作系统  →  字节  →  B 的程序
   │                                                                  │
   └────────────── 双方必须对字节的含义与行为有共同约定 ───────────────┘
```

这个模型还很粗，但已经能阻止几个常见误解：

- 网络不会直接搬运 C# 对象；
- 网络通信的最终双方通常是程序；
- 中间经过多个职责不同的角色；
- 交给下一层不代表最终目的已经完成；
- 送达正确字节不代表应用一定能正确理解。

下一课会遇到一个新的困难：如果每种应用都要自己理解网卡、路由器和各种物理链路，软件根本无法复用。我们将由此推导出“分层”，而不是先背一张分层表。

## 知识锚点

- Stanford CS144 的核心路线同样围绕数据报、封装、从不可靠中构建可靠性、TCP、交换、拥塞控制和路由展开：[CS144 官方课程页](https://cs144.github.io/)。
- Internet 的正式协议规范主要由 RFC 文档记录；以后遇到具体机制时，我们会优先链接 [RFC Editor](https://www.rfc-editor.org/)，但不会在入门阶段直接用规范语言替代解释。

## 小结

1. 网络最终帮助不同程序跨越机器和链路交换数据。
2. 程序对象必须先变成双方约定格式的字节；共同约定就是协议。
3. 一次通信会经过应用、操作系统、接口/链路和中间设备等角色。
4. 延迟描述“走一趟要多久”，带宽描述“单位时间能搬多少”。
5. 发送函数成功通常只证明某一小步成功，不能自动证明对方应用已经理解消息。

---

## 检查站

请不要查资料。用自己的话回答，哪怕不确定也没关系；不确定的位置最能帮助我们决定下一步。

1. 为什么不能直接把一个 Unity `GameObject` 或 C# 对象引用发送给另一台电脑？中间至少需要发生哪两次转换？
2. 程序的发送函数返回成功，为什么仍不能断定对方玩家已经看到角色跳跃？请列出至少两个还可能失败或尚未发生的环节。
3. 延迟与带宽有什么区别？请用一个不是“公路”的例子说明。
4. 如果字节完整到达，但接收方把前 4 字节理解成“时间”，发送方却把它当成“玩家编号”，这是搬运问题还是协议问题？为什么？
5. 开放题：在“数据要交给谁、怎么走、太大怎么办、丢乱重复怎么办、大家一起发怎么办”五个问题里，你觉得实时游戏最敏感的是哪两个？先说直觉即可。

另外，把下面四条观察结果一并告诉我，不必粘贴完整输出：

- `ping 127.0.0.1` 是否成功；
- `nslookup example.com` 是否得到地址；
- `curl.exe -v https://example.com/` 是否得到网页内容；
- `tracert example.com` 大约看到多少跳，是否有星号。

请把答案直接发给我。我会判断你是完全掌握、基本掌握，还是需要一篇针对某个卡点的补充说明，然后更新复习记录并决定是否进入下一课。

### 我的回答（2026-08-31）

1. 因为 `GameObject` 或者 C# 对象本质是二进制码，这些东西本身是应用来决定它们的意义，所以至少要先序列化，目标程序接收还要再反序列化。
2. 发送函数成功可能只是成功被操作系统接收了，或者中间某个步骤成功了。
3. 延迟指的是一整次传输要的时间，带宽是单位时间可以发送的数据。
4. 协议，双方对数据的约定不同。
5. 后两个。

### 教师反馈（2026-08-31）

总体判断：**基本掌握**。

- 第 2、4、5 题正确。
- 第 1 题的过程是对的，但对象不是“二进制代码”。代码是程序指令；对象是运行时内存中的状态，可能包含引用、Unity 原生对象句柄和只对当前进程有意义的地址。发送方选择需要传递的信息并序列化，接收方再反序列化成自己的对象或事件。
- 第 3 题的定义正确。下一次复习时需要给出一个自己想到的类比，证明你能把定义迁移到新情境。
- 四项网络观察均成功。`nslookup` 返回多个地址，可能因为一个域名同时提供 IPv4/IPv6、由多台服务器分担请求、提供故障切换，或由 CDN 根据位置返回不同目标。域名是名字，不是某一台固定机器的身份证。

---

## 我的观察

> 可以在这里写你自己的命令观察、猜测和问题。

## 第一次主动回忆（2026-09-01）

### 我的回答

1. 因为这些东西是本地电脑里面的引用，是地址门牌号，在另一个电脑里面完全不沾边。
2. 一个例子是可能应用层的序列化、反序列化约定不一样。
3. 不知道，好像课程里面没讲过，或者里面的名词看不懂。

### 教师反馈

复习结果：**基本掌握，通过第一次复习**。

- 第 1 题的核心正确。更精确地说，对象包含运行时状态，引用是定位当前进程中对象的地址式标识；另一进程或机器没有相同内存布局，所以必须选择信息并序列化成字节。
- 第 2 题是有效例子：`Send` 只表示某一步接受了字节。即使字节最终送到，对方若按不同应用协议反序列化，也可能无法显示跳跃。
- 第 3 题不是学习者错误。DNS 多地址此前只写在教师反馈里，没有在正文中循序解释。第 3 课会从“名字为什么不等于一台机器”重新讲解。
