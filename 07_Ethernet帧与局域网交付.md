# 第 7 课：Ethernet 帧与局域网交付

> 预计时间：正文与随堂推演 80–110 分钟；检查站 25–40 分钟
>
> 所需工具：课堂阅读只需纸笔；文末有可选的回寝室观察，不影响理论掌握
>
> 当前状态：新课。完成检查站后再判断掌握程度。

第 6 课把路由查询推进到了这里：

```text
最终目标 IP
  ↓ 最长前缀匹配
出口接口 + 当前下一跳 IP
```

但“知道从哪扇门出去、下一跳 IP 是谁”还没有让任何比特真正离开网卡。

假设本机决定把数据交给同一局域网内的 `192.168.1.200`。网线或 Wi-Fi 上传输的不是一句“请交给这个 IP”，而是一段遵守当前链路规则的字节。在常见有线局域网里，这种包装叫 **Ethernet 帧**。

这一课要补上的机制是：

> 在当前一段局域网中，网卡、Ethernet 帧和交换机怎样把一个 IP 数据报交给正确的下一跳？

我们先假设已经知道下一跳的 MAC 地址。下一课再实现“只知道下一跳 IP 时，怎样用 ARP 找到它的 MAC”。

## 学习目标

本课结束后，你应该能够：

1. 区分 IP 地址、MAC 地址和交换机端口各自的作用范围；
2. 解释 Ethernet 帧中目标 MAC、来源 MAC、类型、载荷和 FCS 的作用；
3. 说明 IP 数据报怎样成为 Ethernet 帧的载荷；
4. 手工推演交换机怎样从来源 MAC 学习，并处理已知单播、未知单播和广播；
5. 解释为什么交换机通常不减少 TTL，而路由器会；
6. 跟踪一个 IP 数据报经过路由器时，IP 地址与 MAC 地址分别怎样变化；
7. 区分路由表、ARP 缓存和交换机 MAC 表；
8. 解释 MTU 为什么会限制一帧能够装下的数据量。

---

## 1. “直连”不等于“IP 完全相同”

先关闭一个刚刚出现的混淆。

```text
主机 A：192.168.1.10/24
主机 B：192.168.1.200/24
```

它们的完整 IP 不同，否则就是在争用同一个地址。但是它们的网络部分相同：

```text
192.168.1 | 10
192.168.1 | 200
──────────   ───
网络部分     主机部分
```

两者都匹配直连前缀 `192.168.1.0/24`，所以 A 判断：不必先把数据交给 IP 路由器，B 本身就是当前下一跳。

这里“直连”的准确含义是：

> 根据本机路由配置，目标被认为位于当前链路上，可以直接制作一个交给目标网卡的链路层帧。

它不表示 A 与 B 之间一定只有一根独占网线。中间可能有一台或多台 Ethernet 交换机；只要没有经过执行 IP 转发的路由器，对 IP 层来说仍是同一跳。

### 两种地址同时存在

A 向 B 发送时：

```text
IP 数据报：目标 IP = 192.168.1.200
Ethernet 帧：目标 MAC = B 的 MAC
```

目标 IP 回答“最终是哪台 IP 主机”，目标 MAC 回答“当前 Ethernet 链路把帧交给哪个网络接口”。

---

## 2. 网络接口是协议世界与链路世界的边界

一台电脑可能同时有：

- 有线 Ethernet 网卡；
- Wi-Fi 网卡；
- VPN 虚拟接口；
- 回环接口；
- 虚拟机或容器创建的虚拟接口。

每个接口连接到不同的链路环境，也可能拥有自己的：

- IP 地址与前缀；
- 链路层地址；
- MTU；
- 发送与接收队列。

所以第 6 课中的“出口接口”并不是抽象装饰。它决定接下来采用哪一种链路层规则。

```text
IP 层选择出口接口
  ↓
接口根据自身链路类型包装数据
  ↓
Ethernet 接口制作 Ethernet 帧
Wi-Fi 接口制作 802.11 链路帧
```

本课聚焦 Ethernet。Wi-Fi 的帧格式和接入机制并不完全相同，但 IP 层仍然可以把数据交给统一的网络接口抽象。这正是分层带来的替换能力。

---

## 3. MAC 地址是什么

常见 Ethernet MAC 地址有 48 位，也就是 6 字节，通常写成六组十六进制：

```text
3C:52:82:1A:7B:90
```

每两个十六进制字符表示一个字节：

```text
3C | 52 | 82 | 1A | 7B | 90
 1    2    3    4    5    6  字节
```

当前阶段可以把 MAC 地址理解成：

> Ethernet 链路用来标识发送接口和接收接口的地址。

不过不要把它误解成永不改变、全球永远唯一、可以跨互联网找到设备的身份证：

- 操作系统可以使用软件配置的 MAC 地址；
- 虚拟接口也有 MAC 地址；
- 地址可能被随机化；
- 配置错误或恶意行为可能造成重复；
- 普通路由器不会根据远方主机的 MAC 地址进行互联网路由。

MAC 地址的核心用途是**当前链路的帧交付**，不是替代 IP 地址。

### 广播 MAC 地址

这个特殊地址表示“当前广播域中的所有 Ethernet 接口都应该接收并检查”：

```text
FF:FF:FF:FF:FF:FF
```

广播不会自动穿越整个互联网。路由器通常会把不同的二层广播域隔开。下一课的 ARP 请求会使用这个广播地址，因为发送者一开始还不知道目标的具体 MAC。

---

## 4. Ethernet 帧是一只怎样的盒子

暂时忽略 VLAN 标签，一个常见 Ethernet 帧的核心结构是：

```text
┌──────────┬──────────┬──────────┬──────────────────┬──────┐
│ 目标 MAC │ 来源 MAC │ EtherType│ 载荷             │ FCS  │
│  6 字节  │  6 字节  │  2 字节  │ 46～1500 字节    │4字节 │
└──────────┴──────────┴──────────┴──────────────────┴──────┘
```

从目标 MAC 到 FCS，普通无标签 Ethernet 帧通常为 64～1518 字节。

### 4.1 目标 MAC

表示当前 Ethernet 链路希望把帧交给哪个接口。

它可能是：

- 某一个接口的单播 MAC；
- 广播地址 `FF:FF:FF:FF:FF:FF`；
- 一组接口使用的组播地址，留到后面再展开。

### 4.2 来源 MAC

表示制作当前帧的发送接口。交换机正是通过观察**来源 MAC**，学习“这个地址位于哪个端口后面”。

### 4.3 EtherType

同一个 Ethernet 帧可以装不同的上层协议。接收者需要知道怎样解释载荷：

```text
0x0800 → IPv4
0x0806 → ARP
```

这又是一次解复用：根据类型字段，把载荷交给正确的上层处理程序。

### 4.4 载荷

如果 EtherType 是 IPv4，载荷通常就是一个完整的 IPv4 数据报：

```text
Ethernet 帧
┌─────────────────────────────────────────────────────────┐
│ Ethernet 头 │ IPv4 头 │ UDP 头 │ 应用字节 │ FCS        │
└─────────────────────────────────────────────────────────┘
                 └──── Ethernet 载荷中的 IP 数据报 ────┘
```

载荷少于 46 字节时，Ethernet 会添加填充，让帧达到最小长度。上层协议自己的长度字段帮助接收者区分真实数据和填充。

### 4.5 FCS

FCS 是帧校验序列，用来检测传输中发生的某些比特错误。接收接口重新计算并比较；校验失败时通常丢弃该帧。

要注意：

```text
检测到损坏 ≠ 自动修复损坏
检测到损坏 ≠ Ethernet 一定负责重传
```

基本 Ethernet 帧没有像 TCP 那样的端到端确认和重传语义。需要可靠性的系统会在其他层增加机制。

### 暂时可视为黑箱：线上还有什么

物理线路上还会出现前导码、帧开始定界符以及帧间间隔。它们影响线路实际占用时间，但通常不计入上面所说的 64～1518 字节 Ethernet 帧长度。现阶段不需要背这些数值。

---

## 5. 为什么不能把任意大的 IP 数据报直接塞进一帧

Ethernet 的普通最大载荷通常是 1500 字节。这个上限称为该接口的 **MTU（最大传输单元）**。

```text
Ethernet MTU 1500
→ 一个普通 Ethernet 帧最多承载 1500 字节 IP 数据报
```

注意这里的 1500 已经包含 IP 头部。如果使用 20 字节 IPv4 头和 8 字节 UDP 头，那么忽略其他扩展时，单个 UDP 数据报可在不超过这个 MTU 的情况下携带的应用数据上限是：

```text
1500 - 20 - 8 = 1472 字节
```

这不是说应用永远应该发送 1472 字节，也不是说所有路径 MTU 都必然是 1500。它只是展示各层开销怎样共同占用同一个链路载荷预算。

如果 IP 数据报大于出口 MTU，就必须面对分片、报错或让发送方减小数据等选择。完整机制稍后单独学习。现在只需建立约束：

> 下层能够承载的数据块有大小上限，上层不能假设一条消息可以无限大地整体通过。

---

## 6. 只有多台主机时，为什么需要交换机

假设 A、B、C 三台主机都在同一个局域网：

```text
A ──┐
B ──┼── 交换机
C ──┘
```

现代有线 Ethernet 常以交换机为中心。每台主机连接交换机的一个端口，交换机收到帧后决定应该从哪些其他端口发出。

交换机的核心问题是：

> 目标 MAC 位于哪个端口后面？

它维护一张动态表：

```text
MAC 地址 → 交换机端口
```

例如：

| MAC 地址 | 端口 |
| --- | ---: |
| `AA:AA:AA:AA:AA:AA` | 1 |
| `BB:BB:BB:BB:BB:BB` | 2 |

这张表常被称为 MAC 地址表或转发表。它不是 IP 路由表。

---

## 7. 交换机从来源学习，而不是从目标猜测

交换机刚启动时，表可能为空。A 从端口 1 发来一帧：

```text
来源 MAC：AA
目标 MAC：BB
进入端口：1
```

交换机首先可以确定一件事：

```text
既然来源为 AA 的帧从端口 1 进来，AA 就在端口 1 后面。
```

于是学习：

```text
AA → 端口 1
```

为什么不根据目标 BB 学习？因为交换机只知道“发送者想找 BB”，却不知道 BB 真的位于哪里。进入端口能够证明来源方向，不能证明目标方向。

学习后的表项通常会老化。如果长时间没有再看到某个来源 MAC，交换机会删除旧记录，以适应设备移动、重连或拓扑变化。

---

## 8. 交换机怎样处理三类目标

学习来源后，交换机检查目标 MAC。

### 8.1 已知单播

如果表中已有：

```text
BB → 端口 2
```

交换机只把帧从端口 2 发出：

```text
端口 1 收到 AA→BB
  ↓ 查到 BB 在端口 2
只转发到端口 2
```

其他端口不必看到这次单播流量。

### 8.2 未知单播

如果目标 BB 不在表中，交换机暂时不知道它在哪里。为了不给正确目标漏送，只能把帧复制到除入口之外的其他相关端口：

```text
端口 1 收到 AA→BB
  ↓ 不知道 BB 在哪里
向端口 2、3、4 泛洪
```

这叫**未知单播泛洪**。只有真正目标 BB 接受并交给上层；其他主机的网卡通常看到目标 MAC 不属于自己后丢弃。

当 B 回应时，交换机从回应的来源 MAC 学到：

```text
BB → 端口 2
```

之后发往 BB 的单播就不必继续泛洪。

### 8.3 广播

如果目标是：

```text
FF:FF:FF:FF:FF:FF
```

交换机会向除入口之外的广播域端口转发，让当前广播域内的所有主机都有机会处理。

下一课 ARP 的第一次请求就会利用广播：发送者不知道“谁拥有这个 IP”，所以只能先问当前局域网中的所有人。

### 一个容易漏掉的情况

如果交换机查到目标 MAC 也位于帧进入的同一个端口后面，就没有必要把帧再从其他端口发出。它可以过滤这个帧。这种情况可能出现在该端口后面还连接着另一个交换设备时。

---

## 9. 纸笔实验：亲手建立一张 MAC 表

交换机有四个端口，初始 MAC 表为空：

```text
端口 1 — A（MAC=AA）
端口 2 — B（MAC=BB）
端口 3 — C（MAC=CC）
端口 4 — D（MAC=DD）
```

依次发生下面四件事：

```text
① A 发送给 B
② B 发送给 A
③ C 发送广播
④ D 发送给 B
```

先自己填表：

| 事件 | 新学到的表项 | 目标是否已知 | 从哪些端口发出 |
| --- | --- | --- | --- |
| ① A→B |  |  |  |
| ② B→A |  |  |  |
| ③ C→广播 |  |  |  |
| ④ D→B |  |  |  |

<details>
<summary>推演后展开答案</summary>

| 事件 | 新学到的表项 | 目标是否已知 | 从哪些端口发出 |
| --- | --- | --- | --- |
| ① A→B | `AA→1` | BB 未知 | 2、3、4 |
| ② B→A | `BB→2` | AA 已知 | 1 |
| ③ C→广播 | `CC→3` | 广播 | 1、2、4 |
| ④ D→B | `DD→4` | BB 已知 | 2 |

每次都先学习来源，再处理目标。这使得事件②到来时，交换机既学会 BB 的位置，也已经知道 AA 的位置。

</details>

### 改变一个条件

如果事件②从未发生，事件④的目标 BB 仍然未知，因为交换机不能仅凭事件①的“目标字段”学习 BB 的位置。于是 D→B 仍需泛洪。

---

## 10. 交换机不是路由器

两种设备都在“转发”，但查看的信息、维护的表和作用范围不同。

| 对比 | Ethernet 交换机 | IP 路由器 |
| --- | --- | --- |
| 主要查看 | 目标 MAC | 目标 IP |
| 使用的表 | MAC 地址表 | IP 路由表/转发表 |
| 典型匹配 | MAC 精确匹配 | 最长 IP 前缀匹配 |
| 主要范围 | 一个二层局域网/广播域内 | 不同 IP 网络之间 |
| 未知目标时 | 通常泛洪未知单播 | 没有匹配路由时通常丢弃 |
| 是否减少 IP TTL | 不减少 | 转发 IP 数据报时减少 |
| 是否重做链路帧 | 在同一二层网络中转发帧 | 拆掉入站帧，为出口重新封装 |

交换机通常不处理 IP TTL，因为它没有执行 IP 层的一跳转发。对 `tracert` 而言，同一路径中的普通二层交换机通常不会作为一个 IP 跳出现。

### Hub 是什么位置

历史上的集线器（Hub）更像物理层的信号复制器：从一个端口收到信号后，复制到其他端口，不学习 MAC，也不选择目标端口。现代网络主要使用交换机。这个历史对比只用于帮助理解交换机“学习并有选择地转发”的价值，不要求现在研究 CSMA/CD。

---

## 11. 一份 IP 数据报跨越路由器时，哪些地址变化

路径：

```text
A ── Switch 1 ── R ── Switch 2 ── B
```

假设：

```text
A：IP_A，MAC_A
R 左接口：IP_RL，MAC_RL
R 右接口：IP_RR，MAC_RR
B：IP_B，MAC_B
```

A 发现 B 不在自己的直连前缀，于是当前下一跳是 R 左接口。

### A 到 R 的第一段 Ethernet

```text
Ethernet：来源 MAC_A → 目标 MAC_RL
IP：      来源 IP_A  → 目标 IP_B
```

Switch 1 根据 MAC 转发这个帧，但不会把目标 IP_B 改成路由器地址。

### R 处理

R 收到目标 MAC 为自己的帧：

1. 验证并去掉第一段 Ethernet 包装；
2. 取出目标仍为 IP_B 的 IP 数据报；
3. TTL 减 1并更新必要的 IPv4 头部校验；
4. 查路由表，选择右接口和下一跳 B；
5. 为右侧链路制作一个新 Ethernet 帧。

### R 到 B 的第二段 Ethernet

```text
Ethernet：来源 MAC_RR → 目标 MAC_B
IP：      来源 IP_A   → 目标 IP_B
```

因此，在暂时忽略 NAT、隧道等机制时：

```text
MAC 地址：每跨一个路由器，随新链路帧而变化
IP 地址：继续表示原始来源与最终目标
TTL：    每次经过 IP 路由器减少
```

同一二层网络中的交换机转发帧时，通常不会像路由器那样重新选择一对新的来源/目标 MAC。交换机根据现有帧的目标 MAC，把帧送向正确端口。

---

## 12. 三张表千万不要混在一起

到这里，网络中已经出现三张长得很像的映射表：

| 表 | 谁通常维护 | 输入 | 输出 | 回答的问题 |
| --- | --- | --- | --- | --- |
| IP 路由表 | 主机、路由器 | 目标 IP 前缀 | 下一跳 IP、出口接口 | 最终目标应该朝哪个方向走？ |
| ARP 缓存 | IPv4 主机、路由器接口 | 当前下一跳 IP | MAC 地址 | 当前链路应交给哪个 MAC？ |
| MAC 地址表 | Ethernet 交换机 | 目标 MAC | 交换机端口 | 这个帧应从交换机哪扇门出去？ |

把一次发送串起来：

```text
目标 IP
  ↓ 查 IP 路由表
出口接口 + 下一跳 IP
  ↓ 查 ARP 缓存；没有则发起 ARP
下一跳 MAC
  ↓ 制作 Ethernet 帧
交换机收到帧，查询 MAC 地址表
  ↓
从正确交换机端口转发
```

这三张表的“接口”或“端口”编号也不可以混用：

- 主机的接口编号属于主机；
- 路由器的接口编号属于路由器；
- 某台交换机的端口编号只在该交换机内部有意义。

这和 Lab 0 中“路由表下标不等于接口索引”属于同一种错误：把两个不同命名空间里的整数误认为同一个概念。

---

## 13. Ethernet 提供什么，又不提供什么

在我们当前的简化模型里，Ethernet 提供：

- 在当前链路上传输一个有边界的帧；
- 用来源和目标 MAC 标识当前链路两端；
- 用 EtherType 区分载荷协议；
- 用 FCS 检测许多传输错误；
- 让交换机根据 MAC 学习和转发。

它不向 IP 或应用保证：

- 帧一定送达；
- 帧永远不损坏；
- 延迟固定；
- 带宽独占；
- 能跨任意多个 IP 网络寻找远端 MAC；
- 对方应用一定处理成功。

分层并没有消除不可靠性，而是让每层明确自己解决哪一段问题。

---

## 14. 可选观察：看看本机暴露出的链路信息

回到电脑旁后可以运行：

```powershell
getmac /v
ipconfig /all
arp -a
```

暂时只观察，不要求背输出：

- `getmac /v`：不同网络接口及其 MAC；
- `ipconfig /all`：接口的 IP、前缀/掩码、默认网关和物理地址；
- `arp -a`：本机当前缓存的 IPv4 地址到 MAC 地址映射。

`arp -a` 的内容会随通信和超时变化。它不是一份永不改变的设备名册。下一课会通过实验观察一条 ARP 缓存怎样被建立。

---

## 知识锚点

- 常见 Ethernet MAC 地址为 48 位；广播地址为 `FF:FF:FF:FF:FF:FF`。
- 普通无 VLAN Ethernet 帧的核心字段包括目标 MAC、来源 MAC、EtherType、载荷和 FCS。
- 常见 Ethernet MTU 为 1500 字节；这表示 Ethernet 载荷最多承载的上层数据大小，具体网络可能不同。
- 交换机从进入帧的来源 MAC 学习端口；已知单播定向转发，未知单播和广播通常在相应广播域中泛洪。
- 路由器按目标 IP 前缀选择下一跳与出口；交换机按目标 MAC 选择交换机端口。

进一步核对可参考：

- [IEEE 802.3 Ethernet 工作组](https://www.ieee802.org/3/)
- [RFC 894：IPv4 数据报在 Ethernet 上的封装](https://www.rfc-editor.org/rfc/rfc894)

---

## 小结

1. IP 地址负责端到端的网络层目标，MAC 地址负责当前 Ethernet 链路的帧交付。
2. Ethernet 帧用目标/来源 MAC、EtherType、载荷和 FCS 建立链路层边界。
3. 交换机从来源 MAC 学习端口；目标已知就定向转发，未知单播或广播则泛洪。
4. 交换机主要在二层工作，不执行普通 IP 路由，也不减少 TTL。
5. 数据报跨越路由器时，旧链路帧被拆掉并为新链路重做；MAC 地址改变，而最终目标 IP 通常保持。
6. IP 路由表、ARP 缓存和交换机 MAC 表分别回答方向、下一跳 MAC 和交换机出口三个问题。
7. MTU 限制一帧能承载的 IP 数据报大小；各层头部会共同消耗可用空间。

---

## 检查站

请合上正文或滚动到看不到上文，再用自己的话回答。

1. 主机 A 为 `192.168.1.10/24`，主机 B 为 `192.168.1.200/24`。它们的完整 IP 不同，为什么 A 仍可能把 B 当作直接下一跳？“直连”究竟省略了哪类设备？

2. 分别用一句话说明目标 IP、当前下一跳 IP、目标 MAC 的作用范围。A 经过路由器 R 向 B 发送时，第一段链路的目标 MAC 应属于谁？

3. 写出 Ethernet 帧中目标 MAC、来源 MAC、EtherType、载荷和 FCS 各自解决什么问题。FCS 检测失败是否意味着 Ethernet 会自动恢复数据？

4. 一个普通 Ethernet MTU 为 1500 字节，IPv4 头为 20 字节，UDP 头为 8 字节。忽略其他扩展时，一个不超过该 MTU 的 UDP 数据报最多剩多少字节给应用数据？为什么不是 1500？

5. 一台交换机的 MAC 表最初为空。它从端口 4 收到：

   ```text
   来源 MAC = AA
   目标 MAC = BB
   ```

   它能立刻学到什么？为什么不能从这一帧学到 BB 位于哪个端口？因为 BB 未知，这一帧接下来怎样转发？

6. 接着上一题，交换机从端口 2 收到 `BB→AA`。处理完后 MAC 表至少有什么？这个回应帧会从哪个端口发出？

7. 未知单播与广播都会被交换机泛洪。两者的区别是什么？其他主机收到目标 MAC 不属于自己的未知单播帧时，通常怎样处理？

8. 为什么路径中的普通交换机通常不会出现在 `tracert` 的 IP 跳数里，也不会让 TTL 减 1？

9. 数据报沿 `A → R → B` 传输。请分别写出 A→R 和 R→B 两段中的来源/目标 MAC，以及两段中保持的来源/目标 IP。可以使用 `MAC_A`、`MAC_RL` 等符号。

10. 对下面三张表各写一句用途，不能只展开缩写：

    ```text
    IP 路由表
    ARP 缓存
    交换机 MAC 地址表
    ```

11. 附加设计题：如果交换机永远不让 MAC 表项老化，某台电脑从端口 1 移到端口 3 后可能发生什么？看到什么来源帧能够修正这条记录？

把答案写在本文件下方或发给我。最长前缀匹配已经通过，本次主要看你是否能把“路由选择”和“局域网实际交付”接成一条完整链。
