# 第 32 课：从启动游戏到收到结果——完整旅程与故障定位

> 前置：第 27～31 课。预计 3～4 小时，含纸面推演和检查站；不安排编程 Lab。
> 本课不是从头复述所有章节，而是训练两件新能力：把各层证据串起来，以及用一次观察缩小故障范围。
> 必要的新桥梁：网络配置从哪里来、IPv6 与此前 IPv4 模型的边界、超时预算与安全诊断。

## 1. 先固定一个世界，避免边讲边换设定

玩家电脑在家里，使用 IPv4 上网。网卡地址为 `192.168.1.10/24`，默认网关 `192.168.1.1`，DNS 递归解析器地址已配置。家庭路由器公网侧地址用 `203.0.113.8` 表示。

游戏配置的入口是 `https://api.example.com/rooms`，DNS 在本例返回 `198.51.100.20`。服务器监听 443，并直接处理 TLS 和 HTTP；暂时没有入口代理。示例地址不是可连接的真实测试服务。

玩家先查询房间，再登录并通过 `wss` 接收房间通知。协议之间的切换由应用明确发起，不是网络自动猜出来的。

先预测：如果界面最后显示“加入失败”，它可能是 DNS、TCP、TLS、HTTP 或游戏规则哪一层的问题？**只有这四个字，还不足以判断。** 本课要做的是收集能区分它们的证据。

## 2. 第一个漏洞：自己的 IP、网关与 DNS 是谁告诉我的

前面很多题直接给了主机配置。真实电脑刚接入网络时，如果每台机器都手动填写地址，既麻烦又容易填重。于是可以让本地网络中的配置服务分配地址，并给出掩码、网关和 DNS 等信息。

IPv4 中常见的自动配置协议叫 **DHCP（Dynamic Host Configuration Protocol，动态主机配置协议）**。典型首次取租约的简化过程是：

```text
Discover：客户端询问有没有配置服务
Offer：服务器提出可用地址与配置
Request：客户端请求使用选中的配置
ACK：服务器确认租约
```

这时客户端还没有可正常使用的完整地址配置，因此发现阶段会利用本地广播；跨路由器的部署可以由 DHCP 中继转交，不能假设普通广播会自动越过路由器。这里的 ACK 是 DHCP 消息名，不是 TCP 累计 ACK。

“租约”表示地址使用有期限，客户端需要按规则续租。实际流程还有拒绝、冲突检测和重试等分支，不要求背所有字段。家用路由器可能同时提供 DHCP、DNS 转发、NAT 等服务，**这些是不同职责，不是同一个协议**。[DHCP 规范](https://www.rfc-editor.org/rfc/rfc2131.html)

DHCP 主要解决本机怎样获得网络配置；DNS 解决指定名字对应哪些记录。已经手动配置好的主机也能查 DNS，不能说“每次查域名都先做一次 DHCP”。

## 3. 出门前：拿地址、选路径、找下一跳

应用根据 URL 需要访问 `api.example.com`。本机存根解析器先利用本地可用的缓存或查询机制；需要向递归解析器查询时，由后者利用缓存和委派获得记录。解析过程本身也需要网络，但不一定每次都走完整根服务器链。

拿到 `198.51.100.20` 后，应用通过连接 API 请求连接其 443 端口。操作系统选择合适的本地地址和临时端口，例如 `192.168.1.10:53000`，并为连接维护 TCP 状态。

本机查路由表发现目标不在直连网段，于是使用网关 `192.168.1.1` 作为下一跳。若不知道网关 MAC，在当前链路使用 ARP 解析它；不是隔着公网查询服务器 MAC。

```text
IP 层最终目标：198.51.100.20
当前下一跳 IP：192.168.1.1
当前 Ethernet 帧目标 MAC：家中网关的内侧接口 MAC
```

这里“当前下一跳”是本机路由和接口处理所需的信息，不是额外塞进 TCP 载荷的地址。交换机按 MAC 表转发帧；路由器拆解当前链路包装、查路由并制作下一链路包装。经过家庭 NAT 时，示例来源还会从私有端点改为公网映射端点。

这段只用于衔接旧知识。若你已能讲通，不需要重做 ARP 或最长前缀 Lab。

## 4. 四种建立不能叫成一句“连接成功”

TCP 三次握手成功，只证明两端在当时已建立相应传输状态。随后客户端进行 TLS 握手，验证预期服务身份，再发送 HTTP 请求。HTTP 处理器返回结果后，游戏还要决定是否允许进入房间。

| 观察到的成功 | 已经获得的证据 | 尚未证明 |
| --- | --- | --- |
| DNS 返回地址 | 有名字解析结果 | 端口可达、记录一定新鲜 |
| TCP 建连成功 | 当时的传输连接已建立 | 证书正确、业务正常 |
| TLS 验证成功 | 受保护通道与所验证的身份成立 | 玩家账号有效、房间有空位 |
| HTTP 有响应 | 有服务处理并返回 HTTP 消息 | 游戏动作一定成功 |
| 加入房间成功 | 服务端已接受本次业务操作 | 之后永不掉线、所有客户端 UI 已更新 |

收到错误响应也是信息。例如 403 至少比“完全没结果”多出了一条线索：有 HTTP 处理方在拒绝请求。具体为何拒绝，还需要正文契约和相关日志。

## 5. 收到的字节最终怎样变成界面

对于本课 HTTPS/TCP 示例，接收路径可以这样理解：

```text
本机网卡接收链路帧
→ IP 处理并交给 TCP
→ TCP 检查、重组，提供有序的接收字节
→ 通过 Socket 相关接口交给 TLS 库
→ TLS 验证并解密
→ HTTP 解析器恢复状态码、首部和正文
→ 游戏反序列化并校验结果
→ 按 Unity 的线程与生命周期要求更新界面
```

Socket 是应用使用操作系统网络能力的接口与相关状态，不是重组器之后又经过的一层网络报文。先前的 ByteStream 是理解接收缓冲的教学模型，不要求真实系统里存在同名的 C++ 对象。

HTTP 客户端库可能已经封装 Socket、TLS 和解析过程；调用者一次 await 得到响应，并不说明底下没有这些阶段。异步是在等待期间不必卡住当前执行线程，也不把一次业务操作变成瞬间完成。

## 6. 为什么一次“慢”要拆成几个时间段

假设首次请求没有可复用连接，教学模型给出以下测量：

| 阶段 | 用时 |
| --- | ---: |
| DNS 查询 | 20 ms |
| TCP 建连 | 40 ms |
| TLS 握手 | 40 ms |
| 请求抵达至响应开始抵达，包括往返与服务端处理 | 100 ms |
| 剩余正文接收 | 10 ms |

在这里各阶段顺序执行且没有重叠，总计 210 ms。这个表是情境数据，不是协议规定每次必耗这些时间。缓存、连接复用、不同握手、并行尝试都会改变真实时间。

若下一次复用连接，前面某些阶段就不再发生。若服务器排队 2 秒，提升本地带宽不一定消掉这 2 秒。要先确定慢在解析、建立、安全握手、服务器处理还是数据传输，而不是见到慢就换 UDP。

还要分清超时预算：DNS、连接、一次读取、整个业务请求都可以有不同的时间限制。应用说“我等够了”时，对端可能仍在执行操作。取消等待不自动撤销服务器已接受的购买。

## 7. 一种比随机改配置更有效的排查法

每次写清三个东西：**已观察到什么、还不能推出什么、下一步检查能区分哪两种可能**。

例如客户端提示超时，先问错误发生在 DNS 查询、Connect、TLS 握手还是等待响应。只看统一包装后的“网络异常”往往不够，需要保留底层错误类型与阶段。

| 现象 | 可以先缩小到哪里 | 下一项有区分力的检查 |
| --- | --- | --- |
| 域名没有解析结果 | 名字或解析路径 | 名字拼写、配置的解析器、查询返回的错误 |
| DNS 成功，TCP 超时 | 目标地址/端口与路径仍待查 | 是否监听正确接口、防火墙、路由和 NAT 条件 |
| 立即拒绝连接 | 对方或途中设备主动拒绝 | 服务进程、监听端口、拒绝规则；不是唯一指向服务未启动 |
| TCP 成功，证书名字不匹配 | TLS 身份关口 | URL 主机名、服务证书及入口配置 |
| HTTP 401/403 | 身份或权限等应用入口 | token 有效性、账号权限与服务端拒绝原因 |
| HTTP 成功但 UI 不变化 | 应用结果与显示链 | 正文、业务判断、消息分派、主线程更新 |
| WebSocket 重连后状态陈旧 | 业务恢复链 | 是否重新鉴权、快照版本、漏掉的事件 |

检查监听时别只问“有 7777 吗”，还要看地址与协议：`127.0.0.1:7777` 只在回环上监听，不能由别的电脑直接访问；UDP 7777 也不能替 TCP 7777 接受握手。

不通过“关闭整个防火墙”来猜原因。优先查看日志、规则与限定端口，测试只针对自己有权访问的设备；需要改配置时先明确作用范围。

## 8. 工具给的是证据，不是神谕

本课可以全程在教室阅读。下面是以后使用工具时要知道的边界，不要求现在执行：

- `ipconfig /all`：观察本机地址、网关、DNS 等配置，不证明外部服务可达。
- `nslookup example.com`：观察 DNS 查询，不证明游戏端口开放。
- `Test-NetConnection <你的服务器> -Port 7777`：检查指定 TCP 端口的连接，不验证游戏登录。
- `ping`：测试相应 ICMP 回应；ICMP 被过滤时，HTTPS 仍可能正常。
- `tracert`：利用逐跳探测观察路径线索；星号可能是设备不回探测，不一定代表真实业务断在那里。
- `curl.exe -v https://<你的服务>/...`：观察连接、TLS 与 HTTP；详细输出可能含敏感首部，分享前脱敏。
- 抓包工具：查看接口上实际看到的包；HTTPS 正文通常已加密，抓到包不等于能读业务内容。

检查自己的监听状态可以使用系统网络工具。不要把命令名当答案：每次应先说“我想验证哪一条假设”，再选工具。

## 9. 日志怎样把两台机器上的同一次操作连起来

客户端说“购买超时”，服务端说“今天处理过很多购买”，仍然无法对应。给同一业务操作一个可追踪编号，并让客户端与服务端记录它经过的阶段，才能找到同一条链。

最小有用日志可包含：时间、连接编号、操作或请求编号、当前阶段、结果类别、耗时。登录 token、密码和完整私人数据不应直接记录。跨机器的时钟可能有偏差，比较日志时间时要考虑这一点。

示例：

```text
客户端 op=P17：提交购买 → 等待响应 → 超时
服务器 op=P17：收到 → 权限通过 → 扣款成功 → 响应发送失败
```

现在可解释“用户没看到成功，但扣了钱”。这不是一句“TCP 不可靠”能概括的，而是客户端没有得到业务结果。恢复时查询 P17 的结果，比无条件新建 P18 再买一次更合理。

## 10. IPv6：旧模型哪些能保留，哪些不能照搬

DNS 可能返回 IPv4 的 A 记录，也可能返回 IPv6 的 AAAA 记录。IPv6 地址是 128 位，常用冒号分隔的十六进制表示。方括号用于 URL 中区分 IPv6 地址里的冒号和端口分隔符，例如 `http://[2001:db8::10]:8080/`。

你不需要重学整套网络：目标与下一跳、最长前缀匹配、端口、TCP 字节流、HTTP、TLS 的核心职责仍成立。但下面不能直接照抄：

- IPv6 不使用 ARP，而是用基于 ICMPv6 的邻居发现完成邻居地址解析等工作。
- IPv6 没有 IPv4 式广播；许多本地发现使用多播，即发给一组相关接收者。
- 路由器通告可提供前缀和默认路由等信息，主机可以自动配置地址；也可涉及 DHCPv6，不能把 IPv4 DHCP 流程原封不动搬过来。
- 常见 IPv6 部署不靠家庭 IPv4 式 NAPT 共享一个地址，但**有可路由地址不代表防火墙允许入站**。

这些只是给已有模型画边界，不要求这节课完整掌握 IPv6 管理。排错时至少问清“本次实际尝试的是哪种地址”，避免只测 IPv4 就断定 IPv6 也正常。[IPv6 基础规范](https://www.rfc-editor.org/rfc/rfc8200.html)、[邻居发现](https://www.rfc-editor.org/rfc/rfc4861.html)

## 11. 最后串一次，不背名词堆

不要背“DHCP、DNS、ARP、TCP、TLS、HTTP”这一串。按需求说：

1. 我得先有能用的网络配置，可能来自 DHCP 或静态设置。
2. 应用知道服务名字，通过名字解析取得可尝试的地址。
3. 系统根据目标选路，在本地链路找到当前下一跳的地址。
4. 数据跨链路转发，可能经过 NAT；防火墙可能允许或拦截。
5. TCP 建立并维护字节传输；TLS 确认服务身份并保护内容。
6. HTTP 或 WebSocket 解释消息结构；账号与业务代码决定操作是否有效。
7. 客户端接到结果后更新自己的状态和画面；断线则处理不确定结果与恢复。

并不是每次请求都完整执行七遍：配置、DNS、ARP 可能有缓存，连接可能复用。逻辑依赖存在，不等于每次都重新发同样的网络包。

[打开分层证据与故障定位交互](32_故障定位_交互.html)

## 检查站

1. DHCP 与 DNS 分别解决什么问题？路由器同时提供两项服务，是否意味着它们是同一协议？
2. 本机 `192.168.1.10/24` 发往外部服务器，最终目标 IP、当前下一跳 IP 与当前目标 MAC 分别指向谁？
3. 用一句话分别解释 DNS 成功、TCP 成功、TLS 成功、加入房间成功。后一项能否直接由前一项推出？
4. 写出本课 HTTPS 响应从 TCP 重组后到 UI 更新的主要步骤。Socket 在这里扮演什么角色？
5. 将第 6 节时间表中的服务器相关阶段从 100 ms 改为 300 ms，其余不变，总耗时多少？这能否证明带宽不足？
6. 本机连回环成功，另一台电脑连不上。除了防火墙，还应看监听的哪个字段？为什么？
7. `ping` 不通但 HTTPS 正常，是否矛盾？得到 HTTP 403 能否描述成“没有建立 TCP 连接”？
8. 客户端购买超时，但服务端日志显示成功。下一步为何不该立即生成新订单重试？
9. IPv6 下仍能用最长前缀匹配和 TCP 吗？解析邻居 MAC 是否仍叫 ARP？可路由地址是否等于允许入站？
10. 给“DNS 成功、TCP 成功、证书校验失败”选一项最有用的下一步检查，并说明它在排除哪一种假设。
11. 一份 HTTP 请求为什么不一定触发一次新 DNS 查询、一次新 ARP 和一次新 TCP 握手？

## 理论结课不等于项目经验已经补齐

完成检查后进入 [[第一阶段_结课检查与针对性补充]]。通过意味着能解释核心机制并在情境中推理，不意味着已具备生产游戏服务的完整开发运维经验。

下一阶段读 GameFrameX 时，先找到消息从连接入口到业务处理器的调用链，再看协议、会话、线程与断线处理。你已经有一张地图，但框架 API 仍需要对着代码学习；这两件事并不矛盾。
