# Lesson 5B：TTL 与四种延迟补充

> 预计时间：10–20 分钟
>
> 目标：只修正第 5 课检查中暴露的两个混淆，不增加新课程负担

## 学习目标

完成本页后，你应该能够：

1. 正确推演数据报经过多台路由器时的 TTL；
2. 计算给定数据量和链路速率下的发送延迟；
3. 区分发送延迟与传播延迟；
4. 判断提高带宽会直接改变哪一部分延迟。

---

## 1. TTL：在路由器转发时减少

路径：

```text
A → R1 → R2 → B
```

初始 TTL=3：

```text
A 发出：TTL=3
R1 收到：3 → 2，允许转发
R2 收到：2 → 1，允许转发
B 收到：TTL=1
```

关键是：B 是最终主机，不是第三台负责继续转发的路由器。因此不能因为图中有三条箭头，就把 TTL 减三次。

初始 TTL=2：

```text
A 发出：TTL=2
R1 收到：2 → 1，允许转发
R2 收到：TTL=1，不能继续转发，丢弃
B 收不到
```

可以用一句规则检查自己：

> 数经过一台负责转发的路由器，才进行一次 TTL 转发检查与减少。

### 不要混淆“到达值”和“下一次能否转发”

B 收到 TTL=1 不矛盾。它已经到达最终目标，不需要 B 再把它转发一跳。如果这份数据报还需要经过第三台路由器，那么 TTL=1 就不够。

---

## 2. 发送延迟：把全部比特推出接口要多久

公式：

```text
发送延迟 = 数据长度（bit）/ 链路速率（bit/s）
```

题目给的是 1200 字节，链路速率是 12 Mbit/s。单位必须先统一：

```text
1200 byte × 8 = 9600 bit
12 Mbit/s = 12,000,000 bit/s
```

代入：

```text
9600 / 12,000,000 秒
= 0.0008 秒
= 0.8 毫秒
```

如果链路升级为 120 Mbit/s：

```text
9600 / 120,000,000 秒
= 0.00008 秒
= 0.08 毫秒
```

速率提高十倍，同一份数据的发送延迟缩短十倍。

### 为什么不能写 `1200 / 12M`

1200 的单位是字节，12 M 的单位是每秒多少比特。不同单位直接相除，结果没有正确含义。先乘 8 把字节变成比特，是这个计算最容易漏掉的一步。

---

## 3. 发送延迟与传播延迟不是一回事

### 发送延迟

```text
把一整份数据逐比特推入链路需要多久
```

它主要取决于：

```text
数据有多大
链路每秒能发送多少比特
```

### 传播延迟

```text
某个信号已经进入链路后，从一端传播到另一端需要多久
```

它主要取决于：

```text
距离
信号在介质中的传播速度
```

### 一列火车的类比

想象一列火车经过收费站，再开往远方车站：

- 让所有车厢依次通过收费站所需的时间，类似发送延迟；
- 第一节车厢从收费站开到远方车站所需的路程时间，类似传播延迟。

把收费站通道加宽十倍，可以让整列车更快通过收费站，但不会自动缩短两座车站之间的距离。

所以提高带宽时：

```text
发送延迟：直接下降
传播延迟：不一定改变
处理延迟：不一定改变
排队延迟：可能间接受影响，但不保证按十倍变化
```

---

## 4. 四种延迟放回同一条路径

```text
处理延迟：设备读头部、检查并查表
排队延迟：在出口等待前面的数据报
发送延迟：把全部比特推入链路
传播延迟：信号沿介质到达另一端
```

快速判断：

| 变化 | 最直接影响 |
| --- | --- |
| 数据报从 1200 字节变成 2400 字节 | 发送延迟约变成两倍 |
| 链路从 12 Mbit/s 变成 120 Mbit/s | 发送延迟约缩短十倍 |
| 两台设备之间距离变远 | 传播延迟增加 |
| 很多数据同时争抢同一出口 | 排队延迟增加，队列满时可能丢包 |
| 路由器查表和处理变慢 | 处理延迟增加 |

---

## 小结

1. TTL 按负责转发的路由器逐跳处理；初始 3 经过 R1、R2 后，B 收到 1。
2. 计算发送延迟前，要把字节乘 8 变成比特。
3. 发送延迟等于数据比特数除以链路速率。
4. 提高带宽直接缩短发送延迟，不会自动缩短传播距离。

---

## 复测题

请合上正文，用自己的话回答：

1. 在 `A → R1 → R2 → R3 → B` 中，初始 TTL=4。写出每台路由器处理后的 TTL，以及 B 收到时的 TTL。

2. 同一路径初始 TTL=3，数据报会在哪里被丢弃？请逐跳写出原因。

3. 一份 1500 字节的数据在 10 Mbit/s 链路上的发送延迟是多少毫秒？请写出单位换算。

4. 把链路从 10 Mbit/s 升到 100 Mbit/s，为什么传播延迟不一定缩短？处理、排队、发送、传播四类中，哪一类会在其他条件不变时直接缩短十倍？

把这四题发给我。通过后，第 5 课理论部分可以标记为基本掌握；Lab 0 仍按实践结果单独检查。

---

## 我的首次复测回答（2026-09-02）

1. “Rn 接收，TTL--，若大于零允许转发，B 收到为 0。”
2. “R3，这个时候接收 TTL 减为零，丢。”
3. `1500*8/10 000000`
4. “发送。”

## 教师反馈（2026-09-02）

判断：**仍为部分理解，但已经有明显进展；不需要重读本页。**

- 第 1 题仍有核心混淆。路径中只有 R1、R2、R3 三台路由器负责转发，B 是最终主机，不会作为第四台路由器再减少一次 TTL。因此应是 R1：4→3，R2：3→2，R3：2→1，B 收到 1。
- 第 2 题找对了丢弃位置。按照 Lab 0 的规则，R1 将 3 减为 2，R2 将 2 减为 1；R3 看到 TTL=1 时直接判定过期，不把 TTL=0 的数据报继续转发。
- 第 3 题已经正确完成 `1500 byte × 8 = 12000 bit`，但还缺少数值和单位：`12000 / 10,000,000 = 0.0012 s = 1.2 ms`。
- 第 4 题正确指出“发送延迟”，但还缺少原因：传播延迟主要由距离与介质中的传播速度决定，提高每秒发送比特数不会自动缩短距离。

请直接回答下面两道收尾题，不必再次阅读正文：

1. 在 `A → R1 → R2 → B` 中，初始 TTL=5。R1、R2 处理后分别是多少，B 收到多少？
2. 2000 字节在 20 Mbit/s 链路上的发送延迟是多少毫秒？如果只把带宽提高，为什么传播延迟不一定改变？
