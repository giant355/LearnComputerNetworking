# 第 30 课：我没再问，你怎样通知我——WebSocket、心跳与断线恢复

> 前置：[[29_HTTP_通用请求与响应]]。下一课：[[31_TLS_加密身份与权限]]。
> 预计 2～3 小时，含情境推演与检查站。无 Lab，不要求背 API。
> 本课新增的是推送、连接活性与业务恢复；不重新讲 TCP 重传状态机。

## 1. 玩家一直盯着房间列表，但没有发送新请求

客户端刚用 HTTP 查到房间里有 A、B 两人。十秒后 C 加入，客户端屏幕仍显示两人。TCP 没丢数据，HTTP 也没报错，因为客户端从未要求下一次信息，而服务器也没有沿用的业务通知机制。

最朴素的办法是每秒查询一次。每次都问“变了吗”，叫**轮询**。若每秒问一次，变化刚好发生在上次查询之后，通常要等到下次查询才能知道，另外还要算网络和处理时间。缩短间隔能减少等待，却增加请求和空结果。

另一种办法是服务器收到查询后先不立即响应，等有变化或等到超时再回复；客户端拿到结果后立即发下一次查询。这叫**长轮询**。它仍是一轮请求对应一轮响应，只是响应可以等待一段时间。

这些办法不是错误方案。但当聊天、玩家进入、准备状态等消息频繁发生时，我们希望连接建立后，双方都能在需要时发送消息，而不是为每次通知重新组织一轮查询。

## 2. TCP 已经双向，WebSocket 又加了什么

原生 C# 程序可以在 TCP 上定义自己的双向协议，第 24、25 课正是这条路线。浏览器脚本通常不能随意创建裸 TCP Socket，还需要一个被服务器、浏览器及相关设施共同理解的消息协议。

**WebSocket** 提供建立会话的握手、消息分帧和双向交换规则。它不是 Socket 这个通用词的另一个写法，也不是 UDP。普通 WebSocket 在 TCP 上提供有序、可靠的消息传递；加密形式叫 `wss`，下一课讲它的安全保护。

我们用 `ws://game.example.com:8080/events` 为例。客户端先解析名字，建立 TCP 连接，随后请求使用 WebSocket。下面限定经典的 HTTP/1.1 握手，其他 HTTP 版本有不同的建立方式。

```http
GET /events HTTP/1.1
Host: game.example.com:8080
Upgrade: websocket
Connection: Upgrade
Sec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ==
Sec-WebSocket-Version: 13

```

先看动机：双方原本按 HTTP 解析字节，不能一方突然改发新格式，另一方仍按 HTTP 理解。`Upgrade` 是明确提出“接下来换协议”，`Connection` 指明这里的升级是这条连接上的安排。

`Sec-WebSocket-Key` 是握手中使用的一次性随机值的编码，服务端按标准生成对应的 `Sec-WebSocket-Accept`。客户端验证响应，避免把普通 HTTP 服务错误当成已接受 WebSocket 的服务。**这个 Key 不是给业务正文加密的密钥，也不是玩家登录凭据。** 计算公式可暂时交给库。

服务端接受时，返回 `101 Switching Protocols` 和相应首部。完成握手后，这条连接的后续字节由 WebSocket 协议处理，不是每条通知都再包成一个完整 HTTP 请求。[WebSocket 握手和分帧规范](https://www.rfc-editor.org/rfc/rfc6455.html)

## 3. 谁负责哪一层边界

想象服务器要通知客户端 C 已加入房间。业务消息可能是：`{"kind":"Notification","type":"PlayerJoined","playerId":3}`。

```text
业务代码：创建 PlayerJoined 消息
WebSocket 库：把消息编码成一个或多个 WebSocket 帧
TCP：搬运这些帧的字节，丢失时重传、接收时重组
对端 WebSocket 库：从字节恢复帧，再恢复完整消息
对端业务处理器：解释 PlayerJoined 并更新房间状态
```

这里出现三个尺度：业务消息、WebSocket 帧、TCP 段。它们不必一一对应。一条 WebSocket 消息可分成多个帧；一个帧的字节又可跨多个 TCP 段。不能用抓包里的一个 TCP 段长度判断通知是否完整。

协议有消息边界，并不代表每个接收 API 每次调用都交付一整条大消息。例如分块接收接口可能告诉你“消息尚未结束”，你需要按接口契约继续累积，直到结束标记成立。不要再次把“一次读取”等同于“一条消息”。

WebSocket 库负责恢复它自己的消息边界。你仍要约定消息里的种类、类型、玩家编号等含义。若有请求与响应，还要决定如何关联；WebSocket 不会自动把购买结果配回某个购买按钮。

## 4. 长连接到底是多长，谁让它活着

“长连接”只是连接建立后持续复用的一种用法，没有一个统一的“超过一分钟才算长”的标准。TCP 连接可以保持很久，HTTP 也可以复用连接，WebSocket 常常长期保持。它们不是三个互斥选项。

现在制造一个故障：玩家从 Wi-Fi 切到移动网络，旧路径失效。没有 FIN，也没有立即到达的 RST，应用暂时仍保留一个连接对象。

对象存在不证明路径活着；一个本地 `Connected` 类属性通常也只能反映已知或上次操作得到的状态。对方暂时不发消息与链路断开，在短时间内可能看起来一样。我们需要主动询问并观察反馈。

## 5. 心跳不是保证，而是有时间限制的证据

可以约定：“我发一个 Ping，你收到后回一个 Pong”。这个用来探测活性的周期性交换叫**心跳**。WebSocket 有协议级 Ping/Pong；业务也可以定义自己的心跳消息。两者不能混用而不说明。

TCP keepalive 是操作系统层的探测机制，配置和时间尺度可能不同；WebSocket Pong 可以由库自动回复；业务心跳若由业务循环回复，则能额外观察该循环是否及时工作。因此：“收到了 Pong”能证明什么，取决于谁生成这个 Pong。

为便于推演，做一个教学约定，不是推荐所有游戏照抄的数值：

- 连续 10 秒没有任何接收活动，就发送一个带编号的心跳探测。
- 从发送探测起，再等 5 秒仍没有匹配回复，进入“疑似断开”，停止依赖旧连接并尝试恢复。
- 停止等待不等于证明服务器已崩溃；也可能是拥塞、程序暂停或链路故障。

若 t=0 最后收到消息，t=10 发探测，t=14 收到匹配回复，不超时；t=15 仍无回复，才按约定处理为失败。真实实现应使用单调计时，也就是观察持续经过了多久，而不是拿可能被手动调整的“现在几点几分”直接相减。

心跳可以帮助维持某些中间设备的活动记录，但不能保证所有 NAT 映射永远存在，也不能穿透所有防火墙。TCP 重传能处理部分丢包，心跳超时表达的则是应用不愿无限等下去。

## 6. 能重新 Connect，不等于已经回到游戏

客户端建立一条新的连接，底层只有“现在又有一条通路”。旧连接上的账号身份、房间成员资格、未收到的通知和未确定结果的购买，不会靠新三次握手自动搬过去。

因此恢复过程要分层：

```text
发现旧连接失效
→ 稍后重新连接
→ 重新证明账号身份
→ 请求恢复房间或重新加入
→ 补齐错过的状态
→ 恢复界面可操作状态
```

“重新证明身份”可以携带仍有效的登录凭据；凭据过期则重新登录，第 31 课会解释。不要把新 Socket 的存在当成服务器已经认识你。

连接断开后，许多客户端同时每毫秒重试会进一步压垮服务器。可逐步增加等待，例如 1、2、4、8 秒，再限制最大等待，同时加入少量随机偏移，避免所有人同一时刻撞回来。这叫**退避**与**抖动**；数值是例子，不能无限翻倍。

## 7. 恢复遗漏信息：快照与补发两条路线

客户端离线前看见 A、B，离线期间 C 加入、B 离开。重连后只接受新的通知，它可能永远保留 B、永远漏掉 C。

最简单可靠的做法是重新索取完整的当前状态，例如“现在房间内是 A、C”。这份能重新建立当前世界的状态叫**快照**。它不是之前所有事件的重播。

若事件很多且服务器保留记录，可以给每次业务变化一个递增版本，例如 81、82、83。客户端说“我已经应用到 81”，服务器补发 82 之后的变化。这里的版本是**应用状态版本**，不是 TCP SEQ，也不是请求 ID。

补发需要边界约定：

1. 服务器还保存着缺失范围，才能补发；否则发新快照。
2. 客户端已经处理过的版本，重复到达时不能再做一次不可重复的副作用。
3. 先应用版本 83 的快照，再衔接 84 以后的事件；不要应用完快照又执行 82，导致状态倒退。
4. 快照与后续事件之间要有一致的版本衔接，避免获取快照期间发生的变化掉进缝隙。

对于显示当前位置，直接用新快照可能足够；对于订单，不能只靠“最新画面”判断是否扣过钱，需要向服务器查询业务结果。

[打开心跳、重连与恢复交互](30_连接恢复_交互.html)

## 8. 为什么不能重连后把所有请求再发一遍

购买 P 已在服务器扣钱，响应却在断线时没到客户端。客户端不知道 P 成功还是失败。再次发送一个全新购买编号，服务器可能合理地认为这是第二笔购买。

“没收到成功”只能推断**客户端不知道结果**。可以用稳定的业务操作编号查询结果，或由支持去重的接口识别同一操作的重试。跨连接去重时，键通常还需要账号或业务范围；仅用旧连接内临时 requestId 可能不够。

这与第 25 课不矛盾：请求 ID 首先用于关联响应；是否也承担跨连接去重，要看协议明确约定，不能想当然。日志也应区分连接编号、请求编号和业务操作编号。

## 9. 关闭也有不同层次

玩家正常离开时，可以先发业务上的离开房间消息，再进行 WebSocket 关闭握手，最后结束底层连接。但意外断网不会保证给你这个机会，服务器仍需要超时清理。

WebSocket Close 帧是 WebSocket 协议的控制消息；TCP FIN 是结束一个 TCP 发送方向；“房间里移除这个玩家”是业务状态改变。它们相关，却不是同一事件。

## 10. 怎样选择，而不是把协议排成高级低级

| 需求 | 可考虑的方案 | 仍需自己处理 |
| --- | --- | --- |
| 偶尔查询公告 | HTTP | 缓存、错误状态、重试 |
| 浏览器中的双向房间通知 | WebSocket | 消息意义、鉴权、恢复 |
| 原生客户端已有完整自定义协议 | TCP 加自定义分帧 | 整套消息与连接生命周期 |
| 强调新鲜度的实时运动数据 | 按项目评估 UDP 或相应传输库 | 拥塞控制、可靠性取舍、同步设计 |

不是“游戏就该全部 TCP”，也不是“WebSocket 天然解决实时游戏所有问题”。TCP 上的 WebSocket 仍可能因等待缺失字节影响后续消息交付；协议名称不会消除网络延迟。

## 检查站

1. 每秒查询房间一次有什么代价？长轮询与 WebSocket 的交换方式有什么区别？
2. TCP 已经双向，WebSocket 额外约定了什么？为什么不能把它理解为 Socket 的全称？
3. HTTP/1.1 WebSocket 握手成功后，每条通知是否仍是一份新的 HTTP 请求？握手 Key 是否等于加密密钥？
4. 一条业务通知、一个 WebSocket 帧、一个 TCP 段是否必然一一对应？接收 API 是否一定一次给出整条消息？
5. 按正文的 10 秒空闲、再等 5 秒规则，最后活动 t=20，t=30 发探测，t=34 收到回复，是否超时？t=35 仍无回复又怎样？
6. 没有收到心跳回复，为什么不能断言服务器进程一定崩了？协议级 Pong 又为什么不一定证明购买处理器正常？
7. 新连接建立后，还需要恢复哪些业务状态？为什么不能直接启用所有界面按钮？
8. 客户端已处理版本 41，服务器只有从 50 开始的事件记录。该强行补 42，还是给当前快照？为什么？
9. 购买已扣款但响应没到，重连后直接发新购买请求有什么危险？哪一种编号应保持稳定，需要什么服务端支持？
10. 说明业务离开、WebSocket Close 与 TCP FIN 的区别。

下一课：[[31_TLS_加密身份与权限]]。连得上、恢复得了以后，还要问“路上的人能否偷看，你连的又究竟是谁”。
