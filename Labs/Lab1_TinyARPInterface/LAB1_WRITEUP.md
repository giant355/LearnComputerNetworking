# Lab 1：Tiny ARP Network Interface 报告

> 按精简格式记录。没有出现过的猜测不要虚构。

## 测试摘要

- `ctest` 结果：
- 通过数量：`__/6`

## 核心解释

1. 为什么 D1、D2 要进入以“下一跳 IP”为键的待解析队列，而不是以最终目标 IP 为键？

2. 收到 `X → MAC_X` 的 ARP 消息后，为什么要先更新缓存，再冲刷等待 X 的数据报？

3. 为什么 `recv_frame` 收到属于自己的 IPv4 帧时返回数据报，而不是制作新 Ethernet 帧？这个行为与普通二层交换机有什么不同？

## 一个关键 bug（若有）

- 最难调试的位置：
- 观察到的错误：
- 实际原因：
- 怎样修复：

## 实际用时

- 阅读 Lab：
- 实现：
- 调试：
- 报告：
- 总计：
