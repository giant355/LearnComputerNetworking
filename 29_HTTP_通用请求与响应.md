# 第 29 课：不再各说各话——HTTP 怎样组织请求与响应

> 阅读顺序：[[28_NAT_端口映射与公网连入]] → 本课 → [[30_WebSocket_长连接与断线恢复]]。
> 预计 2～3 小时，包含停下来推演和检查站，不是要求读正文读满两小时。无需写代码，无新 Lab。
> 本课新增：URL、方法、首部、状态码、HTTP 消息边界、连接复用与缓存。不重讲第 25 课的自定义请求 ID。

## 1. 已经能传消息，为什么还要学 HTTP

你的游戏要向房间目录查询可加入的房间。你当然可以沿用自己的格式：`[长度][消息种类][类型][requestId][正文]`。服务器和客户端都由你写时，这能工作。

现在官网也要查房间，测试工具也要查，运维还想记录失败比例。每个工具都先学习你发明的格式，成本就很高。一个更省事的办法是：大家采用同一种请求、响应约定，再把游戏数据放进其中。

这就是引入 **HTTP（Hypertext Transfer Protocol，超文本传输协议）** 的动机之一。名字里有“超文本”，不代表只能传网页。它也能承载 JSON、图片、文件和游戏业务数据。JSON 是一种用文字表示对象、数组和数值的格式，例如 `{"roomId":42}`，不是网络传输协议。

先把层次放正：HTTP 是应用层协议。它不会取代 IP 路由、TCP 重传，也不会自动判断玩家余额够不够。它规定请求与响应怎么表达，业务代码仍要决定请求是否合法。

本课先观察 HTTP/1.1 的可读文本形式；结尾再区分 HTTP/2 和 HTTP/3，不能把这一版的文本格式误当作所有版本的线上形式。

## 2. 从一个地址拆出四个问题

游戏配置中写着：`http://api.example.com:8080/rooms?region=asia`。这个完整地址叫 URL。示例域名用于解释，不保证有真实服务。

| 部分 | 谁使用它 | 回答的问题 |
| --- | --- | --- |
| `http` | HTTP 客户端库 | 使用哪种协议；这里不是 HTTPS |
| `api.example.com` | 名字解析与 HTTP 客户端 | 要找哪个服务名字 |
| `8080` | 连接操作 | 连接对方哪个端口 |
| `/rooms` | 服务器上的 HTTP 路由逻辑 | 访问哪项资源或功能 |
| `region=asia` | 对应业务处理器 | 用什么查询条件 |

这里“HTTP 路由”只是按方法和路径选择业务处理器，**不是 IP 路由表选下一跳**。`/rooms` 也不一定是服务器磁盘上的文件夹，可以对应一个查数据库的函数。

应用把地址交给 HTTP 客户端库。库解析域名，连接 IP 的 8080 端口，随后发送请求。DNS 不负责查 `/rooms`，路由器也不根据 `region=asia` 选出口。

省略端口时，`http` 默认使用 80，`https` 默认使用 443；这不是所有服务器必须使用的固定端口。

## 3. 一份请求究竟长什么样

已经建立连接后，客户端可以发送下面这份 HTTP/1.1 请求。每行实际由 `\r\n` 结束；展示中的空行不能漏。

```http
GET /rooms?region=asia HTTP/1.1
Host: api.example.com:8080
Accept: application/json

```

逐项解释，不要求记格式细节：

1. `GET` 是方法：这次想获取信息。
2. `/rooms?region=asia` 是本次请求的目标路径和条件。
3. `HTTP/1.1` 是使用的版本。
4. `Host` 告诉接收方自己访问的是哪个服务名字。多个名字可以指向同一个 IP，所以连到了 IP 还不够区分应用服务。
5. `Accept` 表达客户端愿意接收的内容类型；这里希望拿到 JSON。
6. 空行结束首部区域。这份 GET 示例没有正文。

首部就是用于说明“怎样解释或处理这次消息”的字段集合。它们是 **HTTP 首部**，不是 TCP 首部。TCP 不认识 `GET` 或 `Host`，只是搬运其字节。

服务器读到完整请求后，HTTP 库解析方法、路径和首部，将它交给查询房间的处理器。处理器拿到结果，再交给 HTTP 库生成响应。

```http
HTTP/1.1 200 OK
Content-Type: application/json
Content-Length: 12

{"rooms":[]}
```

这里正文就是 12 个 ASCII 字节 `{"rooms":[]}`，不包含显示代码块时产生的额外换行。`Content-Type` 告诉客户端正文是 JSON；`Content-Length` 给正文的字节长度。客户端先恢复 HTTP 响应，再把 JSON 交给游戏逻辑，最后更新房间列表 UI。

`200` 是 HTTP 状态码；`OK` 是 HTTP/1.1 中的可读说明文字，程序不应靠这两个英文字母判断成功。

## 4. 为什么方法不能只当成名字记

查询房间与购买物品都可以有 JSON 正文，但它们的副作用完全不同。副作用是执行后改变了业务状态，比如扣钱。

| 方法 | 本课的例子 | 要表达的意图 |
| --- | --- | --- |
| GET | `GET /rooms/42` | 获取房间信息 |
| POST | `POST /purchases` | 提交一笔购买，由服务端处理 |
| PUT | `PUT /settings/audio` | 将这个资源的状态设为提交的内容 |
| DELETE | `DELETE /rooms/42` | 请求删除指定资源 |

这些是接口设计时应遵守的语义，不是 TCP 强制检查的规则。把扣钱写进 GET，网络仍可能传过去，但预取、缓存或自动重试就可能造成危险。因此“查一下”与“改一下”必须分清。

现在问一个新问题：第一次请求可能执行了，但响应丢了，能否再发？

把音量设为 30%，执行一次与执行两次，预期最终效果相同。这种性质叫 **幂等**。重复执行“扣 10 金币”会扣两次，通常不幂等。HTTP 中 PUT、DELETE 按定义具有幂等语义；POST 不默认具有这个保证。重复 DELETE 的响应状态可以不同，不妨碍“资源最终不存在”这一效果相同。

幂等不等于返回值始终相同，也不等于服务器不记录访问日志。购买接口若要安全重试，需要业务操作编号和服务端去重；不能看到 TCP 可靠，便认定业务不会重复执行。

## 5. HTTP 成功，不一定等于你想做的业务成功

先分三层结果：

```text
TCP ACK：对端 TCP 收到了哪些字节
HTTP 状态：HTTP 请求在应用接口层得到怎样的处理结果
游戏结果：房间是否加入成功、物品是否购买成功
```

常见状态码按第一位分组：2xx 表示成功，3xx 表示重定向等进一步处理，4xx 表示请求相关问题，5xx 表示服务器处理问题。这里只学几个会用于排错的例子：

- `200`：这次请求成功处理；仍要按接口文档解释正文。
- `201`：成功创建资源。
- `400`：请求格式或参数等有问题。
- `401`：需要有效的身份凭据；第 31 课解释凭据是什么。
- `403`：服务器拒绝允许这项操作，例如已登录但没有权限。
- `404`：没有找到目标资源，也可能是服务端有意不暴露资源存在。
- `500`：服务器处理时遇到意外问题。

有些接口用 `200` 配合正文 `{"success":false}` 表达业务失败。你可以讨论这种设计好不好，但客户端必须遵守具体契约，不能自己猜。

**得到 404 是得到了 HTTP 响应，不是“TCP 连不上”。** 反过来，TCP 建连超时通常根本没有 HTTP 状态码可读。

## 6. TCP 不保留边界，HTTP 又怎么知道一份响应结束

先限定本例：普通带正文响应，存在合法 `Content-Length: 12`，没有其他传输编码。接收端 HTTP 解析器做的是：

```text
逐步累积收到的字节 → 找到首部结尾的空行 → 得到正文长度 12
→ 正文不足 12 字节就继续等 → 满 12 字节后交付这份正文
```

第一回读到 `{"roo`，第二回读到 `ms":[]}`，仍是一份响应。一次底层读取拿到两份响应的字节，也不能把它们粘成一份。`ReadAsync` 返回多少，不能代替 HTTP 的边界规则。

这与第 24 课是同一个“必须恢复消息边界”的问题，但**不能在 HTTP 前面擅自再加自己的四字节长度前缀**。标准 HTTP 接收方并不期待它。

HTTP/1.1 还有其他边界规则。例如服务端事先不知道最终正文多长，可以使用 chunked 传输编码：每个块先给长度，末尾用结束块收尾；某些响应没有正文，如 204，以及对 HEAD 的响应；某些响应以连接关闭定界。规则有明确优先级，非法冲突要拒绝，实际项目交给成熟 HTTP 库，不手写完整解析器。参考 [HTTP/1.1 消息长度规则](https://httpwg.org/specs/rfc9112.html#message.body.length)。

这里的 chunk 是 HTTP 的块，不是 TCP 段。

## 7. 一次请求必须新建一次 TCP 连接吗

不必。客户端查完房间，又查公告，可以复用已经建立的连接。这节省重复握手的等待和资源。**长时间保留连接**与**主动向客户端推送新消息**不是同一件事；保留一条 HTTP/1.1 连接，不会自动把协议改成随时互发通知。

还要纠正一个容易迁移错的结论：第 25 课的自定义协议允许通过 requestId 匹配不同完成顺序的响应。HTTP/1.1 若在同一连接上，在前一个响应尚未返回时就继续发送后面的请求，这叫请求流水线；它的最终响应必须按请求次序发送。HTTP 库依据协议管理匹配，不要求你加自定义 requestId。HTTP/2 则用不同的流及其标识区分并发交换。

这里 HTTP/2 的“流”是同一连接内的一条协议逻辑通道，不是又创建了一个独立 TCP 连接，也不要和课程中的 ByteStream 类混成同一个对象。

你还可能听到“HTTP 是无状态协议”。这不是说服务器不能保存背包、账号或登录会话，而是不能仅凭“我们上一次说过话”就让本次请求缺少理解它所需的信息。应用可以在请求中携带会话凭据，把独立请求关联到同一账号，第 31 课会具体解释。无状态也不等于每次都关闭 TCP 连接。

## 8. 为什么有时根本不用重新下载

房间规则文本一小时没变化，客户端每次打开界面都重下，浪费传输。于是客户端保留一个旧副本，这叫缓存。但“有旧副本”不能证明它仍有效，需要服务端给规则。

假设响应给出 `Cache-Control: max-age=60`。简化到刚收到的新响应，客户端可在新鲜期内复用；完整实现还要考虑响应已有的年龄等信息。过期后，可以带着服务端上次给的版本标签 `ETag: "v7"` 发条件请求：`If-None-Match: "v7"`。

服务端检查当前版本：没变则返回 `304 Not Modified`，不重新带整份正文；客户端使用已保存的正文。若变为 v8，则发送新正文。没有缓存正文，仅有 304，并不能凭空构造旧内容。

HTTP 缓存保存资源响应；DNS 缓存保存名字解析记录。两者都减少重复工作，但缓存对象和更新规则不同。敏感账号数据不能随便作为公共缓存内容；`no-store` 表达不要存储，`no-cache` 则允许存储但复用前需要验证，不是同义词。参考 [HTTP 缓存规范](https://httpwg.org/specs/rfc9111.html)。

[打开 HTTP 响应接收与缓存交互](29_HTTP_响应与缓存_交互.html)

## 9. HTTP、HTTPS 与各版本别混成一条轴

`HTTP/1.1、HTTP/2、HTTP/3` 描述 HTTP 的版本；`http、https` 描述地址方案及安全要求，不是“HTTPS 比 HTTP/3 新”。

- HTTP/1.1：本课展示的文本首部；常见于 TCP 上。
- HTTP/2：采用二进制分帧和多路复用；仍承载在 TCP 上。同一 TCP 连接缺失的字节会阻碍其后字节交付，这仍可能影响多个 HTTP 流。
- HTTP/3：承载在 QUIC 上。QUIC 基于 UDP，但自己实现可靠传输、流和拥塞控制等机制；不是把可靠性全部丢掉，也不是“UDP 自带这些功能”。本课只建立位置关系，不要求实现 QUIC。

HTTPS 要对通信做加密和身份验证，第 31 课从窃听与冒充的问题推导。HTTP/1.1、HTTP/2 常用 TLS 保护；HTTP/3 的 QUIC 集成了 TLS 握手机制，不能机械套成“TCP→TLS→HTTP/3”。参见 [HTTP/2](https://www.rfc-editor.org/rfc/rfc9113.html)、[HTTP/3](https://www.rfc-editor.org/rfc/rfc9114.html)。

## 10. 在游戏中怎样使用这层知识

游戏启动后获取配置、查询账号背包、提交一笔购买，都可以设计成 HTTP 接口；这不是要求每一帧移动都变成一次 HTTP 请求。决定协议前，先说清是否需要持续推送、对延迟有何要求、失败后能否重试。

使用 C# 的 HTTP 客户端库时，你通常交给它 URL、方法、首部和正文，取得状态码与响应正文。库负责协议编码、连接管理和消息解析。你仍负责业务规则、超时后怎么办，以及是否显示成功。暂时不堆新 API；后续读框架时先定位这几个职责。

## 检查站

合上正文，用自己的话回答。可以分两批回答，不需要交实验报告。

1. `http://api.example.com:8080/rooms?region=eu` 中，DNS 查询使用哪一部分？TCP 连接使用哪些信息？`/rooms` 最终由谁解释？
2. 目标 IP 已确定，为什么 HTTP/1.1 请求还可能需要 `Host`？它和目标 IP 是同一层的字段吗？
3. `Content-Type`、`Content-Length`、HTTP 状态码分别回答什么问题？长度计算字符数还是字节数？
4. 正文应有 12 字节，底层先交来 5 字节。客户端能否立即把这 5 字节当作完整 JSON？之后再来 7 字节会怎样？
5. HTTP 404 与 TCP 建连超时有什么区别？200 是否总能单独证明买到了物品？
6. “设音量为 30%”与“再扣 10 金币”，哪个更容易安全重试？为什么幂等不要求每次返回相同状态码？
7. 一条 HTTP/1.1 TCP 连接能否复用？若同一连接使用请求流水线，能否随意先返回第二份最终响应？
8. 本机缓存 v7，发条件请求后收到 304。此时正文从哪里来？这与 DNS 缓存有何不同？
9. HTTP/3 用 UDP，是否意味着它不负责可靠性与拥塞控制？HTTPS 与 HTTP/2 是否互斥？
10. 为“查询公告”和“持续收到其他玩家加入通知”选择下一步需要考虑的机制。只需说明需求，不必背 API。

## 本课结论与资料

HTTP 不是“能传字节”的另一个名字，而是约定怎样表达操作、目标、元信息和结果。TCP 的可靠字节流之上，仍需要 HTTP 边界和业务校验。完整语义参考 [HTTP Semantics](https://httpwg.org/specs/rfc9110.html)。

下一课：[[30_WebSocket_长连接与断线恢复]]，解决服务器有新消息时，怎样不用等客户端再次来问。
