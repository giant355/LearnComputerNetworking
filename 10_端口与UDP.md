# 第 10 课：IP 找到电脑以后——端口、套接字与 UDP

> [!info] 本课位置
> 前几课解决了“一个 IP 数据报怎样经过链路和路由器，到达目标主机”。本课继续追问：**到达目标主机以后，操作系统怎样把数据交给正确的程序？**

预计用时：正文与随堂推演 90～120 分钟；可选观察 10～15 分钟；检查站 20～30 分钟。

今天到期的第 7、8 课复习仍保留在 [[复习计划]] 中。本次已经明确选择继续新课，所以先用开头的短回忆接上主线，不取消旧复习项。

---

## 0. 先把上一段路接回来

假设你的电脑 A 要给服务器 B 发送一个位置更新。前几课已经得到下面这条链：

```text
应用把位置更新序列化成字节
  ↓
上层数据被放进 IP 数据报
  ↓
A 根据最终目标 IP 查路由表
  ↓
得到出口接口和当前下一跳 IP
  ↓
用 ARP 把当前下一跳 IP 解析成当前链路的目标 MAC
  ↓
制作 Ethernet 帧并交给网卡发送
  ↓
交换机按 MAC 转发；路由器逐跳重新制作链路层帧
  ↓
最终到达 B 的网络接口
```

到这里，三种地址已经各自完成了一部分工作：

| 信息 | 回答的问题 |
| --- | --- |
| 最终目标 IP | 这个 IP 数据报最终要送到哪台主机或哪个网络接口？ |
| 当前下一跳 IP | 当前发送者接下来把数据报交给哪个 IP 接口？ |
| 当前链路目标 MAC | 这一个 Ethernet 帧在当前链路上交给哪个网络接口？ |

现在帧到达了 B。B 的网络接口发现目标 MAC 是自己，EtherType 又说明载荷是一份 IPv4 数据报，于是把它交给本机的 IP 层。

问题来了：**B 上可能同时运行游戏服务器、语音程序、浏览器和更新程序。IP 地址只说明“这是 B 的数据”，却没有说明“这是 B 上哪个程序的数据”。**

> [!question] 停十秒
> 如果只靠目标 IP，操作系统应该把刚收到的数据复制给所有程序，还是随便挑一个程序？这两个朴素办法分别会有什么问题？

复制给所有程序既浪费资源又泄露数据；随便挑一个则大概率交错对象。我们需要一个比“主机地址”更细的编号。

---

## 1. 先认识进程：正在运行的程序

磁盘上的 `GameServer.exe` 是程序文件。它运行起来以后，操作系统会为它维护内存、线程、打开的文件等状态；这个运行中的实例叫作**进程**。

同一个程序甚至可以同时有多个进程。例如同时启动两个本地测试服务器：

```text
GameServer.exe 进程甲
GameServer.exe 进程乙
```

因此，“把数据交给名叫 `GameServer.exe` 的文件”也不是一个可靠的网络规则。操作系统需要一套统一、明确而且能高效查找的交付办法。

我们先尝试三个朴素方案。

### 方案一：一台电脑只能运行一个联网程序

这样目标 IP 确实够用，但现实中显然无法接受。

### 方案二：让应用数据开头写程序名字

例如：

```text
GameServer | Player=7 | X=12.5 | Y=8.0
```

问题是，操作系统必须先理解每一种应用自创的格式，才能知道该交给谁。不同程序还可能使用相同名字，恶意程序也可以伪造名字。

这破坏了分层：操作系统的通用网络部分不应该理解所有游戏、网页和语音协议。

### 方案三：只在 IP 首部写“UDP”或“TCP”

IPv4 首部确实有一个 `Protocol` 字段，用来说明载荷属于哪个上层协议。例如：

```text
Protocol = 17  → UDP
Protocol = 6   → TCP
```

这只能让 IP 层找到本机的 UDP 或 TCP 实现，却仍不能区分两个同时使用 UDP 的程序。

于是最小的新机制出现了：**端口号**。

---

## 2. 端口号：主机内部的交付编号

UDP 和 TCP 都在自己的首部中放入端口号。端口号是一个 16 位无符号整数，因此范围是：

```text
0 ～ 65535
```

先建立最朴素的理解：

> IP 地址把数据送到正确的主机；目标端口号帮助这台主机的操作系统把数据交给正确的套接字。

注意这句话故意写的是“套接字”，还不是“程序”。两者的关系稍后解释。

### 端口不是网线插口

这里的端口是数字编号，不是交换机背后的物理插孔，也不是 USB 接口。

```text
交换机端口：真实或逻辑上的帧进出口
UDP/TCP 端口：操作系统用于交付网络数据的数字编号
```

二者中文都叫“端口”，但不是同一种东西。遇到语境不清时，可以说“交换机端口”或“传输层端口”。

### 端口也不是某个应用在全世界永久拥有的身份证

端口号的意义取决于：

- 哪台主机；
- 哪种传输协议；
- 当前有哪些套接字绑定了它；
- 操作系统的绑定规则。

例如，主机 A 的 UDP 端口 `5000` 和主机 B 的 UDP 端口 `5000` 没有冲突。甚至同一台主机的 UDP `5000` 与 TCP `5000` 通常也属于两套不同的端口空间。

有些服务习惯使用约定俗成的端口，但约定不是物理定律。程序也可以在配置允许时改用其他端口。

---

## 3. 数据到达主机后的逐层分流

假设服务器 B 的某个游戏套接字正在等待 UDP 端口 `7777` 的数据。

收到一帧后，交付过程大致如下：

```text
Ethernet 目标 MAC
  ↓ 这帧是不是交给本机网络接口的？

EtherType = IPv4
  ↓ 帧载荷应该交给本机的哪种网络层协议？

目标 IP = B
  ↓ 这个 IP 数据报是否应该在本机结束转发旅程？

IP Protocol = 17
  ↓ IP 载荷交给 UDP 实现

UDP 目标端口 = 7777
  ↓ UDP 查找绑定该本地地址和端口的套接字

游戏服务器从套接字读取这份应用数据
```

这里发生了多次“看字段，选择接收者”。这种动作叫作**解复用**。

反过来，许多应用的数据都可以经过同一个 UDP 实现、同一个 IP 实现和同一块网卡发送出去，这叫作**复用**。

这两个词不用硬背。先记住画面：

```text
发送：许多来源汇到共同的下层通道       → 复用
接收：共同通道收到数据后再分给正确对象 → 解复用
```

### 路由器通常不靠 UDP 目标端口选路

普通 IP 转发的核心依据仍是**最终目标 IP**。UDP 首部作为 IP 的载荷，通常一路保留到目标主机，目标主机才用其中的目标端口完成应用交付。

以后会遇到 NAT、防火墙和负载均衡器，它们可能查看甚至改写端口。这些是额外机制，不能反过来把普通路由规则理解成“按端口选下一跳”。

---

## 4. 套接字：应用与操作系统网络栈之间的接口

“套接字”英文是 socket。现在先把它理解为：

> 应用通过操作系统使用网络能力时，拿到的一个通信对象或句柄。

在 C# 里，它的感觉类似：

```csharp
Socket socket = new Socket(
    AddressFamily.InterNetwork,
    SocketType.Dgram,
    ProtocolType.Udp);
```

这段代码只是创建一个 UDP/IPv4 套接字对象。要接收发往某个本地端口的数据，通常还要进行**绑定**：

```csharp
socket.Bind(new IPEndPoint(IPAddress.Any, 7777));
```

此处可以先读成：

```text
请操作系统把符合绑定条件、目标 UDP 端口为 7777 的数据交给这个套接字。
```

`IPAddress.Any` 对 IPv4 通常显示为 `0.0.0.0`，意思是接受发往本机合适 IPv4 接口的该端口数据，不是说远端目标真的是 `0.0.0.0`。

### 为什么不直接说“端口对应进程”

为了建立直觉，初学时常说“端口标识程序”。更准确的链条是：

```text
目标端口等字段
  ↓ 操作系统按绑定规则查找
套接字
  ↓ 套接字属于或被某个进程使用
进程中的代码读取数据
```

一个进程可以拥有多个套接字。操作系统也可能在特定选项和规则下允许多个套接字共享某些绑定。因此，“端口直接等于进程编号”并不准确。

### 端点

一个网络通信端点常写成：

```text
IP 地址 : 端口
```

例如：

```text
203.0.113.20:7777
```

它表达“那台主机上的那个传输层交付点”。严谨比较时还要加上传输协议，因为：

```text
UDP 203.0.113.20:7777
TCP 203.0.113.20:7777
```

通常是两个不同的端点。

---

## 5. 为什么一份 UDP 数据报要有两个端口

假设客户端 A 访问游戏服务器 B：

```text
A = 10.0.0.2
B = 203.0.113.20

A 的临时 UDP 端口 = 53000
B 的游戏 UDP 端口 = 7777
```

A 发给 B 的数据包含：

```text
来源 IP   = 10.0.0.2
目标 IP   = 203.0.113.20
来源端口 = 53000
目标端口 = 7777
```

目标端口 `7777` 让 B 知道交给哪个套接字。那么来源端口 `53000` 有什么用？

它告诉 B：**如果要向 A 的这次通信回复，应该把回复发到 A 的哪个 UDP 端口。**

B 回复时：

```text
来源 IP   = 203.0.113.20
目标 IP   = 10.0.0.2
来源端口 = 7777
目标端口 = 53000
```

可以看到，两端的逻辑角色对调了。

> [!warning]
> “回复把来源和目标对调”不等于“回复一定沿完全相同的路由器原路返回”。互联网的去程与回程可能选择不同路径。IP 和端口描述通信端点，不规定中间的物理路线。

### 客户端为什么常用临时端口

服务器希望别人容易找到它，所以通常在一个已知或配置好的端口等待。客户端发起通信时，操作系统可以从可用范围选择一个临时端口。

这样，同一台客户端可以同时进行许多通信：

```text
10.0.0.2:53000 → 服务器甲:7777
10.0.0.2:53001 → 服务器乙:7777
10.0.0.2:53002 → 语音服务器:9000
```

“临时端口的具体范围”由操作系统配置决定，不必在这一课背一个固定区间。

---

## 6. UDP 首部为什么恰好需要这些字段

UDP 是 User Datagram Protocol，中文常译作**用户数据报协议**。

一份 UDP 数据报由 UDP 首部和应用数据组成。基础首部只有 8 字节：

```text
  0               15 16              31
 +------------------+------------------+
 |     来源端口      |      目标端口     |
 +------------------+------------------+
 |       长度        |      校验和       |
 +------------------+------------------+
 |              应用数据 ...            |
 +-------------------------------------+
```

四个字段可以从刚才的问题逐一推出来：

| 字段 | 大小 | 解决的问题 |
| --- | ---: | --- |
| 来源端口 | 16 位 | 接收方回复时，应该发回来源主机的哪个 UDP 端口？ |
| 目标端口 | 16 位 | 目标主机应把数据交给哪个 UDP 套接字？ |
| 长度 | 16 位 | 这份 UDP 数据报一共有多少字节，包括 8 字节首部？ |
| 校验和 | 16 位 | 传输途中某些比特是否发生了可检测的错误？ |

IPv4 首部中已有“总长度”，为什么 UDP 还带长度？因为分层后的 UDP 自己也要描述自己的边界；IP 数据报还可能含有其他网络层相关内容。UDP 实现不必把自己的格式完全寄托在某个特定版本的 IP 上。

### 校验和能做什么，不能做什么

校验和的目标是**检错**。它可以让接收方发现许多传输错误，然后丢弃损坏的数据报。

它不提供：

- 自动修复；
- 自动重传；
- 加密；
- 发送者身份认证；
- “内容绝对没错”的数学保证。

这和第 7 课 Ethernet 的 FCS 边界很像：发现损坏不等于恢复数据。不同层的检错覆盖范围也不同。

> [!note] IPv4 与 UDP 校验和
> 在 IPv4 中，UDP 校验和历史上允许为零，表示发送方未使用它；IPv6 中规则不同。本课重点不是背例外，而是理解校验和只负责检错，不负责可靠交付。

---

## 7. 封装名称再整理一次

同一批应用字节逐层包装后，不同层会给自己的数据单位起不同名字：

```text
应用消息
  ↓ 加 UDP 首部
UDP 数据报
  ↓ 加 IP 首部
IP 数据报
  ↓ 加 Ethernet 头部和尾部
Ethernet 帧
```

所以：

- “UDP 数据报里有来源和目标端口”是对的；
- “IP 数据报承载 UDP 数据报”是对的；
- “Ethernet 帧承载 IP 数据报”是对的；
- “收到 ARP 回复后，把等待的 IP 数据报制作成承载 IPv4 的 Ethernet 帧”比“变成 IPv4 帧”更准确。

在不同教材中，UDP 的数据单位也可能被叫作 UDP segment（UDP 报文段）。你现在只需看清它属于哪一层，不必因术语差异怀疑整个机制。

---

## 8. UDP 提供的服务模型

UDP 给应用的核心感觉可以概括为：

> 无连接、保留消息边界、尽力而为的数据报服务。

这些词必须拆开理解。

### 8.1 无连接

发送 UDP 数据前，不需要像 TCP 那样先建立一条由传输层维护的连接状态。应用可以指定目标端点并发送一份数据报。

但“无连接”不等于没有目的地址，也不等于操作系统完全没有任何状态：

- 套接字有绑定状态；
- ARP 有缓存和待解析状态；
- 路由表有规则；
- 防火墙和 NAT 以后也可能维护状态。

它只是在描述 UDP 本身不提供 TCP 式的连接建立与可靠传输状态机。

### 8.2 保留消息边界

假设发送方调用两次发送：

```text
第一次：发送 100 字节的位置快照 D1
第二次：发送 80 字节的位置快照 D2
```

如果两份 UDP 数据报都完整到达，接收方看到的是两份独立数据报，而不是一条不带边界的 180 字节连续字节流。

这不保证一次应用读取永远写法随意也不会截断；接收 API 提供的缓冲区必须足够，具体 API 还会规定截断行为。这里真正要抓住的是：**UDP 协议保留每份数据报的边界。**

TCP 以后会形成鲜明对比：TCP 向应用提供字节流，不替应用保留“第几次发送”的消息边界。

### 8.3 尽力而为

UDP 本身不保证：

- 一定送达；
- 只到达一次；
- 按发送顺序到达；
- 在某个时间内到达；
- 自动重传；
- 自动根据接收方速度做流量控制；
- 自动避免压垮网络的完整拥塞控制语义。

它也不意味着“必然丢包”或“必然乱序”，只是协议没有向应用作这些承诺。

### 发送函数成功的边界

应用调用 `SendTo` 或类似函数成功，通常只说明：

```text
本机操作系统在这个时刻接受了这次发送请求
```

之后仍可能发生：

- 本地队列拥塞；
- ARP 解析失败；
- 中间链路或路由器丢包；
- 对方主机防火墙丢弃；
- 对方没有对应端口；
- 对方应用收到后解析失败。

因此 API 返回成功和“对方游戏逻辑已经处理”之间相隔了许多层。

---

## 9. 没有程序等待目标端口时会怎样

假设一份 UDP 数据报到达 B，目标端口为 `7777`，但 B 上没有匹配的 UDP 套接字。

操作系统不能随便挑一个程序，通常会丢弃这份 UDP 数据报，并且**可能**向发送方返回一份 ICMP `Port Unreachable`（端口不可达）错误。

之所以说“可能”，是因为主机策略、防火墙或中间设备可以抑制这类反馈。因此：

```text
没有收到错误反馈 ≠ 已经有应用成功接收
```

这和 `ping` 失败不能单独证明网页不可用类似：缺少反馈时，原因可能不止一种。

---

## 10. 一个完整例子：位置更新走到游戏逻辑

客户端 A 要向 B 发送：

```text
PlayerId = 7
Sequence = 105
X = 12.5
Y = 8.0
```

为了聚焦主线，假设序列化后是 20 字节。

### A 的应用层

游戏代码决定字段含义并序列化：

```text
应用消息 = 20 字节
```

### A 的 UDP 层

UDP 增加 8 字节首部：

```text
来源端口 = 53000
目标端口 = 7777
UDP 长度 = 8 + 20 = 28 字节
```

### A 的 IP 层

IPv4 增加自己的首部：

```text
来源 IP = A
目标 IP = B
Protocol = 17
```

这里的 `17` 表示 IP 载荷要交给 UDP，而不是直接表示游戏。

### A 的网络接口

A 查路由表得到出口接口和下一跳 IP，查 ARP 缓存或发 ARP 请求得到下一跳 MAC，然后制作 Ethernet 帧。

### 中间路由器

路由器根据目标 IP 转发。每一跳会更换当前链路的来源和目标 MAC，并减少 TTL；通常不会为了普通转发而改变 UDP 来源端口和目标端口。

### B 的接收路径

```text
目标 MAC 命中 B 的接口
  ↓
EtherType 指向 IPv4
  ↓
目标 IP 是 B，Protocol=17
  ↓
交给 UDP
  ↓
目标端口 7777 命中游戏套接字
  ↓
应用读到 20 字节，再按双方协议反序列化
```

到这里才从“网络送到主机”走到了“目标应用拿到消息”。

---

## 11. UDP 很简单，但应用问题不会自动消失

UDP 没有替应用定义以下问题：

- 这 20 字节中哪几位是 `PlayerId`？
- 多字节整数采用什么字节序？
- 收到 `Sequence=105` 后又收到 `Sequence=103` 怎么办？
- 数据报丢失以后要不要重传？
- 同一个玩家能否伪造另一个玩家的位置？
- 消息版本不同怎样兼容？

这些属于应用协议或 UDP 之上的额外机制。

这正好再次区分“服务、接口和协议”：

| 角度 | 在本例中是什么 |
| --- | --- |
| UDP 服务 | 给应用提供发送和接收独立数据报的能力 |
| Socket API | 本机程序通过 `Bind`、`SendTo`、`ReceiveFrom` 等操作使用这项能力 |
| 游戏协议 | 客户端与服务器约定 20 字节分别代表什么、序号如何处理、哪些消息要确认 |

---

## 12. 游戏消息该不该重传

回到你之前已经形成的直觉。

### 高频位置快照

假设服务器每 50 ms 发一次新位置：

```text
序号 100 → 丢失
序号 101 → 到达
序号 102 → 到达
```

晚到的序号 100 往往已经过时。此时强制等待或重传旧位置，可能让画面使用更旧的信息。应用可以用序号丢弃过时数据，并配合插值或预测。

### 最终比赛结果

比赛结果、购买物品和存档确认通常不能悄悄丢失。可以选择：

- 使用 TCP 或其他可靠传输；
- 在 UDP 之上增加消息 ID、确认、超时和重传；
- 让服务器保存权威状态，客户端重复请求直到得到明确结果。

关键不是背“游戏一定用 UDP”，而是先问：

```text
这条消息丢了能否被下一条新消息取代？
如果不能，谁来检测丢失并恢复？
等待恢复造成的延迟是否可接受？
```

### UDP 不自动等于低延迟

UDP 首部小、没有 TCP 式连接和可靠字节流机制，确实给应用更多选择。但如果应用自己设计了大量重传、队列无限增长，或者发送速度超过链路能力，延迟仍然会很高。

协议名字不能消灭排队、传播和处理时间。

---

## 13. 数据报大小为什么还要克制

UDP 长度字段虽然能表示很大的数据报，但底层链路每帧能承载的大小有限。前面见过常见 Ethernet MTU `1500` 字节；它限制一份不分片 IPv4 数据报的常见大小。

如果 IPv4 首部是 20 字节、UDP 首部是 8 字节，那么常见的粗略上限是：

```text
UDP 应用数据 = 1500 - 20 - 8 = 1472 字节
```

这不是“互联网中永远安全的 UDP 消息大小”。路径中还可能存在更小 MTU，IPv4 选项也会增加首部长度。数据报过大可能触发 IP 分片，而一片丢失就会让整份原始数据报无法重组。

我们会在后续专门学习 MTU、分片和路径 MTU。现在只保留工程直觉：**不要因为 UDP 长度字段装得下，就把巨大状态一次塞进一份数据报。**

---

## 14. 可选观察：看看本机有哪些 UDP 套接字

在 Windows PowerShell 或命令提示符中运行：

```powershell
netstat -ano -p udp
```

先读列名，不要求记参数：

| 列 | 大致含义 |
| --- | --- |
| Proto | 传输协议，这里筛选 UDP |
| Local Address | 本地绑定的 IP 与端口 |
| PID | 使用该套接字的进程编号 |

你可能看到：

```text
UDP    0.0.0.0:5353       *:*       1234
```

它大致表示 PID 为 `1234` 的进程使用一个 UDP 套接字，在本机合适的 IPv4 接口上绑定端口 `5353`。

这里的 `*:*` 不代表已经与一个神秘远端建立 UDP 连接；它反而说明这行并没有列出固定的远端端点。

如果想按 PID 查看进程，可以在任务管理器的“详细信息”页查找。只观察即可，不要结束不认识的系统进程。

> [!tip]
> 输出因操作系统版本、权限和当前运行程序而不同。没有看到 `7777` 很正常；只有程序实际绑定它时才会出现。

---

## 15. 本课最小心智模型

把这一课压成三句话：

1. **IP 地址解决跨网络找到目标主机的问题，传输层目标端口解决目标主机内部把数据交给哪个套接字的问题。**
2. **UDP 用来源端口、目标端口、长度和校验和包装一份应用消息，并保留数据报边界。**
3. **UDP 不保证送达、顺序、不重复或自动重传；需要这些性质时，必须由上层机制或其他传输协议提供。**

把完整接收路径再走一次：

```text
Ethernet 目标 MAC → 网络接口
EtherType          → IPv4
最终目标 IP        → 本机
IP Protocol=17     → UDP
UDP 目标端口       → 套接字
应用协议           → 解释字节含义
```

每一层都只回答自己范围内的问题。

---

## 检查站

请先滚动到看不到正文的位置，再用自己的话回答。不需要写成正式报告，直接在本文件题目下或聊天中作答即可。

1. 为什么一份数据已经到达正确的目标 IP 后，仍不能确定应该交给哪个应用？目标端口准确来说先定位到什么对象？

2. 在下面这次发送中，分别说明四个值的作用：

   ```text
   10.0.0.2:53000 → 203.0.113.20:7777
   ```

   服务器回复时，四个值应怎样填写？“对调端点”是否保证原路返回？

3. IP 首部的 `Protocol=17` 与 UDP 首部的 `目标端口=7777` 分别完成哪一级分流？为什么只保留其中一个不够？

4. 端口号为什么既不是交换机的物理端口，也不是某个程序在全世界永久拥有的编号？请至少说出两个决定端口意义的条件。

5. UDP 首部的四个字段是什么？其中哪一个用于把数据交给正确套接字，哪一个帮助接收者检测传输错误？检测出错后，UDP 会不会自动修复或重传？

6. 应用连续调用两次 UDP 发送，分别发送 D1 和 D2。如果两份数据都到达，接收方从协议上看到的是一条连续字节流，还是两份有边界的数据报？UDP 是否保证 D1 一定先于 D2 到达？

7. `SendTo` 返回成功，为什么不能推出“对方游戏逻辑已经处理了消息”？请从本机操作系统之后列出至少三个仍可能失败的环节。

8. 目标主机没有任何 UDP 套接字绑定目标端口时，操作系统通常怎样处理？发送方为什么不一定收到明确错误？

9. 对“每 50 ms 更新一次的位置”和“最终比赛结果”分别给出一种丢包处理策略，并说明理由。不要只回答“都用 UDP”或“都用 TCP”。

10. 一份应用数据为 200 字节，假设 UDP 首部 8 字节、IPv4 首部 20 字节、Ethernet 头尾控制信息合计 18 字节，忽略其他开销：

    - UDP 长度字段应是多少？
    - IP 数据报共多少字节？
    - Ethernet 帧共多少字节？

可选观察：运行 `netstat -ano -p udp`，找一行输出，说明本地 IP、端口和 PID 分别表达什么。不要粘贴全部输出。

---

## 下一步会遇到的新困难

UDP 已经能把一份消息交给正确套接字，却把丢失、乱序、重复和速度控制问题留给了应用。

下一阶段不会立刻堆出完整 TCP。我们会先造一个很小的**有限容量字节流**，弄清楚：

```text
发送者写得快，接收者读得慢时，数据应该放在哪里？
缓冲区满了以后，接口应该怎样表达“现在写不下”？
一条字节流怎样表示结束？
```

这将成为后续乱序重组、确认、重传和 TCP 收发端的地基。

---

## 资料锚点

- [RFC 768：User Datagram Protocol](https://www.rfc-editor.org/rfc/rfc768.html)——UDP 的原始标准很短。本课不要求通读，先看首页的首部图能否与正文对应。

