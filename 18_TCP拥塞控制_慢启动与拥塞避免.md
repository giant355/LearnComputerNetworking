# 第 18 课：不知道网络能承受多少——慢启动、拥塞避免与丢失响应

> 前置：[[14_TCP发送端_滑动窗口与重传]]、[[17_排队_瓶颈与拥塞窗口]]。
>
> 本课可在教室或手机上完成，不需要编译程序。正文约 60～85 分钟，交互与纸上推演约 20～30 分钟，检查站约 25～35 分钟；合计约 1.75～2.5 小时。
>
> 配套：[打开本课交互](18_TCP拥塞控制_交互.html)。正文解释机制，交互按同一套教学规则逐步改变 `cwnd` 和 `ssthresh`。

## 0. 本课先固定一套规则

拥塞控制有多种实现。为了让状态变化能够手算，本课先构造一套**经典、简化的 Reno 风格教学模型**：

```text
MSS = 1000 byte
初始 cwnd = 1 MSS
初始 ssthresh = 8 MSS
rwnd 足够大，应用始终有数据
每一轮代表一批数据获得新的累计确认，近似一个 RTT
cwnd < ssthresh：慢启动
cwnd >= ssthresh：拥塞避免
```

这套规则不是在宣称所有真实操作系统都从 `1 MSS` 开始。现代实现可能使用更大的初始窗口，也可能使用 CUBIC 等算法；本课末尾会交代边界。现在只用最少状态回答三个问题：

1. 一开始不知道路径容量，应该怎样尝试？
2. 已经接近可用容量，怎样继续试探而不过于激进？
3. 出现丢失信号后，怎样降低压力并恢复？

本课只在普通数据阶段讨论拥塞控制，不重新计算 SYN、FIN 和线上回绕序号。`bytes_in_flight` 仍然表示已经发送、尚未被累计 ACK 确认的字节。

## 1. 固定 cwnd 为什么不够

第 17 课先把 `cwnd` 固定，是为了看清它管什么。真正使用时，固定值会遇到两个相反问题。

路径甲的基础 BDP 约为 `20 KB`，路径乙约为 `2 MB`。若所有连接都固定：

```text
cwnd = 20 KB
```

那么：

- 对路径甲，这个值可能已经足够把瓶颈利用起来；
- 对路径乙，在途量远小于 BDP，发送端可能经常等 ACK，链路没有吃饱。

如果一律固定为 `2 MB`，路径乙可能合适，路径甲却可能突然向小队列灌入大量数据，引起长时间排队和丢包。

而且同一条路径也会变化：上午只有你在用，晚上室友开始下载；Wi-Fi 信号、运营商路径和共享流量都可能改变。发送端不能只在安装软件时写死一个数字。

所以 `cwnd` 必须是状态：根据反馈不断调整。

## 2. 发送端能直接看见什么

发送端通常看不到中间路由器的准确队列长度，也拿不到“这条路径现在还剩 37,000 byte 容量”这样的答案。它能直接处理的是端到端反馈：

- 新累计 ACK：有新的连续数据得到确认；
- 重复 ACK：累计确认位置没有前进，但后面的段可能已经到达；
- 超时：最旧未确认内容等了很久仍未得到确认；
- 某些实现还可使用 ECN 等显式拥塞信号。

这些反馈都不是完美测量。丢包可能来自拥塞，也可能来自链路差错；重复 ACK 也可能来自乱序。经典丢包型算法把这些信号当作路径压力的线索，并采取保守行动。

现在先只看“新 ACK 持续返回”。这说明旧数据确实完成了交付，也意味着发送端可以谨慎提高试探上限。

## 3. 第一次试探：不要一上来灌满

假设发送端刚建立连接，对路径一无所知。朴素的两个极端都不好：

```text
一次只发 1 MSS，永远不增加 → 安全但可能长期浪费高速链路
一开始就发 1000 MSS       → 可能立刻压满小路径的队列
```

更合理的办法是：从较小值开始；如果新数据持续得到确认，就比较快地扩大试探范围。

这就是 **slow start，慢启动**。名字容易误导：它不是说窗口只会慢慢增长，而是说它没有一开始就使用一个很大的窗口。

在本课教学模型中，每个确认新数据的 ACK 最多让 `cwnd` 增加 `1 MSS`。若当前窗口内每个满长度段都得到新 ACK，那么一轮后的窗口大约翻倍：

| 轮次开始 | 本轮最多发 | 本轮产生的新 ACK 数 | 下一轮 `cwnd` |
| ---: | ---: | ---: | ---: |
| 1 | 1 MSS | 1 | 2 MSS |
| 2 | 2 MSS | 2 | 4 MSS |
| 3 | 4 MSS | 4 | 8 MSS |

这是一种指数式增长：`1 → 2 → 4 → 8`。因此“慢”指谨慎起步，不指增长曲线缓慢。

更精确地说，增长依据的是**新确认的数据**，不是任何长得像 ACK 的报文。重复 ACK 没有确认新字节，不能每来一份就照常把窗口永久加大；握手里的 SYN/ACK 也不能据此把数据初始窗口翻倍。

## 4. 为什么不能一直翻倍

继续翻倍会得到：

```text
8 → 16 → 32 → 64 → ... MSS
```

假设路径大约只能平稳容纳 `10 MSS` 在途。从 8 直接跳到 16，可能一下超过可用空间很多，造成突发排队或丢包。

所以我们需要一个状态，表示：

> 到了这里以后，不再使用快速翻倍的试探方式，改成更温和地增加。

这个状态叫 **`ssthresh`，slow-start threshold，慢启动阈值**。

本课约定：

```text
cwnd < ssthresh  → 慢启动
cwnd >= ssthresh → 拥塞避免
```

`ssthresh` 不是路由器告诉发送端的准确容量，也不是一块缓冲区。它是发送端维护的状态，用来选择增长方式。初始时可以给它一个较高值；发生拥塞后，再根据当时的在途量降低它。

## 5. 接近容量后：一次少试一点

进入 **congestion avoidance，拥塞避免** 后，经典目标大致是每经过一个 RTT，只把 `cwnd` 增加约 `1 MSS`：

```text
8 → 9 → 10 → 11 → 12 MSS
```

这是**加性增加，additive increase**。它仍然试探更多容量，只是比慢启动温和。

注意粒度：

- 慢启动中，一轮内多个新 ACK 合起来，窗口可能大约翻倍；
- 拥塞避免中，不是每个 ACK 都永久增加整整 `1 MSS`；
- 多个 ACK 的小幅增长合起来，一整个 RTT 约增加 `1 MSS`。

一种常见近似是每个确认新数据的 ACK 执行：

```text
cwnd += MSS × MSS / cwnd
```

例如 `cwnd=10 MSS`，每个 ACK 大约增加 `0.1 MSS`；一轮约有 10 个相应 ACK，合起来大约增加 `1 MSS`。真实实现还要处理整数计算、延迟 ACK 和按字节计数等细节。本课推演直接按“每 RTT 加 1 MSS”，避免把近似公式误当成每次都精确成立。

## 6. cwnd 增加后，数据会凭空发出去吗

不会。`cwnd` 只改变上限，Sender 仍要检查旧公式：

```text
有效上限 = min(rwnd, cwnd)
新发送额度 = max(0, 有效上限 - bytes_in_flight)
```

假设：

```text
rwnd = 20 MSS
cwnd = 4 MSS
bytes_in_flight = 4 MSS
```

收到新的累计 ACK，确认了 `1 MSS`：

```text
bytes_in_flight：4 → 3 MSS
慢启动使 cwnd：4 → 5 MSS
新发送额度：5 - 3 = 2 MSS
```

这 `2 MSS` 中：

- `1 MSS` 来自旧数据被确认，原先占用的额度释放；
- `1 MSS` 来自拥塞窗口增长，发送端获得额外试探额度。

但应用若没有新数据，Sender 不会为了“填满窗口”凭空制造 payload。`rwnd` 比 `cwnd` 更小时，新增的 `cwnd` 也可能暂时用不上。

## 7. 窗口变大不等于立刻测得更多带宽

`cwnd` 的单位是字节，不是 byte/s。增长 `cwnd` 只是允许更多数据未确认地留在发送—确认循环中。

它能否提高吞吐量，要看：

- 当前是否受 `cwnd` 限制；
- `rwnd` 是否足够大；
- 应用是否有数据；
- RTT 多长；
- 瓶颈是否已经满负荷。

如果瓶颈已经被持续利用，继续增加在途量可能主要增加路由器队列和 RTT，而不是提高吞吐量。拥塞控制必须通过后续反馈发现自己试得过头。

## 8. 丢失为什么会被当作拥塞信号

回到第 17 课的有限队列：持续进入的速度超过出口速度，队列最终装满，新的包可能被丢弃。发送端看不到那次“队尾丢弃”，但能从确认行为推测异常。

经典算法常区分两个丢失线索：

1. **RTO 超时**：最旧未确认段等到重传计时器到期。
2. **三个重复 ACK**：累计 ACK 连续停在同一缺口，但至少有若干后续段到达接收端。

两者表达的严重程度不同。

```text
超时：一段时间内没有足够进展，反馈循环可能已经很不顺畅
三个重复 ACK：有一个缺口，但后续数据仍在到达，ACK 时钟大体还活着
```

所以经典 TCP 对超时通常更保守。

## 9. 超时：退回很小的窗口重新探路

假设超时发生时：

```text
FlightSize = 12 MSS
cwnd = 12 MSS
```

经典规则先记录新的阈值：

```text
ssthresh = max(FlightSize / 2, 2 MSS)
          = 6 MSS
```

然后把拥塞窗口降到一个较小的丢失窗口。本课采用：

```text
cwnd = 1 MSS
```

接下来：

```text
1 → 2 → 4 → 6 MSS     慢启动，增长到阈值附近
6 → 7 → 8 → 9 MSS     拥塞避免，每 RTT 约加 1 MSS
```

这里的 `FlightSize` 是发生丢失时实际未确认的数据量。不能不加判断地用一个可能大于实际在途量的旧 `cwnd` 代替它。

窗口从 12 降到 1 时，已经发出去的未确认数据不会被网络召回，`bytes_in_flight` 也不会被强行改成 1。Sender 暂停普通新数据，执行必要的丢失恢复，等待 ACK 让实际在途量下降。

## 10. RTO 变大与 cwnd 变小是两件事

你在 Lab 5 中实现过：超时后重传最旧段，并让 RTO 指数退避。

现在又多了拥塞响应：降低 `ssthresh` 和 `cwnd`。这两个动作解决不同问题：

| 状态 | 回答的问题 |
| --- | --- |
| RTO | 等多久仍没有进展，才再次判断可能丢失？ |
| `cwnd` | 当前最多允许多少未确认数据在途？ |
| `ssthresh` | 接下来使用快速增长还是温和增长？ |

一次超时可以同时导致：

```text
重传最旧未确认段
RTO 退避，例如 1s → 2s
ssthresh 降低
cwnd 降到较小值
```

不能把“计时器等更久”和“允许在途更少”当成同一个变量。

## 11. 三个重复 ACK：缺口存在，但网络还在流动

假设接收端连续期待序号 `1001`：

```text
段 [1001,2001) 丢失
段 [2001,3001) 到达 → ACK=1001
段 [3001,4001) 到达 → ACK=1001
段 [4001,5001) 到达 → ACK=1001
```

发送端看到三个重复 ACK，都指向同一个缺口。它不必等 RTO 到期，就可以重传从 `1001` 开始的缺失段。这叫 **fast retransmit，快速重传**。

为什么不是看到第一个重复 ACK 就立即断言丢包？因为网络本来就可能乱序。等待多个重复 ACK，是在恢复速度和误判风险之间折中。

收到这些重复 ACK 同时说明：后面的段正在到达接收端，所以至少有一些数据已经离开网络，ACK 时钟没有完全停止。经典 Reno 因而进入 **fast recovery，快速恢复**，比超时后从 `1 MSS` 重新开始更温和。

为便于手算，本课主表把快速恢复压缩为：

```text
ssthresh = max(FlightSize / 2, 2 MSS)
立即快速重传缺失段
恢复完成后 cwnd = ssthresh
随后进入拥塞避免
```

真实 Reno 在恢复期间会临时“膨胀” `cwnd`，把三个重复 ACK 代表的已离网段考虑进去；当新的 ACK 覆盖缺失段后，再把窗口收回 `ssthresh`。理解这段思想即可，本课检查站不要求背临时的 `+3 MSS` 细节。

## 12. 一张完整的状态轨迹

继续使用：

```text
MSS = 1000 byte
初始 cwnd = 1 MSS
初始 ssthresh = 8 MSS
```

| 事件之后 | `cwnd` | `ssthresh` | 下一阶段 | 为什么 |
| --- | ---: | ---: | --- | --- |
| 初始 | 1 | 8 | 慢启动 | 对路径了解少 |
| 一轮新 ACK | 2 | 8 | 慢启动 | 快速试探 |
| 再一轮 | 4 | 8 | 慢启动 | 快速试探 |
| 再一轮 | 8 | 8 | 拥塞避免 | 到达阈值 |
| 再一轮 | 9 | 8 | 拥塞避免 | 每 RTT 约加 1 |
| 再一轮 | 10 | 8 | 拥塞避免 | 继续温和试探 |
| 三个重复 ACK，FlightSize=10 | 5 | 5 | 拥塞避免 | 快速重传；恢复后减半 |
| 一轮新 ACK | 6 | 5 | 拥塞避免 | 每 RTT 约加 1 |
| 超时，FlightSize=6 | 1 | 3 | 慢启动 | 严重信号，重新探路 |
| 一轮新 ACK | 2 | 3 | 慢启动 | 仍低于阈值 |
| 再一轮 | 3 | 3 | 拥塞避免 | 到达阈值 |
| 再一轮 | 4 | 3 | 拥塞避免 | 每 RTT 约加 1 |

请打开[交互](18_TCP拥塞控制_交互.html)，每一步先只看事件文字，猜 `cwnd`、`ssthresh` 和阶段，再点击“下一事件”。

你会看到一个锯齿式过程：试探上升，出现拥塞信号后回退，再继续试探。路径容量会变化，因此发送端不会求出一个数字后永远不动。

## 13. AIMD 为什么有机会让多个发送者靠拢

拥塞避免中的经典思想常概括为 **AIMD**：

```text
AI：additive increase，加性增加
MD：multiplicative decrease，乘性减小
```

假设两个连接共享一个瓶颈：

```text
A 的 cwnd = 12 MSS
B 的 cwnd = 4 MSS
```

如果每轮都各加 `1 MSS`：

```text
12,4 → 13,5 → 14,6
```

两者的绝对差仍约为 8，但比例从 `3:1` 向更接近靠拢。遇到共同拥塞后都减半：

```text
14,6 → 7,3
```

绝对差也从 8 缩为 4。多次重复“都加一点、拥塞时按比例退让”，在理想模型中有助于兼顾利用率和公平性。

真实网络没有保证所有流同时看见相同信号，也不保证每个应用只开一条连接；不同 RTT、不同算法和应用行为都会影响公平性。本节只解释 AIMD 的基本方向。

## 14. 丢包并不严格等于拥塞

经典丢包型拥塞控制之所以降低窗口，是因为路由器队列满导致丢包是常见来源。但无线干扰也可能损坏数据，路径变化也可能导致乱序。

发送端只看到端到端现象，很难立即知道中间的真实原因。把所有丢失都当作拥塞会比较保守，有时会降低无线路径上的吞吐量；完全不退让又可能伤害共享网络。

现代协议还可以使用：

- ECN：路由器在丢包前后用显式标记报告拥塞；
- SACK：告诉发送端除累计 ACK 之外哪些区间已经收到；
- CUBIC、BBR 等不同的拥塞控制思路。

这些机制不会取消本课的基础约束：发送端仍需根据路径反馈控制发送压力，`rwnd` 和 `cwnd` 仍解决不同问题。

## 15. 教学模型与现代实现的边界

本课从 `1 MSS` 开始，是为了清楚展示 `1→2→4→8`。现代 TCP 可以使用更大的初始窗口；[RFC 6928](https://www.rfc-editor.org/rfc/rfc6928.html) 提出了最多约 10 个段的实验性初始窗口上限。

慢启动、拥塞避免、快速重传和快速恢复的经典定义与规则可参考 [RFC 5681](https://www.rfc-editor.org/rfc/rfc5681.html)。RTO 的计算与退避属于 [RFC 6298](https://www.rfc-editor.org/rfc/rfc6298.html)。现代系统的具体算法可能不同，例如 [RFC 9438](https://www.rfc-editor.org/rfc/rfc9438.html) 描述了 CUBIC。

因此，阅读系统配置中的 `cubic` 或其他算法名时，不要得出“本课的 Reno 风格模型是假的”。本课构造的是共同基础：未知容量时试探、接近容量时减缓增长、出现拥塞信号后退让，以及区分流量控制和拥塞控制。

## 16. 常见混淆

### 混淆一：慢启动就是每个 RTT 只加一点

恰好相反。在经典模型中，它从小窗口开始，但新 ACK 会使窗口较快增长，一轮大约翻倍。

### 混淆二：每个 ACK 都让 cwnd 增加一个 MSS

这只适合本课慢启动的简化说法，而且必须是确认新数据的 ACK。拥塞避免阶段一整个 RTT 合起来才约增加一个 MSS。

### 混淆三：三个重复 ACK 确认了三份新数据

重复 ACK 的累计确认位置没有推进。它们提供的是“缺口仍在，但后续段到达”的线索。

### 混淆四：cwnd 降低时顺便把 flight 改小

`cwnd` 是新发送上限；`bytes_in_flight` 是实际未确认量。已经发出的数据不会因为本地改了数字就消失。

### 混淆五：ssthresh 是网络容量

它只是发送端选择增长阶段的阈值，是根据历史反馈调整的估计状态。

### 混淆六：RTO 翻倍等于 cwnd 减半

一个管等待时间，一个管在途额度，作用不同。

## 17. 本课最小模型

```text
发送上限：min(rwnd, cwnd)
实际在途：bytes_in_flight
新发送额度：max(0, min(rwnd,cwnd) - bytes_in_flight)

cwnd < ssthresh：慢启动，约每 RTT 翻倍
cwnd >= ssthresh：拥塞避免，约每 RTT 增加 1 MSS

超时：ssthresh 约取 FlightSize 的一半，cwnd 降至 1 MSS
三个重复 ACK：快速重传；恢复后 cwnd 约回到 ssthresh

RTO：控制等待多久再次判定丢失
cwnd：控制允许多少数据未确认地在途
```

## 检查站

请遮住正文，用本课明确给出的教学规则回答。所有窗口值都以 MSS 为单位；除非题目另说，`rwnd` 足够大、应用数据充足。

1. 为什么固定 `cwnd=20 MSS` 不能同时适合所有路径？为什么也不能让发送端一开始就使用一个任意大的窗口？

有的链路需求大，基础bdp大，有的小，导致有的浪费，有的丢包

2. “慢启动”的“慢”指什么？在每个满长度段都产生新 ACK 的简化条件下，为什么 `cwnd` 可以在一轮 RTT 后大约翻倍？

指的是初始cwnd小，一轮段送达，产生等量ack返回，这一轮新的段就为双倍

3. 初始 `cwnd=1、ssthresh=8`。连续四个无丢失 RTT 结束后，依次写出每轮结束的 `cwnd` 和下一轮阶段。本课在 `cwnd==ssthresh` 时采用哪个阶段？

1，2，3，4，8，后面加性增加
采取拥塞避免阶段

4. `cwnd=10、ssthresh=8`，处于拥塞避免。再经过三个正常 RTT 后，`cwnd` 依次是多少？为什么不是 20、40、80？

11，12，13
加性增加，因为cwnd大于ssthresh采取拥塞避免

5. `rwnd=20、cwnd=4、bytes_in_flight=4`。一份新累计 ACK 确认 `1 MSS`，慢启动又把 `cwnd` 增加 `1 MSS`。此刻 flight、有效上限与新发送额度分别是多少？额度中的两份分别来自哪里？

3，5，2
ack确认，慢启动增大


6. 超时发生时 `FlightSize=12、cwnd=16、MSS=1`。按本课规则计算新的 `ssthresh` 和 `cwnd`。为什么 `ssthresh` 根据 FlightSize 而不是盲目使用 16？现有 `bytes_in_flight` 会不会被直接改成 1？

6，1
因为flight是能够更加准确反映当前多少的在途数据就导致了超时，而不是窗口上限
不会

7. 分别说明 RTO、`cwnd`、`ssthresh` 回答什么问题。一次超时为什么可能同时改变三者？

rto指本次的超时阈值，cwnd指拥塞窗口允许的最大在途量，ssthresh是慢启动阈值，它指定了加性增加和乘性增加的分界点
超时时，rto指数退避，cwnd重新慢启动，慢启动阈值也重新算

8. 缺失段从序号 1001 开始，后面三个段先到达。接收端为什么可能连续回复三个相同的 `ACK=1001`？发送端从中同时推断出哪两件事？

可能是后面的到了，前面的丢了，后面的字节到了，就会回复ack，但是ack没有变
可以推断出，可能是cwnd太大导致了丢包，重新试探

9. 三个重复 ACK 与 RTO 超时都可能触发重传。为什么经典 TCP 通常对超时采用更激烈的窗口回退？

超时是后面的也没有送到，更加可能是cwnd太大导致的

10. 三个重复 ACK 到来时 `FlightSize=10`。按本课简化规则，新的 `ssthresh` 是多少？立即重传什么？恢复完成后 `cwnd` 是多少、进入哪个阶段？

5，重传那个重复ack对应的字节段，cwnd=ssthresh，进入拥塞避免

11. 当前 `cwnd=6、ssthresh=10`，随后发生超时，`FlightSize=6`。写出超时后的阈值和窗口，再写出之后三个正常 RTT 结束后的 `cwnd` 与阶段。

3，1
2，3，4
慢启动，慢启动，拥塞避免

12. 为什么重复 ACK 不能像新累计 ACK 一样反复释放同一份 `bytes_in_flight`，也不能在慢启动中每次永久增加整整一个 MSS？

因为重复ack说明对方还没有收到这些字节

13. 两条连接共享瓶颈，窗口分别为 12 和 4。两轮加性增加后是多少？随后共同减半是多少？这个过程怎样让它们在理想模型中更接近公平？

14，6
7，3
加性增加让他们的差值比缩小，共同减半让他们插值绝对值缩小

14. 一款 UDP 游戏每秒持续发送大量快照。为什么“UDP 没有内置 cwnd”不等于“应用不需要拥塞控制”？至少列出两种可观察反馈和一种可以采取的退让动作。

不管怎么样，中间路由器或者链路的队列任然可能有排队时间长或丢包的问题，可以看字节也没有少，延迟大不大，发现了可以减少在途数据量

15. 结合 Lab 5：超时重传旧段时，`next_seqno_abs` 为什么不推进？加入拥塞控制后，超时还会额外改变哪些与发送压力有关的状态？

    因为算的是旧账，rto，ssthresh，cwnd

## 教师反馈（2026-09-20）

当前判断：**部分掌握，已经接近基本掌握**。窗口限制、慢启动的增长来源、拥塞避免、超时回退、`FlightSize`、RTO、快速重传和 AIMD 主线都已经建立；还需要收口三个会直接影响状态追踪的细节。

1. 第 3 题的四轮结果应为 `2、4、8、9`，不是 `1、2、3、4、8`。初始的 `1` 不算“第一轮结束”；到达 `cwnd==ssthresh==8` 后，下一阶段已经是拥塞避免，所以第四轮只近似增加到 `9`。
2. 第 8 题中，三个重复 `ACK=1001` 能直接说明的是：累计缺口仍从 1001 开始，而且缺口后面的段仍在到达接收端。它不能单独证明 `cwnd` 太大，也不能证明丢失一定由拥塞造成。
3. 第 9 题的方向正确，但“后面的也没有送到”不是超时必然证明的事实。超时只说明在 RTO 内没有得到足够的 ACK 进展；相比三个重复 ACK，发送端掌握的“网络仍在运送后续段”的证据更少，所以采用更保守的回退。
4. 第 11 题数值 `2、3、4` 正确；每轮结束后的下一阶段应写成“慢启动、拥塞避免、拥塞避免”。第二轮结束时已经到达阈值 `3`。
5. 第 12 题需要补上记账依据：重复 ACK 的累计确认位置没有前进，因此没有新字节从 `bytes_in_flight` 中移除。快速恢复期间可能临时调整窗口，但不能把每份重复 ACK 当成一份永久的慢启动增长。
6. 第 14 题有效：延迟上升和丢包都是可观察反馈，降低发送速率或限制在途量属于合理退让。

### 三道收尾题

1. 当前 `cwnd=5、ssthresh=6`。按本课的 RTT 粒度近似，之后三个正常 RTT 结束时，`cwnd` 依次是多少？每次结束后的下一阶段是什么？

1，2，4，6，7
从七开始拥塞避免

2. 接收端连续发回三份相同的 `ACK=4001`。发送端能较有把握地判断哪两件事？它能不能仅凭这些 ACK 判断丢包一定由拥塞造成？

ack位的字节没有收到，因为ack没有音效推进，不能

3. 三份重复 ACK 到来前 `bytes_in_flight=8 MSS`，三份 ACK 的累计确认位置都没有推进。暂不考虑快速恢复的临时窗口膨胀：`bytes_in_flight` 是否减少？若随后一份新累计 ACK 一次向前推进 `3 MSS`，它又变成多少？

不能，5

### 收尾题反馈（第二轮）

- 第 3 题正确：三份重复 ACK 没有确认新字节，所以 flight 保持 `8 MSS`；新累计 ACK 前进 `3 MSS` 后变为 `5 MSS`。
- 第 1 题忽略了题目已经给出的当前状态 `cwnd=5`，不需要从 `1` 重新推演。按本课近似，第一轮从 `5` 增长到阈值 `6`，下一阶段立即是拥塞避免；后两轮依次为 `7、8`。
- 第 2 题书面答案没有写出“缺口后的段仍在到达”，学习者随后确认这一点已经理解，因此按表达省略处理，不记为仍存在的概念缺口。

最后只确认一点：

1. **现在就是** `cwnd=5、ssthresh=6`，不从 1 重算。只经过接下来的一个正常 RTT，结束时 `cwnd` 是多少？下一阶段是什么？

答：`6`，开始拥塞避免。

### 最终评价

回答正确。已经能从题目给定的当前状态继续追踪：一个正常 RTT 后 `cwnd` 到达 `ssthresh=6`，按照本课的判定规则，下一阶段进入拥塞避免。结合前两题，重复 ACK 的证据边界与 flight 记账也已补全。

本课状态：**基本掌握**。后续只需在间隔复习中再次追踪一条包含慢启动、阈值切换、重复 ACK 和超时的混合轨迹，不需要重读全文。

## 下一步

下一项是 Lesson 19 / Lab 6：把 Ethernet、ARP、待解析队列和 IPv4 数据报重新接成更完整的网络接口。复杂 TODO 会直接给出“输入事件 → 判断 → 状态变化 → 输出动作”的代码内路线图，并为公共函数提供必要 XML 注释。
