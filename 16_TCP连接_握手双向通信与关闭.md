# 第 16 课：把两台状态机接起来——TCP 连接、握手、双向通信与关闭

> 预计时间：2.5～3.25 小时
>
> 前置：[[13_TCP接收端_序号_ACK与窗口]]、[[14_TCP发送端_滑动窗口与重传]]、[[Labs/Lab5_TinyTCPSender/README|Lab 5：Tiny TCP Sender]]
>
> 本课不要求写代码。目标是把前两课中分开的发送端与接收端拼成一条完整连接。

---

## 0. 现在缺的不是另一个算法，而是把两边接起来

你已经分别造出了：

```text
TcpSender
  应用字节 → 分段 → SEQ → outstanding → 超时重传

TcpReceiver
  SEQ → 换算位置 → 乱序重组 → ACK + window
```

如果只看一个方向，似乎已经完成了：

```text
A 的 Sender ─────────→ B 的 Receiver
```

但真实游戏服务器通常还要回复：登录结果、世界快照、伤害判定和心跳。于是同一条 TCP 连接里还有相反方向：

```text
A 的 Receiver ←───────── B 的 Sender
```

完整结构其实是：

```text
主机 A                                      主机 B

A 的发送 ByteStream                        B 的接收 ByteStream
       ↓                                          ↑
    A Sender ───────── A→B 的 TCP 段 ─────────→ B Receiver

A 的接收 ByteStream                        B 的发送 ByteStream
       ↑                                          ↓
    A Receiver ←──────── B→A 的 TCP 段 ───────── B Sender
```

> [!important]
> 一条 TCP 连接提供两个互相独立的单向字节流。A 和 B 各自都有一个 Sender 和一个 Receiver。

“独立”意味着两个方向分别拥有自己的：

- ISN；
- SEQ 空间；
- `next_seqno_abs`；
- outstanding 队列；
- 重传计时器；
- 接收 ACK 与接收窗口。

它们属于同一条连接，但不能共用一套序号。

---

## 1. 从一个 Unity 客户端连接服务器开始

假设：

```text
Unity 客户端：192.0.2.10:53000
游戏服务器：  198.51.100.20:7777
传输协议：    TCP
```

`192.0.2.10` 和 `198.51.100.20` 是两端的 IP 地址；`53000` 和 `7777` 是两端的端口。

把一个 IP 地址和一个端口放在一起，可以称为一个 **endpoint，端点**：

```text
客户端端点 = 192.0.2.10:53000
服务器端点 = 198.51.100.20:7777
```

在 TCP 内部，一条已经建立的连接通常由四项共同区分：

```text
(来源 IP，来源端口，目标 IP，目标端口)
```

这叫 TCP 连接的 **四元组**。对于上面的客户端到服务器方向：

```text
(192.0.2.10, 53000, 198.51.100.20, 7777)
```

### 为什么服务器端口 7777 可以同时服务很多玩家

假设又来了两个客户端：

```text
玩家 A：192.0.2.10:53000 → 198.51.100.20:7777
玩家 B：192.0.2.11:53000 → 198.51.100.20:7777
玩家 C：192.0.2.11:53001 → 198.51.100.20:7777
```

目标端口都相同，但四元组不同，所以操作系统能把到达的段交给三个不同的已连接套接字。

服务器通常先有一个 **监听套接字**：

```text
监听：本机 198.51.100.20:7777 上有没有新的连接请求？
```

每完成一位客户端的握手，操作系统再产生一个代表该连接的 **已连接套接字**。监听套接字继续等待其他玩家，不直接替代这些连接套接字。

这补全了之前“一端口是否只能属于一个进程”的问题：

- 同一监听地址和端口通常由一个服务持有；
- 该服务可以拥有很多共享本地端口 7777 的已连接套接字；
- 操作系统利用完整四元组区分它们。

> [!note]
> 若把 TCP/UDP 协议种类也算进去，网络工具常说“五元组”。本课已经限定 TCP，所以重点使用四元组。

---

## 2. 为什么不能拿到四元组以后直接发送应用数据

客户端想发送：

```text
Login{name="A"}
```

按照前几课，这些应用消息最终会变成 TCP 字节。可是客户端若直接发一段：

```text
SEQ = ?
payload = 登录消息
```

第一个问题立刻出现：`SEQ` 从哪里开始？

客户端需要为 A→B 方向选择 ISN，服务器也要为 B→A 方向选择自己的 ISN。接收方必须先知道对方选择的原点，才能把后续 32 位 SEQ 换算成内部绝对位置。

还有第二个问题：客户端怎样知道服务器的 TCP 状态真的收到了请求，并且返回方向可以工作？服务器又怎样知道客户端收到了自己的 ISN？

因此建立连接至少需要交换三类事实：

1. 客户端告诉服务器：“我要建立连接，这是 A→B 方向的 ISN。”
2. 服务器告诉客户端：“你的请求到了；这是 B→A 方向的 ISN。”
3. 客户端再告诉服务器：“你的回复也到了，我已经知道你的 ISN。”

这正是三次握手要完成的最小闭环。

---

## 3. 三次握手逐步发生了什么

假设：

```text
客户端 ISN = 1000
服务器 ISN = 7000
```

### 第一次：客户端发送 SYN

```text
客户端 → 服务器
SYN=1
SEQ=1000
```

SYN 的含义可以先理解为：

> 我要开始本方向的 TCP 序号空间，这个方向的原点在线上显示为 1000。

SYN 占用一个序号位置，因此客户端发送后的内部状态是：

```text
next_seqno_abs = 1
bytes_in_flight = 1
下一份新内容的线上 SEQ = 1001
```

服务器收到 SYN 后，自己的 Receiver 才能记录客户端的 ISN，并产生：

```text
ACK = 1001
```

这表示客户端的 SYN 已被接收，下一个期待位置是 1001。

### 第二次：服务器发送 SYN+ACK

```text
服务器 → 客户端
SYN=1
ACK标志=1
SEQ=7000
ACK字段=1001
```

同一个段同时完成两件事：

- `SEQ=7000, SYN=1`：公布服务器在 B→A 方向的 ISN；
- `ACK=1001`：确认客户端在 A→B 方向的 SYN。

服务器的 SYN 也占一个位置，所以服务器下一份新内容使用：

```text
SEQ = 7001
```

### 第三次：客户端发送 ACK

```text
客户端 → 服务器
ACK标志=1
SEQ=1001
ACK字段=7001
payload=空
```

它告诉服务器：

> 我已经收到你的 SYN，B→A 方向下一个期待位置是 7001。

这是一份纯 ACK：没有 SYN、没有 FIN、没有 payload，所以不占新的序号位置。客户端的 `next_seqno_abs` 仍是 1。

此时双方都知道了对方的 ISN，也都知道自己的 SYN 已经到达对方，连接进入可以交换数据的状态。

[打开三次握手、双向数据与关闭的逐步交互](./16_TCP连接_交互.html)

---

## 4. 为什么是三次，而不是两次

把每次发送后“谁能确定什么”写出来：

### 只有第一次之后

```text
客户端发出 SYN
```

服务器若收到，只能确定：

```text
客户端 → 服务器 的请求能够到达
服务器知道了客户端 ISN
```

客户端还不知道第一次是否到达。

### 第二次之后

```text
服务器发出 SYN+ACK
```

客户端若收到，可以确定：

```text
自己的 SYN 到达了服务器
服务器的回复能够返回
客户端知道了服务器 ISN
```

但服务器还不知道 SYN+ACK 是否到达客户端。它不能仅凭“我已经发送”假装对方已经收到——这正是第 14 课发送端不能在 `send()` 后立刻忘记数据的同一个道理。

### 第三次之后

服务器收到客户端的 ACK，才知道：

```text
客户端已经收到服务器 SYN
客户端已经知道服务器 ISN
```

因此第三次不是多余的礼貌回复，而是补上服务器缺少的确认。

> [!important]
> 三次握手不是让沿途路由器建立一条固定线路。连接状态主要保存在两端 TCP 中；普通 IP 路由器仍然逐个数据报转发，并不知道这条 TCP 字节流的完整状态。

---

## 5. 状态名只是给刚才的事实贴标签

先看刚才的变化，再认识常见名称：

| 时刻 | 客户端状态 | 服务器状态 | 直观含义 |
| --- | --- | --- | --- |
| 开始 | `CLOSED` | `LISTEN` | 客户端尚未连接；服务器等待新请求 |
| 客户端发 SYN | `SYN-SENT` | `LISTEN` | 客户端等待自己的 SYN 被确认 |
| 服务器收 SYN、发 SYN+ACK | `SYN-SENT` | `SYN-RECEIVED` | 服务器知道客户端 ISN，等待自己的 SYN 被确认 |
| 客户端收 SYN+ACK、发 ACK | `ESTABLISHED` | `SYN-RECEIVED` | 客户端的两个方向已经就绪 |
| 服务器收到最终 ACK | `ESTABLISHED` | `ESTABLISHED` | 双方都确认了对方的序号原点 |

暂时不需要死记英文状态。更重要的是看到每个状态在等待哪一份证据。

---

## 6. `ACK` 这个词有两个相邻但不同的用法

查看 TCP 首部时，经常同时看到：

```text
ACK flag = 1
Acknowledgment number = 1004
```

它们不是两个确认进度：

- ACK 标志位：说明“本段中的确认号字段有效”；
- ACK 字段：真正的 32 位线上确认边界，例如 1004。

第一次握手的普通 SYN 通常没有有效确认号，所以 ACK 标志为 0。握手以后，大多数 TCP 段都会设置 ACK 标志，因为双方通常一直在告诉对方“我连续收到了哪里”。

课程中简写：

```text
ACK=1004
```

通常是指“ACK 标志有效，而且确认号字段为 1004”。

---

## 7. 一份 TCP 段中的 SEQ 和 ACK 指向两个相反方向

握手完成后，客户端要发送 `ABC`。它制作：

```text
客户端 → 服务器
SEQ=1001
ACK=7001
payload=ABC
```

这里两个数字的方向不同：

```text
SEQ=1001：我正在发送的 A→B 字节，从本方向位置 1001 开始
ACK=7001：你发给我的 B→A 字节，我下一个期待的是 7001
```

服务器收到 `ABC` 后回复 `OK`：

```text
服务器 → 客户端
SEQ=7001
ACK=1004
payload=OK
```

同样：

```text
SEQ=7001：服务器正在发送的 B→A 数据从 7001 开始
ACK=1004：服务器已经连续接收客户端的 [1001,1004)，下一项期待 1004
```

可以把每个段读成一句话：

> “这是我这个方向从 SEQ 开始的新内容；同时，你那个方向我已经连续收到 ACK 之前。”

这也是为什么同一个 TCP 段既会交给本机 Receiver，又会把确认信息交给本机 Sender：

```text
收到一份完整 TCP 段
  ├─ SEQ、SYN、FIN、payload → 本机 Receiver
  └─ ACK、window             → 本机 Sender
```

---

## 8. ACK 不一定需要单独发送：捎带确认

服务器本来可以先发一份纯 ACK：

```text
SEQ=7001, ACK=1004, payload=空
```

随后再发送 `OK`：

```text
SEQ=7001, ACK=1004, payload=OK
```

但如果服务器正好有数据要回，可以在携带 `OK` 的段中一起填写 `ACK=1004`。这叫 **piggybacking，捎带确认**：

```text
一份段同时完成：
1. 发送本方向的数据
2. 确认反方向的数据
```

捎带确认减少了额外段，但它没有把两个方向的序号空间混在一起。`OK` 仍然占服务器方向的 `[7001,7003)`，ACK 仍然确认客户端方向的 `[起点,1004)`。

如果本机暂时没有数据可捎带，TCP 仍然可以发送纯 ACK。不能为了等待回复数据而无限推迟确认。

---

## 9. 把 Lab 4 和 Lab 5 拼成一个最小连接对象

你已经有：

```text
TcpReceiver::receive(data_part)
TcpReceiver::ackno()
TcpReceiver::window_size()

TcpSender::receive(ack_and_window)
TcpSender::push()
TcpSender::tick(elapsed_ms)
```

如果以后实现一个简化的 `TcpConnection`，收到完整 TCP 段时，它大致做两路分发：

```text
incoming TCP segment
  │
  ├─ segment.seqno / SYN / FIN / payload
  │        ↓
  │   local_receiver.receive(...)
  │
  └─ segment.ackno / window
           ↓
      local_sender.receive(...)
```

而本机 Sender 产生一个待发送段后，连接还要把本机 Receiver 的最新反馈附在它上面：

```text
outgoing segment
  ├─ SEQ / SYN / FIN / payload ← local_sender
  └─ ACK / window              ← local_receiver
```

所以发送端和接收端不是两套互不相干的实验。它们在连接对象里一边负责“我发出去的内容”，另一边负责“对方发给我的内容”。

### 一份段到达时的具体例子

服务器收到：

```text
SEQ=1001, ACK=7003, window=8, payload=ABC
```

服务器本机：

1. Receiver 使用 `SEQ=1001` 放置 `ABC`，可能让本机 ACK 推进到 1004；
2. Sender 使用 `ACK=7003, window=8` 清理服务器此前发送的内容，并尝试发送更多；
3. 下一份服务器段可以携带 Receiver 新生成的 `ACK=1004`。

同一份输入同时影响两个方向，但影响的是两套不同状态。

---

## 10. 握手段丢失以后为什么不会彻底卡死

### 客户端 SYN 丢失

```text
客户端 SYN ───×
```

客户端 Sender 没收到确认，RTO 到期后重传 SYN。SYN 已经在 outstanding 中，因此重传不会再推进 `next_seqno_abs`。

### 服务器 SYN+ACK 丢失

服务器已经进入 `SYN-RECEIVED`，但自己的 SYN 未被确认。它的计时器到期后重传 SYN+ACK。

客户端也可能先重传 SYN。服务器看到重复 SYN 时不会创建一个全新的序号空间，而会根据已有连接状态再次回复。

### 第三次 ACK 丢失

客户端可能已经认为连接建立，但服务器仍在等待自己的 SYN 被确认。服务器会重传 SYN+ACK；客户端识别重复 SYN 后，再发送 ACK。

这再次说明：可靠性不是“每份控制消息绝不会丢”，而是“丢失后仍有状态、序号、确认和计时器帮助恢复”。

---

## 11. `connect()` 成功到底证明了什么

当 C# 或 C++ 的 TCP 连接调用成功返回时，通常意味着操作系统已经完成握手，建立了本机 TCP 连接状态。

它能支持的判断是：

```text
在握手期间，对端 TCP 对这个连接请求作出了响应
双方交换并确认了初始序号
```

它不能单独证明：

- 用户账号已经登录成功；
- 服务器业务线程一定健康；
- 以后网络不会中断；
- 发送的每条游戏消息都被逻辑层处理；
- 对方身份一定可信；
- 连接内容已经加密。

登录、身份认证、业务结果属于应用协议；加密与服务器身份验证通常由 TLS 等机制提供，以后单独学习。

---

## 12. 关闭的第一个关键点：两个方向可以分开结束

假设客户端已经发送完请求，于是关闭自己的 Writer。它最终发送：

```text
客户端 → 服务器
SEQ=1004
FIN=1
ACK=7003
```

客户端 FIN 占用位置 `[1004,1005)`。服务器连续接收到这里后回复：

```text
ACK=1005
```

这只表示：

```text
客户端 → 服务器 方向已经没有更多字节
```

服务器 → 客户端方向仍然可以继续。例如服务器在收到 EOF 后才计算结果，并发送：

```text
SEQ=7003
ACK=1005
payload=BYE
```

这种“一个方向已经结束，另一个方向仍然开放”的状态叫 **half-close，半关闭**。

对应 ByteStream：

```text
客户端关闭发送 Writer
        ↓ FIN 到达且之前无空洞
服务器接收 ByteStream 的 Writer 关闭
        ↓
服务器 Reader 仍可读完已有字节；服务器自己的发送 Writer 不受影响
```

这正是你之前问“A 和 B 是否分别有 Writer 和 Reader”的最终答案：每个方向单独结束，因此一端的发送 Writer 关闭，不等于整条双向连接立刻消失。

---

## 13. 正常关闭为什么常画成四段

继续上面的例子：

```text
客户端                                  服务器

FIN, SEQ=1004  ───────────────────────→
                ←────────────────────── ACK=1005

                ←────────────────────── FIN, SEQ=7006
ACK=7007        ───────────────────────→
```

四个动作分别是：

1. 客户端关闭 A→B 方向；
2. 服务器确认客户端 FIN；
3. 服务器稍后关闭 B→A 方向；
4. 客户端确认服务器 FIN。

若服务器收到客户端 FIN 时也正好准备关闭，可以把第 2 步的 ACK 和第 3 步的 FIN 放在同一份段中。所以抓包时不一定永远看到四个独立段，但逻辑上仍要完成两个 FIN 和各自的确认。

### 常见关闭状态名

主动先关闭的一方可能经过：

```text
ESTABLISHED → FIN-WAIT-1 → FIN-WAIT-2 → TIME-WAIT → CLOSED
```

被动收到 FIN 的一方可能经过：

```text
ESTABLISHED → CLOSE-WAIT → LAST-ACK → CLOSED
```

先不要背状态名。当前只需读懂：

- `FIN-WAIT`：我已经发 FIN，等待确认或等待对方 FIN；
- `CLOSE-WAIT`：我收到了对方 FIN，但我自己的发送方向尚未关闭；
- `LAST-ACK`：我也发出了 FIN，等待最后确认；
- `TIME-WAIT`：最后确认发出后，再保留一段时间。

---

## 14. 为什么最后发完 ACK 还不能马上忘记连接

假设客户端确认了服务器 FIN：

```text
客户端 → 服务器
ACK=7007
```

但这份最终 ACK 可能丢失：

```text
最终 ACK ───×
```

服务器没有收到，就会超时并重传 FIN。客户端若已经彻底删除所有连接状态，收到这份重传 FIN 时就不知道该怎样再次确认。

因此主动关闭的一方通常进入 `TIME-WAIT`，暂时保留足够状态：

```text
服务器重传 FIN
        ↓
客户端仍能识别
        ↓
客户端再次发送最终 ACK
```

TIME-WAIT 还有第二个作用：让旧连接中延迟很久的重复段逐渐从网络消失，降低同一四元组很快被新连接复用时，旧段混进新连接的风险。

常见实现会等待大约 `2 × MSL`。MSL 是对报文段在网络中最大生存时间的一种上界假设。现在不需要记具体秒数，因为不同系统设置可能不同；需要记住的是它服务于“最后 ACK 可能丢失”和“旧重复段仍可能存在”这两个问题。

---

## 15. FIN 与 RST：正常收尾和立即中止

FIN 表示：

> 我这个方向的正常字节流到此结束，FIN 之前的数据仍应可靠、有序地交付。

RST 是 **reset，复位/重置**，表示连接被异常或立即中止。常见情境包括：

- 目标端口没有程序监听；
- 应用选择中止连接，而不是等待缓冲数据正常发送完；
- 收到与现有连接状态不相符的段；
- 程序崩溃或操作系统回收连接。

收到 RST 后，应用通常看到连接错误，而不是正常 EOF。尚未可靠交付的数据可能丢失。

不要把它们混成同一种“断开”：

```text
FIN → 有顺序的正常 EOF，可以半关闭
RST → 立即报告连接异常，不保证剩余字节正常交付
```

---

## 16. Unity/C# 代码中大致会看到什么

概念上可以对应到：

```csharp
var client = new TcpClient();
await client.ConnectAsync(serverAddress, 7777);
NetworkStream stream = client.GetStream();
```

`ConnectAsync` 返回前，操作系统通常已经替应用完成三次握手。应用拿到的 `NetworkStream` 是一条全双工字节流：可以读取，也可以写入。

服务器侧概念上是：

```csharp
var listener = new TcpListener(localAddress, 7777);
listener.Start();
TcpClient playerConnection = await listener.AcceptTcpClientAsync();
```

`listener` 继续监听 7777；每次 `AcceptTcpClientAsync` 返回一个代表具体玩家连接的对象。多个对象可以共享服务器本地端口 7777，因为远端 IP 或远端端口不同。

但 TCP 只给出字节流。握手没有增加登录消息、移动消息和攻击消息的边界。应用仍然要设计：

```text
消息类型 + 长度 + payload
```

或其他 framing 规则。这一点不会因为“连接已经建立”而消失。

---

## 17. 常见混淆

### 混淆一：一条 TCP 连接只有一个 Sender 和一个 Receiver

每台主机各有一个 Sender 和 Receiver。两个 Sender 分别服务两个方向。

### 混淆二：同一条连接的两个方向共用 ISN

不共用。客户端和服务器分别选择本方向的 ISN。

### 混淆三：段里的 SEQ 和 ACK 都描述这段 payload

SEQ 描述本段发送方向；ACK 确认相反方向。

### 混淆四：服务器端口 7777 只能对应一位玩家

监听端口可以产生许多已连接套接字；完整四元组用于区分连接。

### 混淆五：握手成功等于游戏登录成功

握手只建立 TCP 状态。登录是否成功必须由应用协议回答。

### 混淆六：调用 close 后两个方向都立即消失

FIN 只关闭发送它的方向。另一方向可能继续发送，最终再用自己的 FIN 结束。

### 混淆七：纯 ACK 会推进本方向 SEQ

纯 ACK 没有 SYN、FIN 或 payload，不占序号位置。

---

## 18. 和 CS144 主线的关系

CS144 把可靠字节流拆成 ByteStream、Reassembler、Receiver 与 Sender，让每个组件的状态能够单独实现和测试。本课程的 Lab 2～5沿着这条构造路线前进。

本课做的是必要的概念拼装：

```text
一端 Sender ↔ 另一端 Receiver
两个相反方向 ↔ 一条全双工 TCP 连接
SYN 建立序号原点
ACK/window 把接收状态反馈给反向 Sender
FIN 分方向关闭
```

后面学习拥塞控制时，会继续使用 Sender 的 `bytes_in_flight` 和重传信号，但限制发送的不再只有接收方容量，还包括网络本身能够承受的在途量。

---

## 19. 本课总结

```text
TCP 连接由两条相反方向的可靠字节流组成
每端都有 Sender 和 Receiver
连接用两端 IP、两端端口构成的四元组区分

SYN：公布本方向 ISN，并占一个序号位置
三次握手：让双方交换并确认两个方向的序号原点

SEQ：描述本段发送方向的位置
ACK：确认相反方向的连续接收进度
ACK 可以和反方向 payload 捎带在同一段中

FIN：正常结束一个方向，可以形成半关闭
RST：异常或立即中止整个连接
TIME-WAIT：应对最终 ACK 丢失和旧重复段
```

---

## 检查站

请先合上本课或滚动到看不到正文的位置，再根据具体情境回答。

1. 游戏服务器监听 `198.51.100.20:7777`。玩家 A 使用 `192.0.2.10:53000`，玩家 B 使用 `192.0.2.11:53000`。服务器为什么能同时区分两条连接？分别写出两个四元组。

四元组，略
来源ip加来源端口
目标ip加目标端口

2. 客户端 ISN=1000、服务器 ISN=7000。写出三次握手中每份段的 SYN、SEQ 和 ACK；说明哪两个 SYN 各占了哪个序号位置。

1，1001，-
1，7001，1001
0，1001，7001

1000，7000

3. 第二次握手已经到达客户端后，为什么服务器仍需要第三次 ACK？服务器还缺少哪项证据？

缺少了客户端已经收到客户端ack的证据

4. 握手后客户端发送 `SEQ=1001, ACK=7001, payload=ABC`。分别说明 SEQ 和 ACK 描述哪个方向，服务器收到后下一份累计 ACK 应是多少。

客-》服
服-》客
1001

5. 服务器随后发送 `SEQ=7001, ACK=1004, payload=OK`。这份段怎样同时完成“发送数据”和“确认数据”？服务器方向与客户端方向各推进了什么？

发送数据由sender完成，确认数据由之后确认收到接收方ack确认
服务器推进ack，客户端推进seq

6. 上题中的 `OK` 丢失，但客户端稍后重传了 `ABC`。服务器能否把重复 `ABC` 再交给应用一次？它应回复的累计 ACK 仍是多少？

不能，ack不变

7. 客户端第一次 SYN 丢失时，哪个组件保存了什么状态，什么事件会触发恢复？重传 SYN 会不会再次推进 `next_seqno_abs`？

understanding保存flight，timer记录事件，超时触发重传，不会再次推进

8. TCP `ConnectAsync` 已成功，但服务器随后回复“密码错误”。这是否矛盾？分别说明 TCP 握手和应用登录证明了什么。

不矛盾，握手成功只是说明此时的传输连接已经成功建立，和登录成功没有关系

9. 客户端发送完 `ABC` 后用 `FIN` 关闭 A→B 方向。服务器还能不能在 B→A 方向发送 `Result`？这叫什么状态？

可以，两边的传输是分开的

10. 客户端发送一份不带 payload 的 FIN，使用 SEQ=1004，服务器应回复哪个 ACK？另一方向上，假设服务器的下一个新线上序号明确为 7006（先前数据只占用到 7005），随后单独发送 FIN。这个 FIN 使用哪个 SEQ，客户端又回复哪个 ACK？

> 2026-09-19 题干修订：原句“服务器已发送到 7006”无法区分“下一位置为 7006”和“序号 7006 的字节也已发送”。下方保留原答；按后者理解得到 7007、7008 是合理的，不记为概念错误。

1005，你说的是发送到7006，所以我理解为seq=7007，7008

11. 最终 ACK 丢失后，服务器会做什么？客户端处于 TIME-WAIT 为什么能帮助恢复？

超时重传，等待一段时间，没有收到回复就重传

12. 一份入站 TCP 段同时带有 `SEQ、payload、ACK、window`。在你已经实现的组件中，哪些字段交给 `TcpReceiver`，哪些字段交给 `TcpSender`？

seq，payload
ack，window

13. 监听套接字和已连接套接字分别负责什么？为什么它们可以同时使用服务器本地端口 7777？

监听套接字用于监听是否有新的客户端要建立连接
已连接套接字负责负责接收消息
一个端口可以包含一个监听套接字和多个连接套接字

14. 一份 TCP 段包含 `SEQ=50, ACK=80, payload=XYZ`。仅凭它能否说“XYZ 是第 80 个字节”？请指出两个数字各自的真实方向。

不能，payload序号是seq决定的，而且seq是线上序号
seq指向发出
ack指向接收

15. FIN 和 RST 都可能让应用停止通信。为什么正常 EOF 与连接重置不能被应用当作同一种结果？

fin是正常字节流结束
rst表示异常断开





请只做下面三道针对题：
1. 客户端 ISN=3000，服务器 ISN=8000。写出三次握手每份报文的 SYN、SEQ、ACK。

 SYN、SEQ、ACK
 1，3000，-
 1，8000，3001
 0，3001，8001

2. 握手后，客户端用 SEQ=3001 发送四字节 WXYZ。服务器应回复哪个 ACK？

接收成功的话，ack=3005

3. 服务器随后用 SEQ=8001 发送 OK，并携带 ACK=3005：
SEQ=8001 表示什么？服务器从8001开始发送
ACK=3005 表示什么？服务器下一个期待客户端发送的字节序号是3005
发送后服务器的下一个新 SEQ 是多少？8003
---

## 下一步

检查站与三道针对题已通过，继续阅读 [[17_排队_瓶颈与拥塞窗口]]：即使接收方还有很大窗口，为什么发送者也不能毫无节制地把数据塞进网络。课程会从路由器队列被填满的可观察失败出发，引出拥塞窗口。

## 检查反馈（2026-09-19，追加保留原答）

总体：基本掌握，可以进入第 17 课。首次作答对握手中本段 SEQ、数据后的累计 ACK、捎带 ACK 的方向有混淆；三道新情境复测全部正确：

- ISN=3000/8000 时，三次握手的 SEQ 分别为 3000、8000、3001，后两份 ACK 为 3001、8001。
- 客户端从 3001 发送 WXYZ 后，服务器连续收到四字节，ACK=3005。
- 服务器从 8001 发送 OK 并携带 ACK=3005，SEQ 描述服务器发出的方向，ACK 确认客户端发来的方向；服务器下一新线上 SEQ=8003。

措辞补全：ACK 表示“下一个期待收到”的位置，不能据此断言对方尚未发送这个位置。

后续提问又确认了位置关系：普通新数据段的下一新内部绝对序号等于 `unwrap(本段 SEQ)` 加本段 payload 长度；SYN/FIN 各额外占一位，重传不重复推进。实际 Sender 已保存内部位置，通常直接累加再在需要线上值时 wrap。

第 10 题属于教材歧义，已在题干就地澄清。第 11 题已解释“服务器重传未确认 FIN，TIME-WAIT 客户端再次回复最终 ACK”；尚未单独取得新情境复答，放在第 17 课检查站第 12 题与后续短复习中确认，不要求重读整课。
