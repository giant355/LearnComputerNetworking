# 第 31 课：连上的到底是谁——TLS、登录凭据与权限

> 前置：[[29_HTTP_通用请求与响应]]、[[30_WebSocket_长连接与断线恢复]]。
> 预计 3～4 小时，包含威胁情境推演、交互与检查站。不要求实现密码算法，不安排 Lab。
> 目标不是背密码学术语，而是分清：防偷看、防冒充、登录账号、允许操作，分别需要什么证据。

## 1. TCP 可靠，为什么密码仍然可能泄露

游戏用 HTTP 发送登录信息。先假设 TCP 正常工作，所有字节都准确、有序地到达服务端。问题是：路径上的旁观者如果能观察明文流量，也可能读到密码。

TCP 的“可靠”描述交付和顺序，不承诺保密。校验和主要用于发现偶然传输错误；能主动改包的人可以改完数据再计算校验和。因此它也不是抵御恶意篡改的充分机制。

我们希望新增三种能力：

1. **保密性**：旁观者看到传输字节，也不能直接读出正文。
2. **完整性保护**：对方检测到未获授权的修改，而不是把被改的金额当真。
3. **对端身份验证**：确认正在与预期服务通信，而不是只和一个陌生人建立了保密通道。

第三点最容易漏。假服务器也会加密；“有加密”不能单独回答“跟谁加密”。

## 2. 先放对位置：TLS 在哪儿工作

为了给应用协议提供上述保护，可以使用 **TLS（Transport Layer Security，传输层安全协议）**。名字里含传输层，但这里不要因此把它等同于 TCP。对于本课的 TCP 场景，数据的逻辑处理顺序是：

```text
发送：HTTP / WebSocket 明文 → TLS 加密并保护 → TCP 字节流 → IP → 链路
接收：链路 → IP → TCP 恢复字节 → TLS 验证并解密 → HTTP / WebSocket 解析
```

在常见系统中，TLS 库运行于应用进程或相关系统组件中，通过 Socket 使用 TCP；上图不是要求你把一个 TLS 对象写进 TCP 内核。

HTTPS 是受到安全保护的 HTTP，`wss` 是安全的 WebSocket。拿 HTTPS 的服务器地址直接用裸 `NetworkStream` 写明文 GET，并不会自动加密；必须用支持 TLS 的客户端库或流包装。库不是装饰，它要实际完成握手、证书验证和记录保护。

本课解释 TLS 1.3 的典型完整握手，略去恢复会话、客户端证书和早期数据等分支。HTTP/3 所用 QUIC 的承载关系不同，不能把这张 TCP 图套到它上面。

## 3. 如果把密钥直接发送过去呢

假设双方打算用一把共享密钥加密后续消息。如果客户端把密钥明文发给服务器，旁观者同时拿到密钥，就失去意义。

于是需要一种机制：双方各自保留秘密，只交换可以公开的参数，最后各自算出匹配的共享秘密，而旁观者仅凭公开交换内容不应能计算出它。现代 TLS 常用基于临时密钥的密钥协商来做这件事。

这里先把数学算法当黑箱，但输入输出不能糊涂：**双方没有直接把最终流量密钥明文寄过去**；双方根据协商结果与握手信息派生出用于不同方向和用途的密钥。所谓派生，就是用密码学算法从已有秘密及上下文生成所需密钥。

但是这仍没有解决冒充：攻击者可能分别与客户端、服务器协商，变成两段加密连接的中间人。因此还需要把这次协商绑定到正确的身份。

## 4. 服务器说“我是官网”不算证据

客户端要连 `game.example.com`。某个地址上的服务声称“我就是它”，客户端为什么相信？

服务器拿出一份**证书**：其中包含服务名字、公钥、有效期等信息，并带有签发者的数字签名。公钥可以公开；与之配对的私钥必须由持有者保护。数字签名让验证者检查“这份内容是否由相应私钥的持有者签出，内容是否被改过”，不是把整份内容藏起来。

证书签发者称为 **CA（Certificate Authority，证书颁发机构）**。客户端不是相信任意自称 CA 的人，而是从系统或应用已配置的信任锚开始，检查证书链。信任锚通常是被预先信任的根证书；中间证书连接根与服务端证书。

```text
客户端已有的信任配置
→ 验证证书链的签名和限制
→ 检查证书是否适用于 game.example.com、是否在有效期等
→ 验证服务端在本次握手中确实持有对应私钥
```

最后一步不能省：证书本身可以公开，攻击者复制一份证书不等于拿到了私钥。TLS 握手中的签名等证明把身份与当前握手绑定，不能只看一张证书图片。

把 `game.example.com` 的证书放到 `evil.example.net` 上使用，名字检查不能因为“签名有效”就略过。客户端验证的是自己原本打算访问的名字，不是攻击者临时建议的名字。

证书验证通常还包含其他约束与平台策略，这里没有列成实现清单。项目中使用成熟平台验证机制，不自己编写证书验证器。即便地址由 DNS 返回，仍需要验证身份；DNS 给出地址不自动提供上述证明。

## 5. 走一遍简化握手：每一步解决哪个缺口

前提：TCP 已连接，客户端知道预期主机名，并有正常的信任配置。

| 阶段 | 主要发送或执行者 | 做什么，为什么 |
| --- | --- | --- |
| ClientHello | 客户端 | 表达支持的版本、算法及密钥协商材料，给服务端选择依据 |
| ServerHello | 服务端 | 选定兼容参数并给出协商材料，双方据此派生密钥 |
| 服务端认证与握手校验 | 服务端发送，客户端验证 | 用证书、私钥持有证明和握手校验，确认身份及握手未被篡改 |
| 客户端握手校验 | 客户端发送，服务端验证 | 确认客户端掌握相应握手秘密，完成典型握手 |
| 应用数据 | 双方 | 在受保护的通道中交换 HTTP 或游戏消息 |

这是逻辑职责表，不是“一行一定占一个 TCP 段”。在 TLS 1.3 中，ServerHello 之后的大部分握手内容已经加密；也不是等所有身份验证结束后才第一次使用密钥。

默认的典型 HTTPS 场景验证服务器身份，并不要求客户端提交个人证书。因此 TLS 成功时，服务端仍未必知道你是哪位游戏玩家。参见 [TLS 1.3 握手](https://www.rfc-editor.org/rfc/rfc8446.html#section-2)。

## 6. 为什么加密后的数据还能正常路由

路由器需要知道目标 IP 才能转发。如果把外层所有字段都藏起来，普通转发就无法按原方式工作。

在这里，TLS 保护的是应用数据及相关 TLS 内容，不是把外层 IP、TCP 首部全部加密。路径观察者一般仍能看见端点 IP、端口、流量大小和时间特征。不能由“用了 HTTPS”推出“任何人都不知道你和谁通信”。

HTTPS 下，HTTP 路径、普通 HTTP 首部和正文在 TLS 保护范围内；域名是否能从其他地方观察到，还取决于 DNS 及握手隐私等机制。本课不把 HTTPS 宣传成匿名工具。

另一个边界是**终止位置**：有些架构在入口代理解开 TLS，再把请求转给后端。代理就是代替后端接收、处理或转发请求的服务。此时客户端到代理受保护，不自动证明代理到后端也受同样保护；后半段必须另行设计。

## 7. TLS 成功后，为什么还要登录

现在客户端知道自己连的是预期服务，通道也受到保护。但是服务器只知道“有个客户端连进来了”，还不知道账号是谁。

应用于是发送登录请求，由账号系统检查凭据。**身份验证（authentication）**回答“你是谁”；之后的**授权（authorization）**回答“这个身份是否允许做这件事”。它们不是同一个动作。

例如玩家 C2 已正确登录，不代表可以控制玩家 C1 的对象 41。服务器还要根据可信的会话身份，查询对象所有者、当前游戏状态和允许操作。不能直接相信消息正文里的 `playerId=41` 就把发送者当成 41。

这里与你第 22 课的理解接起来：所有权与服务器最终裁决仍然重要。加密不会让恶意客户端自动变诚实。

## 8. 不想每次发送密码，怎么办

登录成功后，服务器可以创建一段难猜的随机字符串，记录它对应哪个账号、何时过期、允许什么，再交给客户端。客户端以后携带这串值，服务器据此识别会话。这类凭据通常称为**令牌（token）**。

用一个非真实示例表示：

```text
服务器会话表：随机令牌 T → 账号 C2、有效期、权限范围
后续请求：带 T → 服务端检查 T → 找到 C2 → 再检查具体操作权限
```

有些令牌不使用这个精确存储结构，但本课先用最容易理解的会话表。不要因此认为所有 token 都必须有一种固定格式，也不要把编码后的字符串误当成加密后的字符串。

HTTP 常见携带方式之一是 `Authorization: Bearer <token>`。Bearer 表示“持有它就可据此访问相应资源”的一类凭据，不是另一种传输层协议。令牌一旦泄露，别人可能冒用，因此必须保护传输和存储，不写进 URL、聊天截图或普通日志。[Bearer token 的使用与风险](https://www.rfc-editor.org/rfc/rfc6750.html)

另一种携带机制是 Cookie：服务器通过响应让客户端保存值，符合范围条件的后续请求再自动带上。Cookie 是保存与携带数据的机制，不天然等于账号；其中可以放会话标识。`Secure`、`HttpOnly` 等属性分别限制传输和脚本访问，但不能取代服务端权限检查。

## 9. 为什么一条加密连接还会返回 401 或 403

考虑四个关口：

```text
TCP 建连 → TLS 身份与保护 → 账号身份验证 → 操作权限和业务条件
```

TCP 成功而证书过期，可以卡在第二关。TLS 成功但 token 过期，可以卡在第三关。token 有效但普通玩家想踢管理员，可以卡在第四关。

HTTP 401 与有效认证凭据有关；403 表示拒绝授权等情况。实际服务也可能为避免泄露信息而隐藏细节，因此排错仍要结合服务端日志，不能凭一个数字猜出所有原因。

**“证书校验失败就关闭校验”不是解决问题。** 那相当于为了让门顺利打开，把核对门后是谁这一环删了。应检查访问的主机名、证书有效期、设备时间与信任链；开发测试使用正确配置的测试证书或信任设置，并限制范围。

[打开连接、身份与权限四关交互](31_安全边界_交互.html)

## 10. TLS 不能替你完成的三件事

第一，服务器收到一个“伤害值一百万”的加密请求，仍要校验伤害是否合法。传输没被路人改，不代表发送者没撒谎。

第二，TLS 不能单独保证购买恰好执行一次。超时重试与业务去重仍然属于应用设计。

第三，TLS 不保证客户端机器本身安全。恶意程序若在解密后读取内存或窃取 token，传输加密无法自动阻止它。

后续读框架时，先找“传输加密在哪里启用、证书在哪里验证、登录会话在哪里建立、权限在哪里检查”，再读对应 API。现在不要求自己实现算法、保存真实密码，或搭一个生产认证服务。

## 检查站

1. TCP 已保证字节有序可靠，为什么登录仍需要 TLS？校验和为什么不是抵御恶意修改的充分机制？
2. 在 TCP 场景中，HTTP、TLS、TCP 的发送与接收顺序是什么？TLS 是否把外层目标 IP 也藏起来？
3. 双方能协商共享秘密，为什么仍需要验证服务端身份？
4. 服务端发来一份证书，客户端至少要检查哪些方面？攻击者复制公开证书，是否就能证明持有对应私钥？
5. 证书适用于 `game.example.com`，客户端原本要访问 `evil.example.net`。签名有效是否足够让验证通过？
6. TLS 成功是否代表服务器已经知道游戏账号？身份验证和授权分别回答什么问题？
7. C2 登录成功并要求移动 C1 的对象，服务端还应该检查什么？为什么不能只看正文声称的玩家 ID？
8. token 为什么可以减少反复提交密码？若它是 Bearer 凭据，为什么不能把它贴进日志？
9. TCP 已连上，证书有效，token 过期，这是什么层次的问题？关闭证书验证能解决吗？
10. TLS 在入口代理处终止，是否说明到后端的整段路径都已受保护？需要再确认什么？
11. 一条合法加密请求携带非法价格，或者购买超时后重试两次，TLS 能否代替业务校验和去重？

下一课：[[32_完整网络旅程与故障定位]]，将这些关口与 DNS、路由和套接字接成一条能解释、能排查的链。
