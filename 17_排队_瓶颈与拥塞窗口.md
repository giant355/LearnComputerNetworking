# 第 17 课：对方收得下，为什么网络还是卡——排队、瓶颈与拥塞窗口

> 前置：[[14_TCP发送端_滑动窗口与重传]]、[[16_TCP连接_握手双向通信与关闭]]。
>
> 本课可在教室或手机上完成，不需要编译程序。正文约 45～65 分钟，交互与纸上推演约 15～25 分钟，检查站约 20～30 分钟；合计约 80～120 分钟，按你的实际速度调整。
>
> 配套：[打开本课交互](17_排队_瓶颈与拥塞窗口_交互.html)。正文是解释的主线；交互里的数值、事件顺序与正文一致。Obsidian 若不能直接运行 HTML，可用浏览器打开同目录文件。

## 0. 先把上一课的三个数放稳

假设 A→B 方向的 ISN=8000。A 用线上 `SEQ=8001` 发送 `OK`。

| 你在问什么 | 数值 |
| --- | --- |
| 本段第一个字节的线上序号 | 8001 |
| 本段起点的内部绝对序号 | 1 |
| 两个字节占用的内部位置 | 1、2 |
| 发送后，下一个新字节的内部绝对序号 | 3 |
| 下一个新字节的线上序号 | 8003 |

`wrap` 把内部位置转换成线上值；`unwrap` 根据原点与检查点选回内部位置。转换本身不消耗序号。

因此，对紧接着发送的新段，可以这样理解：

```text
下一新绝对序号 = 本段起点的绝对序号 + payload 长度 + SYN 占位 + FIN 占位
```

SYN、FIN 各占一个位置；纯 ACK 不占；重传旧内容不会再次推进下一新序号。Sender 已保存内部位置，平时直接累加，不需要为了累加先 wrap 再 unwrap。

本课的发送例子都假设握手已完成、尚未发送 FIN，只数普通数据字节。我们还会继续使用两个旧状态：

```text
next_seqno_abs：下一个新字节的内部绝对序号
acknowledged_abs：累计 ACK 换算后的内部绝对序号
bytes_in_flight = next_seqno_abs - acknowledged_abs
```

这里的 `bytes_in_flight` 是发送端账本中的“已发送、还没被累计确认”的字节数。它可能包括正在链路上移动的字节、路由器里排队的字节、已经丢失的字节，以及已到接收端但 ACK 还没回来的字节。它不是某一个队列的长度。

## 1. 一个接收窗口解决不了的新问题

A 正在向服务器 B 上传文件。B 的应用读取很快，持续通告很大的接收窗口。

```text
A ──100 Mbit/s──→ 路由器 R ──10 Mbit/s──→ 后续网络 ──→ B
                          ↑
                   从这个出口继续发送
```

如果 A 持续以接近 100 Mbit/s 的速度把数据送到 R，而 R 的这个出口最多送出 10 Mbit/s，多出来的数据怎么办？

> 先预测：B 增加一倍接收缓冲区，能让 R 的 10 Mbit/s 出口变成 20 Mbit/s 吗？

不能。多出来的数据先在 R 的出口队列里等待；队列能保存的数量有限，满了之后就可能丢弃新到的数据报。

两件事发生在不同地方：

- **B 收不收得下**：接收端缓冲区和应用读取速度的问题。
- **A 到 B 的路上送不送得过去**：路径上的链路、队列以及其他发送者共同造成的问题。

第 14 课的接收窗口处理了第一件事。本课补上第二件事。

还有一种情况：单个 A 只发送 6 Mbit/s，看起来小于 10；另一个发送者 C 也向同一出口送来 6 Mbit/s。出口面对的总到达速率是 12 Mbit/s，仍然送不完。判断拥堵要看共享出口收到的总量。

## 2. “瓶颈”具体卡在哪里

上图中，10 Mbit/s 的出口限制了整条路径能持续搬运多少数据。这种限制持续吞吐量的环节，叫作**瓶颈，bottleneck**。

这里先区分两个量：

| 量 | 单位 | 问的是什么 |
| --- | --- | --- |
| 链路容量，常被口语称为带宽 | bit/s 或 byte/s | 这个环节单位时间最多能搬多少？ |
| 实际吞吐量 | bit/s 或 byte/s | 这次传输单位时间实际搬了多少？ |

某条链路标着 10 Mbit/s，并不保证你的单条连接总能独享 10 Mbit/s。其他连接也可能使用它，协议首部也占传输量。下面做计算时，我们会明确哪些开销被忽略。

在暂不考虑共享流量的简单串联路径中，最慢链路决定容量上限。真实路径还会受共享、调度与链路变化影响；瓶颈不是永远固定在“家里那个路由器”。

## 3. 先用六个格子看见排队

我们把 R 的出口缩成一个能手算的模型：

- 所有数据报一样大。
- 每一步代表 10 ms。
- 每一步**先从旧队列头部发走最多 2 份，再让本步新到的 3 份依次入队**。
- 队列最多保存 6 份；没有空位时丢弃新来的，这叫队尾丢弃。
- “队列里有几份”只统计正在等待的，不包含已经发走的。

这个先后顺序是教学模型的约定。真实网卡可以在连续时间里收发；本模型的第 1 步因为没有旧包，会先发走 0 份。题目和交互都遵守同一约定，避免“刚来的包能不能立刻发走”的歧义。

先遮住表格，预测前三步结束后分别排着几份。

| 步 | 步初旧队列 | 本步先发走 | 再新到 | 接纳 | 丢弃 | 步末队列 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 0 | 0 | 3 | 3 | 0 | 3 |
| 2 | 3 | 2 | 3 | 3 | 0 | 4 |
| 3 | 4 | 2 | 3 | 3 | 0 | 5 |
| 4 | 5 | 2 | 3 | 3 | 0 | 6 |
| 5 | 6 | 2 | 3 | 2 | 1 | 6 |
| 6 | 6 | 2 | 3 | 2 | 1 | 6 |

第 2～4 步，每步进 3、出 2，所以净增加 1。队列满了以后，这个差额就变成丢包。

可以把每步变化写成：

```text
发走 = min(旧队列数量, 2)
剩余 = 旧队列数量 - 发走
接纳 = min(本步到达数量, 队列容量 - 剩余)
丢弃 = 本步到达数量 - 接纳
新队列数量 = 剩余 + 接纳
```

这里每个数都能对应到一个具体动作。以后做排队 Lab 时，你实现的也是这些状态变化。

此时可以打开[交互的“路由器出口队列”](17_排队_瓶颈与拥塞窗口_交互.html#queue)，逐步验证。再把每步到达改成 0，观察旧队列怎样被排空。每步到达改成 2 后，队列不会继续增长，但原先积下来的等待也不会凭空消失。

## 4. 队列更大，为什么有时反而更卡

把上面的容量从 6 改为 12，发送能力仍是每步 2，到达仍是每步 3。

第一次丢包会更晚发生。但长期的“进得比出得快”没有变化。新包前面可以积下更多旧包，等待也可能更久。

队列有价值：如果只是一次短暂突发，后来到达变少，队列可以暂存它，然后逐渐排空。问题出在**持续超量输入**；单纯增加存储空间不能增加出口的持续发送能力。

现在暂时离开离散步模型，单独计算一份包的排队时间。假设：

```text
出口速率 = 1,000,000 byte/s
每份包的计费长度 = 1,000 byte
新包前面有 8 份等待包
出口恰好空闲，即将发送这 8 份中的第一份
采用先进先出，中途没有更高优先级包插队
```

先发送完前面的 8 份，需要：

```text
排队等待 = 8 × 1,000 / 1,000,000 s = 0.008 s = 8 ms
```

轮到新包后，它自己的发送还要 1 ms。之后还有传播、后续设备处理等时间。

这是 [[05_一次发送的完整旅程|第 5 课]] 四种延迟中的两个不同阶段：前面的包没发完，自己只能**排队**；轮到自己把比特送入链路，才是自己的**发送延迟**。

如果原本已经有一份包发送到一半，还要加上它剩余的发送时间。明确计时起点，算式才不会少一块或多一块。

因此，“能下载很多字节”和“一个小操作能快速得到回复”并不矛盾：大文件可以让出口始终很忙，小消息却排在大量旧包后面。过大的持续积压常被称为 **bufferbloat，缓冲膨胀**。先记现象，不用背英文。

## 5. 别把三种缓冲区混起来

你已经实现过 `pending` 和 `output`。现在又出现路由器队列，很容易把它们当成同一处。

| 存放位置 | 存的是什么 | 为什么等待 | 什么动作使它减少 |
| --- | --- | --- | --- |
| 路由器出口队列 | 等待从某个链路出口发出的包 | 出口暂时忙，轮不到它 | 出口发走包 |
| 接收端重组器 `pending` | 已收到、但前面还有空洞的流字节 | 还不能连续交付 | 缺口补齐后向输出流交付 |
| 接收端 ByteStream `output` | 已连续交付、应用尚未读走的字节 | 应用还没读取 | Reader `pop` |

路由器通常不会为了普通 IP 转发去重建你这条 TCP 字节流，也不靠 `first_unassembled` 决定何时转发。

还有第四个名字 `outstanding`：它是发送端为未确认内容保留的记录，便于确认清理和重传。里面的副本留在发送端，不表示那些字节的网络副本也都留在发送端。

所以，服务器 Reader 很快、`output` 几乎为空时，途中某个路由器的出口队列仍然可能排得很长。

## 6. 接收窗口之外，再加一个发送端上限

回到 Sender。第 14 课已经知道：

```text
对方允许未确认范围多宽？→ 接收端通告的 window
其中已经用掉多少？      → bytes_in_flight
```

我们现在给旧的接收窗口一个更明确的名字：**`rwnd`，receiver window**。它就是对方最近通告的接收窗口大小；这里使用解释后的字节数。

但 Receiver 没有直接看到沿途每台路由器的队列。它不能只凭自己还剩多少缓冲空间，就替整个路径保证“你再发这些绝不会拥堵”。

因此 Sender 还要维护一个本地数：

> 根据目前的网络反馈，我允许本方向有多少字节处于“已发出、未累计确认”的状态？

这个数叫 **拥塞窗口 `cwnd`，congestion window**。

| 对比 | `rwnd` | `cwnd` |
| --- | --- | --- |
| 主要回答 | 接收端允许我占用多大范围？ | 根据路径反馈，我目前允许自己维持多大在途量？ |
| 谁决定 | 对端 Receiver | 本端 Sender 的拥塞控制逻辑 |
| 怎样得到 | 对端 TCP 报文的 Window 信息 | 根据 ACK、丢失等反馈在本地调整 |
| 单位 | 字节 | 本课统一使用字节 |
| 是否是自己的一块存储空间 | 不是，它是通告的范围大小 | 不是，它是控制发送的数值 |

TCP 首部没有一个普通的 `cwnd` 字段让路由器替你填好。`cwnd` 也不是本机网卡队列大小，更不是“整条网络准确还能装多少字节”的测量结果。它是发送端不断调整的控制上限。

一条全双工连接的两个方向各有自己的 Sender，因此也各自维护拥塞控制状态。A→B 和 B→A 不必使用相同的 `cwnd`。

## 7. 两个上限一起起作用

先做预测：对方说最多允许 10,000 字节未确认，而本机暂时只允许 4,000 字节在途。应该听谁的？

必须同时满足两者，所以当前总上限取更小的那个：

```text
有效上限 = min(rwnd, cwnd)
还能新发送 = max(0, 有效上限 - bytes_in_flight)
```

这是普通数据新发送的额度计算。零窗口探测和丢包恢复有各自的规则，本课暂不把它们塞进同一个公式。

| `rwnd` | `cwnd` | 已在途 | 有效上限 | 还能新发 | 当前较紧的限制 |
| ---: | ---: | ---: | ---: | ---: | --- |
| 10,000 | 4,000 | 3,000 | 4,000 | 1,000 | 拥塞窗口 |
| 2,000 | 4,000 | 1,500 | 2,000 | 500 | 接收窗口 |
| 10,000 | 4,000 | 4,000 | 4,000 | 0 | 额度用完，等待变化 |
| 10,000 | 2,000 | 4,000 | 2,000 | 0 | 上限缩小后，旧在途量暂时超过上限 |

最后一行可以发生：发送时允许在途 4,000 字节，后来拥塞控制把 `cwnd` 降到 2,000。已经发出的包不会被隔空召回。Sender 暂停普通新数据发送，继续按协议处理确认与恢复。

所以应把公式理解成“决定这次还能新增多少”，不能断言任意时刻都绝不可能出现 `bytes_in_flight > cwnd`。

C++ 中还要避免无符号减法下溢，可以用这样的局部表达式：

```cpp
const auto limit = std::min(rwnd, cwnd);
const auto allowance = bytes_in_flight < limit ? limit - bytes_in_flight : 0;
```

这里是说明思路，不要求你修改已经通过测试的 Lab 5。旧 Lab 的契约练习的是可靠性与接收窗口；未来加入拥塞控制时再扩展它。

## 8. MSS 在这个公式里负责哪件事

假设刚算出还能新发 2,500 字节，MSS=1,000 字节，应用有足够多的数据。

本模型允许把这些数据分成：

```text
第一段 payload：1,000 字节
第二段 payload：1,000 字节
第三段 payload：  500 字节
```

`rwnd/cwnd` 限制的是未确认数据的总范围；MSS 限制单个段装多少 payload。`cwnd=4,000` 不表示你可以制作一个 4,000 字节 payload 的段。

此外，有额度只表示“允许发送”，不保证应用有那么多数据，也不强迫实现立刻把所有额度塞出去。操作系统还可以安排具体发送时机。

## 9. 用你熟悉的绝对序号追一遍

现在只看 A→B，一个方向。设定：

```text
ISN = 8000，握手已经完成
acknowledged_abs = 1
next_seqno_abs = 1
rwnd = 10,000 字节
cwnd = 4,000 字节（本例固定）
MSS = 1,000 字节
```

这个 `cwnd=4,000` 是为便于手算给定的起始状态，不是在宣布所有操作系统初始值都相同。

A 先发出四段，每段 1,000 字节。内部范围分别是 `[1,1001)`、`[1001,2001)`、`[2001,3001)`、`[3001,4001)`。

| 事件完成后 | `acknowledged_abs` | `next_seqno_abs` | `bytes_in_flight` | 还能新发 |
| --- | ---: | ---: | ---: | ---: |
| 尚未发应用数据 | 1 | 1 | 0 | 4,000 |
| 发出第一段 | 1 | 1001 | 1,000 | 3,000 |
| 四段全部发出 | 1 | 4001 | 4,000 | 0 |
| 收到确认第一段的有效 ACK | 1001 | 4001 | 3,000 | 1,000 |
| 再发一段新数据 | 1001 | 5001 | 4,000 | 0 |

表格用的是内部绝对位置。确认第一段的线上值是 `ACK=9001`；本例的 `unwrap` 把它换算成 `1001`。如果把线上 `9001` 直接拿来和内部 `4001` 相减，账本就混乱了。

注意：ACK 到达时，A 的 `next_seqno_abs` 没变。它只是知道已发送的哪些内容被确认了；真正发出新内容时，`next_seqno_abs` 才推进。

此处还假定 B 持续读走数据，因此示例里的 `rwnd` 保持 10,000；我们暂时固定 `cwnd`，专门观察额度如何被新 ACK 释放。

打开[交互的“ACK 与发送额度”](17_排队_瓶颈与拥塞窗口_交互.html#window)，按“下一步”。每次先猜账本里哪一项会变，再点击。

## 10. 为什么 ACK 像发送节拍

上一节有一个很有用的循环：

```text
发出一些数据 → 在途额度用完 → 数据被接收并确认
                                      ↓
                              新 ACK 释放额度
                                      ↓
                                 再发新数据
```

当瓶颈链路把数据一份份送出去，接收方逐渐产生确认，返回的 ACK 又让发送方逐渐获得额度。发送节奏因此受到交付节奏的反馈，这常叫 **ACK 时钟，ACK clock**。

但请不要把它背成“一个 ACK 必定只能发一个包”：

- 一份累计 ACK 可能一下确认多个段。
- 同一个 ACK 值重复到来，没有新增累计确认，不能仅凭它重复释放相同额度。
- `rwnd` 或 `cwnd` 也可能变化，影响新的额度。
- ACK 可以被延迟或集中送达，所以返回节奏并不总是均匀。

所谓“时钟”是反馈节奏，不是给每个 ACK 设置固定时间间隔的另一只软件计时器。第 14 课的 RTO 计时器负责等待过久后的恢复，是另一个作用。

## 11. 在途上限是字节数，为什么能影响每秒发送量

`cwnd=4,000 字节` 没有“每秒”两个字，它限制数量，不直接规定速率。

要把数量和速率联系起来，还需要知道这些额度多久能周转一次。

**RTT，round-trip time，往返时间**：从发送一段数据，到它的确认返回所经历的时间。它包含去程、接收端生成确认的等待、回程等影响，不只是单程传播距离。

假设一个理想化稳定场景：应用一直有数据，没有丢失，窗口不变，ACK 正常返回，接收端持续读取，发送足够平滑。若约每 40 ms 能周转 4,000 字节额度，则窗口带来的速率上限量级为：

```text
4,000 byte / 0.040 s = 100,000 byte/s
100,000 × 8 = 800,000 bit/s = 0.8 Mbit/s
```

同样的窗口，如果 RTT 为 80 ms，额度周转慢一倍，窗口能支撑的速率量级就减半。

在这些假设下，我们常用下面的估算：

```text
吞吐量 ≲ min(瓶颈可用速率, min(rwnd, cwnd) / RTT)
```

符号 `≲` 读作“大致不超过”。它是理解限制的近似式，不是任何瞬间的精确测量公式；应用供给、丢包、首部开销和发送方式还可能把实际值压低。

这也解释了一个现象：明明链路容量很高，但只允许很少数据在途、又要等很久才能收到 ACK，链路可能在等待确认期间空闲。

## 12. 要把路径利用起来，大约需要多少在途数据

换一个明确的新场景，与第 3 节的六格队列分开：

```text
一条连接独享瓶颈
可用于 payload 的瓶颈速率 R = 1,000,000 byte/s
没有持续排队时的基础 RTT₀ = 40 ms = 0.040 s
rwnd 足够大，应用持续提供数据，暂不考虑丢包与首部开销
```

为了在等确认回来期间仍让瓶颈有数据可发，至少需要一个大约这样的在途量级：

```text
R × RTT₀ = 1,000,000 × 0.040 = 40,000 byte
```

这种“速率 × 时间”得到的数据量叫作**带宽时延积，BDP，bandwidth-delay product**。在本节 TCP 反馈模型里，我们明确使用基础往返时间 RTT₀；有些链路材料用单程时延计算在传播中的比特量，阅读时要检查它使用的是哪一种时间。

先看三个状态的区别：

| 持续在途量 | 在本节理想条件下会看到什么 |
| ---: | --- |
| 10,000 字节 | 窗口只能支撑约 250,000 byte/s；等 ACK 时可能用不满瓶颈 |
| 约 40,000 字节 | 有机会把瓶颈持续利用起来，不必维持很大的长期队列 |
| 约 80,000 字节 | 瓶颈仍只能搬约 1,000,000 byte/s；额外积压可能增加排队时间 |

若最后一种状态里额外的 40,000 字节主要积在同一个出口队列中，则排队增加约 `40,000 / 1,000,000 = 40 ms`，RTT 从约 40 ms 增到约 80 ms。此时 `80,000 / 0.080` 仍只是约 1,000,000 byte/s。

也就是说，在这个模型中，窗口加倍后，变化主要发生在等待时间，吞吐量并没有翻倍。

这里的 40,000 字节不是一份包的大小，也不是要求接收端 `pending` 必须存 40,000 字节。它是维持这条反馈循环所需的在途量级，分布在传输和确认过程中。

不要进一步推导出“所有路由器队列都必须等于 BDP”或“只要窗口等于 BDP 就绝不排队”。突发、共享流量、ACK 生成方式都会影响结果。我们目前只需要看见：在途太少可能浪费容量，太多可能只增加积压。

## 13. Sender 怎样知道该用多大的 cwnd

你现在可能会说：那直接把 `cwnd` 设为刚才算出的 40,000 不就行了？

困难是，发送端往往事先不知道整条路径的可用速率与基础 RTT。即使刚测过，其他用户开始下载后，你能分到的份额也会改变。

发送端只能从反馈逐步判断：

- 新的累计 ACK 持续到达，说明一部分已发数据确实完成了交付。
- ACK 返回变慢，可能与排队有关，也可能与路径、接收端或回程变化有关。
- 超时或某些重复 ACK 模式可能提示丢失；丢失可能由拥塞造成，也可能有其他原因。

这些是线索，不是对某台路由器队列的直接读取。下一课会从“试多一点”和“出现拥塞信号后退让”的问题出发，推导经典慢启动、拥塞避免与丢失响应。

本课先固定 `cwnd`，是为了看清它究竟限制什么。不要把交互中给定的 `4,000` 当成完整拥塞控制算法。

## 14. 可靠性、流量控制、拥塞控制各补了哪一块

看一个具体失败：R 的出口长期忙不过来，丢掉了 A 的数据。

只做重传，可以尝试补齐缺失内容。但如果所有发送者照旧灌入新数据、再叠加重传，出口还会持续过载。拥塞控制需要调节后续发送压力，而不是只关心“丢了哪一段”。

| 机制 | 本课场景里主要解决的困难 |
| --- | --- |
| 序号、重组、ACK、重传 | 数据丢失或乱序后，怎样恢复可靠、有序的交付 |
| 接收窗口带来的流量控制 | 接收端应用读得慢，怎样避免超过它允许的接收范围 |
| 拥塞窗口带来的拥塞控制 | 多个发送者使用有限路径容量时，怎样调节发送压力 |

换成 UDP 不会让瓶颈和排队消失。UDP 本身没有替应用提供 TCP 这一套拥塞控制；需要持续发送大量数据的应用或上层协议仍要考虑发送速率和网络反馈。

对游戏来说，小型移动消息虽然字节少，也可能和大文件共享出口队列。要解释它为什么延迟高，应沿路径找等待发生在哪里，而不能只盯着服务器的 `output` 是否为空。

## 15. 和 CS144 及后续课程怎样连接

你已经通过 Lab 2～5造出了字节流、重组器、Receiver 和 Sender。本课把 Sender 连接到“有限速率、有限队列、反馈需要时间”的路径模型，为拥塞控制补上动机。

Stanford 的 [CS144 课程说明](https://bulletin.stanford.edu/courses/2075241) 将可靠传输、流量控制和拥塞控制列为传输部分主题。本课对应拥塞控制的入门部分；我们的课号不对应某一学年视频号。[2010 年 CS144 的拥塞控制讲义](https://www.scs.stanford.edu/10au-cs144/notes/l4.pdf) 可作历史课程参考，其中某些实现参数有年代背景，不用于规定本课的初始窗口。

`rwnd`、`cwnd` 与 Flight Size 的角色可核对 [RFC 5681 第 2～3 节](https://www.rfc-editor.org/rfc/rfc5681.html#section-2)。本课的六格队列、固定窗口交互及全部数值推演是为理解这些角色设计的教学模型；它们没有实现完整 TCP 拥塞控制或精确的真实网络仿真。

本次不要求新写一个大 Lab。先完成下面的概念检查；下一课研究 `cwnd` 怎样增长与回退，后续再把它接进排队实验。总路线中的网络接口与路由器集成 Lab 仍保留，发布时会继续提供 XML 注释和复杂函数内的分支思路。

## 检查站

请遮住正文，用自己的话作答。可以直接写在题目下面；不要求另外写报告。不确定时指出自己卡在哪一步。

1. A 向 R 的入口持续送入 80 Mbit/s，R 的出口最多发出 10 Mbit/s；B 的 Reader 很快，持续通告很大的 `rwnd`。拥堵主要发生在哪里？为什么 B 增大接收缓冲区不能解决它？

2. 使用第 3 节的精确定序：每步先发走旧队列最多 2 份，再接纳本步新到的包，队列容量为 5。初始为空，前四步每步到达 3 份，第五步到达 0 份。写出五步各自的“步末队列数量、本步丢弃数量”。

3. 一份新包前面有 6 份等待包，每份长度 1,000 byte，出口速率 2,000,000 byte/s，出口恰好空闲并准备发送前面的第一份。新包开始发送前要等多久？自己的发送还需多久？两者各属于哪类延迟？

4. 用具体存放位置区分：路由器出口队列、接收端 `pending`、接收端 `output`。为什么 B 的 `output` 为空，不能证明途中没有排队？

5. `rwnd=9,000`、`cwnd=5,000`、`bytes_in_flight=3,000`，都以字节计。此时还能新发多少字节？若 MSS=1,000、应用数据充足，在本课模型中可以分成几份满长度数据段？

6. 沿用第 5 题，先把新发送额度用完；然后收到一份有效的新累计 ACK，它比之前多确认了 1,000 字节，`rwnd` 与 `cwnd` 不变。在暂不继续发送的瞬间，`bytes_in_flight` 与新发送额度各是多少？若同一个 ACK 值再来一次且窗口不变，这两个数会不会因此再减、再加一次？

7. A 已经有 5,000 字节在途，随后 `cwnd` 从 5,000 降到 2,000，`rwnd` 仍为 9,000。此时普通新数据额度是多少？已发出的包会不会被召回？这是否说明 `bytes_in_flight` 必须立即被代码改为 2,000？

8. 谁维护 A→B 方向的 `cwnd`？A 从 B 的 TCP 首部得到的 Window 信息是 `rwnd` 还是 `cwnd`？`cwnd` 本身是一块保存包的缓冲区吗？

9. 在单流、平滑发送、无丢失、应用供给充足的模型中，瓶颈可用于 payload 的速率为 2,000,000 byte/s，基础 RTT 为 50 ms，`rwnd` 足够大。计算本模型的 BDP；若在途上限只有 20,000 字节，用“窗口除以 RTT”估算它能支撑的速率。写出单位。

10. 两个持续上传的程序共用一个出口。把出口队列从 20 个包扩大到 200 个包，可能改善什么，可能恶化什么？为什么不能据此断言瓶颈吞吐量提高了十倍？

11. ISN=20,000，握手后 `acknowledged_abs=1`、`next_seqno_abs=1`。本端新发一个 1,000 字节的普通数据段，再收到恰好确认它的 ACK。分别写出：本段线上 SEQ、发送后的 `next_seqno_abs`、返回的线上 ACK、收到 ACK 后的 `acknowledged_abs` 与 `bytes_in_flight`。说明哪里用到 wrap/unwrap。

12. 回看第 16 课的一个具体收尾情境：服务器发出自己的 FIN，客户端回复的最终 ACK 丢失，而客户端仍处于 TIME-WAIT。随后谁会超时重传什么？谁收到重复内容后再次发出什么？与第 17 课相比，这主要是在解决可靠收尾、接收缓冲容量，还是路径拥塞？

## 下一步

下一课：从 ACK 与丢失反馈推导拥塞窗口怎样变化。我们会先手动调整一个窗口，观察过小和过大的代价，再给增长与回退规则命名。
