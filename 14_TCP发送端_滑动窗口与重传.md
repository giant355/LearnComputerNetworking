# 第 14 课：TCP 发送端——滑动窗口、超时与重传

> 预计时间：2.5–3.25 小时
>
> 前置：[[13_TCP接收端_序号_ACK与窗口|第 13 课：TCP 接收端]]、[[Labs/Lab4_TinyTCPReceiver/README|Lab 4：Tiny TCP Receiver]]
>
> CS144 对应：TCP Sender、Retransmission Timer 与 flow control 的核心思想。不同年份的 Lab 编号可能不同。

> [!tip] 交互演示
> [打开“TCP 发送端：窗口与重传”](14_TCP发送端_交互.html)。按“下一步”观察发送窗口怎样滑动、最旧未确认段怎样超时重传，以及重传为什么不会重复增加在途字节数。

上一课，我们站在 B 的接收端，看见它能够：

```text
收到 TCP 段 → 换算序号 → 重组字节 → 回复 ACK 和接收窗口
```

现在把视角转回 A。A 收到应用交给它的一串字节之后，不能只做一次 `send()` 就忘掉：

```text
A 应用 Writer → A 的 TCP 发送端 → 不可靠的 IP 网络 → B 的 TCP 接收端
```

IP 可能丢包，B 的缓冲区也可能装满。A 必须回答：

1. 当前允许发送多少字节？
2. 哪些字节发出去了但还没有被确认？
3. ACK 到达后，发送窗口怎样向右移动？
4. ACK 长时间不来时，应该重传哪一段？
5. SYN 和 FIN 怎样占用发送序号空间？

这些问题合起来，就是一个最小 TCP 发送端。

---

## 1. 先观察最朴素方案为什么失败

假设应用一次写入 1 MB，A 立即把全部数据发给 B。

### 失败一：接收端装不下

如果 B 当前只有 8 KB 接收空间，A 一口气发送 1 MB，大量数据即使到达也可能因为窗口外或缓冲区不足而无法接收。

上一课 B 已经在 ACK 中通告了接收窗口：

```text
ACK = 我已经连续收到哪里
window = 从 ACK 开始，我目前还允许你占用多少序号位置
```

A 必须服从它。这叫**流量控制**：发送端不能压垮接收端。

### 失败二：IP 丢了以后没人补

如果某个 TCP 段在途中丢失，A 不能因为操作系统已经接受 `send()` 就认为 B 收到了。A 需要保留尚未确认的数据，并在等待过久时重新发送。

因此发送端至少需要两类约束：

```text
接收窗口：现在最多允许多少数据在外面
重传计时器：最旧未确认数据等了多久
```

> [!important]
> 本课只讨论接收端通告窗口带来的流量控制。网络本身是否拥堵是另一个问题，需要拥塞窗口，后面单独学习。

---

## 2. 发送端到底保存哪些东西

先只观察 `A → B` 方向：

```text
A 应用
  │ push 字节
  ▼
发送 ByteStream
  │ 读取可发送前缀
  ▼
TCP Sender
  ├─ ISN
  ├─ 下一个要分配的绝对序号
  ├─ 接收端最近通告的窗口
  ├─ 已发送但未确认的段
  └─ 最旧未确认段的重传计时器
```

这些状态的职责如下：

| 状态 | 回答的问题 |
| --- | --- |
| `ISN` | 绝对序号 0 在线上写成哪个 32 位数？ |
| `next_seqno` | 下一份新内容应该从哪个内部绝对序号开始？ |
| `acknowledged` | 接收端已经累计确认到哪个内部绝对位置？ |
| `advertised_window` | 接收端允许从 ACK 起再占用多少序号位置？ |
| `outstanding` | 哪些段已经发送，但还不能删除？ |
| `bytes_in_flight` | 还有多少个序号位置尚未得到累计确认？ |
| `timer` | 最旧未确认段已经等待多久？ |
| `RTO` | 等待多久算一次重传超时？ |

这里的 `outstanding` 通常保存 TCP 段的副本。否则第一次发送后丢失，发送端就没有内容可供重传。

---

## 3. 发送窗口是一段会移动的范围

假设 B 最近回复：

```text
ACK = 1004
window = 4
```

它表达的允许范围是：

```text
[1004, 1004+4) = [1004, 1008)
```

也就是序号位置：

```text
1004 1005 1006 1007
```

左边界由累计 ACK 决定，右边界由 `ACK + window` 决定：

```text
发送窗口左边界 = acknowledged
发送窗口右边界 = acknowledged + advertised_window
```

如果 `next_seqno=1005`，说明位置 1004 已经发出但未确认，那么当前还能发送：

```text
1005、1006、1007，共 3 个序号位置
```

### 为什么叫“滑动”窗口

后来 B 回复：

```text
ACK = 1008
window = 3
```

窗口变成：

```text
[1008, 1011)
```

它整体向右移动了，所以叫 **sliding window，滑动窗口**。

这并不是把某个数组真的搬到右边，而是两个逻辑边界发生了变化。

---

## 4. `bytes_in_flight` 不是“网络线缆里的字节”

`bytes_in_flight` 更准确的含义是：

> 已经发送、但尚未被累计 ACK 覆盖的序号空间长度。

可以用内部绝对位置近似写成：

```text
bytes_in_flight = next_seqno - acknowledged
```

假设：

```text
acknowledged = 4
next_seqno = 8
```

那么：

```text
bytes_in_flight = 8 - 4 = 4
```

这 4 个位置可能处于不同现实状态：

- 正在网络中传播；
- 已经到 B，但 ACK 还没回来；
- 某段或它的 ACK 已经丢失；
- 正在等待重传。

所以它不是用仪器数“线缆中现在有几个字节”，而是发送端根据确认状态维护的逻辑账本。

> [!note]
> SYN 和 FIN 各占一个序号位置，因此也计入 `bytes_in_flight`；纯 ACK 不占序号位置。

---

## 5. 一次完整的窗口推进

固定场景：

```text
ISN = 1000
B 通告窗口 = 4
A 应用已经写入 ABCDEFGH
最大 payload 长度 MSS = 3
```

### 第一步：发送 SYN

```text
SEQ=1000, SYN=1, payload=空
```

SYN 占 1 个序号位置：

```text
bytes_in_flight = 1
next_seqno = 1001
```

### 第二步：收到对 SYN 的确认

B 回复：

```text
ACK=1001, window=4
```

A 删除已经被覆盖的 SYN：

```text
bytes_in_flight = 0
允许范围 = [1001,1005)
```

### 第三步：发送 `ABC`

```text
SEQ=1001, payload=ABC
```

它占用 `[1001,1004)`：

```text
bytes_in_flight = 3
next_seqno = 1004
```

### 第四步：窗口只剩一个位置

窗口右边界是 1005，所以还能发位置 1004。即使 MSS 是 3，这次也只能发送一个字节：

```text
SEQ=1004, payload=D
```

现在：

```text
bytes_in_flight = 4
next_seqno = 1005
窗口已满，E 暂时不能发送
```

MSS 是单段 payload 的上限，不是每个段必须达到的长度。

### 第五步：累计 ACK 让窗口向右滑动

B 回复：

```text
ACK=1004, window=4
```

这确认了 `ABC`，但没有确认位置 1004 的 `D`：

```text
新的允许范围 = [1004,1008)
outstanding = D
bytes_in_flight = 1
```

于是 A 又能发送三个位置：

```text
SEQ=1005, payload=EFG
```

发送后重新达到：

```text
bytes_in_flight = 4
```

这就是“ACK 推动窗口、窗口释放发送资格”的完整因果链。

---

## 6. 累计 ACK 怎样清理 outstanding

假设发送端保存：

```text
段 1：[1001,1004)  ABC
段 2：[1004,1005)  D
段 3：[1005,1008)  EFG
```

收到：

```text
ACK=1004
```

意味着所有 `<1004` 的序号位置已经连续收到，所以：

- 段 1 可以删除；
- 段 2、段 3 仍需保留。

后来收到：

```text
ACK=1008
```

三个段都被覆盖，outstanding 清空。

### 重复 ACK 与旧 ACK

如果当前已经确认到 1004，又收到：

```text
ACK=1004
```

它没有推进确认边界，是重复 ACK。收到：

```text
ACK=1002
```

则是旧 ACK。它们都不能把 `acknowledged` 向左退，也不能假装出现了新确认。

本课先只要求：

```text
只有 ACK 真正向右推进，才算 new acknowledgment
```

重复 ACK 后触发快速重传的机制以后再讲。

---

## 7. 为什么通常只给最旧未确认段计时

最朴素的实现可以给每个段各放一个计时器，但状态会迅速变复杂：

```text
段 1 一个 timer
段 2 一个 timer
段 3 一个 timer
……
```

累计 ACK 有一个重要性质：只要最旧的空洞没有补上，ACK 就不能越过它。于是最小发送端只需要盯住 outstanding 队列最前面的段：

```text
outstanding.front() = 最旧未确认段
```

规则是：

1. 第一个需要确认的段发出时，启动计时器；
2. 后续新段发出时，不把计时器重新归零；
3. ACK 真正推进时，移除已确认内容；
4. 若仍有 outstanding，从零重新等待当前最旧段；
5. 若 outstanding 为空，停止计时器。

为什么第 2 条重要？

假如每发送一个新段就重置计时器，只要应用持续产生数据，最旧丢失段可能永远等不到超时。

---

## 8. RTO 与 `tick()`

`RTO` 是 **Retransmission Timeout**，重传超时时间。

假设当前：

```text
RTO = 1000 ms
timer = 0 ms
```

操作系统或测试框架会定期告诉发送端时间过去了多少：

```cpp
sender.tick(200);
sender.tick(300);
sender.tick(500);
```

累计达到 1000 ms 时发生超时：

```text
重传 outstanding 中最旧的段
```

注意，计时器不会因为代码存在就自己变化。必须由时钟事件或 `tick(elapsed_ms)` 推进。

### 重传不会增加 `bytes_in_flight`

第一次发送 `D` 时，它已经计入在途数据：

```text
bytes_in_flight = 4
```

超时后重传同一个 `D`，不是创造一个新的序号位置：

```text
bytes_in_flight 仍然是 4
```

重传的是同一笔未结清的账，而不是新增一笔账。

---

## 9. 为什么连续超时要延长等待

假设第一次超时仍然没有得到新 ACK。如果发送端永远每 1000 ms 重传一次，网络已经拥堵时，它会持续增加负担。

一个基础机制是**指数退避**：

```text
初始 RTO：1000 ms
第一次超时后：2000 ms
第二次超时后：4000 ms
第三次超时后：8000 ms
```

也就是：

```text
RTO ← 2 × RTO
```

同时维护：

```text
consecutive_retransmissions
```

它记录连续发生了多少次重传超时，可供上层判断连接是否已经严重异常。

### 新 ACK 到达时恢复

当 ACK 真正向右推进时：

```text
RTO 恢复为初始值
consecutive_retransmissions = 0
timer 重新开始或停止
```

重复 ACK 没有证明最旧空洞已经补上，所以不能冒充“网络恢复”并重置所有状态。

> [!note]
> 真实 TCP 会根据测得的 RTT 动态估计 RTO。本课和下一次 TinyTCP Lab 先使用给定的初始 RTO，集中理解状态机；RTT 估计稍后再展开。

---

## 10. 零窗口为什么是特殊情况

如果接收端回复：

```text
ACK=5000, window=0
```

发送端按照普通窗口规则，一个新字节也不能发送。但如果 B 的应用后来读走数据，B 发出的窗口更新 ACK 又恰好丢了，双方可能这样僵住：

```text
A：我看到的窗口仍是 0，所以不发送
B：我已经有空间，但窗口更新可能丢了
```

因此发送端会把零窗口暂时视作至少 1 个序号位置，定期尝试一个很小的探测：

```text
effective_window = advertised_window == 0 ? 1 : advertised_window
```

这不是说 B 真的承诺了一个字节空间，而是为了让双方有机会重新交换最新窗口状态。

在 CS144 风格的最小发送端中，零窗口超时重传通常不做指数退避，也不增加普通的连续重传计数；否则探测间隔可能越来越长，连接难以恢复。

---

## 11. SYN、payload 与 FIN 的发送顺序

发送端拥有一条应用 ByteStream。它组织 TCP 段时通常遵循：

1. SYN 尚未发送时，先给本段加 SYN；
2. 在窗口和 MSS 允许的范围内读取 payload；
3. 应用 Writer 已关闭、所有字节都已经取出，并且窗口还有位置时，给末段加 FIN；
4. 段在 TCP 序号空间中的长度为 `SYN + payload.size() + FIN`。

例如：

```text
SYN=1, payload=ABC, FIN=1
```

它占用：

```text
1 + 3 + 1 = 5 个序号位置
```

但一份纯 ACK：

```text
SYN=0, payload=空, FIN=0
```

占用 0 个序号位置，因此不会进入“等待对方确认这份纯 ACK”的发送账本。

### FIN 什么时候才能发送

Writer 调用 `close()` 只表示应用不会再写新字节，不表示 FIN 必须立刻越过窗口发送。发送端必须先把现有字节取完，并确保 FIN 自己也有一个可用序号位置。

这和上一课接收端的关系正好对称：

```text
发送端：FIN 表示自己的 Writer 已结束并且此前字节都已进入 TCP 序号空间
接收端：连续重组到 FIN 所指 EOF 后，关闭接收 ByteStream 的 Writer
```

---

## 12. `wrap` 在发送端哪里使用

发送端内部继续用 64 位绝对位置记账：

```text
next_seqno、acknowledged、outstanding 起止位置
```

真正制作 TCP 首部时才调用：

```text
线上 SEQ = wrap(段的绝对起点, ISN)
```

收到对方 32 位 ACK 时则反过来：

```text
内部 ACK = unwrap(线上 ACK, ISN, 当前发送进度附近的 checkpoint)
```

因此上一 Lab 的 `wrap/unwrap` 并不只服务于接收端。每个方向的发送端和接收端都必须在“线上 32 位”与“本机长期位置”之间转换。

---

## 13. 发送端处理 ACK 的因果顺序

收到带 ACK 和窗口的接收端消息后，可以按这条主线理解：

```text
32 位 ACK
  ↓ unwrap
内部绝对 ACK
  ↓ 检查是否超过 next_seqno
合法吗？
  ↓ 是
更新接收端通告窗口
  ↓
ACK 是否真正推进？
  ├─ 否：不向左退，不重置重传状态
  └─ 是：清理已确认 outstanding
             ↓
          重置 RTO 和连续重传次数
             ↓
          仍有未确认段？重启 timer：停止 timer
             ↓
          尝试发送新数据
```

为什么 ACK 不能超过 `next_seqno`？因为发送端不能接受对方确认自己从未发送过的序号位置。

窗口信息与 ACK 推进是两件事。即使 ACK 没变，对方也可能因为应用读取而通告更大的窗口；发送端应更新窗口，但不能因此假装旧数据获得了新确认。

---

## 14. 一张发送端状态表

沿用前面的场景：

| 事件 | `acknowledged` | `next_seqno` | `bytes_in_flight` | outstanding | timer |
| --- | ---: | ---: | ---: | --- | --- |
| 初始 | 1000 | 1000 | 0 | 空 | 停止 |
| 发送 SYN | 1000 | 1001 | 1 | SYN | 启动 |
| 收到 ACK 1001 | 1001 | 1001 | 0 | 空 | 停止 |
| 发送 ABC | 1001 | 1004 | 3 | ABC | 启动 |
| 发送 D | 1001 | 1005 | 4 | ABC、D | 继续，不归零 |
| 收到 ACK 1004 | 1004 | 1005 | 1 | D | 从零重启 |
| 发送 EFG | 1004 | 1008 | 4 | D、EFG | 继续 |
| 超时，重传 D | 1004 | 1008 | 4 | D、EFG | 归零，RTO 加倍 |
| 收到 ACK 1008 | 1008 | 1008 | 0 | 空 | 停止 |

观察三个不变量：

```text
acknowledged <= next_seqno
bytes_in_flight = next_seqno - acknowledged
重传旧段不会推进 next_seqno
```

---

## 15. 与第 13 课接收端拼起来

现在可以把两个方向的因果链接上：

```text
A Sender                         B Receiver
读取发送 ByteStream
按窗口制作带 SEQ 的段
保存 outstanding
启动 timer
       ───────── TCP 段 ───────→ unwrap SEQ
                                Reassembler 重组
                                计算 ACK 与 window
       ←──── ACK + window ─────
unwrap ACK
清理 outstanding
滑动发送窗口
继续发送
```

如果中间 TCP 段丢失：

```text
A 的 timer 超时 → 重传最旧 outstanding
```

如果 ACK 丢失但数据已经到达，A 可能重传；B 会利用序号识别重复数据，再回复累计 ACK。可靠性不是因为网络不丢，而是两端保存了足够的状态来发现并修复丢失。

---

## 16. 对 C# / Unity 调用意味着什么

假设 Unity 代码执行：

```csharp
await stream.WriteAsync(data);
```

它成功通常只说明这些字节已被本机网络栈接受，不代表：

- 对方已经收到；
- 对方应用已经读取；
- 对方已经处理了游戏逻辑。

本机 TCP 发送端可能仍在：

```text
等待窗口、等待 ACK、进行重传
```

如果对方长期不读取，接收窗口缩小，本机发送缓冲区也可能逐渐填满，后续异步写入会等待。这正是跨主机传播的背压。

Unity 中不要在主线程同步等待阻塞式网络写入；但具体 API 与线程组织以后到应用阶段再展开。本课先掌握底层原因。

---

## 17. 常见混淆

### 混淆一：窗口等于一次最多发多大的段

不对。窗口限制所有未确认序号位置的总量；MSS 才限制单个 TCP 段的 payload 上限。

### 混淆二：ACK 到了就说明应用处理完了

不对。ACK 只证明接收端 TCP 已连续接管相应序号位置。

### 混淆三：每发送一个新段都重置 timer

不对。这样可能让最旧丢失段一直无法超时。timer 盯的是最旧未确认段。

### 混淆四：重传会再次增加 `bytes_in_flight`

不对。它重新发送的是已经记账但尚未确认的相同序号位置。

### 混淆五：接收窗口就是拥塞窗口

不对。接收窗口保护接收端；拥塞窗口保护网络。真实发送上限通常同时受两者约束。

### 混淆六：Writer close 后 FIN 立即发出

不一定。FIN 要排在已有应用字节之后，而且自己也要占一个可用序号位置。

---

## 18. 本课最小模型

只记下面这条链即可：

```text
应用写入 ByteStream
  ↓
发送端在窗口内分段，给新内容分配序号
  ↓
把需要确认的段保存到 outstanding
  ↓
最旧未确认段启动计时器
  ↓
新 ACK：清理、滑动窗口、重置 RTO、继续发送
  ↓
超时：重传最旧段；非零窗口下指数退避
```

三个核心量：

```text
ACK：左边界，已经连续确认到哪里
window：允许范围有多宽
next_seqno：新数据已经分配到哪里
```

---

## 检查站

请先合上本课或滚动到看不到正文的位置，再用自己的话回答。暂时不要求写代码。

1. 为什么 TCP 发送端不能在 `send()` 后立刻忘掉刚发送的数据？请分别从丢包和 ACK 含义解释。
2. `ACK=1004, window=4` 表示发送端允许使用哪个半开序号范围？如果 `next_seqno=1006`，还能发送几个序号位置？
3. 分别用一句话说明 `acknowledged`、`next_seqno` 和 `bytes_in_flight`。写出三者在本课模型中的关系。
4. 已发送 `[1001,1004)` 的 `ABC` 和 `[1004,1006)` 的 `DE`。收到累计 `ACK=1004` 后，哪些内容可以从 outstanding 删除？哪些仍要保留？
5. 为什么后续每发送一个新段时，不应该把最旧未确认段的计时器归零？
6. 当前 `RTO=1000 ms`，连续两次超时且期间没有新 ACK。非零窗口下两次分别重传谁？两次超时后的 RTO 依次是多少？`bytes_in_flight` 会不会因为重传翻倍？
7. 什么样的 ACK 才能把 `consecutive_retransmissions` 清零并把 RTO 恢复为初始值？重复 ACK 为什么不行？
8. 发送端当前已发送 SYN 和 5 个 payload 字节，尚未发送 FIN，也没有任何确认。`bytes_in_flight` 是多少？
9. 一个段同时包含 `SYN=1`、payload=`ABC`、`FIN=1`。它在 TCP 序号空间中占几个位置？应用数据有几个字节？
10. 应用 Writer 已关闭，ByteStream 还有 `XYZ` 未取出，而发送窗口只剩 2 个位置。发送端能不能立刻发送 `XYZ+FIN`？说明一种合理分段过程。
11. 为什么重传一个旧段不会推进 `next_seqno`？
12. 接收窗口为 0 时，如果发送端永远什么都不发，双方可能怎样僵住？零窗口探测解决什么问题？
13. 区分接收窗口、MSS 和以后要学的拥塞窗口：它们分别限制什么？
14. 把以下事件按合理因果顺序排列：`保存 outstanding`、`读取发送 ByteStream`、`制作带 SEQ 的段`、`启动或保持计时器`、`交给 IP`。
15. Unity 的 `WriteAsync` 已完成，为什么仍不能证明远端游戏逻辑处理了消息？TCP ACK 最多能证明到哪里？
16. 附加推演：当前 `acknowledged=20`、`next_seqno=25`、窗口为 8。此时还能发送几个序号位置？随后收到新 `ACK=23, window=4`，在 `next_seqno` 不变时还能发送几个？

---

## 下一步

完成检查站后，我会先判断你是否已经稳定区分：

- 发送窗口与 MSS；
- 新发送与重传；
- 新 ACK 与重复 ACK；
- SYN/FIN 占位；
- 接收窗口与拥塞窗口。

达到基本掌握后开放 **Lab 5：Tiny TCP Sender**。它会复用 TinyStream 与 `wrap/unwrap`，实现窗口填充、outstanding 队列、计时器、超时重传和指数退避。仍然不要求单独实验报告。
