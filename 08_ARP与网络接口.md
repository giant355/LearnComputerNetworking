# 第 8 课：ARP 与网络接口——只知道下一跳 IP，怎样真正发出帧

> 预计时间：正文与随堂推演 90–120 分钟；观察实验 15–25 分钟；检查站 25–40 分钟
>
> 所需工具：课堂阅读只需纸笔；Windows 观察实验可以回到电脑旁再做
>
> 当前状态：新课。读完不自动算掌握；完成文末检查站后再判断。

第 6 课让路由表回答：

```text
最终目标 IP
  ↓ 最长前缀匹配
出口接口 + 当前下一跳 IP
```

第 7 课又告诉我们，Ethernet 帧真正交付时必须写入：

```text
来源 MAC + 目标 MAC
```

现在只差一个非常具体的问题：

> 路由表给了下一跳 IP，可是 Ethernet 帧需要下一跳 MAC。发送者怎样把前者变成后者？

这不是查路由表能够继续回答的问题。路由表负责“往哪走”，却不保存当前局域网中每个 IP 对应的 MAC。为了解开这最后一环，我们需要 **ARP**。

在进入正文前，先把本课涉及的四个角色放到同一张最小地图上：

```text
IP 路由表：用目标 IP 决定出口接口和下一跳 IP
ARP：在当前链路询问“谁拥有这个 IP，你的 MAC 是什么”
ARP 缓存：暂时保存 IP → MAC 的答案
网络接口：在 IP 数据报和链路帧之间完成发送与接收
```

网络资料还常把“当前链路上可以直接交付的下一跳接口”称为**邻居**。所以系统命令中的“邻居缓存”，在这里主要就是这类 IP→链路地址记录；这个名字不表示两台设备在物理位置上一定挨着。

本课只讨论常见的 **IPv4 over Ethernet**。IPv6 不使用 ARP，而使用另一套邻居发现机制；现在先不展开。

## 学习目标

本课结束后，你应该能够：

1. 解释 ARP 为什么存在，以及它为什么只解析当前下一跳而不是最终远端主机；
2. 完整推演一次 ARP 请求、ARP 回复和随后发送 IPv4 帧的过程；
3. 区分“主机不知道下一跳 MAC”和“交换机不知道目标 MAC 在哪个端口”；
4. 说明 ARP 广播为什么不会一路穿过互联网；
5. 解释 ARP 缓存为什么属于会过期的临时状态；
6. 说明等待 ARP 回复期间，尚未发送的 IP 数据报为什么需要排队；
7. 读懂一个简化网络接口的发送、接收和计时状态机；
8. 使用 Windows 命令观察本机的接口、默认网关和邻居缓存。

---

## 0. 先做一个十秒预测

主机 A 要访问远端服务器 B：

```text
A：192.168.1.10/24，MAC_A
默认网关：192.168.1.1，MAC_R
B：203.0.113.20
```

A 查路由表，得到下一跳 `192.168.1.1`。请先在脑中判断：

1. A 应该询问 `203.0.113.20` 的 MAC，还是 `192.168.1.1` 的 MAC？
2. A 发出的第一段 IPv4 帧，目标 MAC 应该属于 B，还是属于默认网关？

<details>
<summary>完成预测后展开</summary>

A 只需解析当前下一跳 `192.168.1.1` 的 MAC。第一段 Ethernet 帧交给默认网关，所以目标 MAC 是 `MAC_R`；帧内 IP 数据报的最终目标仍然是 `203.0.113.20`。

</details>

如果这两个答案感觉矛盾，正好说明 ARP 为什么值得单独学习：IP 地址描述端到端目标，MAC 地址只负责眼前这一段链路交付。

---

## 1. 网络接口卡在最后一步

把 A 已经完成的工作写出来：

```text
应用数据
  ↓
UDP/TCP 包装
  ↓
IPv4 数据报
  ↓ 查路由表
出口：A 的 Ethernet 接口
下一跳 IP：192.168.1.1
```

此时 IP 数据报已经存在，但还不能直接在 Ethernet 上传输，因为帧头缺少目标 MAC：

```text
Ethernet 来源 MAC = MAC_A
Ethernet 目标 MAC = ？
EtherType          = IPv4
载荷               = 完整 IPv4 数据报
```

这里的“网络接口”不只表示一块物理网卡。对本课来说，它是主机或路由器中负责连接 IP 层与某条具体链路的组件：

```text
发送方向：IP 数据报 + 下一跳 IP → Ethernet 帧
接收方向：Ethernet 帧 → IPv4 数据报或 ARP 消息
```

真实系统中，这些工作可能分散在操作系统、驱动程序和网卡硬件中。我们先把它们合并成一个容易推演的逻辑组件，称为 **Network Interface（网络接口）**。

### 1.1 “制作 Ethernet 帧”具体做了什么

可以先把帧看成内存中的一个结构体：

```cpp
struct EthernetFrame {
    MacAddress destination;
    MacAddress source;
    uint16_t ether_type;
    std::vector<uint8_t> payload;
};
```

当接口要承载 IPv4 数据报时，大致执行：

```text
destination = 通过 ARP 得到的下一跳 MAC
source      = 当前出口接口自己的 MAC
ether_type  = IPv4
payload     = 序列化后的完整 IPv4 数据报字节
```

随后这些字段按 Ethernet 规定的顺序变成一串字节，交给网卡发送。真实网卡还可能负责补足过短帧、计算并附加 FCS、添加用于线上同步的前导信息，再把比特转换成网线上的电信号。操作系统与网卡之间究竟分担哪些步骤，可能因为硬件卸载能力而不同。

因此目前不必记某个驱动函数。最稳定的理解是：

```text
上层给出载荷和下一跳
→ 接口确定帧头字段
→ 帧被序列化成字节
→ 网卡把字节变成链路上的信号
```

后面的 TinyNet Lab 会把硬件部分替换成一个可观察的“待发送帧队列”，让我们只实现协议决策。

---

## 2. 为什么不采用更朴素的办法

在给出 ARP 前，先尝试三种看似简单的方案。

### 方案一：把所有 IP 与 MAC 手工写死

```text
192.168.1.1   → 70:AA:BB:CC:DD:01
192.168.1.20  → 70:AA:BB:CC:DD:20
192.168.1.200 → 70:AA:BB:CC:DD:C8
```

小实验室也许能这样做，但设备会加入、离开、更换网卡或移动到别的接口。每台机器都手工维护整张表，很快会过期，也难以管理。

### 方案二：每个 IP 数据报都广播

发送者可以把所有 IPv4 数据报都装进广播帧，让每台主机自己判断目标 IP。这可能把数据送到目的地，但局域网中所有主机都会不断收到无关数据，交换机也无法利用已知目标进行定向转发。

### 方案三：需要时询问，并把答案暂时记住

```text
第一次：谁拥有 192.168.1.1？请告诉 192.168.1.10
回复：192.168.1.1 在 MAC_R
之后一段时间：直接使用已经记住的 MAC_R
```

这就是 ARP 的核心思路：

> **Address Resolution Protocol，地址解析协议**：在当前链路中，把要交付的网络层地址映射成链路层地址。

在本课的 IPv4/Ethernet 场景中，它具体解决：

```text
当前下一跳 IPv4 地址 → 当前下一跳 Ethernet MAC 地址
```

注意“当前下一跳”四个字。ARP 不是用来查询任意互联网服务器网卡地址的全球目录。

---

## 3. 第一次发送：ARP 请求是怎样产生的

继续使用这个场景：

```text
A
IP：  192.168.1.10/24
MAC：AA:AA:AA:AA:AA:AA

R 的左侧接口
IP：  192.168.1.1/24
MAC：RR:RR:RR:RR:RR:01

远端 B
IP：  203.0.113.20
```

A 要发送的 IPv4 数据报已经准备好：

```text
来源 IP = 192.168.1.10
目标 IP = 203.0.113.20
```

A 查路由表得到：

```text
出口接口 = A 的 Ethernet 接口
下一跳 IP = 192.168.1.1
```

但是 A 的 ARP 缓存里没有 `192.168.1.1`。A 暂时不能制作承载该 IPv4 数据报的单播帧，因此先保留这个数据报，随后发出一份 ARP 请求。

### 3.1 外面的 Ethernet 包装

ARP 消息本身也需要放进 Ethernet 帧：

```text
Ethernet 来源 MAC = AA:AA:AA:AA:AA:AA
Ethernet 目标 MAC = FF:FF:FF:FF:FF:FF
EtherType          = ARP
载荷               = ARP 请求
```

为什么目标是广播 MAC？因为 A 正是在询问目标 MAC。如果必须先知道目标 MAC 才能发送询问，问题就会陷入死循环。

### 3.2 里面的 ARP 请求

先不背字段编码，只看它表达的信息：

```text
操作：请求
发送者 IP：  192.168.1.10
发送者 MAC： AA:AA:AA:AA:AA:AA
目标 IP：    192.168.1.1
目标 MAC：   尚未知
```

整句话就是：

> 我是 `192.168.1.10 / AA:...`。谁拥有 `192.168.1.1`？请把你的 MAC 告诉我。

注意，ARP 请求询问的是默认网关 `192.168.1.1`，不是最终服务器 `203.0.113.20`。

---

## 4. 广播经过交换机时发生什么

交换机从某个端口收到这份帧，先像第 7 课一样学习来源：

```text
AA:AA:AA:AA:AA:AA → 入口端口
```

随后它看到目标为：

```text
FF:FF:FF:FF:FF:FF
```

这是发送者有意选择的广播地址，因此交换机把帧从入口以外的相关端口发出。同一广播域中的多台主机都可能收到它。

每台主机收到后会检查 ARP 消息中的目标 IP：

```text
普通主机 C：这个 IP 不是我的，不回复
普通主机 D：这个 IP 不是我的，不回复
路由器 R 左接口：192.168.1.1 正是我的 IP，应该回复
```

这里没有“谁最快谁抢到地址”。正常情况下，拥有该目标 IP 的接口才应该回答。

### 再次区分两个“未知”

```text
ARP 广播：
发送主机不知道下一跳 MAC，所以主动把目标 MAC 写成广播地址

未知单播泛洪：
帧中已有一个具体目标 MAC，只是交换机不知道它位于哪个端口
```

两种情况都可能让交换机从多个端口发帧，但起因不同，不能合并成一个机制。

---

## 5. ARP 回复怎样回来

R 的左接口知道自己的身份：

```text
IP  = 192.168.1.1
MAC = RR:RR:RR:RR:RR:01
```

而且请求中已经写出了 A 的 IP 与 MAC，因此 R 通常可以直接向 A 单播回复：

```text
Ethernet 来源 MAC = RR:RR:RR:RR:RR:01
Ethernet 目标 MAC = AA:AA:AA:AA:AA:AA
EtherType          = ARP
载荷               = ARP 回复
```

ARP 回复表达：

```text
操作：回复
发送者 IP：  192.168.1.1
发送者 MAC： RR:RR:RR:RR:RR:01
目标 IP：    192.168.1.10
目标 MAC：   AA:AA:AA:AA:AA:AA
```

整句话可以读作：

> `192.168.1.1` 在 `RR:...:01`；这份回答交给 `192.168.1.10 / AA:...`。

A 收到回复后，把答案暂时记入自己的 ARP 缓存：

```text
192.168.1.1 → RR:RR:RR:RR:RR:01
```

与此同时，交换机收到 R 发出的回复，也能从来源 MAC 学到：

```text
RR:RR:RR:RR:RR:01 → R 所在端口
```

这里发生了两种完全不同的学习：

| 谁在学习 | 观察或保存什么 | 得到什么答案 |
| --- | --- | --- |
| A 的网络接口 | ARP 消息中的发送者 IP 与发送者 MAC | `192.168.1.1 → MAC_R` |
| 交换机 | Ethernet 帧的来源 MAC 与入口端口 | `MAC_R → 交换机端口` |

不要因为两张表都出现 MAC，就把它们看成同一张表。

---

## 6. 原来的 IPv4 数据报现在终于能发送

A 现在已经具备完整信息：

```text
最终目标 IP = 203.0.113.20
下一跳 IP   = 192.168.1.1
下一跳 MAC  = RR:RR:RR:RR:RR:01
出口接口    = A 的 Ethernet 接口
```

于是 A 为刚才等待的 IPv4 数据报制作单播 Ethernet 帧：

```text
Ethernet：
  来源 MAC = AA:AA:AA:AA:AA:AA
  目标 MAC = RR:RR:RR:RR:RR:01
  EtherType = IPv4

IPv4 数据报：
  来源 IP = 192.168.1.10
  目标 IP = 203.0.113.20
```

交换机只根据目标 MAC 把帧送向 R。R 收到后拆掉这一段 Ethernet 包装，减少 TTL，查询自己的路由表，并为下一段链路重新决定下一跳。

重要的是：

```text
ARP 回复不会替换 IP 数据报里的最终目标 IP
ARP 只让发送者知道这一段链路的目标 MAC
```

---

## 7. 如果最终目标就在当前链路

换一个场景：

```text
A：192.168.1.10/24
B：192.168.1.200/24
```

A 查到目标匹配直连路由：

```text
192.168.1.0/24 → 当前 Ethernet 接口，直接交付
```

此时：

```text
最终目标 IP = 192.168.1.200
下一跳 IP   = 192.168.1.200
```

A 如果不知道 B 的 MAC，就广播询问：

```text
谁拥有 192.168.1.200？
```

B 回复自己的 MAC，随后 A 制作：

```text
Ethernet 目标 MAC = MAC_B
IPv4 目标 IP      = 192.168.1.200
```

因此 ARP 的规则始终相同：解析**下一跳 IP**。只是在直连目标场景中，下一跳恰好等于最终目标。

---

## 8. 为什么不能查询远端服务器的 MAC

假设路径为：

```text
A ── R1 ── R2 ── B
```

A 只和 R1 位于同一条本地链路。即使 A 知道 B 的 MAC，这个 MAC 对 A 所在的 Ethernet 广播域也没有直接交付意义：中间的路由器会拆掉旧帧，为下一条链路制作新帧。

逐跳关系是：

```text
A：  ARP 查询 R1 左接口的 MAC
R1：ARP 查询 R2 入站接口的 MAC
R2：ARP 查询 B 的 MAC
```

每个设备只解决自己当前出口链路上的下一跳。

ARP 请求也通常不会被路由器当作普通 IP 数据报转发。它被包装在本地 Ethernet 广播帧中，而路由器会把不同链路的广播域隔开。所以：

```text
ARP 是当前链路的问题
IP 路由负责跨越多条链路
```

---

## 9. ARP 缓存为什么是“缓存”而不是永久真理

如果每发送一个 IPv4 数据报都先广播一次 ARP，请求会大量重复。因此接口把学到的映射保存一段时间：

```text
IP 地址 → MAC 地址 → 剩余有效时间
```

之后发送到同一下一跳时可以直接复用。

但它不能永远有效，因为现实会变化：

- 网卡可能被更换；
- 设备可能离线后由另一块接口使用该 IP；
- 虚拟机或故障切换可能让 IP 转移；
- 旧记录可能因为配置变化而失效。

因此 ARP 缓存属于 **soft state（软状态）**：

> 为提高效率暂时保存；即使丢失，也能通过协议交互重新建立。

具体缓存时间由系统实现决定，不是所有系统都必须使用同一个秒数。后续 TinyNet Lab 会采用明确的简化时限，方便测试状态变化；不要把实验参数误认为所有真实设备的统一标准。

### 收到请求也能学习吗

ARP 请求已经公开写出了发送者自己的 IP 和 MAC：

```text
发送者 IP  = 192.168.1.10
发送者 MAC = AA:AA:AA:AA:AA:AA
```

所以接收者通常可以从请求中学习发送者映射，不必等到另一份回复。ARP 的原始设计也利用了这一点：通信往往是双向的，看到发送者身份就先记下来。

---

## 10. 等待 ARP 回复时，原数据怎么办

A 发 ARP 请求的目的，是发送已经存在的 IPv4 数据报。但回复不是瞬间凭空出现的。在这段时间里，接口通常需要保存待发送数据：

```text
等待解析 192.168.1.1：
  数据报 D1
  数据报 D2
  数据报 D3
```

这就是一个**待解析队列**。它解决的是：

> MAC 还没得到，但上层数据报已经交给接口，暂时放在哪里？

一个朴素实现还必须防止重复广播：如果 D1、D2、D3 几乎同时到来，不应该立刻发三份相同 ARP 请求。可以采用：

```text
第一个数据报到来：排队，并发送一次 ARP 请求
短时间内更多数据报到来：继续排队，不重复请求
收到 ARP 回复：缓存映射，把等待的数据报逐个封装并发送
等待过久：允许再次请求，或最终丢弃等待数据
```

这里同时出现两类队列，不要混淆：

| 队列 | 为什么等待 |
| --- | --- |
| 待解析队列 | 还不知道下一跳 MAC，暂时无法完成 Ethernet 封装 |
| 出口发送队列 | 帧已经能发送，但链路一次只能发送有限数量，需要排队占用链路 |

前者等待“信息”，后者等待“发送机会”。

---

## 11. 网络接口的最小状态机

下面不是正式 Lab 代码，而是一张必须读懂的行为图。它帮助我们把散落的规则装进同一个组件。

### 11.1 接口持有哪些状态

```text
自己的 IP 地址
自己的 MAC 地址

ARP 缓存：
  next_hop_ip → { mac, 剩余有效时间 }

等待中的数据报：
  next_hop_ip → [D1, D2, ...]

最近请求时间：
  next_hop_ip → 上次发送 ARP 请求的时刻
```

### 11.2 发送一个 IP 数据报

调用者交给接口的不是“最终目标 MAC”，而是：

```text
send_datagram(IP 数据报, 下一跳 IP)
```

简化逻辑如下：

```cpp
void send_datagram(Datagram dgram, IpAddress next_hop) {
    if (arp_cache.contains(next_hop)) {
        send_ipv4_frame(dgram, arp_cache[next_hop].mac);
        return;
    }

    waiting[next_hop].push_back(dgram);

    if (recently_requested(next_hop) == false) {
        send_arp_request(next_hop);
        remember_request_time(next_hop);
    }
}
```

必须读懂的只有三件事：

1. 命中缓存就立即制作 IPv4 类型的 Ethernet 帧；
2. 未命中就按下一跳 IP 保存数据报；
3. 同一个下一跳短时间只发一份 ARP 请求，避免产生大量重复广播。

容器类型和具体计时写法留到 Lab 暖身再处理。

### 11.3 收到一个 Ethernet 帧

在本课只处理普通单播和广播的简化模型中，接口首先只接受与自己有关的帧：

```text
目标 MAC 是自己的单播 MAC
或者
目标 MAC 是广播地址
```

普通情况下，目标 MAC 属于别人的帧直接忽略。若 FCS 等链路校验失败，坏帧也不会作为正常载荷向上交付；基础 Ethernet 不因为 FCS 失败自动完成端到端重传。

接受帧后，再根据 EtherType 分流：

```text
EtherType = IPv4
  → 解析 IPv4 数据报，交给 IP 层

EtherType = ARP
  → 解析 ARP 消息，学习发送者 IP→MAC
  → 如果它在询问自己的 IP，再发 ARP 回复
  → 如果刚刚得到等待中的下一跳映射，发送排队数据报
```

### 11.4 时间流逝

网络接口还需要知道时间经过：

```text
缓存项到期 → 删除旧映射
请求抑制时间结束 → 必要时允许再次发送 ARP 请求
```

这类函数在实验中常被写成 `tick(ms)`。它不是网络上的新协议，而是让组件内部的计时器向前走。

---

## 12. 三张表终于连成一条流水线

一次发送中，三张表按下面的顺序参与：

```text
最终目标 IP
  ↓ 查询 IP 路由表
出口接口 + 下一跳 IP
  ↓ 查询 ARP 缓存；缺失时运行 ARP
下一跳 MAC
  ↓ 写入 Ethernet 目标 MAC
交换机查询 MAC 地址表
  ↓
交换机出口端口
```

| 表 | 通常位于哪里 | 查询键 | 查询结果 | 服务对象 |
| --- | --- | --- | --- | --- |
| IP 路由表 | 主机或路由器 | 最终目标 IP | 出口接口、下一跳 IP | IP 转发决定 |
| ARP 缓存 | IPv4 网络接口所属系统 | 当前下一跳 IP | 当前链路的下一跳 MAC | 制作 Ethernet 帧 |
| 交换机 MAC 表 | 交换机 | 帧的目标 MAC | 交换机出口端口 | 转发现成的帧 |

路由表和 ARP 缓存通常在发送主机或路由器一侧；交换机 MAC 表在交换机内部。普通主机不会查询交换机的 MAC 表，交换机也不会替主机查询 IP 路由表。

---

## 13. ARP 消息里究竟有哪些字段

这一节用于建立结构感，不要求第一次阅读就背数字。

一个 ARP 消息需要表达：

| 字段 | 回答的问题 |
| --- | --- |
| 硬件类型 | 使用哪类链路地址，例如 Ethernet |
| 协议类型 | 正在解析哪类网络层地址，例如 IPv4 |
| 硬件地址长度 | MAC 地址占多少字节 |
| 协议地址长度 | IPv4 地址占多少字节 |
| 操作码 | 这是请求还是回复 |
| 发送者 MAC | 发出消息的接口是谁 |
| 发送者 IP | 发送者声称拥有什么 IP |
| 目标 MAC | 请求中尚未知；回复中可写请求者 MAC |
| 目标 IP | 正在询问或回复给谁 |

在常见 Ethernet/IPv4 ARP 中：

```text
Ethernet MAC 长度 = 6 字节
IPv4 地址长度     = 4 字节
ARP 请求操作码    = 1
ARP 回复操作码    = 2
Ethernet EtherType：ARP = 0x0806，IPv4 = 0x0800
```

这些常量在写解析器时有用，但当前最重要的仍是四元组：

```text
发送者 IP、发送者 MAC、目标 IP、目标 MAC
```

---

## 14. ARP 不提供身份认证

ARP 的基本消息可以理解成一台设备在局域网里公开声明：

```text
“这个 IP 对应这个 MAC。”
```

基础 ARP 本身没有可靠的密码学身份验证。恶意设备可能发送虚假映射，让其他机器把帧交错位置，这类攻击通常称为 **ARP 欺骗或 ARP 投毒**。

现在不要求研究攻击工具，只需要记住协议能力边界：

```text
ARP 解决本地地址解析
ARP 不保证声明者一定诚实
```

这也呼应第 3 课：某一层提供了“可通信”的机制，不代表它同时提供认证、保密和可靠交付。

---

## 15. Windows 观察实验

这一部分不修改网络配置，也不要求管理员权限。课堂上没有电脑可以先跳过。

### 15.1 找到本机接口和默认网关

```powershell
ipconfig
```

寻找当前正在使用的 Ethernet 或 Wi-Fi 接口，记录：

```text
本机 IPv4 地址：________
子网掩码：      ________
默认网关：      ________
```

### 15.2 查看当前 ARP/邻居缓存

```powershell
arp -a
```

输出常按接口分组。找一条动态记录：

```text
Internet Address      Physical Address      Type
192.168.1.1           xx-xx-xx-xx-xx-xx     dynamic
```

这里的含义是：

```text
下一跳 IPv4 地址 → 当前链路 MAC 地址
```

`dynamic` 表示它通常是系统通过协议交互动态学习的，不是永久手工写死。

### 15.3 与默认网关通信后再次观察

```powershell
ping <你的默认网关IP>
arp -a
```

例如：

```powershell
ping 192.168.1.1
arp -a
```

如果记录原本不存在，通信可能促使系统解析并缓存网关 MAC；如果记录已经存在，系统会直接复用，因此你不一定能亲眼看见新的 ARP 请求。这个实验观察的是缓存状态，不把“没有明显变化”判作失败。

### 15.4 可选：使用 Wireshark

如果以后安装并使用 Wireshark，可以过滤：

```text
arp
```

观察请求的广播目标、请求与回复中的发送者/目标 IP 和 MAC。现在不用为了完成理论课专门安装。

---

## 16. 纸笔状态机实验

采用下面的 TinyNet 规则，它是为了训练状态，不代表所有操作系统的固定时间：

```text
ARP 缓存有效 30 秒
同一下一跳在 5 秒内不重复发送 ARP 请求
缓存未命中时，数据报进入按下一跳分类的等待队列
收到有效 ARP 消息时，学习其发送者 IP→MAC
```

初始状态：

```text
时间 = 0
ARP 缓存为空
等待队列为空
```

按顺序推演：

| 时间 | 事件 | ARP 缓存怎样变化 | 等待队列怎样变化 | 发出什么帧 |
| ---: | --- | --- | --- | --- |
| 0 ms | D1 要发给下一跳 X |  |  |  |
| 100 ms | D2 也要发给 X |  |  |  |
| 500 ms | 收到 X 的 ARP 回复 |  |  |  |
| 1000 ms | D3 要发给 X |  |  |  |

完成后再展开：

<details>
<summary>参考推演</summary>

| 时间 | ARP 缓存 | 等待队列 | 发出帧 |
| ---: | --- | --- | --- |
| 0 ms | 无 X | D1 入队 | 一份询问 X 的 ARP 广播 |
| 100 ms | 无 X | D1、D2 | 不重复 ARP 请求 |
| 500 ms | 学到 `X→MAC_X` | 取出 D1、D2 后为空 | 两份目标为 `MAC_X` 的 IPv4 帧 |
| 1000 ms | `X→MAC_X` 仍有效 | 保持为空 | D3 立即成为目标为 `MAC_X` 的 IPv4 帧 |

</details>

---

## 17. 与 CS144 Checkpoint 5 的关系

CS144 的 Network Interface 任务正是把这个机制写成代码：

```text
send_datagram(dgram, next_hop)
recv_frame(frame)
tick(ms_since_last_tick)
```

它要求接口维护 IP 到 Ethernet 地址的临时映射，在未知时广播 ARP 请求、暂存数据报，并在收到映射后发送。我们的下一次实现型 Lab 会借鉴这种结构，但会先提供 C++ 容器和计时状态的短暖身，避免语法遮住协议本身。

参考资料：

- [RFC 826：An Ethernet Address Resolution Protocol](https://www.rfc-editor.org/rfc/rfc826.html)
- [CS144 Checkpoint 5：The Network Interface / ARP](https://cs144.github.io/assignments/check5.pdf)

---

## 知识锚点

- ARP 在本课场景中把当前下一跳 IPv4 地址解析为当前链路的 Ethernet MAC 地址。
- ARP 请求通常使用 Ethernet 广播；拥有目标 IP 的接口返回 ARP 回复。
- ARP 广播局限在当前广播域，普通路由器不会把它一路转发到远端网络。
- ARP 缓存是临时软状态，超时后可以通过新的请求与回复重建。
- 网络接口等待解析时可能暂存多个数据报，并抑制短时间内的重复请求。
- 交换机学习 `来源 MAC→端口`；主机的 ARP 缓存保存 `IP→MAC`，两者不是同一张表。
- 基础 ARP 不提供密码学身份认证，因此不能把它当作可信身份系统。

## 小结：从下一跳 IP 到真正发出帧

```text
1. 用最终目标 IP 查路由表
2. 得到出口接口和下一跳 IP
3. 用下一跳 IP 查 ARP 缓存
4. 命中：立即得到下一跳 MAC
5. 未命中：数据报排队，广播 ARP 请求
6. 收到回复：学习 IP→MAC，发送等待数据报
7. 制作 Ethernet 帧，把下一跳 MAC 写入目标 MAC
8. 交换机再用目标 MAC 查询自己的端口表
```

---

## 检查站

请合上正文或滚动到看不到上文，再用自己的话回答。不会的可以直接写“不知道”。

1. 已经通过路由表得到下一跳 IP 后，为什么还不能立刻在 Ethernet 上发送 IPv4 数据报？ARP 精确解决什么映射？

还没有制作链路帧，不知道下一跳mac，arp解决目标
下一跳到mac的映射

2. A 为 `192.168.1.10/24`，默认网关为 `192.168.1.1`，要访问远端 `203.0.113.20`。ARP 请求中的目标 IP 是谁？随后承载 IPv4 数据报的第一段帧中，目标 MAC 属于谁？IPv4 目标地址又是谁？

默认网关ip地址，默认网关，远端

3. A 要向同一链路上的 `192.168.1.200` 发送。此时最终目标 IP 与下一跳 IP 分别是什么？ARP 应询问谁？

都是192.168.1.200

4. 分别解释下面两句话，不能把它们写成同一件事：

   ```text
   主机不知道下一跳 MAC：主机找到下一跳IP地址但不知道其对应的mac，arp在当前链路广播询问
   交换机不知道目标 MAC 位于哪个端口：mac表里找不到目标mac，泛洪尝试每个接口
   ```

5. 一份 ARP 请求为什么使用广播 Ethernet 目标地址？局域网里的每台主机都会回复吗？谁应该回复？

主机只知道目标IP地址，不知道其mac，需要向链路里面每一个接口询问，只有目标IP对应的会回复

6. ARP 请求至少需要让接收者知道哪些发送者信息和目标信息？收到请求的一方为什么可能顺便学到请求者的映射？

发送者的ip，mac，目标的ip
收到请求的一方知道了发送者的ip和mac会自然的建立映射方便回复和交流

7. 等待 ARP 回复时，原来的 IPv4 数据报为什么需要排队？同一下一跳在极短时间内来了三个数据报，为什么通常不发送三份相同请求？

短时间大量发送同样的arp是浪费资源，所以一起放在带解析队列，等待同一个arp回复

8. ARP 缓存为什么不能永久保存？“软状态”在这里是什么意思？

mac可能会变，设备更换，移动
软状态就是短时间有效的映射状态

9. 路径为 `A → R1 → R2 → B`。分别说明 A、R1、R2 会为谁执行地址解析。A 为什么不直接查询 B 的 MAC？

a->r1
r1->r2
r2->b
它们不在一个以太网内，而且假如查询了也会被中间的路由器更换ethernet封装

10. 一台接口收到 Ethernet 帧后，分别遇到以下情况应怎样处理：

    - 目标 MAC 既不是自己，也不是广播；-----丢
    - FCS 检测失败；-----------------------------丢
    - EtherType 为 IPv4；-------------------------交换机转发
    - EtherType 为 ARP，且请求的目标 IP 是自己。----学习建立映射，单播回复mac

11. 用一句话分别说明 IP 路由表、ARP 缓存和交换机 MAC 表的查询键与查询结果。

目标ip，下一跳ip
下一跳ip，对应mac
下一跳mac，交换机接口

12. 状态推演：ARP 缓存为空。D1 要发给下一跳 X，于是接口发出一次请求；1 秒后 D2 也要发给 X；2 秒后收到 `X→MAC_X` 的回复。写出 D1、D2 的排队和发送顺序，并说明总共需要几份 ARP 请求。

    队列
0：d1
1：d1,d2
3：发送d1，再d2

附加安全题：如果某台恶意主机谎称“默认网关 IP 对应我的 MAC”，其他主机可能把后续帧交给谁？这暴露了 ARP 缺少哪种能力？

交给这台机器，缺少辨别真伪的能力

完成检查站后，我会判断理论掌握情况，再决定是直接进入 Network Interface Lab，还是只补一个很短的 C++ 状态机暖身。

---

## 检查反馈（2026-09-07）

**结论：基本掌握，可以进入 Network Interface Lab。**

你已经稳定掌握本课主线：路由表先给出当前下一跳 IP；ARP 只在当前链路解析这个 IP 的 MAC；路由器逐跳更换 Ethernet 包装。第 2、4、5、6、8、9、12 题以及附加安全题都正确；第 1 题也准确说出了核心映射。

需要带进 Lab 的是下面两条边界：

1. **待解析队列先是在等信息，不只是为了少发 ARP。**没有下一跳 MAC 就无法写出 IPv4 Ethernet 帧的目标 MAC，所以 D1、D2 必须先保存；“同一下一跳只请求一次”是在此基础上避免重复广播。
2. **第 10 题的 IPv4 分支不是交换机转发。**题目的主体是“网络接口收到帧”。接口确认帧属于自己且 EtherType 为 IPv4 后，会拆掉 Ethernet 包装，把 IPv4 数据报交给 IP 层。若这台设备是路由器，IP 层才会接着查路由表并决定下一跳；交换机则是另一台设备，它通常只按 MAC 转发现成的帧。

第 3、11 题只差把隐含信息说完整：

```text
同链路目标：最终目标 IP = 下一跳 IP = 192.168.1.200
             因而 ARP 询问 192.168.1.200

IP 路由表：最终目标 IP → 出口接口 + 当前下一跳 IP
ARP 缓存：当前下一跳 IP → 下一跳 MAC
交换机表：帧的目标 MAC → 交换机出口端口
```

Lab 开始时会用两个很小的测试专门验证这两点：缓存未命中时 D1/D2 应共同等待同一个解析结果；收到 IPv4 帧时应向 IP 层交付，而不是让网络接口扮演交换机。
