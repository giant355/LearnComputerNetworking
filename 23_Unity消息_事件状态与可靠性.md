# 第 23 课：这条消息该不该重传？——事件、状态与时效

> 预计时间：约 1.5～2 小时
> 场景：可在教室阅读；不需要打开 Unity  
> 前置：[[10_端口与UDP|UDP 数据报与 TCP 字节流]]、[[17_排队_瓶颈与拥塞窗口|排队和延迟]]、[[22_Unity联机第一步_两个进程与一个世界|两个 Unity 进程、对象身份与服务器权威]]

[打开交互：旧快照与重要请求](23_Unity消息_交互.html)

## 本课从两个“丢了一条消息”开始

先看一个玩家的位置：

    Tick 10：玩家在 x=10
    Tick 11：玩家在 x=11
    Tick 12：玩家在 x=12

网络把 Tick 11 的位置更新丢了，但 Tick 12 到达了。玩家已经在 x=12，此时还要不要把 Tick 11 补回来？

再看另一条消息：

    客户端：我请求购买一瓶药水，花 100 金币。

如果这条请求丢失，服务器和客户端会不会继续假装购买成功？如果服务器已经购买成功、但成功回复丢了，客户端重试时又会不会被扣两次钱？

两条消息都可能丢，但正确处理方式不同。原因是它们表达的东西不同：

- 位置消息主要告诉我们**现在的状态**；
- 购买消息表达一个需要处理的**请求或事件**。

本课的判断原则是：**先问消息代表什么、过期后还有没有价值，再决定是否重传。**

## 1. 命令、事件、状态不是一回事

### 命令：请求某件事发生

命令表达“请尝试做这件事”，不是结果本身：

    OpenDoorRequest(doorId=17, requestId=203)
    PurchaseRequest(itemId=9, requestId=204)
    MoveInput(playerId=42, tick=105, direction=Right)

命令通常由客户端发给服务器。服务器仍要检查发信客户端是否有资格、游戏规则是否允许，然后才决定如何处理。

### 事件：一件事已经发生

事件表达已发生的事实：

    DoorOpened(doorId=17, eventId=203)
    PlayerJoined(clientId=4)
    MatchEnded(winner=2)

事件常用来通知其他系统或客户端“发生了什么”。如果漏掉，可能漏播音效、漏记战果，或显示错误。

### 状态：某个对象现在是什么样

状态表达当前值：

    PlayerSnapshot(playerId=42, tick=105, x=12, health=80)
    DoorState(doorId=17, isOpen=true)
    InventoryState(playerId=42, coins=300)

状态可以被新状态更新。位置 x=12 到达后，早先的 x=11 通常不再是必须补齐的历史。

“打开宝箱”可能经历三种消息：

    命令：客户端请求打开宝箱
    事件：服务器判定宝箱已打开
    状态：宝箱当前为空，玩家背包里有一件物品

客户端提出请求，不等于事情已经发生；事件通知也不一定是完整的当前状态。

## 2. 位置快照：旧消息为什么可能应该丢掉

假设服务器按 Tick 发送玩家位置：

| 消息 | Tick | 位置 | 含义 |
| --- | ---: | ---: | --- |
| A | 100 | x=10 | 第 100 次服务器模拟后的状态 |
| B | 101 | x=11 | 更新的状态 |
| C | 102 | x=12 | 最新状态 |

网络让 C 先到、B 后到：

    接收端先看到：Tick 102，x=12
    随后迟到：    Tick 101，x=11

如果接收端不比较版本，晚到的旧状态会把画面倒退：12 → 11。因此快照通常带 Tick、序号或时间戳；接收端只应用比当前更新的快照：

    若收到的 tick > last_applied_tick：应用
    否则：旧消息，丢弃

丢掉旧消息不是反对可靠性，而是遵守消息的意义：**位置快照关注最新事实，不要求逐条重放过期历史。**

这类策略常被称为“最新值优先”或“只接受更新版本”。不同框架的术语和实现可能不同；先掌握规则，再看 API。

## 3. 重要请求：为什么丢了不能当作没发生

购买请求丢失时：

    客户端以为：正在购买
    服务器实际：根本没有收到请求

客户端需要知道请求是否被服务器处理。可选做法包括可靠传输、应用层确认并重试，或读取服务器之后同步过来的权威背包状态。无论采用哪种，不能只凭客户端“按钮按过了”就认定购买成功。

| 消息含义 | 丢失后的典型处理 | 迟到后的典型处理 |
| --- | --- | --- |
| 高频位置快照 | 等下一份更新 | 版本较旧就丢弃 |
| 聊天消息、比赛结束通知 | 通常要可靠送达 | 按定义保留顺序或识别重复 |
| 购买、开门请求 | 要得到处理结果，常需确认或重试 | 检查请求是否仍有效 |
| 当前血量、背包状态 | 可用更新的完整状态修复 | 旧版本不能覆盖新版本 |

“典型”不等于“永远如此”。位置是状态；开火可能是事件；按住开火也可能被建模为连续输入状态。分类取决于游戏语义，不取决于字段名称。

## 4. 可靠不等于及时，也不等于游戏已经处理

可靠交付关注“缺失时会不会补送”；它既不保证消息到达时仍有用，也不保证服务器接受了请求。比如 TCP ACK 只确认传输层字节进度，不等于“购买成功”——这是第 10 课 TCP/UDP 与应用协议知识在游戏消息上的应用，本课不再重讲传输机制。

第 17 课已解释 TCP 有序字节流的队头阻塞；这里取它的游戏设计结论即可：不要不加区分地把短命位置更新和重要请求都塞进同一条必须完整、有序重传的通道。具体通道行为取决于传输与框架。

## 5. 重试会造成重复：请求 ID 与幂等处理

即使请求使用可靠传输，应用也要谨慎理解“只执行一次”。客户端可能超时重试；服务器可能已处理请求，只是回复在回程丢失。于是同一笔购买可能再次到达。

给每次逻辑请求分配唯一 ID：

    PurchaseRequest(requestId=204, itemId=9)

服务器记录最近处理过的请求 ID：

    第一次看到 204：验证并执行购买，保存结果“成功，余额 300”
    再次看到 204：不再扣钱，重发已保存的结果

这叫去重；让同一个请求重复执行也不会造成额外副作用，称为幂等。不要只靠“网络应该不会重复”保护金币、道具或比赛结果。

## 6. 新鲜度和可靠性要分开考虑

玩家的瞄准方向每帧都变化。重传几百毫秒前的旧方向，可能比丢掉它更糟；但“玩家买下了物品”通常不会因为过了 100 毫秒就失去含义。

| 问题 | 实际在问什么 |
| --- | --- |
| 可靠性 | 如果消息没到，我是否必须想办法补上？ |
| 新鲜度 | 如果消息晚到，它是否仍值得应用？ |

因此会出现不同组合：

- **重要但仍有效**：尽量可靠送达，并保留请求 ID/处理结果；
- **频繁但会过期**：允许丢包，只接受最新版本；
- **不重要且短命**：丢失或过期都可忽略；
- **状态必须最终一致**：定期发送权威完整状态作为修复依据。

并不是每条消息都必须确认、排序、重放。设计目标是满足游戏语义，同时控制延迟、带宽和复杂度。

## 7. 读任何网络 API 前，先问五件事

1. 这条消息表达命令、事件，还是当前状态？
2. 丢失后，游戏能否从下一份完整状态恢复？
3. 晚到时它还有效吗？怎样识别旧版本？
4. 如果重试造成重复，服务器会不会重复扣款、开门或发奖励？
5. “收到确认”代表网络收到了、应用处理了，还是游戏规则批准了？

回答这些问题后，再去看 API 的可靠性选项，就知道它们解决的是哪一个需求，而不是盲目背函数名。

## 常见混淆

### “可靠”就是“服务器接受”

不是。可靠性是交付语义；服务器可以可靠地收到一条被规则拒绝的请求。

### “不可靠”就是一定丢

不是。它表示机制不承诺缺失时一定补送；消息仍可能正常到达。

### “旧位置迟到就重放，才叫可靠”

对最新位置而言，过时数据反而会倒退画面。带 Tick 并丢弃旧快照可能更正确。

### “重试一次，事情就只做一次”

重试可能让同一请求到达多次。重要副作用需要请求 ID、去重或幂等处理。

### “TCP ACK 表示游戏逻辑处理完了”

不表示。TCP 确认传输层字节接收进度，不确认购买、开门或命中等业务结果。

## 本课小结

- 命令提出请求，事件说明事情发生，状态描述当前事实；三者可能属于同一流程，但含义不同。
- 高频状态常可以丢弃旧版本，只应用最新快照；重要请求则需要确认、重试或状态修复。
- 可靠性回答“丢了怎么办”，新鲜度回答“晚到还有没有用”。
- 重试可能重复到达；请求 ID、去重和幂等处理可以保护重要副作用。
- 传输 ACK 不等于应用已经接受，更不等于游戏规则批准。

## 检查站

先遮住正文，用自己的话回答；结合场景即可，不用背术语。

1. 玩家位置 Tick 103 先到，随后 Tick 101 才到。接收端为什么通常不应用后者？需要保存什么信息？

因为它是过期的状态
 接收端必须记住已应用的最新序号

2. “客户端请求开门”和“门已经打开”分别表达什么？为什么请求到达不等于事件已经发生？

 请求开门 = 命令/意图（command）。它只表达"客户端想这样做"，不是既成事实。
 门已经打开 = 事件/状态（event/state）。它表达"服务器已经裁定并确认了这一事实"。

3. 购买请求已被服务器处理，但成功回复丢失。客户端重试时可能发生什么？服务器怎样避免重复扣款？


  玩家被重复扣款（或买到两个物品）。
  每个请求带一个唯一 requestId，服务器维护一份"已处理 requestId"的去重表。收到重复
  requestId 时，不重新执行，直接返回之前缓存的结果。

4. TCP ACK 已确认服务器主机收到了请求字节，为什么客户端还不能断言“购买成功”？

只是传输层的任务完成了，和应用层无关

5. 高频位置快照与比赛结束消息，哪一种更看重新鲜度，哪一种更需要可靠送达？为什么？

略，太简单了

6. 为什么“所有消息都可靠、有序、重传”不一定是实时游戏的好设计？至少说出一个代价。

因为前面有空洞后面也读不到，一直卡住

7. 下面三条消息分别表达什么含义？各自迟到后通常怎样判断还能否应用？
   - PlayerSnapshot，tick=300，position=…      状态，只要最新的就行
   - PurchaseRequest，requestId=81，itemId=9   命令，迟到只要不重复就可以
   - MatchEnded，eventId=12，winner=2         事件，迟到不重复
7. 选一个熟悉的游戏动作，写出它可能包含的“命令 → 服务器裁定事件 → 权威状态”三步。

玩家按键想射击，服务器允许发生，玩家a子弹减少
## 下一步

第 22 课负责解释“谁模拟、谁决定、状态怎样同步”；本课只补上“不同消息该怎样交付、过期和重试”。后续实践会从游戏消息设计本身入手，不要求先掌握 NGO API。
